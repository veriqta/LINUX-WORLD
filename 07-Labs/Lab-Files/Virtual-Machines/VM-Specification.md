# VM Specification

Use a current supported Linux image, NAT or host-only networking, a named snapshot before mutation, and a separate disposable virtual disk for storage labs. Record image source and checksum, CPU, memory, disks, interfaces, snapshot ID and console method. Never attach a host data disk.
