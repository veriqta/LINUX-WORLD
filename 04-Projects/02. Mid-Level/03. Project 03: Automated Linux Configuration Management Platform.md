# Project 03: Automated Linux Configuration Management Platform

<img width="1672" height="941" alt="image" src="https://github.com/user-attachments/assets/44405eb1-aabf-46d0-87ce-04fadd635793" />

- **Level:** Mid-Level
- **Track:** Linux Systems Administration / Infrastructure Automation / DevOps
- **Primary Environment:** Ubuntu Server 24.04 LTS
- **Estimated Time:** 30–40 hours
- **Cost:** Free with local virtual machines
- **Primary Technologies:** Linux, Ansible, YAML, SSH, Jinja2, systemd, UFW, Bash, Git
- **Demonstrates:** Linux administration, Ansible, automation, configuration management, SSH, security, deployments, idempotency, validation, troubleshooting

---

# 01. Project Title

## Automated Linux Configuration Management Platform

Managing one Linux server manually is straightforward.

You SSH into the machine:

```bash
ssh admin@server01
```

Install a package:

```bash
sudo apt install nginx
```

Create a user:

```bash
sudo useradd alice
```

Edit a configuration file:

```bash
sudo nano /etc/nginx/nginx.conf
```

Restart a service:

```bash
sudo systemctl restart nginx
```

Then you repeat the same work on another server.

And another.

And another.

Eventually your environment looks like this:

```text
server01
├── nginx 1.x
├── user alice
├── UFW enabled
└── SSH configured

server02
├── nginx 1.x
├── user alice
├── UFW enabled
└── SSH configured

server03
├── nginx ???
├── alice missing
├── different firewall rules
└── SSH configuration changed manually
```

At that point, the problem is no longer:

> How do I configure Linux?

The problem becomes:

> How do I make sure every Linux server has the correct configuration, and how do I prove that configuration remains consistent?

That is configuration management.

In this project, you will stop administering servers as isolated machines and start managing them as a fleet.

You will build an Ansible control environment capable of managing multiple Ubuntu servers from one control node.

Your platform will automate:

```text
Package installation
Package updates
User creation
Group membership
SSH keys
Directory creation
File permissions
Configuration files
Nginx deployment
systemd services
Firewall rules
Security settings
Application deployment
Configuration validation
Service restarts
Health checks
Configuration drift correction
```

The finished architecture will look like this:

```text
                    ADMINISTRATOR
                         |
                         v
                 +----------------+
                 |    control01   |
                 |                |
                 |    Ansible     |
                 |    Git         |
                 |    YAML        |
                 |    Jinja2      |
                 +-------+--------+
                         |
                         | SSH
              +----------+----------+
              |          |          |
              v          v          v
        +----------+ +----------+ +----------+
        | server01 | | server02 | | server03 |
        |          | |          | |          |
        |  Web     | |  Web     | |  App     |
        +----------+ +----------+ +----------+
```

But this project is not going to be a collection of disconnected Ansible commands.

You will build a real configuration-management repository with:

```text
Inventories
Group variables
Host variables
Playbooks
Roles
Templates
Handlers
Files
Variables
Facts
Tags
Validation
Deployment logic
Security controls
Testing
Failure simulations
Drift detection
Recovery
Documentation
```

By the end, you should be able to deliberately break the configuration of a managed server, run Ansible, and watch the platform restore the declared state.

That is the core idea behind this project:

```text
MANUAL ADMINISTRATION

Human decides
      |
      v
Human connects
      |
      v
Human changes server
      |
      v
Human hopes other servers match


CONFIGURATION MANAGEMENT

Desired state
      |
      v
Automation
      |
      +-------------------+
      |                   |
      v                   v
 Current state        Desired state
      |                   |
      +---------+---------+
                |
           comparison
                |
        +-------+-------+
        |               |
      matches       differs
        |               |
        v               v
    no change       correction
```

---

# 02. Project Scenario

You have joined the infrastructure team responsible for a growing Linux environment.

The team currently manages servers manually.

A typical deployment looks like this:

```text
Engineer logs into server01
Engineer installs packages
Engineer edits configuration
Engineer restarts services

Engineer logs into server02
Engineer tries to repeat the same steps

Engineer logs into server03
Engineer forgets one step
```

Several months later:

```text
server01 != server02 != server03
```

Nobody is completely sure which machine contains the correct configuration.

This is **configuration drift**.

Your assignment is to replace that workflow with a centralized configuration-management platform.

You will manage four machines:

| Host        | Role                       |      Example IP |
| ----------- | -------------------------- | --------------: |
| `control01` | Ansible control node       | `192.168.56.10` |
| `web01`     | Web server                 | `192.168.56.21` |
| `web02`     | Web server                 | `192.168.56.22` |
| `app01`     | Application/utility server | `192.168.56.23` |

Your control node will define what those servers should look like.

You will divide configuration into reusable roles:

```text
common
users
security
webserver
application
```

The desired relationship is:

```text
                         site.yml
                            |
            +---------------+---------------+
            |               |               |
            v               v               v
          common          security         users
            |
      +-----+-----+
      |           |
      v           v
  webservers   appservers
      |           |
      v           v
  webserver   application
```

Your automation must be repeatable.

Running it once should configure the environment.

Running it again should normally produce little or no change.

That property is called:

# Idempotency

It is one of the most important concepts in this project.

---

# 03. Project Difficulty

## Mid-Level

This is not an introduction to typing:

```bash
ansible all -m ping
```

and stopping there.

At Mid-Level, you need to understand the system around Ansible.

You will work with:

```text
SSH
inventories
host groups
variables
facts
playbooks
roles
modules
templates
handlers
privilege escalation
package management
user management
file ownership
systemd
firewalls
deployment
validation
idempotency
configuration drift
failure handling
troubleshooting
```

You should already understand basic Linux administration.

You do not need previous professional Ansible experience.

The project will build that experience progressively.

---

# 04. Estimated Time

Plan approximately **30–40 hours**.

| Phase                            | Estimated Time |
| -------------------------------- | -------------: |
| Architecture and lab preparation |      2–3 hours |
| SSH and control-node preparation |      2–3 hours |
| Ansible foundations              |      3–4 hours |
| Inventory and variables          |      2–3 hours |
| Common configuration role        |        3 hours |
| User-management role             |        3 hours |
| Security role                    |      3–4 hours |
| Web-server role                  |        4 hours |
| Application deployment           |      3–4 hours |
| Validation and idempotency       |        3 hours |
| Drift testing                    |        2 hours |
| Failure simulations              |      4–6 hours |
| Documentation                    |      2–3 hours |

---

# 05. Prerequisites

You should understand:

```text
Linux command line
SSH
sudo
users and groups
file ownership
permissions
packages
systemd
networking
ports
firewalls
text configuration files
basic Bash
basic YAML syntax
Git fundamentals
```

You do not need to be an Ansible expert.

---

# Lab Requirements

Create four Ubuntu Server 24.04 LTS VMs.

## `control01`

```text
Purpose: Ansible control node
CPU:     2 vCPU
RAM:     2–4 GB
Disk:    25 GB
```

## `web01`

```text
Purpose: Managed web server
CPU:     1–2 vCPU
RAM:     2 GB
Disk:    20 GB
```

## `web02`

```text
Purpose: Managed web server
CPU:     1–2 vCPU
RAM:     2 GB
Disk:    20 GB
```

## `app01`

```text
Purpose: Managed application server
CPU:     1–2 vCPU
RAM:     2 GB
Disk:    20 GB
```

Take clean VM snapshots before beginning.

Suggested snapshot:

```text
project03-clean
```

---

# 06. Learning Objectives

By the end of this project, you should be able to:

1. Explain configuration management.
2. Explain configuration drift.
3. Explain desired-state management.
4. Explain Ansible's architecture.
5. Distinguish control nodes from managed nodes.
6. Build an SSH-based management environment.
7. Install and configure Ansible.
8. Build inventories.
9. Organize hosts into groups.
10. Use inventory variables.
11. Use `group_vars`.
12. Use `host_vars`.
13. Run ad-hoc commands.
14. Gather facts.
15. Write YAML playbooks.
16. Use Ansible modules.
17. Understand `become`.
18. Manage packages.
19. Manage users.
20. Manage groups.
21. Manage SSH authorized keys.
22. Manage directories.
23. Manage permissions.
24. Manage configuration files.
25. Use Jinja2 templates.
26. Use handlers.
27. Manage systemd services.
28. Configure UFW.
29. Deploy Nginx.
30. Deploy application content.
31. Use variables in templates.
32. Use conditionals.
33. Use loops.
34. Use tags.
35. Create reusable roles.
36. Understand role precedence and execution.
37. Validate generated configurations.
38. Protect secrets.
39. Understand Ansible Vault.
40. Build pre-deployment checks.
41. Build post-deployment checks.
42. Test idempotency.
43. Introduce configuration drift.
44. Detect drift.
45. Correct drift.
46. Use check mode.
47. Understand check-mode limitations.
48. Use diff mode.
49. Troubleshoot unreachable hosts.
50. Troubleshoot authentication failures.
51. Troubleshoot privilege-escalation failures.
52. Troubleshoot YAML errors.
53. Troubleshoot undefined variables.
54. Troubleshoot template failures.
55. Troubleshoot service failures.
56. Troubleshoot firewall mistakes.
57. Understand blast radius.
58. Use serial deployments.
59. Build validation playbooks.
60. Document configuration-management architecture professionally.

---

# 07. What You Will Build

Your platform will have five management areas.

## 1. Base Linux Configuration

All managed hosts receive:

```text
Common packages
Timezone
Administrative directories
Standard file permissions
Basic system configuration
```

---

## 2. Identity Management

Ansible manages:

```text
Linux groups
Linux users
SSH public keys
sudo membership
account state
```

---

## 3. Security Configuration

Ansible manages:

```text
UFW
SSH-related settings
unnecessary service state
security configuration files
file permissions
```

---

## 4. Web Infrastructure

The `webservers` group receives:

```text
Nginx
web configuration
website content
firewall rules
service configuration
health checks
```

---

## 5. Application Deployment

The `appservers` group receives a small managed application/service.

The point is not to build a complex application.

The point is to learn how infrastructure automation deploys:

```text
directories
files
configuration
services
permissions
updates
```

---

# 08. Architecture

## Control Plane

```text
                        control01
                            |
                   +--------+--------+
                   |                 |
                Ansible             Git
                   |
                   v
              inventory
                   |
                   v
               site.yml
                   |
       +-----------+-----------+
       |           |           |
       v           v           v
     roles       vars       templates
       |
       v
      SSH
       |
 +-----+------+----------+
 |            |          |
 v            v          v
web01       web02      app01
```

---

# Ansible Is Agentless

You are not going to install an Ansible agent on every managed machine.

The basic model is:

```text
control01
   |
   | SSH
   v
managed host
   |
   | Python/modules
   v
configuration change
```

The managed machines need the requirements necessary for Ansible's modules, but there is no permanently running Ansible agent.

---

# Desired State

Suppose your automation declares:

```text
Package nginx = installed
Service nginx = enabled
Service nginx = running
File /etc/nginx/sites-available/veriqta = specific content
Firewall HTTP rule = present
```

Ansible examines the machine.

If it already matches:

```text
ok
```

If it differs:

```text
changed
```

If something prevents the operation:

```text
failed
```

That distinction will become important throughout the project.

---

# 09. Technologies

| Technology   | Purpose                       |
| ------------ | ----------------------------- |
| Ubuntu 24.04 | Linux platform                |
| Ansible      | Configuration management      |
| YAML         | Playbook/data format          |
| Jinja2       | Configuration templating      |
| SSH          | Remote transport              |
| Python       | Managed-node module execution |
| systemd      | Service management            |
| UFW          | Firewall                      |
| Nginx        | Managed web service           |
| Git          | Automation version control    |
| Bash         | Validation/helper scripts     |

---

# 10. Environment Setup

# Phase 1: Prepare Hostnames

On the four machines:

### control01

```bash
sudo hostnamectl set-hostname control01
```

### web01

```bash
sudo hostnamectl set-hostname web01
```

### web02

```bash
sudo hostnamectl set-hostname web02
```

### app01

```bash
sudo hostnamectl set-hostname app01
```

Verify:

```bash
hostnamectl
```

---

# Phase 2: Record Network Addresses

Run on every machine:

```bash
ip -brief address
```

Then:

```bash
ip route
```

For this guide:

```text
control01  192.168.56.10
web01      192.168.56.21
web02      192.168.56.22
app01      192.168.56.23
```

Use your actual addresses.

---

# Phase 3: Configure Lab Name Resolution

Edit `/etc/hosts` on all machines:

```bash
sudo nano /etc/hosts
```

Add:

```text
192.168.56.10  control01
192.168.56.21  web01
192.168.56.22  web02
192.168.56.23  app01
```

Verify from `control01`:

```bash
getent hosts web01
```

```bash
getent hosts web02
```

```bash
getent hosts app01
```

---

# Phase 4: Create the Automation Administrator

For this lab, create an `ansible` account on each managed server.

On `web01`, `web02`, and `app01`:

```bash
sudo adduser ansible
```

Give it administrative capability:

```bash
sudo usermod -aG sudo ansible
```

Verify:

```bash
id ansible
```

Representative output:

```text
uid=... ansible ... groups=...,sudo
```

For the initial lab, configure sudo carefully rather than immediately creating broad passwordless access.

You will first prove that normal SSH and privilege escalation work.

---

# Phase 5: Install Ansible

On `control01`:

```bash
sudo apt update
```

Then:

```bash
sudo apt install -y ansible git
```

Verify:

```bash
ansible --version
```

You should see information including:

```text
ansible core version
Python version
configuration file
module search paths
collection locations
```

Record the actual version in your `ENVIRONMENT.md`.

Do not write documentation that assumes everyone has the same Ansible release forever.

---

# Phase 6: Create the SSH Key

On `control01`, as your normal user:

```bash
ssh-keygen -t ed25519 -C "ansible-control01"
```

Use a passphrase for a realistic administrative key.

Start an agent if needed:

```bash
eval "$(ssh-agent -s)"
```

Add the key:

```bash
ssh-add ~/.ssh/id_ed25519
```

Copy it to the managed hosts:

```bash
ssh-copy-id ansible@web01
```

```bash
ssh-copy-id ansible@web02
```

```bash
ssh-copy-id ansible@app01
```

Test:

```bash
ssh ansible@web01
```

Exit:

```bash
exit
```

Repeat for every managed host.

---

# Important: Host-Key Verification

Do not disable host-key verification simply because it is convenient.

SSH host keys help protect you from connecting to an unexpected machine.

Inspect:

```bash
ssh-keygen -F web01
```

And:

```bash
ssh-keygen -F web02
```

The control node should know the expected hosts.

---

# Phase 7: Test Privilege Escalation

From `control01`:

```bash
ssh ansible@web01
```

Then:

```bash
sudo whoami
```

Expected:

```text
root
```

Repeat on all managed hosts.

If this does not work, fix sudo before continuing.

Ansible cannot automate privileged configuration reliably if your administrative path itself is broken.

---

# 11. Project Directory Structure

On `control01`:

```bash
mkdir -p ~/ansible-configuration-platform
```

Enter it:

```bash
cd ~/ansible-configuration-platform
```

Create:

```bash
mkdir -p \
    inventories/production/group_vars \
    inventories/production/host_vars \
    playbooks \
    roles \
    scripts \
    tests \
    docs \
    evidence \
    failure-labs \
    runbooks
```

Later, roles will contain:

```text
roles/
├── common/
├── users/
├── security/
├── webserver/
└── application/
```

Each role can contain standard directories such as:

```text
tasks/
handlers/
templates/
files/
defaults/
vars/
```

---

# Initialize Git

```bash
git init
```

Create `.gitignore`:

```bash
nano .gitignore
```

Add:

```gitignore
# Secrets
*.vault
vault-password*
*.key
*.pem

# Runtime files
*.retry

# Temporary files
*.tmp
*.bak
*.swp

# Local caches
__pycache__/
.ansible/

# Evidence that has not been sanitized
evidence/raw/
```

Do not commit private SSH keys.

Do not commit plaintext passwords.

---

# 12. Step-by-Step Implementation

# Phase 8: Create the Inventory

Create:

```bash
nano inventories/production/hosts.ini
```

Add:

```ini
[webservers]
web01 ansible_host=192.168.56.21
web02 ansible_host=192.168.56.22

[appservers]
app01 ansible_host=192.168.56.23

[managed_servers:children]
webservers
appservers

[managed_servers:vars]
ansible_user=ansible
ansible_python_interpreter=/usr/bin/python3
```

Replace the addresses with yours.

---

# Understand the Inventory

You now have:

```text
managed_servers
│
├── webservers
│   ├── web01
│   └── web02
│
└── appservers
    └── app01
```

This lets you target:

```text
one host
one group
multiple groups
all managed servers
```

without rewriting commands.

---

# Phase 9: Verify Inventory Parsing

Run:

```bash
ansible-inventory \
    -i inventories/production/hosts.ini \
    --graph
```

Representative structure:

```text
@all:
  |--@ungrouped:
  |--@managed_servers:
  |  |--@webservers:
  |  |  |--web01
  |  |  |--web02
  |  |--@appservers:
  |     |--app01
```

Then:

```bash
ansible-inventory \
    -i inventories/production/hosts.ini \
    --list
```

This proves Ansible can parse the inventory.

It does **not** prove SSH works.

Those are different checks.

---

# Phase 10: First Ansible Connection

Run:

```bash
ansible \
    managed_servers \
    -i inventories/production/hosts.ini \
    -m ping
```

The Ansible `ping` module is not ICMP ping.

It verifies that Ansible can connect and execute the module successfully.

Representative result:

```text
web01 | SUCCESS => {
    "changed": false,
    "ping": "pong"
}
```

You should receive `pong` from all three hosts.

---

# If It Fails

Use verbose output:

```bash
ansible \
    web01 \
    -i inventories/production/hosts.ini \
    -m ping \
    -vvv
```

Then separately test:

```bash
ssh ansible@web01
```

Ask:

```text
Does name resolution work?
Is SSH reachable?
Does the key work?
Is the user correct?
Is Python installed?
Is host-key verification failing?
```

Do not randomly edit the playbook when the problem is actually SSH.

---

# Phase 11: Run Your First Ad-Hoc Commands

Check hostnames:

```bash
ansible \
    managed_servers \
    -i inventories/production/hosts.ini \
    -m command \
    -a "hostname"
```

Check uptime:

```bash
ansible \
    managed_servers \
    -i inventories/production/hosts.ini \
    -m command \
    -a "uptime"
```

Check disk:

```bash
ansible \
    managed_servers \
    -i inventories/production/hosts.ini \
    -m command \
    -a "df -h /"
```

This is already better than:

```text
SSH web01
run command
exit

SSH web02
run command
exit

SSH app01
run command
exit
```

But ad-hoc commands are not your final configuration-management model.

You need playbooks.

---

# Phase 12: Gather Facts

Run:

```bash
ansible \
    managed_servers \
    -i inventories/production/hosts.ini \
    -m setup
```

That produces a large amount of information.

Filter it:

```bash
ansible \
    managed_servers \
    -i inventories/production/hosts.ini \
    -m setup \
    -a "filter=ansible_distribution*"
```

Try:

```bash
ansible \
    managed_servers \
    -i inventories/production/hosts.ini \
    -m setup \
    -a "filter=ansible_memtotal_mb"
```

Facts allow playbooks to make decisions using discovered machine information.

---

# Phase 13: Create Ansible Configuration

Create:

```bash
nano ansible.cfg
```

Add:

```ini
[defaults]
inventory = inventories/production/hosts.ini
roles_path = roles
retry_files_enabled = False
interpreter_python = auto_silent

[ssh_connection]
pipelining = True
```

Now:

```bash
ansible managed_servers -m ping
```

You no longer need `-i` every time while working from this project directory.

Verify the configuration source:

```bash
ansible --version
```

Look for:

```text
config file = ...
```

Make sure it points to your project.

---

# 13. Configuration

# Phase 14: Build the Common Role

Create the role skeleton:

```bash
ansible-galaxy role init roles/common
```

Inspect:

```bash
find roles/common -maxdepth 2 -type f -print
```

You should see directories for tasks, handlers, defaults, templates, and others.

---

# Define Common Packages

Edit:

```bash
nano roles/common/defaults/main.yml
```

Add:

```yaml
---
common_packages:
  - curl
  - vim
  - git
  - unzip
  - ca-certificates
  - ufw
```

---

# Create Common Tasks

Edit:

```bash
nano roles/common/tasks/main.yml
```

Add:

```yaml
---
- name: Refresh APT package cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Install common administration packages
  ansible.builtin.apt:
    name: "{{ common_packages }}"
    state: present

- name: Create VERIQTA operations directory
  ansible.builtin.file:
    path: /opt/veriqta
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: Create VERIQTA configuration directory
  ansible.builtin.file:
    path: /etc/veriqta
    state: directory
    owner: root
    group: root
    mode: "0755"
```

Notice what you are **not** doing:

```yaml
shell: apt install ...
```

when a dedicated package module already exists.

Prefer purpose-built modules because they understand state.

---

# Phase 15: Create the First Playbook

Create:

```bash
nano playbooks/common.yml
```

Add:

```yaml
---
- name: Apply common Linux configuration
  hosts: managed_servers
  become: true
  gather_facts: true

  roles:
    - common
```

---

# Syntax Check

Always validate first:

```bash
ansible-playbook \
    playbooks/common.yml \
    --syntax-check
```

Expected:

```text
playbook: playbooks/common.yml
```

---

# Check Mode

Run:

```bash
ansible-playbook \
    playbooks/common.yml \
    --check \
    --diff
```

This asks Ansible to predict changes where supported.

Important:

**Check mode is useful, but it is not a perfect simulation.**

Some modules cannot completely predict future state.

A later task may also depend on something an earlier task would have created.

Use check mode as a safety tool, not as proof that deployment will succeed.

---

# Apply

```bash
ansible-playbook playbooks/common.yml
```

Watch the recap.

Representative:

```text
PLAY RECAP

app01  : ok=... changed=... unreachable=0 failed=0
web01  : ok=... changed=... unreachable=0 failed=0
web02  : ok=... changed=... unreachable=0 failed=0
```

---

# Verify

```bash
ansible \
    managed_servers \
    -b \
    -m command \
    -a "ls -ld /opt/veriqta /etc/veriqta"
```

Then:

```bash
ansible \
    managed_servers \
    -m command \
    -a "git --version"
```

---

# Phase 16: Prove Idempotency

Run the exact playbook again:

```bash
ansible-playbook playbooks/common.yml
```

You want to see approximately:

```text
changed=0
```

for resources that already match.

This is one of your most important project tests.

The first run answers:

> Can Ansible configure the server?

The second run asks:

> Can Ansible leave correct configuration alone?

---

# Phase 17: Build Group Variables

Create:

```bash
nano inventories/production/group_vars/all.yml
```

Add:

```yaml
---
platform_name: "VERIQTA Linux Platform"

platform_timezone: "Etc/UTC"

managed_admin_group: "platform-admins"

managed_users:
  - name: alice
    groups:
      - platform-admins

  - name: bob
    groups:
      - platform-admins
```

This is data.

Your tasks should consume the data.

That separation matters.

Instead of hard-coding:

```text
alice
bob
```

inside ten tasks, you define the desired users once.

---

# Phase 18: Manage Timezone

Add to `roles/common/tasks/main.yml`:

```yaml
- name: Configure system timezone
  community.general.timezone:
    name: "{{ platform_timezone }}"
```

Before using a collection-specific module, verify it is available:

```bash
ansible-galaxy collection list
```

If `community.general` is unavailable:

```bash
ansible-galaxy collection install community.general
```

A better repository should record external collection dependencies.

Create:

```bash
nano requirements.yml
```

Add:

```yaml
---
collections:
  - name: community.general
```

Future users can install them with:

```bash
ansible-galaxy collection install -r requirements.yml
```

---

# Phase 19: Build the Users Role

Create:

```bash
ansible-galaxy role init roles/users
```

Edit:

```bash
nano roles/users/tasks/main.yml
```

Add:

```yaml
---
- name: Create managed administrative group
  ansible.builtin.group:
    name: "{{ managed_admin_group }}"
    state: present

- name: Create managed users
  ansible.builtin.user:
    name: "{{ item.name }}"
    groups: "{{ item.groups | join(',') }}"
    append: true
    create_home: true
    shell: /bin/bash
    state: present
  loop: "{{ managed_users }}"
```

---

# Understand the Loop

Instead of:

```yaml
create alice
create bob
create carol
create david
```

you describe a collection:

```yaml
managed_users:
  - alice
  - bob
```

and process each item.

This is one of the ways automation scales.

---

# Create the Users Playbook

```bash
nano playbooks/users.yml
```

Add:

```yaml
---
- name: Manage Linux identities
  hosts: managed_servers
  become: true
  gather_facts: false

  roles:
    - users
```

Validate:

```bash
ansible-playbook \
    playbooks/users.yml \
    --syntax-check
```

Preview:

```bash
ansible-playbook \
    playbooks/users.yml \
    --check \
    --diff
```

Apply:

```bash
ansible-playbook playbooks/users.yml
```

---

# Verify Centrally

```bash
ansible \
    managed_servers \
    -m command \
    -a "id alice"
```

Then:

```bash
ansible \
    managed_servers \
    -m command \
    -a "id bob"
```

---

# SSH Public Keys

Do not invent fake production keys.

For your lab, generate dedicated lab keys or use public keys you deliberately created for the project.

A variable might eventually look like:

```yaml
managed_users:
  - name: alice
    groups:
      - platform-admins
    ssh_public_keys:
      - "ssh-ed25519 AAAA... alice-lab"
```

Then use:

```yaml
- name: Install managed SSH public keys
  ansible.posix.authorized_key:
    user: "{{ item.0.name }}"
    key: "{{ item.1 }}"
    state: present
  loop: "{{ managed_users | subelements('ssh_public_keys', skip_missing=True) }}"
```

If using `ansible.posix`, add it to `requirements.yml`:

```yaml
collections:
  - name: community.general
  - name: ansible.posix
```

Never commit private keys.

---

# 14. Automation

# Phase 20: Build the Security Role

Create:

```bash
ansible-galaxy role init roles/security
```

Your security role will manage:

```text
UFW
SSH configuration
basic access controls
```

But you must be careful.

Automating security configuration can lock you out of every managed machine simultaneously.

That means the deployment method matters just as much as the desired setting.

---

# Configure UFW

Edit:

```bash
nano roles/security/tasks/main.yml
```

Start with:

```yaml
---
- name: Install UFW
  ansible.builtin.apt:
    name: ufw
    state: present

- name: Allow SSH before enabling firewall
  community.general.ufw:
    rule: allow
    name: OpenSSH

- name: Set default incoming policy
  community.general.ufw:
    direction: incoming
    policy: deny

- name: Set default outgoing policy
  community.general.ufw:
    direction: outgoing
    policy: allow

- name: Enable UFW
  community.general.ufw:
    state: enabled
```

Notice the order.

You permit SSH **before** enabling the restrictive firewall.

That reduces the risk of locking out your management channel.

---

# Web-Specific Firewall Rules

Create:

```bash
nano inventories/production/group_vars/webservers.yml
```

Add:

```yaml
---
web_http_port: 80
web_https_port: 443
```

Later, the webserver role will allow those ports.

---

# Phase 21: Manage SSH Configuration Safely

Do not start by replacing the entire `/etc/ssh/sshd_config`.

Use a managed drop-in.

Create:

```bash
mkdir -p roles/security/templates
```

Create:

```bash
nano roles/security/templates/99-veriqta-hardening.conf.j2
```

Add:

```text
# Managed by Ansible.
# Manual changes may be overwritten.

PermitRootLogin no
MaxAuthTries 4
X11Forwarding no
```

We are deliberately **not** disabling password authentication yet.

Why?

Because you should first prove that key-based access works for every required administrative account before removing fallback access.

Security automation should not turn into self-inflicted lockout automation.

---

# Add SSH Tasks

Append to `roles/security/tasks/main.yml`:

```yaml
- name: Install managed SSH hardening drop-in
  ansible.builtin.template:
    src: 99-veriqta-hardening.conf.j2
    dest: /etc/ssh/sshd_config.d/99-veriqta-hardening.conf
    owner: root
    group: root
    mode: "0644"
    validate: "/usr/sbin/sshd -t -f %s"
  notify: Restart SSH
```

---

# Why `validate` Matters

The template is written to a temporary location first.

Ansible runs:

```text
sshd -t -f TEMPORARY_FILE
```

before accepting it.

The exact behavior of configuration validation deserves testing because SSH configurations can include other files and depend on surrounding configuration.

For critical services, do not assume template syntax alone proves the complete live configuration is valid.

Your post-deployment validation will still run:

```bash
sudo sshd -t
```

on the actual host.

---

# Create the Handler

Edit:

```bash
nano roles/security/handlers/main.yml
```

Add:

```yaml
---
- name: Restart SSH
  ansible.builtin.service:
    name: ssh
    state: restarted
```

A handler runs only when notified by a changed task.

So:

```text
template unchanged
        |
        v
no restart


template changed
        |
        v
handler notified
        |
        v
SSH restarted
```

This is much better than restarting SSH every time the playbook runs.

---

# Phase 22: Deploy Security Gradually

Create:

```bash
nano playbooks/security.yml
```

Add:

```yaml
---
- name: Apply Linux security baseline
  hosts: managed_servers
  become: true
  gather_facts: true
  serial: 1

  roles:
    - security
```

The important line is:

```yaml
serial: 1
```

You are telling Ansible:

> Process one managed server at a time.

That reduces blast radius.

If the SSH configuration breaks `web01`, Ansible does not immediately destroy access to `web02` and `app01`.

---

# Validate

```bash
ansible-playbook \
    playbooks/security.yml \
    --syntax-check
```

Preview:

```bash
ansible-playbook \
    playbooks/security.yml \
    --check \
    --diff
```

Apply:

```bash
ansible-playbook playbooks/security.yml
```

---

# Immediately Verify SSH

```bash
ansible managed_servers -m ping
```

Then:

```bash
ansible \
    managed_servers \
    -b \
    -m command \
    -a "/usr/sbin/sshd -t"
```

Also inspect effective SSH configuration where useful:

```bash
ansible \
    managed_servers \
    -b \
    -m shell \
    -a "/usr/sbin/sshd -T | grep -E '^(permitrootlogin|maxauthtries|x11forwarding) '"
```

Use `shell` here because the pipeline requires shell interpretation.

Do not use `shell` by habit when a dedicated module or `command` is sufficient.

---

# 15. Security

# Phase 23: Understand Secret Management

Sooner or later configuration management encounters secrets:

```text
database passwords
API tokens
TLS private keys
service credentials
application secrets
```

Do not place them directly in plaintext Git-tracked YAML files.

Bad:

```yaml
database_password: SuperSecretPassword123
```

Instead, Ansible provides Vault.

Create a lab vault file:

```bash
ansible-vault create \
    inventories/production/group_vars/vault.yml
```

Enter a lab-only secret such as:

```yaml
vault_demo_secret: "replace-with-lab-secret"
```

Inspect the file:

```bash
cat \
    inventories/production/group_vars/vault.yml
```

You should see encrypted vault content rather than plaintext.

---

# View It

```bash
ansible-vault view \
    inventories/production/group_vars/vault.yml
```

---

# Edit It

```bash
ansible-vault edit \
    inventories/production/group_vars/vault.yml
```

---

# Important Vault Limitation

Ansible Vault protects encrypted content at rest.

It does not solve every secrets-management problem.

Once a secret is decrypted for use:

```text
processes
temporary files
application configuration
logs
permissions
backup systems
```

can still become part of your threat model.

Production environments may use dedicated secret-management systems.

For this project, you need to understand the principle:

```text
Secret
   |
   X
plaintext Git


Secret
   |
   v
encrypted storage
   |
   v
controlled runtime use
```

---

# Phase 24: Build the Webserver Role

Create:

```bash
ansible-galaxy role init roles/webserver
```

---

# Default Variables

Edit:

```bash
nano roles/webserver/defaults/main.yml
```

Add:

```yaml
---
web_package: nginx
web_service: nginx
web_root: /var/www/veriqta
web_server_name: "_"
web_http_port: 80
```

---

# Create the Nginx Template

```bash
nano roles/webserver/templates/veriqta.conf.j2
```

Add:

```nginx
server {
    listen {{ web_http_port }} default_server;
    listen [::]:{{ web_http_port }} default_server;

    server_name {{ web_server_name }};

    root {{ web_root }};
    index index.html;

    access_log /var/log/nginx/veriqta_access.log;
    error_log /var/log/nginx/veriqta_error.log;

    location / {
        try_files $uri $uri/ =404;
    }
}
```

---

# Create Website Template

```bash
nano roles/webserver/templates/index.html.j2
```

Add:

```html
<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>{{ inventory_hostname }} | VERIQTA</title>
</head>
<body>
    <h1>VERIQTA Managed Linux Platform</h1>

    <p>This server is managed by Ansible.</p>

    <ul>
        <li>Host: {{ inventory_hostname }}</li>
        <li>Environment: Production Lab</li>
        <li>Platform: {{ platform_name }}</li>
    </ul>
</body>
</html>
```

Now `web01` and `web02` can receive similar configuration while still rendering host-specific information.

---

# Create Webserver Tasks

Edit:

```bash
nano roles/webserver/tasks/main.yml
```

Add:

```yaml
---
- name: Install Nginx
  ansible.builtin.apt:
    name: "{{ web_package }}"
    state: present

- name: Create website document root
  ansible.builtin.file:
    path: "{{ web_root }}"
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: Deploy website
  ansible.builtin.template:
    src: index.html.j2
    dest: "{{ web_root }}/index.html"
    owner: root
    group: root
    mode: "0644"

- name: Deploy Nginx virtual host
  ansible.builtin.template:
    src: veriqta.conf.j2
    dest: /etc/nginx/sites-available/veriqta
    owner: root
    group: root
    mode: "0644"
  notify: Validate and reload Nginx

- name: Enable VERIQTA site
  ansible.builtin.file:
    src: /etc/nginx/sites-available/veriqta
    dest: /etc/nginx/sites-enabled/veriqta
    state: link
  notify: Validate and reload Nginx

- name: Disable default Nginx site
  ansible.builtin.file:
    path: /etc/nginx/sites-enabled/default
    state: absent
  notify: Validate and reload Nginx

- name: Allow HTTP through firewall
  community.general.ufw:
    rule: allow
    port: "{{ web_http_port | string }}"
    proto: tcp

- name: Ensure Nginx is enabled and running
  ansible.builtin.service:
    name: "{{ web_service }}"
    enabled: true
    state: started
```

---

# Nginx Handler

Create:

```bash
nano roles/webserver/handlers/main.yml
```

Add:

```yaml
---
- name: Validate Nginx configuration
  ansible.builtin.command:
    cmd: nginx -t
  changed_when: false
  listen: Validate and reload Nginx

- name: Reload Nginx
  ansible.builtin.service:
    name: nginx
    state: reloaded
  listen: Validate and reload Nginx
```

Now a changed configuration triggers:

```text
Template changed
      |
      v
handler notification
      |
      v
nginx -t
      |
      +------ failure ------> stop
      |
    success
      |
      v
reload nginx
```

That is much safer than blindly restarting Nginx after every file change.

---

# Create Webserver Playbook

```bash
nano playbooks/webservers.yml
```

Add:

```yaml
---
- name: Configure managed web servers
  hosts: webservers
  become: true
  gather_facts: true
  serial: 1

  roles:
    - webserver
```

Validate:

```bash
ansible-playbook \
    playbooks/webservers.yml \
    --syntax-check
```

Preview:

```bash
ansible-playbook \
    playbooks/webservers.yml \
    --check \
    --diff
```

Deploy:

```bash
ansible-playbook playbooks/webservers.yml
```

---

# Verify Nginx

```bash
ansible \
    webservers \
    -b \
    -m command \
    -a "nginx -t"
```

Check service:

```bash
ansible \
    webservers \
    -m command \
    -a "systemctl is-active nginx"
```

Check HTTP from `control01`:

```bash
curl http://web01
```

Then:

```bash
curl http://web02
```

You should see different hostnames rendered by the same template.

---

# 16. Testing

# Phase 25: Build Validation Playbooks

Create:

```bash
nano playbooks/validate.yml
```

Add:

```yaml
---
- name: Validate managed Linux environment
  hosts: managed_servers
  become: true
  gather_facts: true

  tasks:

    - name: Verify VERIQTA directory exists
      ansible.builtin.stat:
        path: /opt/veriqta
      register: veriqta_directory

    - name: Assert VERIQTA directory exists
      ansible.builtin.assert:
        that:
          - veriqta_directory.stat.exists
          - veriqta_directory.stat.isdir
        fail_msg: "/opt/veriqta is missing"
        success_msg: "/opt/veriqta exists"

    - name: Verify Alice exists
      ansible.builtin.command:
        cmd: id alice
      changed_when: false

    - name: Verify firewall status
      ansible.builtin.command:
        cmd: ufw status
      register: ufw_status
      changed_when: false

    - name: Assert firewall is active
      ansible.builtin.assert:
        that:
          - "'Status: active' in ufw_status.stdout"
        fail_msg: "UFW is not active"
        success_msg: "UFW is active"
```

Run:

```bash
ansible-playbook playbooks/validate.yml
```

---

# Web Validation

Add another play:

```yaml
- name: Validate web servers
  hosts: webservers
  become: true
  gather_facts: false

  tasks:

    - name: Validate Nginx configuration
      ansible.builtin.command:
        cmd: nginx -t
      changed_when: false

    - name: Check Nginx service
      ansible.builtin.command:
        cmd: systemctl is-active nginx
      register: nginx_state
      changed_when: false

    - name: Assert Nginx is active
      ansible.builtin.assert:
        that:
          - nginx_state.stdout == "active"

    - name: Verify local website
      ansible.builtin.uri:
        url: http://127.0.0.1/
        status_code: 200
        return_content: true
      register: web_response

    - name: Verify managed page content
      ansible.builtin.assert:
        that:
          - "'VERIQTA Managed Linux Platform' in web_response.content"
```

Now validation is part of the repository.

It is not something you remember to do manually only when convenient.

---

# Phase 26: Test Idempotency Properly

Run:

```bash
ansible-playbook site.yml
```

Record the recap.

Run it again:

```bash
ansible-playbook site.yml
```

The second run should normally show:

```text
changed=0
```

for stable configuration.

If something changes every time, investigate.

A task that reports `changed` forever may be:

```text
poorly designed
using shell unnecessarily
generating non-deterministic content
rewriting timestamps
restarting services unnecessarily
```

Idempotency is not cosmetic.

It is an operational property.

---

# 17. Failure Simulation

# Failure Lab 1: Configuration Drift

Manually change `web01`.

SSH into it:

```bash
ssh ansible@web01
```

Change the website:

```bash
sudo nano /var/www/veriqta/index.html
```

Replace it with:

```html
<h1>MANUAL CONFIGURATION DRIFT</h1>
```

Exit.

Now:

```bash
curl http://web01
```

The machine no longer matches declared state.

Preview the difference:

```bash
ansible-playbook \
    playbooks/webservers.yml \
    --check \
    --diff \
    --limit web01
```

You should see that Ansible wants to restore the managed template.

Apply:

```bash
ansible-playbook \
    playbooks/webservers.yml \
    --limit web01
```

Verify:

```bash
curl http://web01
```

The unauthorized manual change should be gone.

This is configuration drift correction.

---

# Failure Lab 2: Package Removed Manually

On `web01`:

```bash
sudo apt remove -y nginx
```

Check:

```bash
dpkg -l nginx
```

Now run:

```bash
ansible-playbook \
    playbooks/webservers.yml \
    --limit web01
```

Ansible should restore the declared package state.

Then verify:

```bash
curl http://web01
```

---

# Failure Lab 3: Service Stopped

On `web02`:

```bash
sudo systemctl stop nginx
```

Check:

```bash
systemctl is-active nginx
```

Run:

```bash
ansible-playbook \
    playbooks/webservers.yml \
    --limit web02
```

Verify:

```bash
systemctl is-active nginx
```

Expected:

```text
active
```

You have now demonstrated three kinds of drift:

```text
file drift
package drift
service-state drift
```

---

# Failure Lab 4: SSH Unreachable

Do this only from a VM console or snapshot-backed lab where you can recover access.

On one managed host, temporarily stop SSH:

```bash
sudo systemctl stop ssh
```

From `control01`:

```bash
ansible web01 -m ping
```

You should receive an unreachable result.

This distinction matters:

```text
FAILED
```

usually means Ansible reached the machine but a task failed.

```text
UNREACHABLE
```

means Ansible could not establish the management connection.

Restore SSH from the VM console:

```bash
sudo systemctl start ssh
```

Then:

```bash
ansible web01 -m ping
```

---

# Failure Lab 5: Wrong SSH User

Temporarily change inventory:

```ini
ansible_user=does-not-exist
```

Run:

```bash
ansible web01 -m ping -vv
```

Observe the authentication/user failure.

Restore:

```ini
ansible_user=ansible
```

Verify.

---

# Failure Lab 6: Invalid YAML

Back up a playbook:

```bash
cp \
    playbooks/common.yml \
    playbooks/common.yml.before-test
```

Break indentation deliberately.

Run:

```bash
ansible-playbook \
    playbooks/common.yml \
    --syntax-check
```

Observe the parser error.

Restore:

```bash
mv \
    playbooks/common.yml.before-test \
    playbooks/common.yml
```

Validate again.

This is why syntax validation belongs before deployment.

---

# Failure Lab 7: Undefined Variable

Temporarily reference:

```yaml
{{ variable_that_does_not_exist }}
```

Run the playbook.

Observe the undefined-variable error.

Then restore the correct variable.

The lesson:

```text
YAML valid
does not mean
Ansible data model valid
```

---

# Failure Lab 8: Invalid Nginx Configuration

Do not deploy an intentionally broken live Nginx configuration without protection.

Create a temporary broken template.

For example:

```nginx
server {
    THIS_IS_NOT_VALID_NGINX;
}
```

Use validation logic to prove it is rejected before a service reload.

Your desired pipeline should be:

```text
Render
  |
  v
Validate
  |
  +-- invalid --> reject
  |
  +-- valid ----> install/reload
```

Never:

```text
Render
  |
  v
Replace live config
  |
  v
Restart
  |
  v
Discover outage
```

---

# Failure Lab 9: Privilege Escalation Failure

On a disposable/snapshot-backed managed host, remove the `ansible` user from the sudo group:

```bash
sudo deluser ansible sudo
```

From `control01`:

```bash
ansible \
    web01 \
    -b \
    -m command \
    -a "id"
```

Observe the privilege escalation failure.

Restore from another administrative account or VM console:

```bash
sudo usermod -aG sudo ansible
```

Reconnect so group membership is refreshed.

Verify:

```bash
ssh ansible@web01
```

Then:

```bash
sudo whoami
```

---

# Failure Lab 10: Firewall Misconfiguration

This is a high-risk failure simulation because the firewall protects your management path.

Use a VM console and snapshot.

Do not perform it on a remote machine you cannot access another way.

Your investigation should answer:

```text
Is SSH listening?
Is the service active?
Is the IP correct?
Is routing correct?
Is the firewall blocking the connection?
```

That distinction is important.

A firewall problem can make a healthy SSH daemon appear unavailable from the network.

---

# Failure Lab 11: Wrong Inventory Address

Change one host's `ansible_host` to an unused lab IP.

Run:

```bash
ansible web01 -m ping -vv
```

Observe the network failure.

Restore the correct IP.

Verify:

```bash
ansible web01 -m ping
```

---

# Failure Lab 12: Template Variable Mistake

Change:

```jinja2
{{ platform_name }}
```

to:

```jinja2
{{ platform_nam }}
```

Run:

```bash
ansible-playbook \
    playbooks/webservers.yml \
    --check \
    --limit web01
```

Observe the undefined variable.

Restore.

---

# 18. Troubleshooting

When Ansible fails, avoid immediately rewriting the playbook.

Determine which layer failed.

Use:

```text
CONTROL NODE
    |
    v
INVENTORY
    |
    v
NAME / IP
    |
    v
NETWORK
    |
    v
SSH
    |
    v
AUTHENTICATION
    |
    v
PYTHON
    |
    v
SUDO
    |
    v
VARIABLES
    |
    v
MODULE
    |
    v
TARGET RESOURCE
    |
    v
SERVICE
    |
    v
APPLICATION
```

---

## Scenario: Host Is Unreachable

Start with:

```bash
ansible web01 -m ping -vvv
```

Then test:

```bash
getent hosts web01
```

Then:

```bash
ssh ansible@web01
```

Then:

```bash
ip route
```

If necessary:

```bash
nc -vz -w 3 web01 22
```

Do not debug YAML if TCP 22 is unreachable.

---

## Scenario: SSH Works but Ansible Fails

Check:

```bash
ansible web01 -m ping -vvv
```

Then:

```bash
ansible web01 -m raw -a "python3 --version"
```

Check inventory:

```bash
ansible-inventory --host web01
```

The problem may involve:

```text
Python
interpreter selection
inventory variables
module execution
```

---

## Scenario: Non-Privileged Tasks Work but Privileged Tasks Fail

Test:

```bash
ansible web01 -m command -a "whoami"
```

Then:

```bash
ansible web01 -b -m command -a "whoami"
```

Compare the results.

Check manually:

```bash
ssh ansible@web01
```

Then:

```bash
sudo whoami
```

You have now isolated the problem to privilege escalation.

---

## Scenario: Playbook Parses but Task Fails

Use:

```bash
ansible-playbook \
    playbooks/webservers.yml \
    --limit web01 \
    -vv
```

Read the failing task.

Look at:

```text
module arguments
stderr
stdout
return code
host
task name
```

Do not focus only on the final `failed=1`.

The useful evidence is usually above it.

---

## Scenario: Configuration Changes Every Run

Run:

```bash
ansible-playbook site.yml --diff
```

Identify the task repeatedly reporting `changed`.

Ask:

```text
Does it contain a timestamp?
Does it use shell unnecessarily?
Does it recreate a file?
Does it generate random data?
Does it restart a service unconditionally?
Is the module's changed status being interpreted correctly?
```

This is an idempotency investigation.

---

## Scenario: Handler Never Runs

Check whether the notifying task actually reports:

```text
changed
```

Handlers are normally triggered by changes.

If the task reports:

```text
ok
```

there is no notification.

Also verify:

```yaml
notify: Validate and reload Nginx
```

matches the handler listener/name.

---

## Scenario: Handler Runs Every Time

The upstream task is probably reporting changes every time.

Fix the task.

Do not hide a bad task by trying to suppress the handler.

---

# 19. Final Validation

Now validate the whole platform.

## Inventory

```bash
ansible-inventory --graph
```

---

## Connectivity

```bash
ansible managed_servers -m ping
```

---

## Facts

```bash
ansible \
    managed_servers \
    -m setup \
    -a "filter=ansible_distribution*"
```

---

## Users

```bash
ansible \
    managed_servers \
    -m command \
    -a "id alice"
```

---

## Packages

```bash
ansible \
    managed_servers \
    -m command \
    -a "git --version"
```

---

## Managed Directories

```bash
ansible \
    managed_servers \
    -b \
    -m command \
    -a "stat -c '%U %G %a %n' /opt/veriqta /etc/veriqta"
```

---

## Firewall

```bash
ansible \
    managed_servers \
    -b \
    -m command \
    -a "ufw status"
```

---

## SSH

```bash
ansible \
    managed_servers \
    -b \
    -m command \
    -a "/usr/sbin/sshd -t"
```

---

## Nginx

```bash
ansible \
    webservers \
    -b \
    -m command \
    -a "nginx -t"
```

---

## Nginx Service

```bash
ansible \
    webservers \
    -m command \
    -a "systemctl is-active nginx"
```

---

## Website

```bash
curl http://web01
```

```bash
curl http://web02
```

---

## Validation Playbook

```bash
ansible-playbook playbooks/validate.yml
```

---

## Idempotency

Run:

```bash
ansible-playbook site.yml
```

Then immediately:

```bash
ansible-playbook site.yml
```

Investigate unexpected changes.

---

# Build the Master Playbook

Create:

```bash
nano site.yml
```

Add:

```yaml
---
- name: Configure base Linux platform
  hosts: managed_servers
  become: true
  gather_facts: true

  roles:
    - common
    - users

- name: Apply security baseline
  hosts: managed_servers
  become: true
  gather_facts: true
  serial: 1

  roles:
    - security

- name: Configure web infrastructure
  hosts: webservers
  become: true
  gather_facts: true
  serial: 1

  roles:
    - webserver

- name: Configure application infrastructure
  hosts: appservers
  become: true
  gather_facts: true

  roles:
    - application
```

We still need the application role.

Let's build it properly.

---

# Application Role

Create:

```bash
ansible-galaxy role init roles/application
```

We will deploy a simple systemd-managed service.

This teaches something more useful than copying another HTML file.

---

# Create the Application Script

Create:

```bash
nano roles/application/templates/veriqta-status-server.py.j2
```

Add:

```python
#!/usr/bin/env python3

from http.server import BaseHTTPRequestHandler, HTTPServer
import json
import socket
from datetime import datetime, timezone

HOST = "127.0.0.1"
PORT = {{ application_port }}

class Handler(BaseHTTPRequestHandler):
    def do_GET(self):
        if self.path == "/health":
            payload = {
                "status": "ok",
                "host": socket.gethostname(),
                "service": "veriqta-status",
                "time": datetime.now(timezone.utc).isoformat()
            }

            body = json.dumps(payload).encode()

            self.send_response(200)
            self.send_header("Content-Type", "application/json")
            self.send_header("Content-Length", str(len(body)))
            self.end_headers()
            self.wfile.write(body)
            return

        self.send_response(404)
        self.end_headers()

    def log_message(self, format, *args):
        print(
            "%s - %s" %
            (
                self.address_string(),
                format % args
            ),
            flush=True
        )

HTTPServer((HOST, PORT), Handler).serve_forever()
```

The service listens only on:

```text
127.0.0.1
```

It is not intended to be exposed externally.

---

# Application Defaults

Edit:

```bash
nano roles/application/defaults/main.yml
```

Add:

```yaml
---
application_user: veriqta-app
application_group: veriqta-app
application_directory: /opt/veriqta/status-service
application_port: 8080
application_service_name: veriqta-status
```

---

# systemd Template

Create:

```bash
nano roles/application/templates/veriqta-status.service.j2
```

Add:

```ini
[Unit]
Description=VERIQTA Status Service
After=network.target

[Service]
Type=simple
User={{ application_user }}
Group={{ application_group }}
WorkingDirectory={{ application_directory }}
ExecStart=/usr/bin/python3 {{ application_directory }}/server.py
Restart=on-failure
RestartSec=3

NoNewPrivileges=true
PrivateTmp=true

[Install]
WantedBy=multi-user.target
```

---

# Application Tasks

Edit:

```bash
nano roles/application/tasks/main.yml
```

Add:

```yaml
---
- name: Create application group
  ansible.builtin.group:
    name: "{{ application_group }}"
    system: true
    state: present

- name: Create application user
  ansible.builtin.user:
    name: "{{ application_user }}"
    group: "{{ application_group }}"
    system: true
    create_home: false
    shell: /usr/sbin/nologin
    state: present

- name: Create application directory
  ansible.builtin.file:
    path: "{{ application_directory }}"
    state: directory
    owner: "{{ application_user }}"
    group: "{{ application_group }}"
    mode: "0750"

- name: Deploy application
  ansible.builtin.template:
    src: veriqta-status-server.py.j2
    dest: "{{ application_directory }}/server.py"
    owner: "{{ application_user }}"
    group: "{{ application_group }}"
    mode: "0750"
  notify: Restart VERIQTA status service

- name: Deploy systemd service
  ansible.builtin.template:
    src: veriqta-status.service.j2
    dest: "/etc/systemd/system/{{ application_service_name }}.service"
    owner: root
    group: root
    mode: "0644"
  notify:
    - Reload systemd
    - Restart VERIQTA status service

- name: Ensure application service is enabled and running
  ansible.builtin.systemd_service:
    name: "{{ application_service_name }}"
    enabled: true
    state: started
    daemon_reload: true
```

---

# Application Handlers

Edit:

```bash
nano roles/application/handlers/main.yml
```

Add:

```yaml
---
- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart VERIQTA status service
  ansible.builtin.systemd_service:
    name: "{{ application_service_name }}"
    state: restarted
```

---

# Deploy

```bash
ansible-playbook site.yml --syntax-check
```

Then:

```bash
ansible-playbook site.yml
```

---

# Verify Application

```bash
ansible \
    appservers \
    -m command \
    -a "systemctl is-active veriqta-status"
```

Then from `app01`:

```bash
curl http://127.0.0.1:8080/health
```

Representative response:

```json
{
  "status": "ok",
  "host": "app01",
  "service": "veriqta-status",
  "time": "..."
}
```

You now manage an actual application service through Ansible.

---

# Tags

As your platform grows, running everything every time becomes inefficient.

Add tags at play or role level.

For example:

```yaml
roles:
  - role: common
    tags:
      - common

  - role: users
    tags:
      - users
```

Then:

```bash
ansible-playbook site.yml --tags users
```

Or:

```bash
ansible-playbook site.yml --tags webserver
```

Or skip:

```bash
ansible-playbook site.yml --skip-tags users
```

Use tags deliberately.

They are not a replacement for good architecture.

---

# Limit Deployments

You should also be comfortable with:

```bash
ansible-playbook \
    site.yml \
    --limit web01
```

This becomes extremely useful during:

```text
testing
canary deployment
troubleshooting
recovery
```

---

# 20. Portfolio Documentation

Your README should explain the platform as an engineering system.

Recommended structure:

```text
# Automated Linux Configuration Management Platform

## Overview
## Problem
## Project Goals
## Architecture
## Environment
## Control Node
## Managed Nodes
## SSH Architecture
## Inventory Design
## Variable Design
## Role Architecture
## Common Configuration
## User Management
## Security Configuration
## Web Infrastructure
## Application Deployment
## Jinja2 Templates
## Handlers
## Firewall Management
## Secrets Management
## Validation
## Idempotency
## Configuration Drift
## Deployment Strategy
## Failure Simulations
## Troubleshooting
## Security Considerations
## Known Limitations
## Production Improvements
## Cleanup
## Lessons Learned
```

---

# Architecture Documentation

Include:

```text
                     Git Repository
                          |
                          v
                      control01
                          |
                     Ansible Core
                          |
             +------------+------------+
             |            |            |
             v            v            v
           web01        web02        app01
             |            |            |
             +------------+------------+
                          |
                  Desired Linux State
```

---

# Document Configuration Ownership

Make it clear which files are managed.

For example:

| Resource            | Owner   |
| ------------------- | ------- |
| `/opt/veriqta`      | Ansible |
| `/etc/veriqta`      | Ansible |
| Managed users       | Ansible |
| UFW baseline        | Ansible |
| SSH drop-in         | Ansible |
| Nginx site          | Ansible |
| Website content     | Ansible |
| Application service | Ansible |

Once a resource is declared as automation-managed, manual edits should normally be treated as drift.

---

# 21. GitHub Structure

Place the project under:

```text
04-Projects/
└── Mid-Level/
    └── 03-Automated-Linux-Configuration-Management-Platform/
```

Recommended final structure:

```text
03-Automated-Linux-Configuration-Management-Platform/
│
├── README.md
├── PREREQUISITES.md
├── ENVIRONMENT.md
├── SETUP.md
├── ansible.cfg
├── requirements.yml
├── site.yml
├── .gitignore
│
├── inventories/
│   └── production/
│       ├── hosts.ini
│       ├── group_vars/
│       │   ├── all.yml
│       │   └── webservers.yml
│       └── host_vars/
│
├── playbooks/
│   ├── common.yml
│   ├── users.yml
│   ├── security.yml
│   ├── webservers.yml
│   └── validate.yml
│
├── roles/
│   ├── common/
│   ├── users/
│   ├── security/
│   ├── webserver/
│   └── application/
│
├── scripts/
│   ├── preflight.sh
│   ├── validate.sh
│   └── idempotency-test.sh
│
├── tests/
│   ├── connectivity.md
│   ├── idempotency.md
│   ├── drift.md
│   ├── web-validation.md
│   └── application-validation.md
│
├── failure-labs/
│   ├── 01-file-drift.md
│   ├── 02-package-drift.md
│   ├── 03-service-drift.md
│   ├── 04-ssh-unreachable.md
│   ├── 05-wrong-ssh-user.md
│   ├── 06-invalid-yaml.md
│   ├── 07-undefined-variable.md
│   ├── 08-invalid-nginx-config.md
│   ├── 09-sudo-failure.md
│   ├── 10-firewall-lockout.md
│   ├── 11-wrong-inventory-address.md
│   └── 12-template-variable-error.md
│
├── runbooks/
│   ├── host-unreachable.md
│   ├── privilege-escalation-failure.md
│   ├── playbook-failure.md
│   ├── service-deployment-failure.md
│   ├── configuration-drift.md
│   └── rollback.md
│
├── docs/
│   ├── architecture.md
│   ├── inventory-design.md
│   ├── role-design.md
│   ├── variable-design.md
│   ├── security.md
│   ├── secrets.md
│   ├── idempotency.md
│   ├── deployment-strategy.md
│   ├── limitations.md
│   └── production-improvements.md
│
├── evidence/
│   └── README.md
│
└── verification/
    └── FINAL-VALIDATION.md
```

---

# 22. Resume Entry

## Automated Linux Configuration Management Platform

**Technologies:** Linux, Ansible, YAML, Jinja2, SSH, systemd, UFW, Nginx, Git

After completing the project, you can truthfully describe it with bullets such as:

* Built an Ansible-based configuration management platform to enforce consistent package, user, filesystem, firewall, SSH, service, and application configuration across multiple Ubuntu Linux servers.

* Designed reusable Ansible inventories, variables, roles, Jinja2 templates, handlers, validation playbooks, staged deployments, and idempotency tests to manage infrastructure through declared state rather than manual server configuration.

* Simulated and recovered from configuration drift, package removal, stopped services, SSH failures, privilege-escalation failures, inventory errors, template errors, and invalid service configurations while documenting operational troubleshooting runbooks.

---

# 23. Interview Preparation

### What is configuration management?

Configuration management is the systematic definition, application, and maintenance of desired system state across infrastructure.

### What is configuration drift?

Drift occurs when the actual configuration of a system diverges from the intended configuration.

### Why is manual configuration dangerous at scale?

Because repeated human changes become inconsistent, difficult to audit, difficult to reproduce, and prone to omission.

### What is Ansible?

Ansible is an automation and configuration-management platform that can manage remote systems, commonly through SSH.

### Does Ansible require an agent?

Its common Linux management model is agentless. The control node connects to managed hosts, typically through SSH.

### What is an inventory?

An inventory defines the hosts and groups Ansible manages.

### Why use groups?

Groups let you apply shared configuration to machines with common responsibilities.

For example:

```text
webservers
appservers
database_servers
```

### What is a playbook?

A playbook is YAML describing plays and tasks that Ansible should execute against selected hosts.

### What is a role?

A role packages related:

```text
tasks
handlers
templates
files
variables
defaults
```

into a reusable structure.

### What is idempotency?

An idempotent operation can be repeated without unnecessarily changing a system that already matches the desired state.

### Why is idempotency important?

Because automation should converge systems toward a desired state rather than repeatedly disrupting already-correct resources.

### What is a handler?

A handler is a task normally triggered by a change notification.

A common example is reloading Nginx only when its configuration changes.

### What is Jinja2?

Jinja2 is a templating engine Ansible uses to generate configuration from variables and templates.

### What is `become`?

`become` allows Ansible to perform tasks using privilege escalation, commonly through sudo.

### What is check mode?

Check mode attempts to predict changes without applying them.

It is useful but not guaranteed to perfectly simulate every module or dependency.

### What is diff mode?

Diff mode shows differences for supported resources, especially managed text files and templates.

### Why use `serial`?

`serial` limits how many hosts are processed at once.

It reduces deployment blast radius.

### Why validate Nginx before reloading it?

A malformed configuration can cause a reload or restart to fail. Validation catches the problem before disrupting the live service.

### Why should Ansible not use `shell` for everything?

Dedicated modules understand resource state and are usually more idempotent, structured, and predictable.

### What is Ansible Vault?

Vault encrypts sensitive Ansible data at rest.

### Does Vault solve all secrets-management problems?

No. Secrets still need protection when decrypted and used by applications or automation.

### What is the difference between `failed` and `unreachable`?

`unreachable` generally indicates Ansible could not establish the management connection.

`failed` indicates a task reached execution but did not complete successfully.

### How would you troubleshoot one unreachable host?

Start with inventory, name/IP resolution, routing, TCP 22, SSH authentication, host keys, and then Ansible-specific configuration.

### Why should automation be stored in Git?

It provides version history, reviewability, collaboration, rollback context, and an auditable record of intended infrastructure configuration.

### Why is manual modification of Ansible-managed files dangerous?

Because Ansible may overwrite those changes the next time the declared state is applied.

### What is infrastructure as code?

It is the practice of representing infrastructure configuration and desired state in version-controlled machine-readable definitions.

This project is primarily configuration management, but it applies many infrastructure-as-code principles.

---

# 24. Cleanup

Configuration management creates a special cleanup problem.

Ansible deliberately changed multiple servers.

You must know exactly which resources belong to the project.

Do not use broad destructive commands.

---

## Project-Created Resources

Potential managed resources include:

```text
/opt/veriqta
/etc/veriqta

alice
bob
platform-admins

/etc/ssh/sshd_config.d/99-veriqta-hardening.conf

Nginx
/var/www/veriqta
/etc/nginx/sites-available/veriqta
/etc/nginx/sites-enabled/veriqta

veriqta-app user
veriqta-app group

/opt/veriqta/status-service

/etc/systemd/system/veriqta-status.service

UFW rules
```

Some packages may have existed before the project.

Do not remove shared packages blindly.

---

# Build a Cleanup Playbook

Create:

```bash
nano playbooks/cleanup.yml
```

Start with explicit project-owned resources.

For example:

```yaml
---
- name: Remove project-specific web configuration
  hosts: webservers
  become: true

  tasks:

    - name: Remove VERIQTA Nginx site symlink
      ansible.builtin.file:
        path: /etc/nginx/sites-enabled/veriqta
        state: absent

    - name: Remove VERIQTA Nginx configuration
      ansible.builtin.file:
        path: /etc/nginx/sites-available/veriqta
        state: absent

    - name: Remove VERIQTA website
      ansible.builtin.file:
        path: /var/www/veriqta
        state: absent

- name: Remove project application
  hosts: appservers
  become: true

  tasks:

    - name: Stop and disable project application
      ansible.builtin.systemd_service:
        name: veriqta-status
        state: stopped
        enabled: false
      failed_when: false

    - name: Remove project systemd unit
      ansible.builtin.file:
        path: /etc/systemd/system/veriqta-status.service
        state: absent

    - name: Reload systemd
      ansible.builtin.systemd_service:
        daemon_reload: true

    - name: Remove project application directory
      ansible.builtin.file:
        path: /opt/veriqta/status-service
        state: absent
```

Do not immediately automate removal of users unless you are certain their accounts were created only for the disposable lab.

The same applies to packages and firewall rules.

Cleanup automation must be just as deliberate as deployment automation.

---

# 25. Cleanup Verification

After cleanup, verify project resources are gone.

For example:

```bash
ansible \
    webservers \
    -b \
    -m stat \
    -a "path=/var/www/veriqta"
```

Check the systemd unit:

```bash
ansible \
    appservers \
    -m command \
    -a "systemctl status veriqta-status"
```

Check:

```bash
ansible \
    managed_servers \
    -b \
    -m command \
    -a "sshd -t"
```

Check:

```bash
ansible managed_servers -m ping
```

The machines should still be administratively healthy after project cleanup.

Cleanup is not successful if you remove project files but leave the servers broken.

---

# 26. Project Reflection

Answer these after completing the project.

1. What problem does configuration management solve?
2. What is configuration drift?
3. Why does drift increase as infrastructure grows?
4. What is desired state?
5. How does Ansible compare current and desired state?
6. What is idempotency?
7. Why is idempotency operationally important?
8. What is an Ansible control node?
9. What is a managed node?
10. Why is Ansible described as agentless?
11. What transport did your lab use?
12. Why must SSH be reliable before Ansible can be reliable?
13. Why did you use SSH keys?
14. What are SSH host keys protecting?
15. Why did you avoid disabling host-key checking?
16. What is an inventory?
17. What is `ansible_host`?
18. What is `ansible_user`?
19. What is an inventory group?
20. What is a child group?
21. Why did you create `managed_servers`?
22. What is `group_vars`?
23. What is `host_vars`?
24. When should a variable be global?
25. When should a variable be group-specific?
26. When should it be host-specific?
27. What are Ansible facts?
28. When are facts useful?
29. What is an ad-hoc command?
30. Why are ad-hoc commands not a replacement for playbooks?
31. What is a playbook?
32. What is a play?
33. What is a task?
34. What is a module?
35. Why should you prefer modules over arbitrary shell commands?
36. When is `shell` appropriate?
37. What does `become` do?
38. Why is sudo configuration part of the automation trust model?
39. What is a role?
40. Why are roles useful?
41. What belongs in `defaults`?
42. What belongs in `tasks`?
43. What belongs in `handlers`?
44. What belongs in `templates`?
45. What belongs in `files`?
46. What is Jinja2?
47. How did you use Jinja2 in this project?
48. Why can two servers use the same template but render different content?
49. What is a handler?
50. Why did Nginx use a handler?
51. Why should services not restart unnecessarily?
52. What does `notify` do?
53. What is check mode?
54. What can check mode not guarantee?
55. What is diff mode?
56. Why is diff useful before deployment?
57. What is `--limit`?
58. When would you use it?
59. What is `serial`?
60. How does `serial` reduce blast radius?
61. Why did the security play use `serial: 1`?
62. What could happen if you broke SSH on every server simultaneously?
63. Why should SSH access be tested immediately after security changes?
64. Why did you use an SSH drop-in?
65. What is configuration ownership?
66. What does it mean when a file is Ansible-managed?
67. What happens when somebody manually edits it?
68. How did you simulate drift?
69. How did Ansible correct the drift?
70. What types of drift did you test?
71. How can package state drift?
72. How can service state drift?
73. How can permissions drift?
74. How can firewall state drift?
75. How could user state drift?
76. What is Ansible Vault?
77. What does it protect?
78. What does it not protect?
79. Why should plaintext secrets not enter Git?
80. What is a collection?
81. Why did you create `requirements.yml`?
82. Why should dependency versions eventually be pinned?
83. What happens if a collection disappears or changes behavior?
84. How did you manage UFW?
85. Why did you allow SSH before enabling the firewall?
86. Why is firewall automation dangerous?
87. How would you recover from an automation-created lockout?
88. Why are VM snapshots useful during this project?
89. How did you manage Nginx?
90. Why validate Nginx before reload?
91. What happens if `nginx -t` fails?
92. Why is that safer than blind restart?
93. How did you deploy the application service?
94. Why did the application use a dedicated system account?
95. Why did it use `/usr/sbin/nologin`?
96. Why did the service bind only to `127.0.0.1`?
97. What does `Restart=on-failure` do?
98. What does `NoNewPrivileges=true` provide?
99. What does `PrivateTmp=true` do conceptually?
100. Why did you create validation playbooks?
101. How is validation different from deployment?
102. Why should tests live with the automation?
103. What does `changed=0` tell you on a second run?
104. Is `changed=0` alone proof that the application works?
105. Why not?
106. What additional functional tests are required?
107. What is the difference between `failed` and `unreachable`?
108. How do you troubleshoot an unreachable host?
109. How do you troubleshoot sudo failure?
110. How do you troubleshoot an undefined variable?
111. How do you troubleshoot YAML syntax?
112. Why is verbose mode useful?
113. What does `-vvv` provide?
114. Why shouldn't you start every investigation with maximum verbosity?
115. What is blast radius?
116. How does grouping affect blast radius?
117. How does `--limit` affect blast radius?
118. How does `serial` affect blast radius?
119. Why should automation be version-controlled?
120. What should never be committed?
121. What evidence would you save from this project?
122. What evidence should be sanitized?
123. How would you add database servers?
124. How would you support staging and production inventories?
125. How would you manage different values per environment?
126. How would you introduce CI for Ansible?
127. What would `ansible-lint` add?
128. How would Molecule improve role testing?
129. How would you manage hundreds of hosts?
130. When might Ansible Automation Platform or AWX become useful?
131. How would dynamic inventory change this design?
132. How would cloud inventory change it?
133. How would secrets management change in production?
134. How would you implement approvals?
135. How would you implement rollback?
136. How would you implement canary deployments?
137. How would you prevent concurrent conflicting runs?
138. How would you monitor failed automation?
139. How would you audit who changed infrastructure code?
140. What would you improve before calling this production-ready?

---

# 27. Final Completion Checklist

## Lab

* [ ] `control01` created.
* [ ] `web01` created.
* [ ] `web02` created.
* [ ] `app01` created.
* [ ] Hostnames configured.
* [ ] IP addresses recorded.
* [ ] Name resolution works.
* [ ] VM snapshots created.

## Control Node

* [ ] Ansible installed.
* [ ] Git installed.
* [ ] Ansible version recorded.
* [ ] Project directory created.
* [ ] Git repository initialized.
* [ ] `.gitignore` created.

## SSH

* [ ] Administrative SSH key created.
* [ ] Key passphrase considered/used.
* [ ] SSH agent tested.
* [ ] Public key installed on `web01`.
* [ ] Public key installed on `web02`.
* [ ] Public key installed on `app01`.
* [ ] Host keys verified.
* [ ] SSH tested manually.
* [ ] sudo tested manually.

## Inventory

* [ ] Inventory created.
* [ ] `webservers` group created.
* [ ] `appservers` group created.
* [ ] `managed_servers` group created.
* [ ] Inventory graph verified.
* [ ] Inventory host variables inspected.

## Connectivity

* [ ] `ansible ping` succeeds for `web01`.
* [ ] `ansible ping` succeeds for `web02`.
* [ ] `ansible ping` succeeds for `app01`.
* [ ] Facts gathered successfully.

## Ansible Configuration

* [ ] `ansible.cfg` created.
* [ ] Inventory path configured.
* [ ] Role path configured.
* [ ] Active configuration verified.

## Common Role

* [ ] Common role created.
* [ ] Package list defined.
* [ ] Packages managed.
* [ ] `/opt/veriqta` managed.
* [ ] `/etc/veriqta` managed.
* [ ] Timezone managed.
* [ ] First run completed.
* [ ] Second run tested for idempotency.

## Users

* [ ] Administrative group created.
* [ ] Managed user data defined.
* [ ] Users created through loops.
* [ ] Group membership verified.
* [ ] SSH key-management approach understood.
* [ ] Private keys excluded from repository.

## Security

* [ ] Security role created.
* [ ] UFW installed.
* [ ] SSH allowed before firewall activation.
* [ ] Default incoming policy managed.
* [ ] Default outgoing policy managed.
* [ ] UFW enabled.
* [ ] SSH drop-in created.
* [ ] SSH configuration validated.
* [ ] SSH handler created.
* [ ] Security deployment uses controlled blast radius.
* [ ] Post-change SSH connectivity verified.

## Web Servers

* [ ] Webserver role created.
* [ ] Nginx installed automatically.
* [ ] Document root created.
* [ ] Website template created.
* [ ] Nginx template created.
* [ ] Site enabled.
* [ ] Default site handled.
* [ ] HTTP firewall rule created.
* [ ] Nginx enabled.
* [ ] Nginx running.
* [ ] `nginx -t` passes.
* [ ] `web01` returns HTTP 200.
* [ ] `web02` returns HTTP 200.
* [ ] Host-specific template values verified.

## Application

* [ ] Application role created.
* [ ] Service user created.
* [ ] Service group created.
* [ ] Application directory created.
* [ ] Application deployed.
* [ ] systemd unit deployed.
* [ ] systemd daemon reloaded.
* [ ] Application enabled.
* [ ] Application running.
* [ ] Health endpoint works.

## Secrets

* [ ] Vault concept understood.
* [ ] Lab vault created.
* [ ] Vault file encrypted.
* [ ] Plaintext secrets excluded from Git.
* [ ] Vault limitations documented.

## Validation

* [ ] Syntax checks pass.
* [ ] Check mode tested.
* [ ] Diff mode tested.
* [ ] Validation playbook created.
* [ ] Directory validation passes.
* [ ] User validation passes.
* [ ] Firewall validation passes.
* [ ] SSH validation passes.
* [ ] Nginx validation passes.
* [ ] HTTP validation passes.
* [ ] Application validation passes.

## Idempotency

* [ ] Full platform deployed once.
* [ ] Full platform deployed again.
* [ ] Unexpected changes investigated.
* [ ] Stable tasks report no unnecessary changes.

## Drift

* [ ] File drift introduced.
* [ ] File drift detected.
* [ ] File drift corrected.
* [ ] Package drift introduced.
* [ ] Package drift corrected.
* [ ] Service drift introduced.
* [ ] Service drift corrected.

## Failure Labs

* [ ] SSH-unreachable failure completed.
* [ ] Wrong SSH user tested.
* [ ] Invalid YAML tested.
* [ ] Undefined variable tested.
* [ ] Invalid Nginx configuration tested safely.
* [ ] Privilege escalation failure tested.
* [ ] Firewall failure understood/tested safely.
* [ ] Wrong inventory address tested.
* [ ] Template-variable failure tested.
* [ ] All controlled failures recovered.
* [ ] Recovery verified.

## GitHub

* [ ] README completed.
* [ ] Architecture documented.
* [ ] Inventory design documented.
* [ ] Role design documented.
* [ ] Variable design documented.
* [ ] Security model documented.
* [ ] Secrets approach documented.
* [ ] Deployment strategy documented.
* [ ] Idempotency documented.
* [ ] Drift documented.
* [ ] Failure labs documented.
* [ ] Runbooks completed.
* [ ] Evidence sanitized.
* [ ] No passwords committed.
* [ ] No private SSH keys committed.
* [ ] No vault passwords committed.

## Cleanup

* [ ] Project-created resources identified.
* [ ] Cleanup playbook reviewed before execution.
* [ ] Application service removed safely.
* [ ] Project web configuration removed safely.
* [ ] Project directories removed where intended.
* [ ] Shared packages preserved.
* [ ] Shared accounts preserved unless explicitly lab-only.
* [ ] SSH still works.
* [ ] Ansible connectivity still works.
* [ ] Managed servers remain healthy.

---

# When You Can Call This Project Complete

Do not call this project complete because this works:

```bash
ansible all -m ping
```

That proves connectivity and basic module execution.

It does not prove configuration management.

You should be able to explain and operate this complete lifecycle:

```text
                DESIRED STATE
                     |
                     v
                  Git
                     |
                     v
                 Ansible
                     |
          +----------+----------+
          |          |          |
          v          v          v
        web01      web02      app01
          |          |          |
          v          v          v
      packages     users     services
      configs      groups    application
      firewall     keys      permissions
          |          |          |
          +----------+----------+
                     |
                     v
                 VALIDATION
                     |
          +----------+----------+
          |                     |
          v                     v
       CORRECT                 DRIFT
          |                     |
          v                     v
       NO CHANGE             CORRECT
          |                     |
          +----------+----------+
                     |
                     v
                 TEST AGAIN
```

And when automation fails, you should be able to reason through the management path:

```text
Is the inventory correct?

Is the host address correct?

Can the control node reach the host?

Is TCP 22 reachable?

Does SSH authentication work?

Is the expected SSH host key present?

Can Ansible execute Python modules?

Can the automation user escalate privileges?

Did YAML parse correctly?

Were all variables defined?

Did the template render correctly?

Did the module perform the intended operation?

Did the generated configuration validate?

Did the service accept the configuration?

Did the service start?

Is the firewall allowing the required traffic?

Does the application actually work?

Does a second automation run remain idempotent?

Can Ansible detect and correct deliberate drift?
```

That is what makes this a **Mid-Level configuration-management project**.

You are no longer proving that you know how to install Ansible or write a YAML file.

You are proving that you can take a collection of Linux machines and turn them into a **controlled, repeatable, versioned, testable, security-aware infrastructure platform where desired state is defined centrally, configuration drift is detectable and correctable, deployments have a controlled blast radius, service changes are validated before activation, and the environment can be rebuilt consistently without manually configuring every server.**
