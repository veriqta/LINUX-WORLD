# Portfolio Project 09: Linux CI/CD Deployment Server

- **Level:** Mid-Level
- **Track:** Linux Systems Administration / Infrastructure Engineering / DevOps Operations
- **Primary Environment:** Ubuntu Server 24.04 LTS
- **Architecture:** Dedicated CI/CD server + separate Linux deployment target
- **Estimated Time:** 45–60 hours
- **Cost:** Free with local virtual machines
- **Primary Technologies:** Linux, Git, GitHub, Jenkins, Bash, systemd, OpenSSH, Nginx, Python, curl
- **Primary Skills:** Linux administration, Git, CI/CD, SSH, automation, deployment engineering, service management, permissions, testing, rollback, troubleshooting

**Demonstrates:** Linux, Git, CI/CD, automation, deployment, SSH, systemd, release management, operational troubleshooting.

---

# 01. Project Title

## Linux CI/CD Deployment Server

A CI/CD pipeline that only prints:

```text
Hello World
```

is useful when you are learning pipeline syntax.

It is not enough for this project.

You are going to build a real Linux deployment path:

```text
DEVELOPER
    |
    | git push
    v
GIT REPOSITORY
    |
    | checkout
    v
CI/CD SERVER
    |
    +--> validate source
    |
    +--> run tests
    |
    +--> build release artifact
    |
    +--> calculate checksum
    |
    +--> transfer artifact
    |
    v
DEPLOYMENT SERVER
    |
    +--> create versioned release
    |
    +--> update current symlink
    |
    +--> restart service
    |
    +--> run health check
    |
    +--> verify deployment
    |
    +--> rollback on failure
    |
    v
RUNNING APPLICATION
```

The important part is not Jenkins itself.

The important part is the engineering system around it.

By the end of this project, a source-code change should travel through a controlled path from Git to a running Linux service without you manually copying application files onto the target server.

You will build:

* a dedicated CI/CD server;
* a separate application server;
* Git-based source control;
* automated source retrieval;
* syntax and test stages;
* immutable release artifacts;
* artifact checksums;
* SSH-based deployment;
* dedicated Linux service accounts;
* restricted filesystem permissions;
* systemd-managed application runtime;
* Nginx reverse proxying;
* versioned release directories;
* atomic release activation;
* deployment locking;
* health validation;
* automatic rollback;
* deployment evidence;
* failure simulations;
* operational runbooks.

This is where CI/CD stops being a diagram and becomes Linux infrastructure.

---

# 02. Project Scenario

Your engineering team maintains an internal application called:

**VERIQTA Release Portal**

The current deployment process is manual.

An engineer logs into a server, pulls or copies the newest files, installs dependencies, restarts the application, and checks whether the website still works.

The process resembles:

```text
Developer changes code
        |
        v
git push
        |
        v
Engineer SSHs into server
        |
        v
Copies files
        |
        v
Restarts application
        |
        v
Opens website
        |
        v
Hopes everything works
```

Several problems have appeared.

Nobody has a completely reproducible deployment procedure.

Different engineers perform slightly different steps.

A deployment can leave old and new files mixed together.

There is no reliable record of exactly which commit is running.

Tests can be skipped.

A broken release may be discovered only after deployment.

Rollback depends on somebody remembering what the previous version contained.

Deployment credentials are poorly separated from ordinary user access.

Your task is to replace that process with:

```text
COMMIT
   |
   v
CHECKOUT
   |
   v
VALIDATION
   |
   v
TEST
   |
   v
BUILD
   |
   v
ARTIFACT
   |
   v
TRANSFER
   |
   v
DEPLOY
   |
   v
HEALTH CHECK
   |
   +------ PASS ------> RELEASE COMPLETE
   |
   +------ FAIL ------> ROLLBACK
```

---

# 03. Project Difficulty

## Mid-Level

You already know how to:

```bash
git clone
```

and:

```bash
systemctl restart
```

That is not enough here.

A deployment system must answer harder questions:

```text
What commit produced this release?

Did the tests actually pass?

What exactly was deployed?

Was the artifact changed during transfer?

Which account performed the deployment?

Can that account do more than it needs?

What happens if two deployments start together?

What happens if artifact transfer fails halfway through?

What happens if the new process starts but is unhealthy?

What happens if Nginx is healthy but the application is not?

Can the previous release be restored quickly?

Can you prove what is currently running?

Can you reproduce the deployment?
```

You will treat deployment as an operational transaction:

```text
PREPARE
  ↓
VALIDATE
  ↓
BUILD
  ↓
TRANSFER
  ↓
STAGE
  ↓
ACTIVATE
  ↓
RESTART
  ↓
VERIFY
  ↓
COMMIT RELEASE

or

ROLL BACK
```

---

# 04. Estimated Time

Plan approximately **45–60 hours**.

| Phase                            | Estimated Time |
| -------------------------------- | -------------: |
| Architecture and design          |      3–4 hours |
| Two-server Linux environment     |      3–4 hours |
| Git repository preparation       |        3 hours |
| Application/service construction |      4–5 hours |
| Jenkins installation             |      3–4 hours |
| SSH trust and permissions        |      4–5 hours |
| Pipeline construction            |      6–8 hours |
| Deployment automation            |      6–8 hours |
| Rollback engineering             |        4 hours |
| Security hardening               |      4–5 hours |
| Testing and validation           |      4–5 hours |
| Failure engineering              |     8–10 hours |
| Documentation                    |      3–4 hours |

---

# 05. Prerequisites

You should already understand:

* Linux command-line administration;
* files and directories;
* ownership and permissions;
* users and groups;
* sudo;
* systemd;
* SSH;
* SSH keys;
* TCP/IP;
* ports;
* Nginx fundamentals;
* Bash;
* Git fundamentals;
* process management;
* logs;
* basic application deployment.

You should be comfortable with:

```bash
ssh
```

```bash
scp
```

```bash
rsync
```

```bash
git clone
```

```bash
git status
```

```bash
git log
```

```bash
systemctl status
```

```bash
journalctl
```

```bash
ss -lntp
```

```bash
curl
```

This project teaches CI/CD engineering.

It should not be your first introduction to Linux permissions, Git, SSH, or systemd.

---

# 06. Learning Objectives

By the end, you should be able to:

1. Explain continuous integration.
2. Explain continuous delivery.
3. Explain continuous deployment.
4. Distinguish build from deployment.
5. Explain pipeline stages.
6. Explain pipeline failure boundaries.
7. Build a dedicated Linux CI/CD server.
8. Build a separate deployment target.
9. Install and operate Jenkins.
10. Understand the Jenkins service account.
11. Understand Jenkins workspace ownership.
12. Integrate Jenkins with Git.
13. Retrieve source code automatically.
14. Identify the exact commit being built.
15. Validate source before deployment.
16. Run automated tests.
17. Stop a pipeline when tests fail.
18. Build a release artifact.
19. Create deterministic release metadata.
20. Calculate artifact checksums.
21. Preserve build evidence.
22. Transfer artifacts securely over SSH.
23. Create dedicated deployment identities.
24. manage SSH host trust.
25. Avoid disabling SSH host-key checking.
26. Apply least privilege to deployment accounts.
27. Separate CI responsibilities from runtime responsibilities.
28. Create versioned release directories.
29. Use symbolic links for release activation.
30. Understand atomic deployment concepts.
31. prevent concurrent deployments.
32. Manage application services with systemd.
33. Run application processes as dedicated non-root users.
34. Use Nginx as a reverse proxy.
35. Perform post-deployment health checks.
36. Distinguish process health from application health.
37. Detect a failed deployment.
38. Roll back to a previous release.
39. Verify rollback.
40. Retain previous releases.
41. Clean old releases safely.
42. Avoid deleting the active release.
43. Handle deployment credentials securely.
44. Protect private SSH keys.
45. Understand Jenkins credential exposure risks.
46. Understand why CI systems are security-sensitive.
47. Audit deployments.
48. Correlate a release with a Git commit.
49. inspect Jenkins logs.
50. inspect systemd application logs.
51. inspect Nginx logs.
52. troubleshoot Git failures.
53. troubleshoot SSH failures.
54. troubleshoot permissions.
55. troubleshoot failed tests.
56. troubleshoot build failures.
57. troubleshoot transfer failures.
58. troubleshoot systemd failures.
59. troubleshoot Nginx 502 errors.
60. troubleshoot health-check failures.
61. troubleshoot deployment race conditions.
62. troubleshoot incorrect symlinks.
63. diagnose a bad release.
64. understand rollback limitations.
65. validate recovery after reboot.
66. document CI/CD architecture professionally.
67. explain the security boundaries in a deployment platform.
68. explain what would need to change before production use.

---

# 07. What You Will Build

Use two Linux servers.

```text
                    SOURCE CONTROL
                         Git
                          |
                          | checkout
                          v
                +-------------------+
                |      ci01         |
                | Ubuntu 24.04      |
                | Jenkins           |
                | Git               |
                | Tests             |
                | Artifact Build    |
                +---------+---------+
                          |
                          | SSH/SCP
                          | Release Artifact
                          v
                +-------------------+
                |      app01        |
                | Ubuntu 24.04      |
                | Nginx             |
                | systemd           |
                | Python App        |
                +---------+---------+
                          |
                          v
                /opt/veriqta-portal/
                ├── releases/
                │   ├── release-A/
                │   ├── release-B/
                │   └── release-C/
                │
                ├── current -> releases/release-C
                └── shared/
```

Only `app01` serves application traffic.

`ci01` builds and deploys.

That separation matters.

---

# 08. Architecture

## Server Inventory

| Host    | Purpose              |
| ------- | -------------------- |
| `ci01`  | Jenkins CI/CD server |
| `app01` | Deployment target    |

Example lab network:

```text
ci01   192.168.60.20
app01  192.168.60.30
```

Use addresses appropriate to your own isolated lab.

---

## Deployment Identities

Use separate Linux accounts.

On `ci01`:

```text
jenkins
```

is the automation identity.

On `app01`:

```text
deploy
```

performs controlled deployment operations.

The application itself runs as:

```text
veriqta
```

Do not run the application as:

```text
root
```

and do not make Jenkins:

```text
root
```

just because permissions become inconvenient.

---

## Deployment Filesystem

On `app01`:

```text
/opt/veriqta-portal/
│
├── releases/
│   ├── 20260924-001-abc1234/
│   ├── 20260924-002-def5678/
│   └── 20260924-003-987abcd/
│
├── current -> releases/20260924-003-987abcd/
│
└── shared/
    └── deployment-history.log
```

Nginx does not point at a randomly copied folder.

The service starts the application from:

```text
/opt/veriqta-portal/current/
```

A new deployment creates a new release directory and then changes:

```text
current
```

to the new release.

Rollback changes it back.

---

# 09. Technologies

| Technology          | Purpose                           |
| ------------------- | --------------------------------- |
| Ubuntu Server 24.04 | Host operating system             |
| Jenkins             | CI/CD automation                  |
| Git                 | Source control                    |
| OpenSSH             | Secure deployment transport       |
| Bash                | Deployment automation             |
| Python              | Demonstration application         |
| systemd             | Runtime process management        |
| Nginx               | Reverse proxy                     |
| curl                | Health validation                 |
| tar                 | Artifact packaging                |
| SHA-256             | Artifact integrity                |
| flock               | Deployment concurrency protection |
| journald            | Service logs                      |

---

# 10. Environment Setup

# Phase 1: Create the Servers

Create:

```text
ci01
app01
```

Recommended per VM:

```text
2 vCPU
4 GB RAM
30 GB disk
Ubuntu Server 24.04 LTS
```

For `ci01`, 4 GB is preferable because Jenkins runs on the JVM.

---

## Configure Hostnames

On CI server:

```bash
sudo hostnamectl set-hostname ci01
```

On deployment server:

```bash
sudo hostnamectl set-hostname app01
```

Reconnect if necessary.

---

# Establish a Baseline

On both servers:

```bash
mkdir -p ~/project09-baseline
```

Capture:

```bash
hostnamectl > ~/project09-baseline/hostname.txt
```

```bash
ip -brief address > ~/project09-baseline/network.txt
```

```bash
ip route > ~/project09-baseline/routes.txt
```

```bash
sudo ss -lntup > ~/project09-baseline/listeners.txt
```

```bash
df -hT > ~/project09-baseline/storage.txt
```

```bash
free -h > ~/project09-baseline/memory.txt
```

```bash
systemctl --failed > ~/project09-baseline/failed-units.txt
```

```bash
sudo ufw status verbose > ~/project09-baseline/firewall.txt
```

Do this before installing the platform.

Later, when a port appears or a service fails, you can compare the system against a known baseline.

---

# Verify Server-to-Server Connectivity

From `ci01`:

```bash
ping -c 3 APP01_IP
```

Then:

```bash
nc -vz APP01_IP 22
```

If `nc` is unavailable:

```bash
sudo apt install -y netcat-openbsd
```

You need to prove:

```text
ci01
 |
 | TCP 22
 v
app01
```

before blaming Jenkins for an SSH problem.

---

# 11. Project Directory Structure

Your public project repository should eventually resemble:

```text
09-Linux-CI-CD-Deployment-Server/
│
├── README.md
├── PREREQUISITES.md
├── ENVIRONMENT.md
├── ARCHITECTURE.md
├── SECURITY.md
├── CHANGE-PLAN.md
├── ROLLBACK.md
├── Jenkinsfile
├── .gitignore
│
├── application/
│   ├── app.py
│   ├── test_app.py
│   └── VERSION
│
├── deployment/
│   ├── deploy.sh
│   ├── rollback.sh
│   ├── cleanup-releases.sh
│   └── veriqta-portal.service
│
├── scripts/
│   ├── build.sh
│   ├── test.sh
│   ├── verify-artifact.sh
│   ├── health-check.sh
│   └── collect-diagnostics.sh
│
├── tests/
│   ├── test-source.sh
│   ├── test-build.sh
│   ├── test-deployment.sh
│   ├── test-health.sh
│   ├── test-rollback.sh
│   └── test-reboot.sh
│
├── failure-labs/
├── runbooks/
├── docs/
├── evidence/
└── verification/
```

---

# 12. Step-by-Step Implementation

# Phase 2: Build the Application

Start in your Git working directory.

Create:

```bash
mkdir -p application deployment scripts tests
```

---

## Create the Application

Create:

```bash
nano application/app.py
```

Add:

```python
from http.server import BaseHTTPRequestHandler, HTTPServer
import json
import os
import socket

HOST = "127.0.0.1"
PORT = int(os.getenv("PORT", "8000"))

BASE_DIR = os.path.dirname(os.path.abspath(__file__))
VERSION_FILE = os.path.join(BASE_DIR, "VERSION")


def get_version():
    try:
        with open(VERSION_FILE, "r", encoding="utf-8") as file:
            return file.read().strip()
    except OSError:
        return "unknown"


class Handler(BaseHTTPRequestHandler):

    def send_json(self, status_code, payload):
        body = json.dumps(payload).encode("utf-8")

        self.send_response(status_code)
        self.send_header("Content-Type", "application/json")
        self.send_header("Content-Length", str(len(body)))
        self.end_headers()

        self.wfile.write(body)

    def do_GET(self):
        if self.path == "/":
            self.send_json(
                200,
                {
                    "application": "VERIQTA Release Portal",
                    "version": get_version(),
                    "hostname": socket.gethostname(),
                    "status": "running",
                },
            )
            return

        if self.path == "/health":
            self.send_json(
                200,
                {
                    "status": "ok",
                    "version": get_version(),
                },
            )
            return

        self.send_json(
            404,
            {
                "status": "not-found",
            },
        )

    def log_message(self, format, *args):
        print(
            "%s - - [%s] %s"
            % (
                self.client_address[0],
                self.log_date_time_string(),
                format % args,
            ),
            flush=True,
        )


if __name__ == "__main__":
    server = HTTPServer((HOST, PORT), Handler)

    print(
        f"VERIQTA Release Portal listening on {HOST}:{PORT}",
        flush=True,
    )

    server.serve_forever()
```

---

## Create Version File

```bash
printf '1.0.0\n' > application/VERSION
```

The application exposes:

```text
/
```

and:

```text
/health
```

The `/health` endpoint will become your deployment verification target.

---

# Create Automated Tests

Create:

```bash
nano application/test_app.py
```

Add:

```python
import os
import unittest

import app


class ApplicationTests(unittest.TestCase):

    def test_version_is_not_empty(self):
        self.assertTrue(app.get_version())

    def test_version_file_exists(self):
        self.assertTrue(
            os.path.isfile(app.VERSION_FILE)
        )

    def test_default_port_is_valid(self):
        self.assertGreater(app.PORT, 0)
        self.assertLessEqual(app.PORT, 65535)


if __name__ == "__main__":
    unittest.main()
```

This is intentionally a small application.

The infrastructure is the project.

But the pipeline still needs genuine tests that can pass and fail.

---

# Test Locally

```bash
cd application
```

Run:

```bash
python3 -m unittest -v
```

Expected:

```text
...
OK
```

Return:

```bash
cd ..
```

---

# Create Test Script

```bash
nano scripts/test.sh
```

Add:

```bash
#!/usr/bin/env bash

set -Eeuo pipefail

PROJECT_DIR="$(
    cd "$(dirname "${BASH_SOURCE[0]}")/.." \
    && pwd
)"

cd "${PROJECT_DIR}/application"

echo "Running Python syntax validation."

python3 -m py_compile \
    app.py \
    test_app.py

echo "Running automated tests."

python3 -m unittest -v
```

Make executable:

```bash
chmod 0755 scripts/test.sh
```

Run:

```bash
./scripts/test.sh
```

Do not build the CI pipeline until the same test procedure works manually.

That separates:

```text
application/test problem
```

from:

```text
Jenkins problem
```

---

# Phase 3: Build the Artifact Process

The deployment server should not receive a random working directory.

Create a release artifact.

Create:

```bash
nano scripts/build.sh
```

Add:

```bash
#!/usr/bin/env bash

set -Eeuo pipefail

PROJECT_DIR="$(
    cd "$(dirname "${BASH_SOURCE[0]}")/.." \
    && pwd
)"

BUILD_DIR="${PROJECT_DIR}/build"
ARTIFACT_DIR="${PROJECT_DIR}/artifacts"

COMMIT_SHA="$(
    git -C "$PROJECT_DIR" rev-parse HEAD
)"

SHORT_SHA="$(
    git -C "$PROJECT_DIR" rev-parse --short=7 HEAD
)"

BUILD_TIME="$(
    date -u '+%Y-%m-%dT%H:%M:%SZ'
)"

RELEASE_ID="$(
    date -u '+%Y%m%d-%H%M%S'
)-${SHORT_SHA}"

rm -rf "$BUILD_DIR"

mkdir -p \
    "$BUILD_DIR" \
    "$ARTIFACT_DIR"

cp -a \
    "${PROJECT_DIR}/application/." \
    "$BUILD_DIR/"

cat > "${BUILD_DIR}/BUILD-INFO" <<EOF
release_id=${RELEASE_ID}
commit_sha=${COMMIT_SHA}
build_time=${BUILD_TIME}
EOF

ARTIFACT="${ARTIFACT_DIR}/${RELEASE_ID}.tar.gz"

tar \
    -C "$BUILD_DIR" \
    -czf "$ARTIFACT" \
    .

sha256sum "$ARTIFACT" \
    > "${ARTIFACT}.sha256"

printf '%s\n' "$RELEASE_ID" \
    > "${ARTIFACT_DIR}/LATEST_RELEASE"

printf '%s\n' "$ARTIFACT"
```

Make executable:

```bash
chmod 0755 scripts/build.sh
```

Run:

```bash
./scripts/build.sh
```

Inspect:

```bash
find artifacts \
  -maxdepth 1 \
  -type f \
  -print
```

You should have:

```text
RELEASE_ID.tar.gz
RELEASE_ID.tar.gz.sha256
LATEST_RELEASE
```

---

# Why BUILD-INFO Matters

Every release should be traceable.

Inside each artifact:

```text
BUILD-INFO
```

contains:

```text
release_id
commit_sha
build_time
```

Therefore:

```text
RUNNING RELEASE
      ↓
BUILD-INFO
      ↓
GIT COMMIT
```

You can prove what code is running.

---

# Verify Artifact

Get the latest release:

```bash
RELEASE_ID="$(cat artifacts/LATEST_RELEASE)"
```

Then:

```bash
sha256sum -c \
  "artifacts/${RELEASE_ID}.tar.gz.sha256"
```

Expected:

```text
...: OK
```

Inspect without extracting:

```bash
tar -tzf \
  "artifacts/${RELEASE_ID}.tar.gz"
```

---

# Phase 4: Prepare `app01`

Install packages:

```bash
sudo apt update
```

```bash
sudo apt install -y \
  nginx \
  python3 \
  openssh-server \
  curl \
  rsync
```

Check:

```bash
systemctl is-active ssh
```

```bash
systemctl is-active nginx
```

---

# Create Runtime User

```bash
sudo useradd \
  --system \
  --home /opt/veriqta-portal \
  --shell /usr/sbin/nologin \
  veriqta
```

Check:

```bash
getent passwd veriqta
```

The application account does not need an interactive shell.

---

# Create Deployment User

```bash
sudo adduser \
  --disabled-password \
  --gecos "" \
  deploy
```

The deployment identity and application runtime identity are intentionally different.

```text
deploy
   |
   | manages releases
   v
filesystem/systemd operation

veriqta
   |
   | executes application
   v
runtime process
```

---

# Create Deployment Directories

```bash
sudo install \
  -d \
  -o deploy \
  -g deploy \
  -m 0750 \
  /opt/veriqta-portal
```

```bash
sudo install \
  -d \
  -o deploy \
  -g deploy \
  -m 0750 \
  /opt/veriqta-portal/releases
```

```bash
sudo install \
  -d \
  -o deploy \
  -g deploy \
  -m 0750 \
  /opt/veriqta-portal/shared
```

Create incoming area:

```bash
sudo install \
  -d \
  -o deploy \
  -g deploy \
  -m 0750 \
  /opt/veriqta-portal/incoming
```

Inspect:

```bash
namei -l /opt/veriqta-portal/releases
```

---

# Phase 5: Create systemd Service

Create:

```bash
sudo nano /etc/systemd/system/veriqta-portal.service
```

Add:

```ini
[Unit]
Description=VERIQTA Release Portal
After=network.target

[Service]
Type=simple

User=veriqta
Group=veriqta

WorkingDirectory=/opt/veriqta-portal/current

Environment=PORT=8000

ExecStart=/usr/bin/python3 /opt/veriqta-portal/current/app.py

Restart=on-failure
RestartSec=3

NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true

[Install]
WantedBy=multi-user.target
```

Reload:

```bash
sudo systemctl daemon-reload
```

Do not start it yet.

There is no:

```text
current
```

release.

That is expected.

---

# Understand the Runtime Boundary

The application listens on:

```text
127.0.0.1:8000
```

not:

```text
0.0.0.0:8000
```

because only local Nginx needs direct access.

The external path will be:

```text
CLIENT
  ↓
app01:80
  ↓
NGINX
  ↓
127.0.0.1:8000
  ↓
PYTHON APPLICATION
```

---

# Phase 6: Configure Nginx

Create:

```bash
sudo nano /etc/nginx/sites-available/veriqta-portal
```

Add:

```nginx
server {
    listen 80;
    listen [::]:80;

    server_name _;

    access_log /var/log/nginx/veriqta-portal-access.log;
    error_log /var/log/nginx/veriqta-portal-error.log warn;

    server_tokens off;

    location / {
        proxy_pass http://127.0.0.1:8000;

        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 5s;
        proxy_read_timeout 30s;
    }
}
```

Enable:

```bash
sudo ln -s \
  /etc/nginx/sites-available/veriqta-portal \
  /etc/nginx/sites-enabled/veriqta-portal
```

If the default site is not needed:

```bash
sudo rm -f \
  /etc/nginx/sites-enabled/default
```

Validate:

```bash
sudo nginx -t
```

Only after validation:

```bash
sudo systemctl reload nginx
```

---

# Phase 7: Firewall

Before enabling UFW on a remote machine, preserve SSH.

Check:

```bash
sudo ufw app list
```

Allow SSH:

```bash
sudo ufw allow OpenSSH
```

If the profile does not exist, use the actual SSH port deliberately.

Allow HTTP from your lab network.

Example:

```bash
sudo ufw allow \
  from 192.168.60.0/24 \
  to any \
  port 80 \
  proto tcp
```

Enable:

```bash
sudo ufw enable
```

Inspect:

```bash
sudo ufw status verbose
```

Port:

```text
8000
```

should not need external firewall exposure because the application binds to loopback.

---

# Phase 8: Create Deployment Privilege Boundary

The `deploy` account needs to perform only a small number of privileged operations.

Do not give it unrestricted:

```text
ALL=(ALL) NOPASSWD: ALL
```

Create a controlled helper instead.

Create:

```bash
sudo nano /usr/local/sbin/veriqta-service-control
```

Add:

```bash
#!/usr/bin/env bash

set -Eeuo pipefail

ACTION="${1:-}"

case "$ACTION" in
    restart)
        exec /usr/bin/systemctl restart veriqta-portal.service
        ;;

    start)
        exec /usr/bin/systemctl start veriqta-portal.service
        ;;

    stop)
        exec /usr/bin/systemctl stop veriqta-portal.service
        ;;

    status)
        exec /usr/bin/systemctl status \
            veriqta-portal.service \
            --no-pager
        ;;

    is-active)
        exec /usr/bin/systemctl is-active \
            veriqta-portal.service
        ;;

    *)
        echo "Usage: $0 {restart|start|stop|status|is-active}" >&2
        exit 2
        ;;
esac
```

Protect it:

```bash
sudo chown root:root \
  /usr/local/sbin/veriqta-service-control
```

```bash
sudo chmod 0755 \
  /usr/local/sbin/veriqta-service-control
```

---

# Configure sudo

Create:

```bash
sudo visudo \
  -f /etc/sudoers.d/veriqta-deploy
```

Add:

```text
deploy ALL=(root) NOPASSWD: /usr/local/sbin/veriqta-service-control *
```

Validate:

```bash
sudo visudo -cf \
  /etc/sudoers.d/veriqta-deploy
```

You now have a much smaller privilege surface than unrestricted passwordless sudo.

For a stricter production design, you would narrow command arguments further or use a dedicated deployment service/API rather than broad shell-based remote execution.

---

# Phase 9: Build the Deployment Script on `app01`

Create:

```bash
sudo nano /usr/local/sbin/veriqta-deploy-release
```

Add:

```bash
#!/usr/bin/env bash

set -Eeuo pipefail

BASE_DIR="/opt/veriqta-portal"
INCOMING_DIR="${BASE_DIR}/incoming"
RELEASES_DIR="${BASE_DIR}/releases"
CURRENT_LINK="${BASE_DIR}/current"
HISTORY_FILE="${BASE_DIR}/shared/deployment-history.log"
LOCK_FILE="${BASE_DIR}/shared/deployment.lock"

RELEASE_ID="${1:-}"

if [[ -z "$RELEASE_ID" ]]; then
    echo "Usage: $0 RELEASE_ID" >&2
    exit 2
fi

if [[ ! "$RELEASE_ID" =~ ^[A-Za-z0-9._-]+$ ]]; then
    echo "Invalid release ID." >&2
    exit 2
fi

ARTIFACT="${INCOMING_DIR}/${RELEASE_ID}.tar.gz"
CHECKSUM="${ARTIFACT}.sha256"
NEW_RELEASE="${RELEASES_DIR}/${RELEASE_ID}"

exec 9>"$LOCK_FILE"

if ! flock -n 9; then
    echo "Another deployment is already running." >&2
    exit 1
fi

log() {
    printf '%s %s\n' \
        "$(date -u '+%Y-%m-%dT%H:%M:%SZ')" \
        "$*" \
        | tee -a "$HISTORY_FILE"
}

[[ -f "$ARTIFACT" ]] || {
    log "ERROR release=${RELEASE_ID} artifact_missing"
    exit 1
}

[[ -f "$CHECKSUM" ]] || {
    log "ERROR release=${RELEASE_ID} checksum_missing"
    exit 1
}

cd "$INCOMING_DIR"

sha256sum -c \
    "$(basename "$CHECKSUM")"

if [[ -e "$NEW_RELEASE" ]]; then
    log "ERROR release=${RELEASE_ID} already_exists"
    exit 1
fi

PREVIOUS_RELEASE=""

if [[ -L "$CURRENT_LINK" ]]; then
    PREVIOUS_RELEASE="$(
        readlink -f "$CURRENT_LINK"
    )"
fi

mkdir -p "$NEW_RELEASE"

cleanup_failed_release() {
    rm -rf "$NEW_RELEASE"
}

trap cleanup_failed_release ERR

tar \
    -xzf "$ARTIFACT" \
    -C "$NEW_RELEASE"

chown -R \
    veriqta:veriqta \
    "$NEW_RELEASE"

find "$NEW_RELEASE" \
    -type d \
    -exec chmod 0750 {} \;

find "$NEW_RELEASE" \
    -type f \
    -exec chmod 0640 {} \;

chmod 0750 \
    "${NEW_RELEASE}/app.py"

python3 -m py_compile \
    "${NEW_RELEASE}/app.py"

ln -sfn \
    "$NEW_RELEASE" \
    "${CURRENT_LINK}.new"

mv -Tf \
    "${CURRENT_LINK}.new" \
    "$CURRENT_LINK"

log \
    "ACTIVATE release=${RELEASE_ID} previous=${PREVIOUS_RELEASE:-none}"

if ! sudo /usr/local/sbin/veriqta-service-control restart; then
    log "ERROR release=${RELEASE_ID} service_restart_failed"

    if [[ -n "$PREVIOUS_RELEASE" ]]; then
        ln -sfn \
            "$PREVIOUS_RELEASE" \
            "${CURRENT_LINK}.rollback"

        mv -Tf \
            "${CURRENT_LINK}.rollback" \
            "$CURRENT_LINK"

        sudo /usr/local/sbin/veriqta-service-control restart || true
    fi

    exit 1
fi

HEALTH_OK=0

for attempt in {1..10}; do

    if curl \
        --fail \
        --silent \
        --show-error \
        http://127.0.0.1:8000/health \
        >/dev/null; then

        HEALTH_OK=1
        break
    fi

    sleep 2
done

if (( HEALTH_OK == 0 )); then

    log "ERROR release=${RELEASE_ID} health_check_failed"

    if [[ -n "$PREVIOUS_RELEASE" ]]; then

        log \
            "ROLLBACK from=${NEW_RELEASE} to=${PREVIOUS_RELEASE}"

        ln -sfn \
            "$PREVIOUS_RELEASE" \
            "${CURRENT_LINK}.rollback"

        mv -Tf \
            "${CURRENT_LINK}.rollback" \
            "$CURRENT_LINK"

        sudo /usr/local/sbin/veriqta-service-control restart

        sleep 2

        curl \
            --fail \
            --silent \
            --show-error \
            http://127.0.0.1:8000/health \
            >/dev/null

        log "ROLLBACK_OK release=${PREVIOUS_RELEASE}"
    fi

    exit 1
fi

trap - ERR

log "SUCCESS release=${RELEASE_ID}"

rm -f \
    "$ARTIFACT" \
    "$CHECKSUM"

echo "Deployment successful: ${RELEASE_ID}"
```

Protect:

```bash
sudo chown root:root \
  /usr/local/sbin/veriqta-deploy-release
```

```bash
sudo chmod 0755 \
  /usr/local/sbin/veriqta-deploy-release
```

---

# Give `deploy` Controlled Access

The deployment script writes under:

```text
/opt/veriqta-portal
```

and performs the deployment itself.

For this lab, allow `deploy` to execute it through sudo.

Add through:

```bash
sudo visudo \
  -f /etc/sudoers.d/veriqta-deploy
```

the exact deployment command:

```text
deploy ALL=(root) NOPASSWD: /usr/local/sbin/veriqta-deploy-release *
```

Revalidate:

```bash
sudo visudo -cf \
  /etc/sudoers.d/veriqta-deploy
```

This is still much better than:

```text
deploy ALL=(ALL) NOPASSWD: ALL
```

---

# 13. Configuration

# Phase 10: Establish SSH Trust

Now return to `ci01`.

Jenkins needs to authenticate to `app01`.

But do not begin by generating a key directly inside random Jenkins directories.

First understand the identity:

```bash
getent passwd jenkins
```

You need:

```text
Jenkins
   |
   | SSH private key
   v
deploy@app01
```

Generate a dedicated deployment key in a protected administrative staging location:

```bash
sudo ssh-keygen \
  -t ed25519 \
  -f /root/veriqta-jenkins-deploy \
  -C "jenkins-ci-deployment"
```

Use this key only for this lab deployment path.

Copy the public key to `deploy@app01`.

For example:

```bash
sudo ssh-copy-id \
  -i /root/veriqta-jenkins-deploy.pub \
  deploy@APP01_IP
```

Then test using the explicit key:

```bash
sudo ssh \
  -i /root/veriqta-jenkins-deploy \
  deploy@APP01_IP \
  'hostname'
```

Expected:

```text
app01
```

The private key will later be stored using Jenkins credentials rather than left in a source repository.

---

# SSH Host-Key Trust

Do **not** solve host-key verification problems with:

```text
StrictHostKeyChecking=no
```

That disables an important SSH identity check.

Instead, obtain and verify the target host key through a trusted channel.

On `app01`, inspect the fingerprint directly:

```bash
sudo ssh-keygen \
  -lf /etc/ssh/ssh_host_ed25519_key.pub
```

From `ci01`, retrieve the advertised key:

```bash
ssh-keyscan \
  -t ed25519 \
  APP01_IP
```

Compare the fingerprint against the one you verified on `app01`.

Only after verification should the key be trusted by the automation environment.

This distinction matters:

```text
SSH USER KEY
    proves
client identity to server

SSH HOST KEY
    proves
server identity to client
```

You need both.

---

# 14. Automation

# Phase 11: Install Jenkins on `ci01`

Install Jenkins using its supported package repository for Ubuntu and a supported Java runtime.

After installation, verify:

```bash
java -version
```

Then:

```bash
jenkins --version
```

Check:

```bash
sudo systemctl status \
  jenkins \
  --no-pager
```

Then:

```bash
sudo systemctl is-enabled jenkins
```

Expected:

```text
enabled
```

Check listener:

```bash
sudo ss -lntp | grep ':8080'
```

Jenkins commonly listens on TCP 8080 unless you configure another port.

Do not expose Jenkins broadly to the internet for this lab.

Restrict access to your administration network.

---

# Jenkins Firewall

Preserve SSH.

Then allow Jenkins only from your lab/admin subnet.

Example:

```bash
sudo ufw allow OpenSSH
```

```bash
sudo ufw allow \
  from 192.168.60.0/24 \
  to any \
  port 8080 \
  proto tcp
```

Enable UFW if appropriate:

```bash
sudo ufw enable
```

Verify:

```bash
sudo ufw status verbose
```

---

# Initial Jenkins Setup

Retrieve the initial administrative password:

```bash
sudo cat \
  /var/lib/jenkins/secrets/initialAdminPassword
```

Open Jenkins from your management machine:

```text
http://CI01_IP:8080
```

Complete the initial setup.

Install only plugins you actually need.

At minimum, you need Git and Pipeline capabilities.

Avoid turning the Jenkins server into a plugin collection.

Every plugin increases maintenance and security responsibilities.

---

# Phase 12: Add Deployment Credentials to Jenkins

Store the deployment private key in Jenkins Credentials.

Create a credential such as:

```text
Kind:
SSH Username with private key

Username:
deploy

ID:
veriqta-app01-deploy
```

Paste the dedicated private key.

Do not put it into:

```text
Jenkinsfile
Git
environment file
shell script
README
```

After confirming Jenkins has the credential, securely remove unnecessary staging copies of the private key from locations where they are no longer needed.

Keep the public key and documented fingerprint as appropriate.

---

# Phase 13: Create Jenkins Host Trust

The Jenkins account also needs a trusted `known_hosts` entry.

Create:

```bash
sudo install \
  -d \
  -o jenkins \
  -g jenkins \
  -m 0700 \
  /var/lib/jenkins/.ssh
```

After you have independently verified the `app01` host fingerprint, add the verified public host key to:

```text
/var/lib/jenkins/.ssh/known_hosts
```

Protect:

```bash
sudo chown \
  jenkins:jenkins \
  /var/lib/jenkins/.ssh/known_hosts
```

```bash
sudo chmod 0600 \
  /var/lib/jenkins/.ssh/known_hosts
```

---

# Phase 14: Prepare Git Repository

Initialize if needed:

```bash
git init
```

Create:

```bash
nano .gitignore
```

Add:

```text
build/
artifacts/
__pycache__/
*.pyc
*.log
*.key
*.pem
.env
credentials/
evidence/private/
```

Add files:

```bash
git add .
```

Commit:

```bash
git commit \
  -m "Build Linux CI/CD deployment platform"
```

Push to your remote Git repository.

For a public Git repository, never commit:

```text
private SSH keys
Jenkins secrets
tokens
passwords
production host secrets
unsanitized credentials
```

---

# Phase 15: Create the Jenkins Pipeline

Create:

```bash
nano Jenkinsfile
```

Use a declarative pipeline:

```groovy
pipeline {

    agent any

    options {
        timestamps()
        disableConcurrentBuilds()
        buildDiscarder(
            logRotator(
                numToKeepStr: '20',
                artifactNumToKeepStr: '10'
            )
        )
    }

    environment {
        APP_HOST = 'APP01_IP'
        APP_USER = 'deploy'
    }

    stages {

        stage('Checkout') {
            steps {
                checkout scm

                sh '''
                    set -eux

                    git rev-parse HEAD
                    git status --short
                '''
            }
        }

        stage('Validate') {
            steps {
                sh '''
                    set -eux

                    python3 -m py_compile \
                        application/app.py \
                        application/test_app.py

                    bash -n scripts/test.sh
                    bash -n scripts/build.sh
                '''
            }
        }

        stage('Test') {
            steps {
                sh '''
                    set -eux

                    ./scripts/test.sh
                '''
            }
        }

        stage('Build') {
            steps {
                sh '''
                    set -eux

                    ./scripts/build.sh

                    RELEASE_ID="$(cat artifacts/LATEST_RELEASE)"

                    sha256sum -c \
                        "artifacts/${RELEASE_ID}.tar.gz.sha256"

                    tar -tzf \
                        "artifacts/${RELEASE_ID}.tar.gz"
                '''
            }
        }

        stage('Archive') {
            steps {
                archiveArtifacts(
                    artifacts: 'artifacts/*.tar.gz,artifacts/*.sha256,artifacts/LATEST_RELEASE',
                    fingerprint: true
                )
            }
        }

        stage('Deploy') {
            steps {
                sshagent(
                    credentials: ['veriqta-app01-deploy']
                ) {
                    sh '''
                        set -eux

                        RELEASE_ID="$(cat artifacts/LATEST_RELEASE)"

                        scp \
                            "artifacts/${RELEASE_ID}.tar.gz" \
                            "${APP_USER}@${APP_HOST}:/opt/veriqta-portal/incoming/"

                        scp \
                            "artifacts/${RELEASE_ID}.tar.gz.sha256" \
                            "${APP_USER}@${APP_HOST}:/opt/veriqta-portal/incoming/"

                        ssh \
                            "${APP_USER}@${APP_HOST}" \
                            "sudo /usr/local/sbin/veriqta-deploy-release '${RELEASE_ID}'"
                    '''
                }
            }
        }

        stage('Verify') {
            steps {
                sh '''
                    set -eux

                    curl \
                        --fail \
                        --silent \
                        --show-error \
                        "http://${APP_HOST}/health"

                    curl \
                        --fail \
                        --silent \
                        --show-error \
                        "http://${APP_HOST}/"
                '''
            }
        }
    }

    post {

        success {
            echo 'CI/CD deployment completed successfully.'
        }

        failure {
            echo 'Pipeline failed. Inspect the failed stage before retrying.'
        }

        always {
            sh '''
                echo "Build: ${BUILD_NUMBER}"
                echo "Commit: ${GIT_COMMIT:-unknown}"
            '''
        }
    }
}
```

Replace:

```text
APP01_IP
```

with your lab target.

---

# Understand the Pipeline

Your pipeline is:

```text
CHECKOUT
   ↓
VALIDATE
   ↓
TEST
   ↓
BUILD
   ↓
ARCHIVE
   ↓
DEPLOY
   ↓
VERIFY
```

Every stage has a purpose.

---

## Checkout

Answers:

> What source are we building?

---

## Validate

Answers:

> Is the source structurally valid enough to continue?

---

## Test

Answers:

> Does the code satisfy the automated tests?

---

## Build

Answers:

> Can we produce a deployable release artifact?

---

## Archive

Answers:

> Can we preserve the exact artifact and evidence produced by this build?

---

## Deploy

Answers:

> Can we transfer and activate this release safely?

---

## Verify

Answers:

> Is the deployed application actually serving traffic successfully?

That final stage matters.

A successful:

```text
scp
```

is not a successful deployment.

---

# Phase 16: Create the Jenkins Job

Create a Pipeline job.

Configure it to load:

```text
Jenkinsfile
```

from source control.

This gives you:

```text
PIPELINE DEFINITION
        |
        v
GIT REPOSITORY
```

instead of hiding the entire deployment process inside Jenkins' web interface.

That makes pipeline changes reviewable and version-controlled.

---

# First Deployment

Run the pipeline.

Watch each stage.

Do not immediately retry if something fails.

Investigate the failed layer.

When successful, go to `app01`.

Run:

```bash
find \
  /opt/veriqta-portal/releases \
  -maxdepth 2 \
  -type f \
  -print
```

Inspect:

```bash
readlink -f \
  /opt/veriqta-portal/current
```

Check:

```bash
sudo systemctl status \
  veriqta-portal \
  --no-pager
```

Then:

```bash
curl \
  http://127.0.0.1:8000/health
```

Then through Nginx:

```bash
curl \
  http://127.0.0.1/health
```

---

# Verify Release Metadata

```bash
cat \
  /opt/veriqta-portal/current/BUILD-INFO
```

You should be able to compare:

```text
commit_sha
```

with:

```bash
git log -1 --format='%H'
```

from the corresponding source repository.

This creates deployment traceability.

---

# Release 2

Change:

```text
application/VERSION
```

from:

```text
1.0.0
```

to:

```text
1.1.0
```

Commit:

```bash
git add application/VERSION
```

```bash
git commit \
  -m "Release portal version 1.1.0"
```

Push.

Run the pipeline again.

Now inspect:

```bash
find \
  /opt/veriqta-portal/releases \
  -mindepth 1 \
  -maxdepth 1 \
  -type d \
  -printf '%f\n'
```

You should have multiple releases.

Check:

```bash
readlink -f \
  /opt/veriqta-portal/current
```

The symlink should point to the latest successful release.

---

# 15. Security

A CI/CD server is security-sensitive infrastructure.

Why?

Because it can often:

```text
read source code
access credentials
build artifacts
connect to servers
change production software
restart services
```

A compromised deployment server can become a path to every server it controls.

Treat Jenkins as privileged infrastructure.

---

## Security Boundary

```text
DEVELOPER
   |
   v
SOURCE CONTROL
   |
   v
JENKINS
   |
   | dedicated credential
   v
DEPLOY USER
   |
   | controlled sudo
   v
DEPLOYMENT HELPER
   |
   v
APPLICATION RELEASE
   |
   v
VERIQTA RUNTIME USER
```

Each identity has a different job.

---

## Do Not Run Jenkins as Root

Verify:

```bash
ps -eo user,pid,cmd \
  | grep '[j]enkins'
```

You should see the Jenkins service under its dedicated account.

---

## Protect Credentials

Do not place secrets in:

```text
Jenkinsfile
Git repository
shell scripts
build artifacts
console output
BUILD-INFO
```

Use Jenkins credentials for deployment secrets.

---

## Protect SSH Host Verification

Never make this your normal automation:

```bash
ssh \
  -o StrictHostKeyChecking=no \
  ...
```

It makes automation easier by removing an important security check.

Solve trust correctly instead.

---

## Restrict Deployment Privilege

`deploy` should not be:

```text
root
```

and should not have unrestricted passwordless sudo.

Its privileged path should be small, reviewable, and purpose-built.

---

## Protect Runtime Account

Check:

```bash
getent passwd veriqta
```

The runtime account should not have an ordinary interactive login shell.

---

## Protect Jenkins Network Exposure

Do not expose port:

```text
8080
```

to networks that do not need Jenkins access.

For a production design you would normally add stronger ingress controls, TLS, centralized identity, hardened authentication, authorization, patching, and monitoring.

---

# 16. Testing

You need tests at several levels.

```text
SOURCE TEST
    ↓
BUILD TEST
    ↓
ARTIFACT TEST
    ↓
TRANSFER TEST
    ↓
DEPLOYMENT TEST
    ↓
SERVICE TEST
    ↓
HTTP TEST
    ↓
ROLLBACK TEST
    ↓
REBOOT TEST
```

---

## Source Test

```bash
python3 -m py_compile \
  application/app.py \
  application/test_app.py
```

---

## Unit Test

```bash
./scripts/test.sh
```

---

## Build Test

```bash
./scripts/build.sh
```

Verify artifact exists.

---

## Artifact Integrity

```bash
RELEASE_ID="$(cat artifacts/LATEST_RELEASE)"
```

```bash
sha256sum -c \
  "artifacts/${RELEASE_ID}.tar.gz.sha256"
```

---

## Artifact Content

```bash
tar -tzf \
  "artifacts/${RELEASE_ID}.tar.gz"
```

Verify:

```text
app.py
VERSION
BUILD-INFO
```

---

## Deployment Test

On `app01`:

```bash
readlink -f \
  /opt/veriqta-portal/current
```

---

## Runtime Test

```bash
sudo systemctl is-active \
  veriqta-portal
```

Expected:

```text
active
```

---

## Listener Test

```bash
sudo ss -lntp \
  | grep ':8000'
```

The application should listen only on loopback.

---

## Nginx Test

```bash
sudo nginx -t
```

---

## Health Test

```bash
curl -f \
  http://127.0.0.1/health
```

---

## External Test

From another lab machine:

```bash
curl -f \
  http://APP01_IP/health
```

---

## Release Traceability Test

```bash
cat \
  /opt/veriqta-portal/current/BUILD-INFO
```

Compare its commit SHA with the build commit.

---

# 17. Failure Simulation

This is where the project becomes operational.

Do not skip these labs.

---

## Failure Lab 01: Break a Unit Test

Change:

```python
self.assertGreater(app.PORT, 0)
```

to a deliberately false assertion.

Commit the controlled failure to a test branch or make the experiment in a safe lab workflow.

Run the pipeline.

Expected:

```text
Checkout   PASS
Validate   PASS
Test       FAIL
Build      NOT RUN
Deploy     NOT RUN
```

The important outcome is:

> Broken code never reaches `app01`.

Restore the test.

Run again.

---

# Failure Lab 02: Introduce Python Syntax Error

Introduce a controlled syntax error in:

```text
application/app.py
```

Run the pipeline.

Expected:

```text
Validate FAIL
```

The deployment stage should never execute.

Restore.

---

# Failure Lab 03: Break the Build Script

Temporarily reference a nonexistent file from:

```text
scripts/build.sh
```

Run.

Expected:

```text
Test   PASS
Build  FAIL
Deploy NOT RUN
```

This teaches you that:

```text
tests passed
```

does not guarantee:

```text
artifact can be built
```

---

# Failure Lab 04: Corrupt the Artifact

After building a controlled local artifact:

```bash
printf 'corruption\n' \
  >> "artifacts/${RELEASE_ID}.tar.gz"
```

Then:

```bash
sha256sum -c \
  "artifacts/${RELEASE_ID}.tar.gz.sha256"
```

Expected:

```text
FAILED
```

A deployment should never trust an artifact whose integrity check fails.

Rebuild it.

---

# Failure Lab 05: Wrong SSH Key

Temporarily configure the pipeline with a different test credential.

Expected deployment-stage failure.

Investigate from `ci01`:

```bash
ssh -vvv \
  deploy@APP01_IP
```

Look at:

```text
key offered
key accepted/rejected
known_hosts
authentication methods
```

On `app01`:

```bash
sudo journalctl \
  -u ssh \
  --since "10 minutes ago" \
  --no-pager
```

Restore the correct credential.

---

# Failure Lab 06: Wrong SSH Host Key

In a controlled lab, change the target VM's SSH host identity or deliberately provide an incorrect `known_hosts` entry.

The connection should fail.

Do not “fix” it with:

```text
StrictHostKeyChecking=no
```

Verify the real fingerprint and repair the trust record.

This is exactly what host-key verification is supposed to protect.

---

# Failure Lab 07: Deployment Permission Failure

Temporarily remove `deploy` write access from:

```text
/opt/veriqta-portal/incoming
```

Run deployment.

Inspect:

```bash
namei -l \
  /opt/veriqta-portal/incoming
```

Then:

```bash
sudo -u deploy \
  test -w /opt/veriqta-portal/incoming
```

Restore correct ownership/permissions.

Do not use:

```bash
chmod 777
```

---

# Failure Lab 08: Application Service Fails to Start

Create a release whose `app.py` contains a runtime startup failure that passes basic syntax validation.

Deploy.

Investigate on `app01`:

```bash
sudo systemctl status \
  veriqta-portal \
  --no-pager
```

Then:

```bash
sudo journalctl \
  -u veriqta-portal \
  -n 100 \
  --no-pager
```

The deployment logic should detect failed health and restore the previous release when one exists.

Verify:

```bash
readlink -f \
  /opt/veriqta-portal/current
```

---

# Failure Lab 09: Health Endpoint Fails

Create a controlled release where:

```text
/health
```

returns a non-2xx response.

The process may still run.

systemd may show:

```text
active
```

But the deployment health check should fail.

This teaches:

```text
PROCESS RUNNING
```

is not the same as:

```text
APPLICATION HEALTHY
```

Rollback should occur.

---

# Failure Lab 10: Nginx Is Down

Stop:

```bash
sudo systemctl stop nginx
```

Test externally.

The application may still respond directly on:

```text
127.0.0.1:8000
```

from `app01`.

But external users cannot reach it.

Investigate:

```bash
sudo systemctl status nginx --no-pager
```

```bash
sudo ss -lntp
```

```bash
curl -f http://127.0.0.1:8000/health
```

This isolates:

```text
APPLICATION HEALTHY
NGINX UNAVAILABLE
```

Recover:

```bash
sudo systemctl start nginx
```

---

# Failure Lab 11: Application Is Down but Nginx Is Up

Stop:

```bash
sudo systemctl stop \
  veriqta-portal
```

Nginx remains active.

Request:

```bash
curl -i \
  http://127.0.0.1/
```

You should see an upstream failure such as a 502 response.

Inspect:

```bash
sudo tail -n 100 \
  /var/log/nginx/veriqta-portal-error.log
```

Then:

```bash
sudo journalctl \
  -u veriqta-portal \
  -n 100 \
  --no-pager
```

Recover.

---

# Failure Lab 12: Port 8000 Conflict

Stop the application first.

Start a disposable process on:

```text
127.0.0.1:8000
```

Then start the application.

It should fail to bind.

Investigate:

```bash
sudo ss -lntp \
  | grep ':8000'
```

Find the actual process owner.

Remove the controlled conflicting process.

Start the application again.

---

# Failure Lab 13: Two Deployments at Once

Start one deliberately slowed deployment in your lab.

While it is still running, trigger another.

The second deployment should encounter:

```text
Another deployment is already running.
```

because of:

```text
flock
```

Without deployment locking:

```text
DEPLOYMENT A
     |
     +--> changes current
     |
DEPLOYMENT B
     |
     +--> changes current
```

can create race conditions.

---

# Failure Lab 14: Incorrect `current` Symlink

Inspect:

```bash
readlink -f \
  /opt/veriqta-portal/current
```

In a controlled lab, point it at a nonexistent release.

Restart.

Investigate:

```bash
sudo systemctl status \
  veriqta-portal \
  --no-pager
```

Then:

```bash
sudo journalctl \
  -u veriqta-portal \
  -n 100 \
  --no-pager
```

Repair the symlink to a known-good release.

---

# Failure Lab 15: CI Server Cannot Reach `app01`

Block the deployment path in a controlled lab firewall rule or disconnect the network interface in your VM environment.

From `ci01`:

```bash
ping -c 3 APP01_IP
```

Then:

```bash
nc -vz APP01_IP 22
```

Then:

```bash
ip route
```

Then:

```bash
ssh -vvv \
  deploy@APP01_IP
```

Differentiate:

```text
DNS/name problem
routing problem
firewall timeout
connection refused
SSH authentication failure
```

These are not the same incident.

---

# Failure Lab 16: Jenkins Service Stops

On `ci01`:

```bash
sudo systemctl stop jenkins
```

Check:

```bash
systemctl status \
  jenkins \
  --no-pager
```

Then:

```bash
sudo ss -lntp \
  | grep ':8080'
```

Recover:

```bash
sudo systemctl start jenkins
```

Then inspect:

```bash
sudo journalctl \
  -u jenkins \
  -n 100 \
  --no-pager
```

---

# Failure Lab 17: Disk Pressure on CI Server

Do not fill the filesystem to 100%.

Inspect:

```bash
df -hT
```

Then:

```bash
sudo du -sh \
  /var/lib/jenkins
```

Inspect large workspace/build areas.

Ask:

```text
Are old builds accumulating?

Are artifacts accumulating?

Are workspaces unusually large?

Are logs growing?

Is another service consuming the disk?
```

This is why build retention matters.

---

# Failure Lab 18: Disk Pressure on Deployment Server

Inspect:

```bash
df -hT
```

Then:

```bash
sudo du -sh \
  /opt/veriqta-portal/releases
```

Then:

```bash
sudo du -sh \
  /var/log/nginx
```

Versioned deployments require retention management.

You cannot keep infinite releases.

But you also should not blindly delete:

```text
current
```

or the previous rollback candidate.

---

# 18. Troubleshooting

When someone says:

> Jenkins deployment failed.

Do not restart Jenkins immediately.

Find the failed boundary.

Use:

```text
SOURCE
  ↓
CHECKOUT
  ↓
VALIDATION
  ↓
TEST
  ↓
BUILD
  ↓
ARTIFACT
  ↓
CHECKSUM
  ↓
SSH
  ↓
TRANSFER
  ↓
REMOTE PERMISSIONS
  ↓
RELEASE EXTRACTION
  ↓
SYMLINK ACTIVATION
  ↓
SYSTEMD
  ↓
APPLICATION LISTENER
  ↓
HEALTH ENDPOINT
  ↓
NGINX
  ↓
CLIENT
```

---

## Problem: Checkout Fails

Investigate:

```text
repository URL
repository availability
Git credentials
branch name
network connectivity
repository permissions
```

Do not debug Nginx.

The pipeline has not reached deployment.

---

## Problem: Tests Fail

Run exactly the same command Jenkins runs:

```bash
./scripts/test.sh
```

The CI environment should not have a completely different test procedure from your documented local workflow.

---

## Problem: Build Fails

Run:

```bash
bash -x \
  scripts/build.sh
```

Inspect:

```text
working directory
Git state
file paths
permissions
disk space
tar
checksum generation
```

---

## Problem: SSH Times Out

From `ci01`:

```bash
nc -vz \
  APP01_IP \
  22
```

Timeout usually points you toward:

```text
routing
firewall
network path
VM network
```

rather than SSH credentials.

---

## Problem: SSH Says Permission Denied

Now investigate:

```text
username
private key
authorized_keys
permissions
SSH policy
account state
```

On target:

```bash
sudo journalctl \
  -u ssh \
  -n 100 \
  --no-pager
```

---

## Problem: Host-Key Verification Failed

Do not disable checking.

Verify:

```bash
sudo ssh-keygen \
  -lf /etc/ssh/ssh_host_ed25519_key.pub
```

on `app01`.

Compare with the trusted record.

Determine whether:

```text
server was rebuilt
IP was reused
host key legitimately changed
known_hosts is stale
```

or whether the identity change is unexpected.

---

## Problem: Artifact Checksum Fails

Do not deploy it.

Determine whether:

```text
artifact changed after checksum creation
wrong checksum file transferred
transfer was incomplete
release IDs were mixed
```

Rebuild/retransfer from a trusted source.

---

## Problem: `sudo` Fails for Deploy User

Test on `app01`:

```bash
sudo -l -U deploy
```

Inspect:

```bash
sudo visudo -cf \
  /etc/sudoers.d/veriqta-deploy
```

Do not respond by granting unrestricted sudo.

---

## Problem: systemd Service Fails

Use:

```bash
sudo systemctl status \
  veriqta-portal \
  --no-pager
```

Then:

```bash
sudo journalctl \
  -u veriqta-portal \
  -n 100 \
  --no-pager
```

Then inspect:

```bash
readlink -f \
  /opt/veriqta-portal/current
```

Then:

```bash
namei -l \
  /opt/veriqta-portal/current/app.py
```

Then:

```bash
sudo -u veriqta \
  test -r \
  /opt/veriqta-portal/current/app.py
```

Work from evidence.

---

## Problem: Service Active but Health Fails

Check:

```bash
curl -v \
  http://127.0.0.1:8000/health
```

Then:

```bash
sudo journalctl \
  -u veriqta-portal \
  -n 100 \
  --no-pager
```

The process exists.

Now investigate application behavior.

---

## Problem: Direct Application Works but Nginx Fails

If:

```bash
curl -f \
  http://127.0.0.1:8000/health
```

works but:

```bash
curl -f \
  http://127.0.0.1/health
```

fails, your investigation has already narrowed dramatically.

Check:

```bash
sudo nginx -t
```

```bash
sudo systemctl status nginx --no-pager
```

```bash
sudo tail -n 100 \
  /var/log/nginx/veriqta-portal-error.log
```

You are now debugging:

```text
NGINX → APPLICATION
```

not:

```text
JENKINS → GIT
```

---

# Operational Troubleshooting Model

Use this every time:

```text
SYMPTOM
   ↓
BLAST RADIUS
   ↓
RECENT CHANGE
   ↓
FAILED PIPELINE STAGE
   ↓
WHAT STILL WORKS?
   ↓
WHERE DID THE WORKFLOW STOP?
   ↓
COLLECT LOGS + STATE
   ↓
FORM HYPOTHESIS
   ↓
TEST HYPOTHESIS
   ↓
NARROW FAILURE DOMAIN
   ↓
ROOT CAUSE
   ↓
SMALLEST SAFE FIX
   ↓
VERIFY COMPONENT
   ↓
VERIFY FULL DEPLOYMENT PATH
   ↓
DOCUMENT
   ↓
PREVENT RECURRENCE
```

---

# Rollback Engineering

A rollback is not:

```text
git pull something old
```

Your releases are immutable directories.

Example:

```text
releases/
├── release-A
├── release-B
└── release-C

current -> release-C
```

Rollback means:

```text
current -> release-B
```

followed by:

```text
restart
health check
verification
```

---

# Manual Rollback Script

Create on `app01`:

```bash
sudo nano /usr/local/sbin/veriqta-rollback
```

Add:

```bash
#!/usr/bin/env bash

set -Eeuo pipefail

BASE_DIR="/opt/veriqta-portal"
RELEASES_DIR="${BASE_DIR}/releases"
CURRENT_LINK="${BASE_DIR}/current"
HISTORY_FILE="${BASE_DIR}/shared/deployment-history.log"
LOCK_FILE="${BASE_DIR}/shared/deployment.lock"

TARGET_RELEASE="${1:-}"

if [[ -z "$TARGET_RELEASE" ]]; then
    echo "Usage: $0 RELEASE_ID" >&2
    exit 2
fi

if [[ ! "$TARGET_RELEASE" =~ ^[A-Za-z0-9._-]+$ ]]; then
    echo "Invalid release ID." >&2
    exit 2
fi

TARGET_PATH="${RELEASES_DIR}/${TARGET_RELEASE}"

[[ -d "$TARGET_PATH" ]] || {
    echo "Release does not exist: $TARGET_RELEASE" >&2
    exit 1
}

exec 9>"$LOCK_FILE"

flock -n 9 || {
    echo "Another deployment operation is running." >&2
    exit 1
}

CURRENT_RELEASE="$(
    readlink -f "$CURRENT_LINK" 2>/dev/null \
    || true
)"

ln -sfn \
    "$TARGET_PATH" \
    "${CURRENT_LINK}.rollback"

mv -Tf \
    "${CURRENT_LINK}.rollback" \
    "$CURRENT_LINK"

sudo /usr/local/sbin/veriqta-service-control restart

for attempt in {1..10}; do

    if curl \
        --fail \
        --silent \
        http://127.0.0.1:8000/health \
        >/dev/null; then

        printf '%s ROLLBACK from=%s to=%s\n' \
            "$(date -u '+%Y-%m-%dT%H:%M:%SZ')" \
            "${CURRENT_RELEASE:-unknown}" \
            "$TARGET_PATH" \
            >> "$HISTORY_FILE"

        echo "Rollback successful."
        exit 0
    fi

    sleep 2
done

echo "Rollback target failed health validation." >&2
exit 1
```

Protect it and allow the exact command through your controlled sudo policy if Jenkins/deploy needs it.

---

# Important Rollback Limitation

Filesystem rollback is straightforward because releases are versioned.

Database rollback is not.

If release `1.2.0` changes a database schema incompatibly, pointing the application back to `1.1.0` may not restore service.

That introduces a deeper production principle:

> Application deployment and database migration strategy must be designed together.

For this project, the demonstration application intentionally avoids schema migrations.

Document this as a limitation.

---

# Release Retention

Keeping every release forever eventually becomes a storage problem.

A sensible lab policy might retain:

```text
current release
previous several successful releases
```

Never write cleanup automation that blindly runs:

```bash
rm -rf releases/*
```

Your cleanup process must identify:

```text
current release
rollback candidates
age/order
```

before deleting anything.

---

# Deployment History

Inspect:

```bash
cat \
  /opt/veriqta-portal/shared/deployment-history.log
```

A useful history might contain:

```text
2026-09-24T12:10:00Z ACTIVATE release=... previous=...
2026-09-24T12:10:03Z SUCCESS release=...
```

or:

```text
... health_check_failed
... ROLLBACK ...
... ROLLBACK_OK ...
```

Now deployment activity exists outside a human's memory.

---

# 19. Final Validation

Do this only after repairing all failure labs.

## `ci01`

Check:

```bash
systemctl is-active jenkins
```

```bash
systemctl is-enabled jenkins
```

```bash
git --version
```

```bash
python3 --version
```

```bash
df -hT
```

```bash
free -h
```

```bash
systemctl --failed
```

---

## `app01`

Check:

```bash
systemctl is-active nginx
```

```bash
systemctl is-enabled nginx
```

```bash
systemctl is-active veriqta-portal
```

```bash
systemctl is-enabled veriqta-portal
```

If the application service has not yet been enabled:

```bash
sudo systemctl enable \
  veriqta-portal
```

---

## Nginx

```bash
sudo nginx -t
```

---

## Listener Verification

```bash
sudo ss -lntp
```

Expected conceptual exposure:

```text
22      SSH
80      Nginx
8000    loopback only
```

On `ci01`, Jenkins may additionally expose its restricted administration port.

---

## Release Verification

```bash
readlink -f \
  /opt/veriqta-portal/current
```

Then:

```bash
cat \
  /opt/veriqta-portal/current/BUILD-INFO
```

---

## Runtime User

```bash
ps -eo user,pid,cmd \
  | grep '[a]pp.py'
```

The application should run as:

```text
veriqta
```

not root.

---

## Direct Health

```bash
curl -f \
  http://127.0.0.1:8000/health
```

---

## Proxied Health

```bash
curl -f \
  http://127.0.0.1/health
```

---

## Remote Health

From `ci01`:

```bash
curl -f \
  http://APP01_IP/health
```

---

## Deployment Account

```bash
sudo -l -U deploy
```

Review the privileges.

You should not see unrestricted administrative access.

---

## Permissions

```bash
namei -l \
  /opt/veriqta-portal/current/app.py
```

---

## Logs

```bash
sudo journalctl \
  -u veriqta-portal \
  -n 50 \
  --no-pager
```

```bash
sudo tail -n 50 \
  /var/log/nginx/veriqta-portal-access.log
```

---

# Reboot Validation

Reboot `app01`:

```bash
sudo reboot
```

Reconnect.

Do not manually repair anything first.

Check:

```bash
systemctl is-active nginx
```

```bash
systemctl is-active veriqta-portal
```

```bash
readlink -f \
  /opt/veriqta-portal/current
```

```bash
curl -f \
  http://127.0.0.1/health
```

The current release should survive because it is filesystem state.

The service should return because systemd is enabled.

Now reboot `ci01`.

After reconnecting:

```bash
systemctl is-active jenkins
```

Verify Jenkins is available again.

Finally, run another deployment after both servers have recovered.

That proves more than boot recovery.

It proves the deployment path still works.

---

# 20. Portfolio Documentation

Your public README should explain the engineering decisions, not merely list commands.

Recommended sections:

```text
Overview

Problem Statement

What I Built

Architecture

Server Roles

CI/CD Workflow

Source-Control Strategy

Pipeline Stages

Build Process

Artifact Strategy

Artifact Integrity

Release Naming

Deployment Identity

SSH Trust Model

Filesystem Architecture

Versioned Releases

Atomic Activation

Deployment Locking

systemd Runtime

Nginx Reverse Proxy

Health Checks

Rollback Strategy

Release Retention

Security Model

Credential Management

Least Privilege

Firewall Design

Logging

Deployment Audit Trail

Testing Strategy

Failure Engineering

Troubleshooting Methodology

Reboot Validation

Known Limitations

Production Improvements

Lessons Learned
```

---

# Architecture Diagram for README

```text
                         DEVELOPER
                             |
                             | git push
                             v
                    +----------------+
                    |  Git Repository|
                    +--------+-------+
                             |
                             | checkout
                             v
+------------------------------------------------------+
|                       ci01                           |
|                                                      |
|   Jenkins                                            |
|      |                                               |
|      +--> Validate                                   |
|      +--> Test                                       |
|      +--> Build                                      |
|      +--> SHA-256                                    |
|      +--> Archive                                    |
|      |                                               |
+------|-----------------------------------------------+
       |
       | SSH / SCP
       | deployment credential
       v
+------------------------------------------------------+
|                       app01                          |
|                                                      |
| deploy                                               |
|   |                                                  |
|   v                                                  |
| /opt/veriqta-portal/releases/                        |
|   |                                                  |
|   +--> release-A                                     |
|   +--> release-B                                     |
|   +--> release-C                                     |
|                                                      |
| current --------------------------> release-C        |
|                                          |           |
|                                          v           |
|                                    systemd           |
|                                          |           |
|                                          v           |
|                                      Python          |
|                                   127.0.0.1:8000     |
|                                          ^           |
|                                          |           |
|                                       Nginx          |
|                                          ^           |
+------------------------------------------|-----------+
                                           |
                                           v
                                         CLIENT
```

---

# 21. GitHub Structure

Use:

```text
04-Projects/
└── Mid-Level/
    └── 09-Linux-CI-CD-Deployment-Server/
```

Recommended:

```text
09-Linux-CI-CD-Deployment-Server/
│
├── README.md
├── PREREQUISITES.md
├── ENVIRONMENT.md
├── SETUP.md
├── ARCHITECTURE.md
├── SECURITY.md
├── CHANGE-PLAN.md
├── ROLLBACK.md
├── Jenkinsfile
├── .gitignore
│
├── application/
│   ├── app.py
│   ├── test_app.py
│   └── VERSION
│
├── deployment/
│   ├── deploy.sh
│   ├── rollback.sh
│   ├── cleanup-releases.sh
│   └── veriqta-portal.service
│
├── scripts/
│   ├── test.sh
│   ├── build.sh
│   ├── verify-artifact.sh
│   ├── health-check.sh
│   └── collect-diagnostics.sh
│
├── tests/
│   ├── test-source.sh
│   ├── test-build.sh
│   ├── test-artifact.sh
│   ├── test-ssh.sh
│   ├── test-deployment.sh
│   ├── test-health.sh
│   ├── test-rollback.sh
│   └── test-reboot.sh
│
├── failure-labs/
│   ├── 01-test-failure.md
│   ├── 02-syntax-failure.md
│   ├── 03-build-failure.md
│   ├── 04-artifact-corruption.md
│   ├── 05-ssh-authentication.md
│   ├── 06-host-key-failure.md
│   ├── 07-permission-failure.md
│   ├── 08-service-startup-failure.md
│   ├── 09-health-check-failure.md
│   ├── 10-nginx-failure.md
│   ├── 11-application-failure.md
│   ├── 12-port-conflict.md
│   ├── 13-concurrent-deployment.md
│   ├── 14-symlink-failure.md
│   ├── 15-network-failure.md
│   ├── 16-jenkins-failure.md
│   ├── 17-ci-disk-pressure.md
│   └── 18-app-disk-pressure.md
│
├── runbooks/
│   ├── pipeline-failed.md
│   ├── checkout-failed.md
│   ├── tests-failed.md
│   ├── build-failed.md
│   ├── ssh-failed.md
│   ├── deployment-failed.md
│   ├── service-failed.md
│   ├── nginx-502.md
│   ├── health-check-failed.md
│   ├── rollback-release.md
│   └── disk-pressure.md
│
├── docs/
│   ├── pipeline-flow.md
│   ├── release-strategy.md
│   ├── ssh-trust-model.md
│   ├── permissions-model.md
│   ├── deployment-locking.md
│   ├── rollback-strategy.md
│   ├── security-model.md
│   ├── troubleshooting-methodology.md
│   ├── known-limitations.md
│   └── production-improvements.md
│
├── evidence/
│   └── README.md
│
└── verification/
    └── FINAL-VALIDATION.md
```

---

# Never Commit

Never publish:

```text
SSH private keys
Jenkins credential exports
passwords
API tokens
Git access tokens
session cookies
real production IP information when sensitive
Jenkins secret files
private environment files
unsanitized logs containing credentials
```

---

# 22. Resume Entry

## Linux CI/CD Deployment Server

**Technologies:** Linux, Jenkins, Git, Bash, OpenSSH, systemd, Nginx, Python, CI/CD

After you have actually completed the project, you can accurately write:

* Built a Linux-based CI/CD platform using Jenkins and Git to automatically validate source code, execute tests, generate versioned release artifacts, verify SHA-256 integrity, and deploy applications to a separate Ubuntu server over SSH.

* Implemented versioned release directories, atomic symlink-based activation, systemd service management, post-deployment health validation, deployment locking, release traceability, and automated rollback to known-good releases.

* Hardened the deployment workflow using dedicated CI, deployment, and runtime identities, restricted sudo operations, SSH host-key verification, protected credentials, network controls, and operational troubleshooting across source, build, transport, service, proxy, and deployment layers.

---

# 23. Interview Preparation

### What is CI?

Continuous Integration is the practice of frequently integrating code changes and automatically validating them through activities such as builds and tests.

### What is continuous delivery?

A practice where software remains in a deployable state and releases can be promoted through an automated process, often with an explicit production approval step.

### What is continuous deployment?

A model where qualifying changes can be automatically released to production without a manual production deployment decision.

### Is this lab automatically a full continuous-deployment production system?

No.

It demonstrates an automated deployment pipeline in a controlled lab. Production promotion policy is a separate organizational and technical decision.

### Why separate `ci01` and `app01`?

Because build infrastructure and runtime infrastructure have different responsibilities, privileges, failure domains, and security boundaries.

### Why shouldn't Jenkins run the application?

Jenkins is the deployment system, not the application runtime.

### Why create a deployment artifact?

It gives you a defined unit that can be identified, checksummed, archived, transferred, deployed, and audited.

### Why not simply run `git pull` on `app01`?

Direct repository pulls couple the target server to source-control state and can create less controlled deployments. A built artifact provides a clearer boundary between source, build, and release.

### Why include the Git SHA in `BUILD-INFO`?

To connect the deployed release back to the exact source revision.

### Why calculate SHA-256?

To verify artifact integrity.

### Does a checksum prove who created the artifact?

No.

A checksum proves that the bytes match the expected digest. It is not by itself artifact signing or provenance verification.

### Why use versioned release directories?

They prevent old and new application files from being mixed and preserve known releases for rollback.

### Why use a `current` symlink?

It gives the runtime a stable path while allowing the target release to change quickly.

### Why use a temporary symlink followed by `mv`?

It makes activation much closer to an atomic filesystem operation than deleting and rebuilding the active application directory in place.

### Why use `flock`?

To prevent overlapping deployment operations from changing release state concurrently.

### Why does the application use systemd?

systemd provides service lifecycle management, restart behavior, startup at boot, identity configuration, and journald integration.

### Why run the application as `veriqta`?

The application does not need root privileges.

### Why have a separate `deploy` account?

Deployment and runtime are different responsibilities.

### Why not give `deploy` unrestricted sudo?

A compromised deployment credential should not automatically become unrestricted root access.

### Why use a controlled privileged helper?

It creates a smaller, auditable administrative interface for the deployment process.

### Why is SSH host-key verification important?

It helps the client verify that it is connecting to the intended server.

### What is the difference between an SSH user key and host key?

The user key authenticates the client/user to the server. The host key authenticates the server to the client.

### Why is `StrictHostKeyChecking=no` dangerous?

It removes an important server-identity check and can make man-in-the-middle or host-substitution attacks easier.

### What is a health check?

A test of whether the deployed application is functioning according to a defined condition.

### Why isn't `systemctl is-active` enough?

A process can be running while the application is unable to serve valid requests.

### Why check through Nginx after deployment?

Because users reach the application through Nginx. Testing only the backend does not validate the complete request path.

### What is rollback?

Restoring a previously known-good application release when the new release fails.

### Why can application rollback become difficult with database changes?

Because an older application version may not be compatible with a newer database schema.

### Why archive artifacts in Jenkins?

To preserve the build output associated with the pipeline execution.

### Why limit build retention?

Build logs, workspaces, and artifacts consume disk space.

### What happens if the CI server runs out of disk?

Builds, checkouts, artifact creation, Jenkins operations, or even system services may fail.

### What happens if `app01` runs out of disk?

New releases may fail to transfer or extract, logs may fail, and services may become unstable.

### What causes an Nginx 502 here?

Nginx is available but cannot successfully communicate with its upstream application.

### How would you investigate?

Check:

```text
Nginx status
Nginx error log
application status
application journal
127.0.0.1:8000 listener
direct backend health request
```

### Why should a pipeline stop after a failed test?

A known failing build should not continue toward deployment.

### What is fail-fast behavior?

Stopping a workflow as soon as a required condition fails rather than continuing with invalid state.

### What is deployment traceability?

The ability to connect a running release to the build, artifact, commit, deployment event, and relevant operational evidence.

### Why is CI/CD infrastructure a high-value security target?

Because it often has access to source code, credentials, artifacts, and deployment targets.

### What is the most important troubleshooting principle in this project?

Identify the stage where the workflow stopped before changing the system.

---

# 24. Cleanup

This project spans two servers.

Clean them separately.

Do not begin with destructive commands.

---

## Step 1: Preserve Evidence

Keep sanitized copies of:

```text
architecture
Jenkinsfile
build/test scripts
deployment scripts
failure-lab results
screenshots
deployment history
final validation
```

Do not preserve secrets in the public repository.

---

## Step 2: Stop Application

On `app01`:

```bash
sudo systemctl stop \
  veriqta-portal
```

Disable if removing permanently:

```bash
sudo systemctl disable \
  veriqta-portal
```

---

## Step 3: Remove Nginx Configuration

```bash
sudo rm -f \
  /etc/nginx/sites-enabled/veriqta-portal
```

```bash
sudo rm -f \
  /etc/nginx/sites-available/veriqta-portal
```

Validate:

```bash
sudo nginx -t
```

Then:

```bash
sudo systemctl reload nginx
```

---

## Step 4: Remove systemd Unit

```bash
sudo rm -f \
  /etc/systemd/system/veriqta-portal.service
```

Then:

```bash
sudo systemctl daemon-reload
```

Then:

```bash
sudo systemctl reset-failed \
  veriqta-portal.service 2>/dev/null || true
```

---

## Step 5: Remove Deployment Helpers

Only project-specific helpers:

```bash
sudo rm -f \
  /usr/local/sbin/veriqta-service-control
```

```bash
sudo rm -f \
  /usr/local/sbin/veriqta-deploy-release
```

```bash
sudo rm -f \
  /usr/local/sbin/veriqta-rollback
```

Remove:

```bash
sudo rm -f \
  /etc/sudoers.d/veriqta-deploy
```

---

## Step 6: Remove Application Files

Inspect first:

```bash
sudo find \
  /opt/veriqta-portal \
  -maxdepth 3 \
  -print
```

If everything belongs to this disposable project:

```bash
sudo rm -rf \
  /opt/veriqta-portal
```

---

## Step 7: Remove Project Accounts

If created only for this lab:

```bash
sudo userdel deploy
```

Then:

```bash
sudo userdel veriqta
```

Verify:

```bash
getent passwd deploy
```

```bash
getent passwd veriqta
```

Neither should return an account if removal was intended.

---

## Step 8: Firewall Cleanup

Inspect:

```bash
sudo ufw status numbered
```

Remove only rules created specifically for this project.

Do not remove SSH protection blindly.

---

## Step 9: Jenkins Cleanup

On `ci01`, decide whether Jenkins existed before the project.

If Jenkins belongs only to this disposable VM, destroying the lab VM may be cleaner than attempting to manually return it to a pristine state.

If you retain Jenkins:

* delete the project job if appropriate;
* delete project-specific credentials;
* delete project-specific workspaces/artifacts through supported Jenkins controls;
* remove the dedicated host-trust entry if no longer needed.

Do not delete unrelated Jenkins configuration.

---

# 25. Cleanup Verification

On `app01`:

```bash
systemctl --failed
```

```bash
sudo ss -lntup
```

```bash
sudo ufw status verbose
```

```bash
getent passwd deploy
```

```bash
getent passwd veriqta
```

```bash
test ! -e /opt/veriqta-portal \
  && echo "Project directory removed"
```

On `ci01`:

verify the project job, credential, workspace, firewall rule, and any project-specific SSH trust material were removed if cleanup required them.

Compare both systems with your baseline.

---

# 26. Project Reflection

You should be able to answer all of these without guessing.

1. What problem did CI/CD solve in this project?
2. What did it not solve?
3. What is CI?
4. What is continuous delivery?
5. What is continuous deployment?
6. Which model did your lab demonstrate?
7. Why were `ci01` and `app01` separate?
8. What security boundary exists between them?
9. Why is Jenkins security-sensitive?
10. Which Linux account runs Jenkins?
11. Why doesn't Jenkins run as root?
12. Which account receives deployments?
13. Which account runs the application?
14. Why are those identities different?
15. What privilege does `deploy` require?
16. What privilege does it not require?
17. Why is unrestricted passwordless sudo dangerous?
18. What did your privileged helper solve?
19. What is the source of truth for the application?
20. What is the source of truth for the pipeline?
21. Why is the Jenkinsfile stored in Git?
22. What happens during checkout?
23. How do you identify the exact commit?
24. What happens during validation?
25. What happens during testing?
26. Why does deployment stop when testing fails?
27. What happens during build?
28. What is a release artifact?
29. Why package one?
30. What files are inside your artifact?
31. What is `BUILD-INFO`?
32. Why is it useful?
33. What does `commit_sha` provide?
34. What does the release ID provide?
35. What does the build timestamp provide?
36. Why use UTC for deployment timestamps?
37. What is SHA-256 doing?
38. What does a successful checksum prove?
39. What does it not prove?
40. What is artifact signing?
41. How is signing different from checksumming?
42. Why archive build artifacts?
43. Why limit artifact retention?
44. What happens when Jenkins storage fills?
45. What happens when `app01` storage fills?
46. How is the artifact transferred?
47. Why use SSH?
48. What authenticates Jenkins to `app01`?
49. What authenticates `app01` to Jenkins?
50. Why are those different mechanisms?
51. What is `known_hosts`?
52. Why verify fingerprints independently?
53. Why shouldn't you use `StrictHostKeyChecking=no`?
54. What happens if a server is rebuilt and its host key changes?
55. How do you determine whether that change is legitimate?
56. Where are incoming artifacts placed?
57. Why not deploy directly over the active release?
58. What is a release directory?
59. Why are release directories versioned?
60. What is the `current` symlink?
61. Why does systemd reference `current`?
62. What is atomic activation?
63. Why use a temporary symlink?
64. Why use `mv` to activate it?
65. What could happen if activation is not controlled?
66. What is `flock`?
67. Why is deployment locking required?
68. What could happen if two deployments overlap?
69. How does the deployment script identify the previous release?
70. When does it change `current`?
71. What happens after activation?
72. Why restart the service?
73. What happens if restart fails?
74. What happens if health validation fails?
75. How many health attempts are made?
76. Why retry rather than test only once?
77. What is rollback?
78. What release becomes the rollback target?
79. How is rollback activated?
80. How is rollback verified?
81. What happens if the rollback target is also unhealthy?
82. Why should rollback itself be monitored?
83. Why can database migrations complicate rollback?
84. What is a backward-compatible database migration?
85. Why are schema migrations excluded from this lab?
86. How would you add them safely later?
87. What does systemd provide?
88. Why is the application bound to `127.0.0.1`?
89. Why doesn't it listen directly on the external interface?
90. What role does Nginx play?
91. What is the complete client request path?
92. What happens if Nginx stops?
93. What happens if the application stops?
94. What does a 502 indicate in this architecture?
95. Can Nginx be healthy while the application is down?
96. Can the application be healthy while Nginx is down?
97. How do you prove which case you have?
98. What does the `/health` endpoint test?
99. Why is process state insufficient?
100. Why is the final pipeline verification important?
101. What does direct backend testing prove?
102. What does proxied testing prove?
103. What does remote testing prove?
104. What happens if tests fail?
105. What happens if syntax validation fails?
106. What happens if the build fails?
107. What happens if checksum validation fails?
108. What happens if SSH times out?
109. How is timeout different from authentication failure?
110. What happens if the SSH key is wrong?
111. Where do you inspect SSH server logs?
112. What happens if the host key is wrong?
113. What happens if `deploy` lacks filesystem access?
114. How do you inspect a directory permission chain?
115. Why is `namei -l` useful?
116. Why is `chmod 777` a poor solution?
117. What happens if `app.py` has a syntax error?
118. What happens if it has a runtime error?
119. Why are those detected at different stages?
120. What happens if port 8000 is occupied?
121. How do you identify the listener?
122. Why shouldn't you kill an unknown process immediately?
123. What happens if Jenkins stops?
124. What logs do you inspect?
125. What happens if Git is unreachable?
126. Which pipeline stage fails?
127. Why shouldn't you troubleshoot systemd in that case?
128. What is a failure domain?
129. What is the failure domain of Git?
130. What is the failure domain of Jenkins?
131. What is the failure domain of SSH?
132. What is the failure domain of systemd?
133. What is the failure domain of Nginx?
134. How does stage separation help troubleshooting?
135. What is fail-fast behavior?
136. Why is it useful?
137. What deployment evidence did you retain?
138. How can you determine which commit is currently running?
139. How can you determine when it was built?
140. How can you determine which release directory is active?
141. How can you inspect previous releases?
142. Why retain previous releases?
143. How many should production retain?
144. Why is there no universal answer?
145. What happens when release storage grows indefinitely?
146. How should old releases be deleted?
147. Which release must never be deleted accidentally?
148. Which rollback candidate should usually be preserved?
149. What does the deployment history contain?
150. Why is an audit trail useful?
151. What secrets does Jenkins hold?
152. Why should they never appear in console logs?
153. Why should they never appear in Git?
154. What would happen if the deployment key were compromised?
155. How could you rotate it?
156. How could you restrict it further?
157. Could you restrict it by source address?
158. Could you use forced SSH commands?
159. What would that improve?
160. Why might a production team use a deployment API instead?
161. What firewall ports are required on `app01`?
162. Which ports should not be externally exposed?
163. Why is port 8000 loopback-only?
164. Which port does Jenkins use in the lab?
165. Why should Jenkins access be restricted?
166. What changes would you make before internet exposure?
167. Where are application logs?
168. Where are Nginx logs?
169. Where are Jenkins logs?
170. Where are SSH logs?
171. Why are logs essential during pipeline failures?
172. How do you distinguish code failure from infrastructure failure?
173. How do you distinguish network failure from authentication failure?
174. How do you distinguish process failure from proxy failure?
175. What is the blast radius of Jenkins failure?
176. Does Jenkins failure automatically stop the already-running application?
177. Why is that useful?
178. What is the blast radius of `app01` failure?
179. What happens if `app01` is completely lost?
180. Where are your releases then?
181. Does this project provide application-server high availability?
182. How could Project 04 improve that?
183. Does this project provide disaster recovery?
184. How could Project 06 improve that?
185. How could Ansible manage both servers?
186. How could Project 03 integrate here?
187. How could centralized logging improve the platform?
188. How could Project 02 integrate here?
189. How could Project 05 monitor suspicious deployment activity?
190. How could Project 07 provide internal DNS?
191. How could Project 08 replace the Python process with containers?
192. Would Jenkins still be able to deploy container releases?
193. How might a registry enter that architecture?
194. How could immutable images improve deployment consistency?
195. How would blue-green deployment differ from this design?
196. How would canary deployment differ?
197. What is zero-downtime deployment?
198. Does this lab guarantee zero downtime?
199. Why not?
200. What would you change to approach zero downtime?
201. How would multiple application servers change deployment?
202. How would a load balancer change verification?
203. How would you remove one node from service before deployment?
204. How would you validate it before returning it?
205. What is deployment promotion?
206. How would dev, staging, and production differ?
207. Why shouldn't one credential necessarily deploy everywhere?
208. What is separation of duties?
209. How does CI/CD intersect with supply-chain security?
210. Why does artifact provenance matter?
211. Why might production builds use signed artifacts?
212. Why might dependencies be pinned?
213. Why might build environments be ephemeral?
214. What is configuration drift on a Jenkins server?
215. How could configuration management reduce it?
216. What should happen when a deployment fails?
217. What should happen before retrying?
218. Why can repeated blind retries make incidents worse?
219. What evidence should be collected first?
220. Can you trace one commit from Git all the way to the running Linux process?

---

# 27. Final Completion Checklist

## Architecture

* [ ] `ci01` created.
* [ ] `app01` created.
* [ ] Server roles documented.
* [ ] Network path documented.
* [ ] CI/CD flow documented.
* [ ] Security boundaries documented.
* [ ] Deployment filesystem documented.
* [ ] Rollback design documented.

## Linux Hosts

* [ ] Baselines captured.
* [ ] Hostnames configured.
* [ ] Network connectivity verified.
* [ ] Routes inspected.
* [ ] Disk capacity checked.
* [ ] Memory checked.
* [ ] Existing listeners documented.
* [ ] Failed units checked.
* [ ] Firewall state documented.

## Source Control

* [ ] Git repository created.
* [ ] Application tracked.
* [ ] Tests tracked.
* [ ] Jenkinsfile tracked.
* [ ] Deployment scripts tracked.
* [ ] `.gitignore` created.
* [ ] No secrets committed.
* [ ] Commit history available.
* [ ] Pipeline retrieves source automatically.

## Application

* [ ] Application created.
* [ ] Version endpoint/data available.
* [ ] Health endpoint available.
* [ ] Automated tests created.
* [ ] Tests pass locally.
* [ ] Syntax validation works.
* [ ] Application binds to loopback.
* [ ] Application runs as dedicated user.

## Build

* [ ] Build script created.
* [ ] Build directory isolated.
* [ ] Release ID generated.
* [ ] Git SHA captured.
* [ ] Build timestamp captured.
* [ ] `BUILD-INFO` generated.
* [ ] Artifact created.
* [ ] SHA-256 generated.
* [ ] Artifact integrity verified.
* [ ] Artifact contents inspected.

## Jenkins

* [ ] Jenkins installed.
* [ ] Java verified.
* [ ] Jenkins service active.
* [ ] Jenkins service enabled.
* [ ] Jenkins access restricted.
* [ ] Required plugins installed.
* [ ] Pipeline job created.
* [ ] Pipeline loaded from source control.
* [ ] Build retention configured.
* [ ] Concurrent builds disabled.
* [ ] Deployment credential stored securely.

## SSH

* [ ] Dedicated deployment key created.
* [ ] Public key installed on `app01`.
* [ ] Private key protected.
* [ ] Jenkins credential configured.
* [ ] `app01` host fingerprint verified.
* [ ] `known_hosts` configured.
* [ ] `StrictHostKeyChecking=no` not used as normal solution.
* [ ] SSH connectivity tested.
* [ ] SSH failure troubleshooting performed.

## Deployment Target

* [ ] `deploy` account created.
* [ ] `veriqta` runtime account created.
* [ ] Release directory created.
* [ ] Incoming directory created.
* [ ] Shared directory created.
* [ ] Permissions validated.
* [ ] Runtime user cannot perform unnecessary administration.
* [ ] Deployment user has limited privileges.

## systemd

* [ ] Application unit created.
* [ ] Unit syntax reviewed.
* [ ] Dedicated runtime identity configured.
* [ ] Working directory points to `current`.
* [ ] Restart policy configured.
* [ ] Security options configured.
* [ ] Service enabled.
* [ ] Service starts correctly.
* [ ] Journald logs verified.

## Nginx

* [ ] Nginx installed.
* [ ] Reverse proxy configured.
* [ ] Configuration validated.
* [ ] Custom access log created.
* [ ] Custom error log created.
* [ ] Nginx enabled.
* [ ] Nginx active.
* [ ] External HTTP works.
* [ ] Backend remains loopback-only.

## Pipeline

* [ ] Checkout stage works.
* [ ] Validation stage works.
* [ ] Test stage works.
* [ ] Build stage works.
* [ ] Archive stage works.
* [ ] Deployment stage works.
* [ ] Verification stage works.
* [ ] Failed stages stop later stages.
* [ ] Build number retained.
* [ ] Git commit retained.
* [ ] Artifact retained.
* [ ] Release traceable to commit.

## Deployment Safety

* [ ] Versioned releases used.
* [ ] Active release represented by symlink.
* [ ] Activation performed safely.
* [ ] Deployment lock implemented.
* [ ] Checksum verified before activation.
* [ ] Existing release cannot be overwritten accidentally.
* [ ] Health check performed after restart.
* [ ] Previous release recorded.
* [ ] Automatic rollback tested.
* [ ] Manual rollback tested.
* [ ] Rollback health verified.
* [ ] Deployment history recorded.

## Security

* [ ] Jenkins does not run as root.
* [ ] Application does not run as root.
* [ ] Deploy account is separate.
* [ ] Deploy account does not have unrestricted sudo.
* [ ] SSH private key excluded from Git.
* [ ] Credentials excluded from scripts.
* [ ] Credentials excluded from console output.
* [ ] SSH host identity verified.
* [ ] Jenkins exposure restricted.
* [ ] Application backend not externally exposed.
* [ ] Firewall reviewed.
* [ ] Filesystem permissions reviewed.
* [ ] Privileged helpers root-owned.
* [ ] Sudoers syntax validated.

## Observability

* [ ] Jenkins console logs reviewed.
* [ ] Jenkins service logs reviewed.
* [ ] SSH logs reviewed.
* [ ] Application journal reviewed.
* [ ] Nginx access log reviewed.
* [ ] Nginx error log reviewed.
* [ ] Deployment history reviewed.
* [ ] Active release identifiable.
* [ ] Current Git SHA identifiable.
* [ ] Listener state identifiable.

## Failure Engineering

* [ ] Test failure simulated.
* [ ] Syntax failure simulated.
* [ ] Build failure simulated.
* [ ] Artifact corruption simulated.
* [ ] SSH authentication failure simulated.
* [ ] SSH host-key failure simulated.
* [ ] Filesystem permission failure simulated.
* [ ] Application startup failure simulated.
* [ ] Health-check failure simulated.
* [ ] Nginx failure simulated.
* [ ] Application-down/Nginx-up failure simulated.
* [ ] Port conflict simulated.
* [ ] Concurrent deployment tested.
* [ ] Broken symlink tested.
* [ ] Network failure tested.
* [ ] Jenkins outage tested.
* [ ] CI disk pressure investigated.
* [ ] Deployment-server disk pressure investigated.
* [ ] Every controlled failure recovered.

## Reboot

* [ ] `app01` rebooted.
* [ ] Nginx returned automatically.
* [ ] Application returned automatically.
* [ ] Current release remained correct.
* [ ] Health check passed.
* [ ] `ci01` rebooted.
* [ ] Jenkins returned automatically.
* [ ] New deployment succeeded after reboot.

## Portfolio

* [ ] Public README written.
* [ ] Architecture diagram included.
* [ ] CI/CD workflow explained.
* [ ] Pipeline stages explained.
* [ ] Artifact strategy explained.
* [ ] SSH trust model explained.
* [ ] Permission model explained.
* [ ] Release strategy explained.
* [ ] Rollback strategy explained.
* [ ] Security model explained.
* [ ] Failure labs documented.
* [ ] Troubleshooting runbooks created.
* [ ] Screenshots/evidence sanitized.
* [ ] Known limitations documented.
* [ ] Production improvements documented.
* [ ] Resume entry prepared.
* [ ] Reflection completed.

---

# Production Limitations

This is a serious mid-level CI/CD lab, but do not describe it as a complete enterprise production platform.

It still has important limitations.

The application server is a single node:

```text
             CLIENT
                |
                v
             app01
            /     \
         Nginx   Application
```

If `app01` disappears, the service disappears.

Jenkins is also a single CI/CD server.

A stronger production architecture might eventually become:

```text
                        DEVELOPERS
                            |
                            v
                      SOURCE CONTROL
                            |
                            v
                     CI BUILD SYSTEM
                            |
                            v
                    ARTIFACT REGISTRY
                            |
                            v
                     RELEASE PIPELINE
                            |
              +-------------+-------------+
              |                           |
              v                           v
           app01                       app02
              |                           |
              +-------------+-------------+
                            |
                            v
                      LOAD BALANCER
                            |
                            v
                          USERS
```

You would also consider:

```text
TLS
webhooks
branch protection
pull-request validation
artifact registry
signed artifacts
SBOM generation
dependency scanning
secret scanning
SAST
image scanning if containers are used
central secrets management
credential rotation
central authentication
role-based authorization
approval gates
separate environments
deployment promotion
staging
production
blue-green deployment
canary deployment
multiple application nodes
load balancer draining
zero-downtime strategies
central logging
metrics
alerting
off-host backups
Jenkins backup/recovery
configuration management
ephemeral build agents
hardened build workers
artifact provenance
release attestations
```

Those are natural extensions.

They are not excuses to skip understanding the Linux machinery underneath them.

---

# When This Project Is Complete

Do not call it complete because Jenkins shows a green checkmark.

You should understand the entire delivery path:

```text
DEVELOPER
    |
    | commit
    v
GIT
    |
    | checkout
    v
JENKINS
    |
    +--> VALIDATE
    |
    +--> TEST
    |
    +--> BUILD
    |
    +--> METADATA
    |
    +--> SHA-256
    |
    +--> ARCHIVE
    |
    | SSH
    v
DEPLOY USER
    |
    v
INCOMING ARTIFACT
    |
    +--> VERIFY CHECKSUM
    |
    +--> EXTRACT
    |
    v
VERSIONED RELEASE
    |
    +--> VALIDATE
    |
    v
CURRENT SYMLINK
    |
    v
SYSTEMD
    |
    v
VERIQTA RUNTIME USER
    |
    v
PYTHON :8000
    |
    v
HEALTH CHECK
    |
    +------ FAIL ------> PREVIOUS RELEASE
    |                         |
    |                         v
    |                     ROLLBACK
    |
    +------ PASS
             |
             v
           NGINX
             |
             v
           CLIENT
```

And when it fails, your reasoning should be:

```text
WHAT FAILED?
     |
     v
WHICH PIPELINE STAGE?
     |
     v
WHAT STILL WORKS?
     |
     v
WHAT CHANGED?
     |
     v
IS SOURCE AVAILABLE?
     |
     v
DID VALIDATION PASS?
     |
     v
DID TESTS PASS?
     |
     v
WAS THE ARTIFACT BUILT?
     |
     v
IS ITS CHECKSUM VALID?
     |
     v
CAN ci01 REACH app01?
     |
     v
IS SSH HOST IDENTITY VALID?
     |
     v
DOES AUTHENTICATION WORK?
     |
     v
CAN deploy WRITE THE ARTIFACT?
     |
     v
CAN THE RELEASE BE EXTRACTED?
     |
     v
IS current POINTING TO THE EXPECTED RELEASE?
     |
     v
CAN systemd START THE APPLICATION?
     |
     v
IS :8000 LISTENING?
     |
     v
DOES /health PASS?
     |
     v
CAN NGINX REACH THE APPLICATION?
     |
     v
CAN THE CLIENT REACH NGINX?
     |
     v
ROOT CAUSE
     |
     v
SMALLEST SAFE FIX
     |
     v
VERIFY
     |
     v
DOCUMENT
     |
     v
PREVENT RECURRENCE
```

That is **Mid-Level Project 09: Linux CI/CD Deployment Server**.

The goal is not to prove that you know how to click **Build Now** in Jenkins. It is to prove that you understand the Linux systems, identities, network paths, artifacts, permissions, processes, release state, verification, rollback, and failure boundaries that make automated deployment trustworthy.
