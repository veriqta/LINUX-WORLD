# Portfolio Project 07: Enterprise Linux Automation Platform

- **Level:** Senior
- **Reference managed OS:** Ubuntu Server 24.04 LTS
- **Automation control node:** Ubuntu Server 24.04 LTS
- **Primary automation engine:** Ansible
- **Supporting languages:** Bash and Python 3

- **Demonstrates:** Linux, Ansible, Bash, Python, automation architecture, configuration management, idempotency, security automation, monitoring deployment, drift detection, remediation, rolling changes, validation, failure handling and rollback.

---

# 01. Project Title

## Enterprise Linux Automation Platform

Build a centralized Linux automation platform capable of:

```text
Discovering servers
        ↓
Validating connectivity
        ↓
Bootstrapping hosts
        ↓
Applying operating-system baselines
        ↓
Managing packages
        ↓
Managing users and SSH access
        ↓
Applying security configuration
        ↓
Configuring firewalls
        ↓
Deploying services
        ↓
Deploying monitoring
        ↓
Verifying desired state
        ↓
Detecting configuration drift
        ↓
Remediating drift
        ↓
Performing controlled rolling changes
        ↓
Handling failures
        ↓
Rolling back unsafe changes
        ↓
Generating operational evidence
```

This is not a collection of unrelated playbooks.

You are building an **automation control plane for Linux infrastructure**.

---

# 02. Project Scenario

An organization operates an expanding Linux estate.

Initially it managed five servers manually.

Now it operates dozens of systems and expects continued growth.

Administrators currently perform work such as:

```text
Create users manually.

Copy SSH keys manually.

Install packages manually.

Edit sshd_config manually.

Configure UFW manually.

Install monitoring manually.

Restart services manually.

Check configuration manually.

Patch systems manually.

Investigate configuration drift manually.
```

This produces inconsistent systems.

For example:

```text
WEB01
SSH root login disabled
UFW enabled
node exporter installed
chrony configured

WEB02
SSH root login enabled
UFW enabled
node exporter missing
chrony configured

WEB03
SSH root login disabled
UFW disabled
old package versions
monitoring broken
```

The problem is not simply that administration takes too long.

The larger problem is that nobody can reliably answer:

```text
What should every server look like?

Which systems differ?

Why do they differ?

Who changed them?

Can the intended state be reproduced?

Can 50 new systems receive the same configuration?

Can a failed deployment be stopped before every server is affected?

Can configuration drift be detected?

Can drift be corrected automatically?

Can an unsafe change be rolled back?
```

You have been asked to design an automation platform that makes infrastructure configuration repeatable, testable and auditable.

---

# 03. Project Difficulty

## Senior

The project requires more than knowing Ansible syntax.

You must demonstrate:

* automation architecture;
* inventory design;
* variable precedence awareness;
* role design;
* idempotency;
* configuration templating;
* validation;
* privilege escalation;
* secrets management;
* controlled execution;
* error handling;
* rolling changes;
* configuration drift;
* remediation;
* monitoring integration;
* Bash automation;
* Python automation;
* failure engineering;
* rollback;
* evidence generation;
* operational documentation.

The engineering model is:

```text
Define desired state
        ↓
Validate inputs
        ↓
Preview change
        ↓
Apply safely
        ↓
Verify outcome
        ↓
Measure
        ↓
Detect deviation
        ↓
Remediate
        ↓
Record evidence
```

---

# 04. Estimated Time

A serious implementation will normally require multiple lab sessions.

Suggested planning range:

| Phase                         | Approximate effort |
| ----------------------------- | -----------------: |
| Architecture and requirements |              3–5 h |
| Lab construction              |              3–5 h |
| Ansible control plane         |              3–5 h |
| Inventory/variables           |              3–5 h |
| Base configuration role       |              4–6 h |
| Security automation           |              5–8 h |
| Web/service automation        |              4–6 h |
| Monitoring automation         |              4–6 h |
| Validation framework          |              4–6 h |
| Drift/remediation             |              4–7 h |
| Rolling deployment            |              3–5 h |
| Bash/Python tooling           |              4–7 h |
| Failure engineering           |             7–12 h |
| Documentation/evidence        |              4–7 h |

Expect roughly **50–85 hours**, depending on existing Ansible and Linux experience.

Do not measure success by time spent.

Measure it by reproducibility.

---

# 05. Prerequisites

You should already understand:

* Linux administration;
* SSH;
* users and groups;
* permissions;
* sudo;
* systemd;
* package management;
* networking;
* UFW/firewalls;
* YAML basics;
* Git basics;
* Bash fundamentals;
* Python fundamentals.

You should understand that Ansible normally operates from a **control node** against **managed nodes**.

---

## Lab infrastructure

Build:

| Host         | Role                 |       Address |
| ------------ | -------------------- | ------------: |
| automation01 | Ansible control node | 192.168.56.10 |
| web01        | Web server           | 192.168.56.21 |
| web02        | Web server           | 192.168.56.22 |
| web03        | Web server           | 192.168.56.23 |
| app01        | Application server   | 192.168.56.31 |
| db01         | Database server      | 192.168.56.41 |
| monitor01    | Monitoring server    | 192.168.56.51 |

Use Ubuntu Server 24.04 LTS.

Each VM should have:

```text
NIC 1
NAT
Used for package downloads

NIC 2
Host-only / isolated lab network
192.168.56.0/24
```

Do not configure a second default gateway on the lab interface.

---

# 06. Learning Objectives

By completing this project, you should be able to:

1. Design an automation control plane.
2. Build structured Ansible inventories.
3. Organize group and host variables.
4. Create reusable Ansible roles.
5. Write idempotent automation.
6. Use handlers correctly.
7. Template configuration safely.
8. Validate configuration before service restart.
9. Manage users and SSH keys.
10. Automate Linux security baselines.
11. Manage packages.
12. Manage services.
13. Configure firewalls.
14. Deploy applications.
15. Deploy monitoring components.
16. Protect automation secrets.
17. Use Ansible Vault.
18. Build Bash wrappers around automation.
19. Build Python inventory/reporting utilities.
20. Detect configuration drift.
21. Remediate configuration drift.
22. Preview changes.
23. Perform rolling deployments.
24. Limit blast radius.
25. Handle unreachable hosts.
26. Handle task failures.
27. Recover from partial automation failures.
28. Roll back unsafe changes.
29. Measure automation performance.
30. Generate audit evidence.
31. Explain automation design decisions.
32. Defend the architecture in a senior engineering interview.

---

# 07. What You Will Build

The platform architecture:

```text
                        Git Repository
                             │
                             ▼
                    ┌─────────────────┐
                    │  AUTOMATION01   │
                    │                 │
                    │ Ansible         │
                    │ Bash            │
                    │ Python          │
                    │ Vault           │
                    │ Validation      │
                    └────────┬────────┘
                             │
                          SSH
                             │
           ┌─────────────────┼─────────────────┐
           │                 │                 │
           ▼                 ▼                 ▼
     ┌───────────┐     ┌───────────┐     ┌───────────┐
     │ Web Tier  │     │ App Tier  │     │ Data Tier │
     │           │     │           │     │           │
     │ web01     │     │ app01     │     │ db01      │
     │ web02     │     └───────────┘     └───────────┘
     │ web03     │
     └───────────┘
           │
           │ telemetry
           ▼
     ┌─────────────┐
     │ monitor01   │
     └─────────────┘
```

Desired-state hierarchy:

```text
ALL LINUX HOSTS
│
├── baseline packages
├── time synchronization
├── administrator account
├── SSH baseline
├── firewall baseline
├── audit/logging baseline
└── monitoring agent
     │
     ├── WEB SERVERS
     │    ├── nginx
     │    ├── web configuration
     │    └── HTTP health check
     │
     ├── APPLICATION SERVERS
     │    └── application baseline
     │
     ├── DATABASE SERVERS
     │    └── database baseline
     │
     └── MONITORING SERVERS
          └── monitoring configuration
```

---

# 08. Architecture

## 8.1 Control-plane model

AUTOMATION01 is the automation control node.

It stores:

```text
Inventory
Variables
Playbooks
Roles
Templates
Validation scripts
Automation scripts
Encrypted variables
Documentation
```

Managed systems do not require an Ansible daemon.

Management occurs primarily over SSH.

---

## 8.2 Desired state

Instead of saying:

```text
Run apt install nginx.
```

define:

```text
Nginx must be installed.
Nginx must be enabled.
Nginx must be running.
Its configuration must match the approved template.
The configuration must pass validation.
Port 80 must be reachable from the approved network.
```

Automation should converge systems toward that state.

---

## 8.3 Idempotency

A fundamental project requirement is:

```text
First execution:

changed=many


Second execution without infrastructure changes:

changed=0
```

Not every command or third-party component is inherently idempotent.

Where idempotency cannot be guaranteed, document the reason.

---

## 8.4 Blast-radius control

Never think:

```text
Automation makes changes faster.
```

without also thinking:

```text
Automation can break systems faster.
```

The platform therefore implements:

```text
syntax validation
      ↓
linting
      ↓
inventory validation
      ↓
check mode where meaningful
      ↓
small-host test
      ↓
rolling batch
      ↓
post-change verification
      ↓
broader rollout
```

---

## 8.5 Environment model

This lab uses one environment:

```text
lab
```

But structure it so that future environments can become:

```text
inventories/
├── lab/
├── staging/
└── production/
```

without rewriting every role.

---

# 09. Technologies

Primary:

```text
Ubuntu Server 24.04
Ansible
ansible-core
OpenSSH
Git
Bash
Python 3
systemd
UFW
Nginx
Prometheus Node Exporter
```

Supporting tooling:

```text
ansible-playbook
ansible-inventory
ansible-config
ansible-doc
ansible-vault
ansible-lint
yamllint
ssh-keygen
curl
jq
systemctl
ss
journalctl
```

Use Fully Qualified Collection Names such as:

```yaml
ansible.builtin.apt:
ansible.builtin.template:
ansible.builtin.service:
ansible.builtin.user:
```

for clarity.

---

# 10. Environment Setup

## 10.1 Configure AUTOMATION01

Set hostname:

```bash
sudo hostnamectl set-hostname automation01
```

Verify:

```bash
hostnamectl
```

Update:

```bash
sudo apt update
```

Install prerequisites:

```bash
sudo apt install -y \
  python3 \
  python3-pip \
  python3-venv \
  git \
  openssh-client \
  curl \
  jq \
  yamllint
```

---

## 10.2 Create dedicated automation user

Do not operate the platform permanently from a personal root shell.

Create:

```bash
sudo adduser automation
```

Switch:

```bash
sudo -iu automation
```

Create workspace:

```bash
mkdir -p ~/automation-platform
```

Enter:

```bash
cd ~/automation-platform
```

---

## 10.3 Create Python virtual environment

Create:

```bash
python3 -m venv ~/.venvs/ansible
```

Activate:

```bash
source ~/.venvs/ansible/bin/activate
```

Upgrade packaging tooling:

```bash
python -m pip install --upgrade pip
```

Install:

```bash
python -m pip install \
  ansible-core \
  ansible-lint
```

Verify:

```bash
ansible --version
```

Then:

```bash
ansible-playbook --version
```

Then:

```bash
ansible-lint --version
```

Record actual versions in project evidence.

Do not hard-code a version claim in the README that was never tested.

---

## 10.4 Create automation SSH key

As `automation`:

```bash
ssh-keygen \
  -t ed25519 \
  -a 100 \
  -f ~/.ssh/automation_ed25519 \
  -C "linux-automation-platform"
```

Do not commit:

```text
~/.ssh/automation_ed25519
```

to Git.

---

## 10.5 Bootstrap managed nodes

Before Ansible can manage a fresh server, it needs an initial access path.

On each managed host, using console or the initial provisioning account:

```bash
sudo adduser automation
```

Add temporary bootstrap sudo:

```bash
sudo usermod -aG sudo automation
```

Create SSH directory:

```bash
sudo install \
  -d \
  -o automation \
  -g automation \
  -m 0700 \
  /home/automation/.ssh
```

Place the contents of:

```text
~/.ssh/automation_ed25519.pub
```

from AUTOMATION01 into:

```text
/home/automation/.ssh/authorized_keys
```

on each managed host.

Then:

```bash
sudo chown \
  automation:automation \
  /home/automation/.ssh/authorized_keys
```

```bash
sudo chmod 0600 \
  /home/automation/.ssh/authorized_keys
```

---

## 10.6 Configure lab sudo access

For the lab, create:

```bash
sudo visudo \
  -f /etc/sudoers.d/automation
```

Use:

```text
automation ALL=(ALL) NOPASSWD: ALL
```

Set:

```bash
sudo chmod 0440 \
  /etc/sudoers.d/automation
```

Validate:

```bash
sudo visudo -c
```

This is deliberately broad for the controlled automation lab.

Document it as a limitation.

A real enterprise implementation should evaluate narrower privilege, controlled credential elevation, PAM, short-lived credentials or another approved privilege-management architecture.

---

## 10.7 Test manual SSH first

From AUTOMATION01:

```bash
ssh \
  -i ~/.ssh/automation_ed25519 \
  automation@192.168.56.21
```

Repeat for every host.

Do not troubleshoot Ansible until plain SSH works.

---

# 11. Project Directory Structure

From:

```bash
cd ~/automation-platform
```

create:

```bash
mkdir -p \
  inventories/lab/group_vars/all \
  inventories/lab/group_vars/webservers \
  inventories/lab/group_vars/appservers \
  inventories/lab/group_vars/dbservers \
  inventories/lab/group_vars/monitoring \
  inventories/lab/host_vars \
  playbooks \
  roles/common/{tasks,handlers,templates,files,defaults,vars} \
  roles/security/{tasks,handlers,templates,files,defaults,vars} \
  roles/webserver/{tasks,handlers,templates,files,defaults,vars} \
  roles/monitoring_agent/{tasks,handlers,templates,files,defaults,vars} \
  scripts/bash \
  scripts/python \
  tests \
  evidence \
  reports \
  runbooks \
  failure-labs \
  measurements \
  docs \
  adrs
```

Create `.gitignore`:

```bash
cat > .gitignore <<'EOF'
.venv/
venv/
*.retry
*.log
.vault-pass
vault-password*
secrets/
__pycache__/
*.pyc
evidence/raw-sensitive/
EOF
```

---

# 12. Step-by-Step Implementation

## 12.1 Create inventory

Create:

```bash
cat > inventories/lab/hosts.yml <<'EOF'
---
all:
  children:

    webservers:
      hosts:
        web01:
          ansible_host: 192.168.56.21
        web02:
          ansible_host: 192.168.56.22
        web03:
          ansible_host: 192.168.56.23

    appservers:
      hosts:
        app01:
          ansible_host: 192.168.56.31

    dbservers:
      hosts:
        db01:
          ansible_host: 192.168.56.41

    monitoring:
      hosts:
        monitor01:
          ansible_host: 192.168.56.51
EOF
```

---

## 12.2 Create common variables

```bash
cat > inventories/lab/group_vars/all/main.yml <<'EOF'
---
ansible_user: automation
ansible_ssh_private_key_file: ~/.ssh/automation_ed25519
ansible_python_interpreter: /usr/bin/python3

platform_environment: lab

common_packages:
  - curl
  - jq
  - vim
  - git
  - chrony
  - rsyslog

managed_admin_group: linux-admins

ssh_permit_root_login: "no"
ssh_password_authentication: "no"

node_exporter_port: 9100
EOF
```

---

## 12.3 Validate inventory

Run:

```bash
ansible-inventory \
  -i inventories/lab/hosts.yml \
  --graph
```

Then:

```bash
ansible-inventory \
  -i inventories/lab/hosts.yml \
  --list
```

Check host targeting:

```bash
ansible \
  -i inventories/lab/hosts.yml \
  all \
  --list-hosts
```

Do not continue until every expected host appears in the correct group.

---

## 12.4 Create Ansible configuration

Create:

```bash
cat > ansible.cfg <<'EOF'
[defaults]
inventory = inventories/lab/hosts.yml
roles_path = roles
host_key_checking = True
retry_files_enabled = False
forks = 10
timeout = 20
interpreter_python = auto_silent

[privilege_escalation]
become = True
become_method = sudo
EOF
```

Check active configuration:

```bash
ansible-config dump --only-changed
```

---

## 12.5 Establish SSH host trust

Do not solve host-key problems by permanently disabling SSH host-key verification.

Connect manually to each host once:

```bash
ssh \
  -i ~/.ssh/automation_ed25519 \
  automation@192.168.56.21
```

Verify the fingerprint through your lab provisioning information before accepting it.

Repeat for every managed host.

Then:

```bash
ansible all \
  -m ansible.builtin.ping
```

Expected pattern:

```text
SUCCESS
```

for every reachable correctly configured node.

---

## 12.6 Gather facts

Run:

```bash
ansible all \
  -m ansible.builtin.setup \
  -a 'filter=ansible_distribution*'
```

Then:

```bash
ansible all \
  -m ansible.builtin.setup \
  -a 'filter=ansible_default_ipv4'
```

Then:

```bash
ansible all \
  -m ansible.builtin.setup \
  -a 'filter=ansible_memtotal_mb'
```

This demonstrates that inventory data and discovered system facts are different concepts.

---

## 12.7 Build common role

Create:

```bash
cat > roles/common/tasks/main.yml <<'EOF'
---
- name: Install common packages
  ansible.builtin.apt:
    name: "{{ common_packages }}"
    state: present
    update_cache: true
    cache_valid_time: 3600

- name: Ensure managed administrator group exists
  ansible.builtin.group:
    name: "{{ managed_admin_group }}"
    state: present

- name: Ensure chrony is enabled and running
  ansible.builtin.service:
    name: chrony
    state: started
    enabled: true

- name: Ensure rsyslog is enabled and running
  ansible.builtin.service:
    name: rsyslog
    state: started
    enabled: true
EOF
```

---

## 12.8 Create baseline playbook

```bash
cat > playbooks/baseline.yml <<'EOF'
---
- name: Apply Linux baseline
  hosts: all
  become: true

  roles:
    - common
EOF
```

Syntax check:

```bash
ansible-playbook \
  playbooks/baseline.yml \
  --syntax-check
```

Preview:

```bash
ansible-playbook \
  playbooks/baseline.yml \
  --check \
  --diff
```

Review before applying.

Apply:

```bash
ansible-playbook \
  playbooks/baseline.yml
```

Run it again:

```bash
ansible-playbook \
  playbooks/baseline.yml
```

The second execution should ideally report no unnecessary changes.

Record the actual recap.

---

## 12.9 Verify idempotency

Create:

```bash
cat > scripts/bash/test-idempotency.sh <<'EOF'
#!/usr/bin/env bash
set -euo pipefail

PLAYBOOK="${1:-playbooks/baseline.yml}"

echo "Running first convergence..."
ansible-playbook "${PLAYBOOK}"

echo
echo "Running second convergence..."
ansible-playbook "${PLAYBOOK}"

echo
echo "Review the second PLAY RECAP."
echo "Unexpected changed tasks require investigation."
EOF
```

Make executable:

```bash
chmod +x scripts/bash/test-idempotency.sh
```

Run:

```bash
./scripts/bash/test-idempotency.sh
```

Do not simply declare a role idempotent because it uses Ansible.

Prove it.

---

## 12.10 Build SSH security role

Back up SSH configuration through automation before changing it.

Create:

```bash
cat > roles/security/tasks/main.yml <<'EOF'
---
- name: Ensure OpenSSH server is installed
  ansible.builtin.apt:
    name: openssh-server
    state: present
    update_cache: true
    cache_valid_time: 3600

- name: Ensure SSH drop-in directory exists
  ansible.builtin.file:
    path: /etc/ssh/sshd_config.d
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: Deploy managed SSH security policy
  ansible.builtin.template:
    src: 60-automation-security.conf.j2
    dest: /etc/ssh/sshd_config.d/60-automation-security.conf
    owner: root
    group: root
    mode: "0644"
    validate: "/usr/sbin/sshd -t -f %s"
  notify: Reload SSH

- name: Ensure UFW is installed
  ansible.builtin.apt:
    name: ufw
    state: present

- name: Ensure auditd is installed
  ansible.builtin.apt:
    name: auditd
    state: present

- name: Ensure auditd is enabled and running
  ansible.builtin.service:
    name: auditd
    state: started
    enabled: true
EOF
```

---

## 12.11 Create SSH template

```bash
cat > roles/security/templates/60-automation-security.conf.j2 <<'EOF'
# Managed by Ansible.
# Manual changes may be overwritten.

PermitRootLogin {{ ssh_permit_root_login }}
PasswordAuthentication {{ ssh_password_authentication }}
PubkeyAuthentication yes
PermitEmptyPasswords no
X11Forwarding no
EOF
```

---

## 12.12 Create SSH handler

```bash
cat > roles/security/handlers/main.yml <<'EOF'
---
- name: Reload SSH
  ansible.builtin.service:
    name: ssh
    state: reloaded
EOF
```

The handler runs only when notified by a changed task.

That prevents unnecessary SSH reloads.

---

## 12.13 Build security playbook

```bash
cat > playbooks/security.yml <<'EOF'
---
- name: Apply Linux security baseline
  hosts: all
  become: true
  serial: 1

  roles:
    - security
EOF
```

Using:

```yaml
serial: 1
```

limits the change to one host at a time.

That is appropriate for an SSH policy capable of locking administrators out.

---

## 12.14 Validate before security rollout

Run:

```bash
ansible-playbook \
  playbooks/security.yml \
  --syntax-check
```

Then:

```bash
ansible-playbook \
  playbooks/security.yml \
  --list-hosts
```

Then:

```bash
ansible-playbook \
  playbooks/security.yml \
  --list-tasks
```

Then test one server:

```bash
ansible-playbook \
  playbooks/security.yml \
  --limit web01
```

Open a separate terminal.

Verify:

```bash
ssh \
  -i ~/.ssh/automation_ed25519 \
  automation@192.168.56.21
```

Only after successful login should the rollout continue.

---

## 12.15 Build user-management automation

Create variables:

```bash
cat >> inventories/lab/group_vars/all/main.yml <<'EOF'

managed_users:
  - name: alice
    groups:
      - linux-admins
    shell: /bin/bash

  - name: carol
    groups:
      - linux-admins
    shell: /bin/bash
EOF
```

Create task file:

```bash
cat > roles/common/tasks/users.yml <<'EOF'
---
- name: Ensure managed users exist
  ansible.builtin.user:
    name: "{{ item.name }}"
    groups: "{{ item.groups | join(',') }}"
    append: true
    shell: "{{ item.shell }}"
    create_home: true
    state: present
  loop: "{{ managed_users }}"
EOF
```

Include it from common role:

```bash
cat >> roles/common/tasks/main.yml <<'EOF'

- name: Manage Linux users
  ansible.builtin.include_tasks:
    file: users.yml
EOF
```

Validate:

```bash
ansible-playbook \
  playbooks/baseline.yml \
  --syntax-check
```

Apply:

```bash
ansible-playbook \
  playbooks/baseline.yml
```

Verify:

```bash
ansible all \
  -m ansible.builtin.command \
  -a 'id alice'
```

This command is being used for verification, not configuration.

---

## 12.16 Build webserver role

Create:

```bash
cat > roles/webserver/tasks/main.yml <<'EOF'
---
- name: Install nginx
  ansible.builtin.apt:
    name: nginx
    state: present
    update_cache: true
    cache_valid_time: 3600

- name: Deploy managed nginx landing page
  ansible.builtin.template:
    src: index.html.j2
    dest: /var/www/html/index.html
    owner: root
    group: root
    mode: "0644"

- name: Ensure nginx is enabled and running
  ansible.builtin.service:
    name: nginx
    state: started
    enabled: true
EOF
```

Create template:

```bash
cat > roles/webserver/templates/index.html.j2 <<'EOF'
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>{{ inventory_hostname }}</title>
</head>
<body>
  <h1>VERIQTA Linux Automation Platform</h1>
  <p>Host: {{ inventory_hostname }}</p>
  <p>Environment: {{ platform_environment }}</p>
  <p>Managed by Ansible.</p>
</body>
</html>
EOF
```

---

## 12.17 Create webserver playbook

```bash
cat > playbooks/webservers.yml <<'EOF'
---
- name: Configure web tier
  hosts: webservers
  become: true
  serial: 1

  roles:
    - common
    - security
    - webserver

  post_tasks:
    - name: Verify nginx locally
      ansible.builtin.uri:
        url: http://127.0.0.1/
        status_code: 200
      register: nginx_health

    - name: Show nginx verification
      ansible.builtin.debug:
        var: nginx_health.status
EOF
```

Validate:

```bash
ansible-playbook \
  playbooks/webservers.yml \
  --syntax-check
```

Deploy only WEB01 first:

```bash
ansible-playbook \
  playbooks/webservers.yml \
  --limit web01
```

External verification:

```bash
curl -fsS http://192.168.56.21/
```

Then roll out:

```bash
ansible-playbook \
  playbooks/webservers.yml
```

---

## 12.18 Verify all web servers

From AUTOMATION01:

```bash
for host in \
  192.168.56.21 \
  192.168.56.22 \
  192.168.56.23
do
    echo "=== ${host} ==="
    curl -fsS "http://${host}/"
    echo
done
```

---

## 12.19 Build firewall automation

Install required collection:

```bash
ansible-galaxy collection install \
  community.general
```

Create:

```bash
cat > collections/requirements.yml <<'EOF'
---
collections:
  - name: community.general
EOF
```

If `collections/` does not exist:

```bash
mkdir -p collections
```

Then recreate the file if necessary.

Future installation becomes:

```bash
ansible-galaxy collection install \
  -r collections/requirements.yml
```

---

## 12.20 Add UFW configuration

Append to:

```text
roles/security/tasks/main.yml
```

using:

```bash
cat >> roles/security/tasks/main.yml <<'EOF'

- name: Set default incoming firewall policy
  community.general.ufw:
    direction: incoming
    policy: deny

- name: Set default outgoing firewall policy
  community.general.ufw:
    direction: outgoing
    policy: allow

- name: Allow SSH from automation network
  community.general.ufw:
    rule: allow
    port: "22"
    proto: tcp
    src: "192.168.56.0/24"

- name: Enable UFW
  community.general.ufw:
    state: enabled
EOF
```

For the web tier, add port 80 separately rather than opening it globally.

Create:

```bash
cat >> roles/webserver/tasks/main.yml <<'EOF'

- name: Allow HTTP
  community.general.ufw:
    rule: allow
    port: "80"
    proto: tcp
EOF
```

This demonstrates role-specific policy.

---

## 12.21 Build monitoring agent role

For the lab, use Ubuntu's packaged Prometheus Node Exporter rather than downloading an unverified binary from an arbitrary URL.

Create:

```bash
cat > roles/monitoring_agent/tasks/main.yml <<'EOF'
---
- name: Install Prometheus Node Exporter
  ansible.builtin.apt:
    name: prometheus-node-exporter
    state: present
    update_cache: true
    cache_valid_time: 3600

- name: Ensure Prometheus Node Exporter is enabled and running
  ansible.builtin.service:
    name: prometheus-node-exporter
    state: started
    enabled: true

- name: Allow Node Exporter from monitoring server
  community.general.ufw:
    rule: allow
    port: "{{ node_exporter_port | string }}"
    proto: tcp
    src: "192.168.56.51"
EOF
```

---

## 12.22 Deploy monitoring agent

Create:

```bash
cat > playbooks/monitoring-agents.yml <<'EOF'
---
- name: Deploy monitoring agents
  hosts: all:!monitoring
  become: true
  serial: 2

  roles:
    - monitoring_agent

  post_tasks:
    - name: Verify Node Exporter locally
      ansible.builtin.uri:
        url: "http://127.0.0.1:{{ node_exporter_port }}/metrics"
        status_code: 200
      register: node_exporter_health

    - name: Confirm Node Exporter response
      ansible.builtin.assert:
        that:
          - node_exporter_health.status == 200
EOF
```

Syntax:

```bash
ansible-playbook \
  playbooks/monitoring-agents.yml \
  --syntax-check
```

Deploy:

```bash
ansible-playbook \
  playbooks/monitoring-agents.yml
```

---

## 12.23 Verify monitoring endpoint

On a managed node:

```bash
curl -fsS \
  http://127.0.0.1:9100/metrics \
  | head
```

From MONITOR01:

```bash
curl -fsS \
  http://192.168.56.21:9100/metrics \
  | head
```

From a source not permitted by firewall policy, access should be denied.

This verifies that monitoring deployment and network policy agree.

---

# 13. Configuration

## 13.1 Build site playbook

Create:

```bash
cat > playbooks/site.yml <<'EOF'
---
- name: Configure Linux baseline
  hosts: all
  become: true

  roles:
    - common
    - security

- name: Configure web servers
  hosts: webservers
  become: true
  serial: 1

  roles:
    - webserver

- name: Configure monitoring agents
  hosts: all:!monitoring
  become: true
  serial: 2

  roles:
    - monitoring_agent
EOF
```

This becomes the high-level desired-state entry point.

---

## 13.2 Create project validation configuration

Create:

```bash
cat > .yamllint <<'EOF'
---
extends: default

rules:
  line-length:
    max: 120
    level: warning

  truthy:
    allowed-values:
      - "true"
      - "false"
EOF
```

---

## 13.3 Validate YAML

Run:

```bash
yamllint \
  inventories \
  playbooks \
  roles
```

Fix errors.

---

## 13.4 Run Ansible lint

Run:

```bash
ansible-lint \
  playbooks/site.yml
```

Do not blindly suppress lint failures.

Understand them.

If a rule is intentionally waived, document why.

---

## 13.5 Syntax validation

```bash
ansible-playbook \
  playbooks/site.yml \
  --syntax-check
```

---

## 13.6 Inventory validation

```bash
ansible-inventory \
  --graph
```

---

## 13.7 Check mode

Run:

```bash
ansible-playbook \
  playbooks/site.yml \
  --check \
  --diff
```

Check mode is useful but is not a perfect simulation of every real change.

Do not treat it as proof that a production execution cannot fail.

---

# 14. Automation

## 14.1 Build safe execution wrapper

Create:

```bash
cat > scripts/bash/run-platform.sh <<'EOF'
#!/usr/bin/env bash
set -euo pipefail

PLAYBOOK="${1:-playbooks/site.yml}"
LIMIT="${2:-all}"

echo "========================================"
echo " VERIQTA Linux Automation Platform"
echo "========================================"
echo "Playbook: ${PLAYBOOK}"
echo "Limit:    ${LIMIT}"
echo

echo "[1/5] YAML validation"
yamllint inventories playbooks roles

echo
echo "[2/5] Ansible syntax validation"
ansible-playbook "${PLAYBOOK}" --syntax-check

echo
echo "[3/5] Host selection"
ansible-playbook "${PLAYBOOK}" \
  --limit "${LIMIT}" \
  --list-hosts

echo
echo "[4/5] Check mode"
ansible-playbook "${PLAYBOOK}" \
  --limit "${LIMIT}" \
  --check \
  --diff

echo
echo "[5/5] Validation completed."
echo
echo "No live change has been executed by this wrapper."
EOF
```

Make executable:

```bash
chmod +x scripts/bash/run-platform.sh
```

Run:

```bash
./scripts/bash/run-platform.sh \
  playbooks/site.yml \
  web01
```

Notice the wrapper deliberately does not automatically execute the live deployment.

Validation and deployment remain separate decisions.

---

## 14.2 Build live deployment wrapper

Create:

```bash
cat > scripts/bash/deploy-platform.sh <<'EOF'
#!/usr/bin/env bash
set -euo pipefail

PLAYBOOK="${1:-playbooks/site.yml}"
LIMIT="${2:-all}"

echo "Running syntax validation..."
ansible-playbook "${PLAYBOOK}" --syntax-check

echo
echo "Target hosts:"
ansible-playbook "${PLAYBOOK}" \
  --limit "${LIMIT}" \
  --list-hosts

echo
read -r -p "Type DEPLOY to continue: " confirmation

if [[ "${confirmation}" != "DEPLOY" ]]; then
    echo "Deployment cancelled."
    exit 1
fi

ansible-playbook \
  "${PLAYBOOK}" \
  --limit "${LIMIT}"
EOF
```

Protect:

```bash
chmod 0750 \
  scripts/bash/deploy-platform.sh
```

---

## 14.3 Build Python inventory reporter

Create:

```bash
cat > scripts/python/inventory_report.py <<'EOF'
#!/usr/bin/env python3

import json
import subprocess
import sys
from pathlib import Path


def run_inventory():
    command = [
        "ansible-inventory",
        "-i",
        "inventories/lab/hosts.yml",
        "--list",
    ]

    result = subprocess.run(
        command,
        check=True,
        capture_output=True,
        text=True,
    )

    return json.loads(result.stdout)


def main():
    try:
        inventory = run_inventory()
    except subprocess.CalledProcessError as exc:
        print("Inventory command failed.", file=sys.stderr)
        print(exc.stderr, file=sys.stderr)
        return 1
    except json.JSONDecodeError as exc:
        print(f"Inventory returned invalid JSON: {exc}", file=sys.stderr)
        return 1

    hostvars = inventory.get("_meta", {}).get("hostvars", {})

    report = {
        "host_count": len(hostvars),
        "hosts": [],
    }

    for hostname in sorted(hostvars):
        variables = hostvars[hostname]

        report["hosts"].append(
            {
                "hostname": hostname,
                "ansible_host": variables.get("ansible_host"),
                "ansible_user": variables.get("ansible_user"),
            }
        )

    output_path = Path("reports/inventory-report.json")
    output_path.parent.mkdir(parents=True, exist_ok=True)

    output_path.write_text(
        json.dumps(report, indent=2) + "\n",
        encoding="utf-8",
    )

    print(f"Hosts discovered: {report['host_count']}")

    for host in report["hosts"]:
        print(
            f"{host['hostname']:<12} "
            f"{host['ansible_host'] or '-':<16} "
            f"{host['ansible_user'] or '-'}"
        )

    print(f"\nReport written to {output_path}")
    return 0


if __name__ == "__main__":
    sys.exit(main())
EOF
```

Make executable:

```bash
chmod +x \
  scripts/python/inventory_report.py
```

Run:

```bash
./scripts/python/inventory_report.py
```

Inspect:

```bash
jq . reports/inventory-report.json
```

---

## 14.4 Build infrastructure health report

Create:

```bash
cat > playbooks/health.yml <<'EOF'
---
- name: Collect Linux health information
  hosts: all
  gather_facts: true
  become: true

  tasks:
    - name: Read root filesystem usage
      ansible.builtin.command:
        cmd: df -P /
      register: root_disk
      changed_when: false

    - name: Read failed systemd units
      ansible.builtin.command:
        cmd: systemctl --failed --no-legend
      register: failed_units
      changed_when: false
      failed_when: false

    - name: Show health summary
      ansible.builtin.debug:
        msg:
          hostname: "{{ inventory_hostname }}"
          distribution: "{{ ansible_distribution }}"
          release: "{{ ansible_distribution_version }}"
          memory_mb: "{{ ansible_memtotal_mb }}"
          root_filesystem: "{{ root_disk.stdout_lines }}"
          failed_units: "{{ failed_units.stdout_lines }}"
EOF
```

Run:

```bash
ansible-playbook \
  playbooks/health.yml
```

This playbook gathers state.

It does not remediate.

That distinction matters.

---

## 14.5 Create remediation playbook

Create:

```bash
cat > playbooks/remediate.yml <<'EOF'
---
- name: Remediate managed Linux baseline
  hosts: all
  become: true
  serial: 2

  roles:
    - common
    - security
    - monitoring_agent
EOF
```

The desired-state roles become the remediation mechanism.

Avoid creating a separate collection of arbitrary repair commands when convergence can restore the approved state.

---

# 15. Security

## 15.1 Protect Ansible secrets

Ansible Vault encrypts data at rest.

Create vault directory:

```bash
mkdir -p \
  inventories/lab/group_vars/all
```

Create encrypted file:

```bash
ansible-vault create \
  inventories/lab/group_vars/all/vault.yml
```

Inside it, for example:

```yaml
---
vault_example_api_token: "replace-with-lab-secret"
```

Do not place the vault password in Git.

---

## 15.2 View encrypted data safely

```bash
ansible-vault view \
  inventories/lab/group_vars/all/vault.yml
```

Edit:

```bash
ansible-vault edit \
  inventories/lab/group_vars/all/vault.yml
```

---

## 15.3 Demonstrate no_log

If a task must use a secret:

```yaml
- name: Demonstrate protected secret use
  ansible.builtin.debug:
    msg: "A secret was loaded for the operation."
  no_log: true
```

Vault protects stored data.

It does not automatically prevent decrypted values from being leaked during execution.

---

## 15.4 Protect control node

Check permissions:

```bash
stat ~/.ssh/automation_ed25519
```

Expected:

```text
owner-readable only
```

Correct if necessary:

```bash
chmod 0600 \
  ~/.ssh/automation_ed25519
```

Check project:

```bash
grep -RniE \
  'password|secret|token|private.?key' \
  . \
  --exclude-dir=.git
```

Review every match.

---

## 15.5 Security automation principle

Never automate:

```text
Disable firewall to fix connectivity.
Disable AppArmor because deployment failed.
Set chmod 777 because permission denied.
Disable SSH host-key checking permanently.
Put passwords in inventory.
Use curl | bash without verification.
```

Automation should enforce engineering discipline, not automate shortcuts.

---

# 16. Testing

Create:

```bash
cat > tests/ACCEPTANCE-TESTS.md <<'EOF'
# Enterprise Linux Automation Platform Acceptance Tests

AUTO-001 Control node can reach every managed host.

AUTO-002 Inventory parses successfully.

AUTO-003 Every managed host appears in the intended inventory group.

AUTO-004 ansible.builtin.ping succeeds against reachable managed nodes.

AUTO-005 baseline playbook passes syntax validation.

AUTO-006 security playbook passes syntax validation.

AUTO-007 site playbook passes syntax validation.

AUTO-008 YAML linting succeeds.

AUTO-009 Ansible linting is reviewed and blocking errors are resolved.

AUTO-010 common packages converge to desired state.

AUTO-011 chrony is enabled and running.

AUTO-012 rsyslog is enabled and running.

AUTO-013 managed users exist.

AUTO-014 root SSH login is disabled.

AUTO-015 password SSH authentication is disabled.

AUTO-016 SSH configuration validates before reload.

AUTO-017 automation account retains tested access after SSH hardening.

AUTO-018 UFW is enabled.

AUTO-019 inbound firewall default is deny.

AUTO-020 SSH remains available from the approved management network.

AUTO-021 nginx is installed on all web servers.

AUTO-022 nginx is enabled on all web servers.

AUTO-023 web page is deployed from template.

AUTO-024 HTTP returns status 200.

AUTO-025 Node Exporter is installed on managed nodes.

AUTO-026 Node Exporter is enabled and running.

AUTO-027 monitoring endpoint responds locally.

AUTO-028 monitoring server can reach approved metrics endpoint.

AUTO-029 unauthorized source is not granted metrics access.

AUTO-030 second unchanged convergence produces no unexplained changes.

AUTO-031 manually removed baseline package is restored.

AUTO-032 manually stopped managed service is restored.

AUTO-033 manually changed managed configuration is restored.

AUTO-034 removed managed user is restored.

AUTO-035 firewall drift is remediated.

AUTO-036 check mode can preview supported changes.

AUTO-037 single-host deployment works.

AUTO-038 rolling deployment works.

AUTO-039 failed host does not produce false successful completion.

AUTO-040 unreachable host is reported.

AUTO-041 invalid SSH configuration is rejected before deployment.

AUTO-042 invalid YAML is caught before deployment.

AUTO-043 inventory error is detected.

AUTO-044 secret file is encrypted.

AUTO-045 vault password is absent from Git.

AUTO-046 private SSH key is absent from Git.

AUTO-047 automation report is generated.

AUTO-048 health playbook reports infrastructure state.

AUTO-049 remediation playbook restores intended state.

AUTO-050 rollback procedure is documented and tested.
EOF
```

---

## 16.1 Verify services

```bash
ansible all \
  -m ansible.builtin.command \
  -a 'systemctl is-active chrony'
```

Because this is a read-only verification:

```bash
ansible all \
  -m ansible.builtin.command \
  -a 'systemctl is-enabled chrony'
```

---

## 16.2 Verify SSH policy

```bash
ansible all \
  -b \
  -m ansible.builtin.command \
  -a '/usr/sbin/sshd -T'
```

For focused verification:

```bash
ansible all \
  -b \
  -m ansible.builtin.shell \
  -a "/usr/sbin/sshd -T | grep -E 'permitrootlogin|passwordauthentication'"
```

This shell use is for inspection.

Configuration itself remains module/template-driven.

---

## 16.3 Verify web tier

```bash
ansible webservers \
  -m ansible.builtin.uri \
  -a 'url=http://127.0.0.1/ status_code=200'
```

---

## 16.4 Verify monitoring

```bash
ansible 'all:!monitoring' \
  -m ansible.builtin.uri \
  -a 'url=http://127.0.0.1:9100/metrics status_code=200'
```

---

## 16.5 Measure automation execution

Run:

```bash
/usr/bin/time -v \
  ansible-playbook \
  playbooks/site.yml
```

Record:

```text
host count
task count
changed count
failed count
unreachable count
elapsed time
control-node resource use
```

Do not convert this lab measurement into unsupported production scalability claims.

---

## 16.6 Controlled concurrency experiment

Baseline:

```bash
/usr/bin/time -p \
  ansible-playbook \
  playbooks/baseline.yml \
  -f 1
```

Then:

```bash
/usr/bin/time -p \
  ansible-playbook \
  playbooks/baseline.yml \
  -f 5
```

Then:

```bash
/usr/bin/time -p \
  ansible-playbook \
  playbooks/baseline.yml \
  -f 10
```

Record actual timings.

Explain why increasing forks does not guarantee proportional performance improvement.

---

# 17. Failure Simulation

Every failure lab follows:

```text
Known-good baseline
        ↓
Inject controlled failure
        ↓
Observe automation
        ↓
Measure blast radius
        ↓
Collect evidence
        ↓
Investigate
        ↓
Mitigate
        ↓
Recover
        ↓
Run desired-state automation
        ↓
Verify
        ↓
Document prevention
```

Take VM snapshots before destructive experiments.

---

## Failure Lab 01 — Configuration drift

On WEB01 manually edit:

```bash
sudo nano \
  /var/www/html/index.html
```

Replace managed content with:

```text
MANUAL CONFIGURATION DRIFT
```

Verify:

```bash
curl \
  http://192.168.56.21/
```

Preview remediation:

```bash
ansible-playbook \
  playbooks/webservers.yml \
  --limit web01 \
  --check \
  --diff
```

Review the proposed difference.

Apply:

```bash
ansible-playbook \
  playbooks/webservers.yml \
  --limit web01
```

Verify:

```bash
curl -fsS \
  http://192.168.56.21/
```

The approved template should return.

This is configuration convergence.

---

## Failure Lab 02 — Package drift

On WEB02:

```bash
sudo apt remove -y jq
```

Verify from AUTOMATION01:

```bash
ansible web02 \
  -m ansible.builtin.command \
  -a 'command -v jq'
```

The check should fail.

Run:

```bash
ansible-playbook \
  playbooks/baseline.yml \
  --limit web02
```

Verify:

```bash
ansible web02 \
  -m ansible.builtin.command \
  -a 'command -v jq'
```

---

## Failure Lab 03 — Service drift

On WEB03:

```bash
sudo systemctl stop chrony
```

Check:

```bash
systemctl is-active chrony
```

From AUTOMATION01:

```bash
ansible-playbook \
  playbooks/baseline.yml \
  --limit web03
```

Verify:

```bash
ansible web03 \
  -m ansible.builtin.command \
  -a 'systemctl is-active chrony'
```

---

## Failure Lab 04 — User drift

On APP01:

```bash
sudo userdel alice
```

Verify:

```bash
id alice
```

Run:

```bash
ansible-playbook \
  playbooks/baseline.yml \
  --limit app01
```

Verify:

```bash
ansible app01 \
  -m ansible.builtin.command \
  -a 'id alice'
```

---

## Failure Lab 05 — Invalid SSH configuration

Do **not** deploy broken SSH configuration blindly.

Make a temporary copy of the template:

```bash
cp \
  roles/security/templates/60-automation-security.conf.j2 \
  /tmp/60-automation-security.conf.j2.safe
```

Introduce an invalid SSH directive:

```text
ThisDirectiveDoesNotExist yes
```

into the project template.

Run against WEB01:

```bash
ansible-playbook \
  playbooks/security.yml \
  --limit web01 \
  --diff
```

The template validation should reject the candidate before replacing the live configuration.

Restore:

```bash
cp \
  /tmp/60-automation-security.conf.j2.safe \
  roles/security/templates/60-automation-security.conf.j2
```

Syntax check again.

This is safer automation:

```text
Generate
   ↓
Validate
   ↓
Install
   ↓
Reload
```

rather than:

```text
Install broken file
   ↓
Lose SSH
```

---

## Failure Lab 06 — Unreachable server

On WEB03 console:

```bash
sudo systemctl stop ssh
```

From AUTOMATION01:

```bash
ansible webservers \
  -m ansible.builtin.ping
```

Observe WEB03.

Do not call the deployment successful simply because WEB01 and WEB02 succeeded.

Restore through console:

```bash
sudo systemctl start ssh
```

Verify:

```bash
ansible web03 \
  -m ansible.builtin.ping
```

---

## Failure Lab 07 — Invalid inventory

Back up:

```bash
cp \
  inventories/lab/hosts.yml \
  /tmp/hosts.yml.safe
```

Introduce malformed YAML deliberately.

Run:

```bash
ansible-inventory \
  -i inventories/lab/hosts.yml \
  --graph
```

Observe failure.

Restore:

```bash
cp \
  /tmp/hosts.yml.safe \
  inventories/lab/hosts.yml
```

Verify again.

---

## Failure Lab 08 — Wrong target selection

Before a live run:

```bash
ansible-playbook \
  playbooks/webservers.yml \
  --list-hosts
```

Then deliberately test:

```bash
ansible-playbook \
  playbooks/webservers.yml \
  --limit db01 \
  --list-hosts
```

Observe whether the intersection produces any target.

This teaches an important automation lesson:

```text
Always know exactly which hosts a command will affect.
```

---

## Failure Lab 09 — Firewall drift

On WEB01 inspect:

```bash
sudo ufw status numbered
```

Remove the HTTP rule using its current number:

```bash
sudo ufw delete RULE_NUMBER
```

From AUTOMATION01:

```bash
curl \
  --connect-timeout 3 \
  http://192.168.56.21/
```

Observe failure.

Remediate:

```bash
ansible-playbook \
  playbooks/webservers.yml \
  --limit web01
```

Retest.

---

## Failure Lab 10 — Monitoring agent failure

On APP01:

```bash
sudo systemctl stop \
  prometheus-node-exporter
```

Verify:

```bash
curl \
  --connect-timeout 3 \
  http://127.0.0.1:9100/metrics
```

Run:

```bash
ansible-playbook \
  playbooks/monitoring-agents.yml \
  --limit app01
```

Verify:

```bash
ansible app01 \
  -m ansible.builtin.uri \
  -a 'url=http://127.0.0.1:9100/metrics status_code=200'
```

---

## Failure Lab 11 — Partial rolling deployment failure

Create temporary test playbook:

```bash
cat > failure-labs/rolling-failure.yml <<'EOF'
---
- name: Demonstrate controlled rolling failure
  hosts: webservers
  serial: 1
  any_errors_fatal: true

  tasks:
    - name: Show current host
      ansible.builtin.debug:
        msg: "Processing {{ inventory_hostname }}"

    - name: Inject failure on web02
      ansible.builtin.fail:
        msg: "Controlled failure on web02"
      when: inventory_hostname == "web02"

    - name: Task that should not continue after fatal failure
      ansible.builtin.debug:
        msg: "Post-failure task on {{ inventory_hostname }}"
EOF
```

Run:

```bash
ansible-playbook \
  failure-labs/rolling-failure.yml
```

Observe:

```text
WEB01 processed
WEB02 controlled failure
rollout stops according to failure policy
```

Record actual behavior.

This demonstrates blast-radius management.

---

## Failure Lab 12 — Broken Nginx content deployment and rollback

Create current backup:

```bash
cp \
  roles/webserver/templates/index.html.j2 \
  /tmp/index.html.j2.known-good
```

Change template to:

```text
BROKEN RELEASE DEMONSTRATION
```

Deploy only WEB01:

```bash
ansible-playbook \
  playbooks/webservers.yml \
  --limit web01
```

Verify:

```bash
curl -fsS \
  http://192.168.56.21/
```

Assume the content fails acceptance criteria.

Rollback source:

```bash
cp \
  /tmp/index.html.j2.known-good \
  roles/webserver/templates/index.html.j2
```

Deploy rollback:

```bash
ansible-playbook \
  playbooks/webservers.yml \
  --limit web01
```

Verify known-good content.

The important principle is:

```text
Rollback is not "edit the server manually."

Rollback is redeploy a known-good desired state.
```

---

## Failure Lab 13 — Vault exposure prevention

Run:

```bash
head \
  inventories/lab/group_vars/all/vault.yml
```

The file should contain encrypted Vault material rather than the plaintext secret.

Then:

```bash
git status
```

Verify `.vault-pass` or other vault-password material is not staged.

---

## Failure Lab 14 — Control-node Python tool failure

Temporarily rename inventory:

```bash
mv \
  inventories/lab/hosts.yml \
  inventories/lab/hosts.yml.disabled
```

Run:

```bash
./scripts/python/inventory_report.py
```

The script should exit unsuccessfully rather than fabricate an empty successful report.

Restore:

```bash
mv \
  inventories/lab/hosts.yml.disabled \
  inventories/lab/hosts.yml
```

Run again.

---

# 18. Troubleshooting

Use this model:

```text
Symptom
   ↓
Affected hosts
   ↓
Blast radius
   ↓
Last automation run
   ↓
Target inventory
   ↓
Variables
   ↓
SSH connectivity
   ↓
Privilege escalation
   ↓
Module/task
   ↓
Rendered configuration
   ↓
Service state
   ↓
Logs
   ↓
Competing hypotheses
   ↓
Test
   ↓
Narrow
   ↓
Mitigate
   ↓
Recover
   ↓
Verify desired state
   ↓
Prevent recurrence
```

---

## Host unreachable

Test:

```bash
ping -c 2 \
  192.168.56.21
```

Then:

```bash
ssh \
  -vvv \
  -i ~/.ssh/automation_ed25519 \
  automation@192.168.56.21
```

Then:

```bash
ansible web01 \
  -m ansible.builtin.ping \
  -vvvv
```

Check:

```text
IP address
route
SSH service
firewall
SSH key
user
permissions
host key
inventory
```

---

## sudo failure

Manual:

```bash
ssh \
  -i ~/.ssh/automation_ed25519 \
  automation@192.168.56.21
```

Then:

```bash
sudo -n true
```

Check:

```bash
sudo -l
```

Do not diagnose a privilege-escalation problem as an Ansible connection problem.

---

## Wrong variable

Inspect:

```bash
ansible-inventory \
  --host web01
```

Use:

```bash
ansible-config dump \
  --only-changed
```

Remember that Ansible has variable precedence rules.

Do not create the same variable in many locations without understanding which value wins.

---

## Task always reports changed

Ask:

```text
Is the module idempotent?

Am I using shell/command unnecessarily?

Does the task actually alter something every run?

Should changed_when be defined?

Am I generating nondeterministic content?

Does a template include a timestamp?
```

Do not hide a genuine idempotency problem with:

```yaml
changed_when: false
```

unless the task truly does not modify state.

---

## Handler does not run

Check:

```text
Did the notifying task report changed?

Does notify exactly match the handler?

Was the handler loaded?

Did execution fail before handlers ran?
```

---

## Template breaks service

Inspect rendered candidate.

For SSH use validation before deployment.

For other services, use the application's native configuration validator where available.

The preferred pattern is:

```text
Render candidate
       ↓
Validate candidate
       ↓
Install candidate
       ↓
Notify handler
       ↓
Reload/restart
       ↓
Health check
```

---

# 19. Final Validation

Create:

```bash
cat > evidence/FINAL-VALIDATION.md <<'EOF'
# Enterprise Linux Automation Platform Final Validation

Record actual results only.

## Control Plane

Ansible version:
Result:

Inventory validation:
Result:

Managed host count:
Result:

SSH connectivity:
Result:

Privilege escalation:
Result:

## Baseline

Common packages:
Result:

chrony:
Result:

rsyslog:
Result:

managed users:
Result:

## Security

root SSH:
Result:

password authentication:
Result:

SSH validation:
Result:

UFW:
Result:

auditd:
Result:

## Web Tier

WEB01 HTTP:
Result:

WEB02 HTTP:
Result:

WEB03 HTTP:
Result:

## Monitoring

WEB01 Node Exporter:
Result:

WEB02 Node Exporter:
Result:

WEB03 Node Exporter:
Result:

APP01 Node Exporter:
Result:

DB01 Node Exporter:
Result:

## Idempotency

First run changed count:
Result:

Second run changed count:
Result:

Unexplained changes:
Result:

## Drift

package drift detected:
Result:

package drift remediated:
Result:

service drift detected:
Result:

service drift remediated:
Result:

configuration drift detected:
Result:

configuration drift remediated:
Result:

firewall drift detected:
Result:

firewall drift remediated:
Result:

## Failure Handling

unreachable host:
Result:

invalid inventory:
Result:

invalid SSH configuration:
Result:

rolling failure:
Result:

rollback:
Result:

## Secrets

Vault encryption:
Result:

Vault password absent from Git:
Result:

SSH private key absent from Git:
Result:

## Measurements

forks=1:
Result:

forks=5:
Result:

forks=10:
Result:

## Remaining Limitations

Record:
EOF
```

Never fill this with imaginary PASS values.

---

# 20. Portfolio Documentation

## Architecture document

Create:

```bash
cat > docs/ARCHITECTURE.md <<'EOF'
# Enterprise Linux Automation Platform Architecture

## Control Plane

AUTOMATION01 provides centralized configuration management using Ansible.

## Managed Tiers

- Web servers
- Application servers
- Database servers
- Monitoring server

## Automation Model

Desired state is defined through:

- inventory
- variables
- playbooks
- roles
- templates

## Change Pipeline

Validate
→ Preview
→ Limit
→ Deploy
→ Verify
→ Expand
→ Measure
→ Record

## Drift Model

Desired state
→ actual state changes
→ automation detects difference
→ automation converges host
→ validation proves recovery

## Failure Model

Automation failures are treated as infrastructure incidents rather than hidden
with ignore_errors.

## Security Model

- SSH key authentication
- SSH host-key verification
- privilege escalation through sudo
- secrets protected with Vault
- host firewall management
- security configuration validation
- private keys excluded from Git
EOF
```

---

## Limitations

```bash
cat > docs/LIMITATIONS.md <<'EOF'
# Limitations

This project is a senior automation laboratory, not a complete enterprise
automation service.

The lab does not claim:

- production-scale inventory
- enterprise RBAC
- enterprise SSO
- production secrets-management integration
- high-availability automation controllers
- enterprise workflow approvals
- full CMDB integration
- full CI/CD integration
- automatic patch approval
- enterprise compliance certification
- production change-management integration

The lab automation user uses broad passwordless sudo for learning purposes.
That decision must be reassessed for a real environment.

Ansible Vault protects encrypted data at rest but does not by itself provide a
complete enterprise secrets-management architecture.

Check mode does not guarantee that a real execution will succeed.

Performance measurements from this small laboratory must not be presented as
production capacity measurements.
EOF
```

---

## ADR 001

```bash
cat > adrs/ADR-001-ANSIBLE.md <<'EOF'
# ADR-001 — Ansible as Configuration Management Engine

## Status

Accepted.

## Decision

Use Ansible as the primary configuration-management engine.

## Reasons

- agentless managed-node model for this lab
- SSH-based Linux management
- declarative modules
- reusable roles
- inventory grouping
- check mode
- handlers
- mature Linux automation ecosystem

## Consequences

The control node becomes an important operational and security dependency.

SSH access, privilege escalation, inventory quality and secret handling must be
managed carefully.
EOF
```

---

## ADR 002

```bash
cat > adrs/ADR-002-ROLES.md <<'EOF'
# ADR-002 — Role-Based Automation Structure

## Status

Accepted.

## Decision

Reusable configuration is implemented through roles rather than one large
playbook.

## Reason

Roles separate responsibilities and improve reuse, testing and maintenance.

## Consequence

Role boundaries and variables must remain intentionally designed.
EOF
```

---

## ADR 003

```bash
cat > adrs/ADR-003-CONTROLLED-ROLLOUT.md <<'EOF'
# ADR-003 — Controlled Rollouts

## Status

Accepted.

## Decision

Risky infrastructure changes use limited-host testing followed by rolling
deployment.

## Reason

Automation increases both deployment speed and potential failure speed.

## Consequence

Changes may take longer to reach the full fleet, but blast radius is reduced.
EOF
```

---

## ADR 004

```bash
cat > adrs/ADR-004-DRIFT-REMEDIATION.md <<'EOF'
# ADR-004 — Desired State as Remediation Mechanism

## Status

Accepted.

## Decision

Configuration drift should normally be corrected by reapplying approved
desired-state automation.

## Reason

Manual repair can create additional undocumented state.

## Consequence

Roles must remain idempotent and sufficiently complete to restore the intended
configuration.
EOF
```

---

# 21. GitHub Structure

The final repository should contain only files actually built or documented.

```text
07-Enterprise-Linux-Automation-Platform/
│
├── README.md
├── .gitignore
├── .yamllint
├── ansible.cfg
│
├── inventories/
│   └── lab/
│       ├── hosts.yml
│       ├── group_vars/
│       │   ├── all/
│       │   │   ├── main.yml
│       │   │   └── vault.yml
│       │   ├── webservers/
│       │   ├── appservers/
│       │   ├── dbservers/
│       │   └── monitoring/
│       └── host_vars/
│
├── collections/
│   └── requirements.yml
│
├── playbooks/
│   ├── baseline.yml
│   ├── security.yml
│   ├── webservers.yml
│   ├── monitoring-agents.yml
│   ├── health.yml
│   ├── remediate.yml
│   └── site.yml
│
├── roles/
│   ├── common/
│   │   ├── tasks/
│   │   │   ├── main.yml
│   │   │   └── users.yml
│   │   ├── handlers/
│   │   ├── templates/
│   │   ├── files/
│   │   ├── defaults/
│   │   └── vars/
│   │
│   ├── security/
│   │   ├── tasks/
│   │   │   └── main.yml
│   │   ├── handlers/
│   │   │   └── main.yml
│   │   ├── templates/
│   │   │   └── 60-automation-security.conf.j2
│   │   ├── files/
│   │   ├── defaults/
│   │   └── vars/
│   │
│   ├── webserver/
│   │   ├── tasks/
│   │   │   └── main.yml
│   │   ├── templates/
│   │   │   └── index.html.j2
│   │   ├── handlers/
│   │   ├── files/
│   │   ├── defaults/
│   │   └── vars/
│   │
│   └── monitoring_agent/
│       ├── tasks/
│       │   └── main.yml
│       ├── handlers/
│       ├── templates/
│       ├── files/
│       ├── defaults/
│       └── vars/
│
├── scripts/
│   ├── bash/
│   │   ├── test-idempotency.sh
│   │   ├── run-platform.sh
│   │   └── deploy-platform.sh
│   └── python/
│       └── inventory_report.py
│
├── tests/
│   └── ACCEPTANCE-TESTS.md
│
├── failure-labs/
│   └── rolling-failure.yml
│
├── evidence/
│   └── FINAL-VALIDATION.md
│
├── reports/
│   └── inventory-report.json
│
├── measurements/
│
├── runbooks/
│
├── docs/
│   ├── ARCHITECTURE.md
│   └── LIMITATIONS.md
│
└── adrs/
    ├── ADR-001-ANSIBLE.md
    ├── ADR-002-ROLES.md
    ├── ADR-003-CONTROLLED-ROLLOUT.md
    └── ADR-004-DRIFT-REMEDIATION.md
```

`README.md` is this project document when it is placed in the repository.

Do not commit empty directories merely for appearance. Git does not track empty directories anyway.

---

# 22. Resume Entry

After actually completing and testing the project:

**Senior Linux Automation Project — Enterprise Linux Automation Platform**

Designed and implemented a centralized Linux configuration-management platform using Ansible, Bash and Python to automate operating-system baselines, user management, SSH hardening, host firewall policy, service deployment and monitoring-agent configuration across a multi-tier Ubuntu environment.

Developed reusable Ansible roles, structured inventories, Jinja templates, validation workflows and controlled rolling deployments, with idempotency testing, configuration-drift remediation, Ansible Vault secret protection and automated infrastructure health reporting.

Performed controlled failure and recovery exercises covering package drift, service drift, configuration drift, firewall drift, unreachable nodes, invalid inventory, invalid SSH configuration, partial rolling deployments and known-good rollback.

Only use these statements after completing the corresponding work.

---

# 23. Interview Preparation

You should be able to answer:

1. What problem does configuration management solve?
2. What is Ansible?
3. What is an Ansible control node?
4. What is a managed node?
5. Does Ansible require a daemon on the managed Linux node in this architecture?
6. Why does SSH need to work before Ansible?
7. What is inventory?
8. Static versus dynamic inventory.
9. What is a host group?
10. What are `group_vars`?
11. What are `host_vars`?
12. Why separate environments?
13. What is an Ansible play?
14. What is a playbook?
15. What is a task?
16. What is a module?
17. What is a role?
18. Why use roles?
19. What is a handler?
20. When does a handler run?
21. Why use handlers instead of restarting services after every task?
22. What is idempotency?
23. How did you prove idempotency?
24. Why can a task still be non-idempotent even though it is inside Ansible?
25. Why prefer modules over arbitrary shell commands?
26. When is `command` appropriate?
27. When is `shell` required?
28. Why is `shell` riskier?
29. What are Ansible facts?
30. How did you inspect facts?
31. What is variable precedence?
32. Why can duplicate variable definitions become dangerous?
33. What is Jinja templating?
34. How did you template Nginx content?
35. How did you template SSH configuration?
36. What does template validation accomplish?
37. Why validate SSH before replacing live configuration?
38. What does `--syntax-check` do?
39. What does `--check` do?
40. What are the limitations of check mode?
41. What does `--diff` do?
42. Why can `--diff` expose sensitive information?
43. What does `--list-hosts` provide?
44. Why check target hosts before deployment?
45. What does `--list-tasks` provide?
46. What does `--limit` do?
47. How did you use `--limit` to reduce blast radius?
48. What is `serial`?
49. How does `serial` affect rollout?
50. Why was `serial: 1` used for SSH hardening?
51. What is `forks`?
52. What did your concurrency experiment measure?
53. Why shouldn't lab timing be treated as production capacity?
54. What is configuration drift?
55. Give an example of package drift.
56. Give an example of service drift.
57. Give an example of configuration drift.
58. Give an example of firewall drift.
59. How did the project remediate drift?
60. Why is desired-state convergence preferable to random manual repair?
61. What is Ansible Vault?
62. What does Vault protect?
63. What does Vault not protect?
64. Why might `no_log` be needed?
65. Why should the Vault password not be committed?
66. Why should SSH private keys not be committed?
67. Why did you retain SSH host-key checking?
68. Why is disabling host-key checking globally dangerous?
69. What is privilege escalation?
70. What does `become` do?
71. Why is broad passwordless sudo a lab limitation?
72. How would you improve privilege management in an enterprise?
73. How did you automate UFW?
74. Why are web firewall rules role-specific?
75. Why shouldn't port 80 be opened on every Linux server?
76. How did you deploy monitoring?
77. Why was Node Exporter installed from the Ubuntu package in this lab?
78. How did you verify Node Exporter?
79. How did you restrict monitoring network access?
80. What happens when a host becomes unreachable?
81. How does Ansible report unreachable versus failed?
82. How did you test an unreachable host?
83. How did you test malformed inventory?
84. How did you test broken SSH configuration?
85. How did you test partial rollout failure?
86. What does `any_errors_fatal` change?
87. Why is `ignore_errors: true` dangerous when used indiscriminately?
88. What is a rollback?
89. How did this project implement rollback?
90. Why is manually repairing a host not necessarily a good rollback?
91. What is a known-good desired state?
92. What is configuration convergence?
93. What did the Bash wrappers provide?
94. What did the Python inventory tool provide?
95. Why should automation tooling return non-zero on failure?
96. How would you manage 100 servers?
97. How would you manage 1,000 servers?
98. How would you add CI validation?
99. How would you add approval workflows and centralized execution?
100. What would you change before calling this a production enterprise automation platform?

---

# 24. Cleanup

Do not destroy the lab until evidence is collected.

## Remove temporary files

Check:

```bash
find /tmp \
  -maxdepth 1 \
  -type f \
  -name '*automation*' \
  -o -name '*hosts.yml.safe'
```

Review before deleting anything.

---

## Remove temporary failure-lab changes

Verify SSH templates are known-good:

```bash
ansible-playbook \
  playbooks/security.yml \
  --syntax-check
```

Verify inventory:

```bash
ansible-inventory \
  --graph
```

Reapply desired state:

```bash
ansible-playbook \
  playbooks/site.yml
```

---

## Run final convergence again

```bash
ansible-playbook \
  playbooks/site.yml
```

Then repeat.

Investigate unexplained changes on the second run.

---

# 25. Cleanup Verification

Check Git:

```bash
git status
```

Review files:

```bash
find . \
  -type f \
  | sort
```

Search private-key markers:

```bash
grep -RIlE \
  'BEGIN (OPENSSH |RSA |EC |DSA )?PRIVATE KEY' \
  . \
  --exclude-dir=.git
```

Expected:

```text
no committed private keys
```

Search secrets:

```bash
grep -RniE \
  'password|secret|token' \
  . \
  --exclude-dir=.git
```

Review every result.

Check Vault file:

```bash
head \
  inventories/lab/group_vars/all/vault.yml
```

Verify it remains encrypted.

Run final lint:

```bash
yamllint \
  inventories \
  playbooks \
  roles
```

Run:

```bash
ansible-lint \
  playbooks/site.yml
```

Run:

```bash
ansible-playbook \
  playbooks/site.yml \
  --syntax-check
```

---

# 26. Project Reflection

Answer these from actual evidence.

## Architecture

Why did you use a dedicated control node?

What happens if AUTOMATION01 is compromised?

What credentials can it access?

What is its blast radius?

How would you protect it in a real enterprise?

---

## Inventory

Why did you separate:

```text
webservers
appservers
dbservers
monitoring
```

rather than place every host in one flat list?

Which configuration belongs globally?

Which belongs to a specific role?

---

## Idempotency

Which tasks changed during the first run?

What changed during the second run?

Did any task change every time?

Why?

How did you fix it?

---

## Security

What SSH controls were automated?

What would happen if the SSH template contained invalid syntax?

What protected the server?

Why was rollout limited?

What remains risky about the automation account?

---

## Drift

Which manual changes did you introduce?

Which were detected?

Which were remediated?

Could your platform detect drift without running Ansible?

What additional tooling would be needed for continuous drift detection?

---

## Monitoring

How did monitoring deployment differ from monitoring operation?

Did installing Node Exporter mean Prometheus was actually scraping it?

Why not?

What additional automation would be required?

---

## Failure handling

What happened when WEB03 became unreachable?

Did the remaining hosts continue?

What happened during the controlled rolling failure?

What determined the blast radius?

---

## Rollback

What exactly did you roll back?

Was the rollback:

```text
manual repair
```

or:

```text
redeployment of known-good desired state
```

Why does that distinction matter?

---

## Scale

How would this architecture change at:

```text
10 servers?

100 servers?

1,000 servers?

10,000 servers?

multiple regions?

multiple Linux distributions?

multiple business units?

regulated environments?
```

Consider:

```text
dynamic inventory
centralized execution
RBAC
approval workflows
secret managers
CI/CD
automated testing
artifact versioning
configuration promotion
execution environments
HA control plane
observability
CMDB integration
policy as code
compliance reporting
```

---

# 27. Final Completion Checklist

## Environment

* [ ] AUTOMATION01 created.
* [ ] WEB01 created.
* [ ] WEB02 created.
* [ ] WEB03 created.
* [ ] APP01 created.
* [ ] DB01 created.
* [ ] MONITOR01 created.
* [ ] Hostnames configured.
* [ ] Lab networking verified.
* [ ] SSH connectivity verified manually.

## Control Node

* [ ] Automation user created.
* [ ] Python virtual environment created.
* [ ] ansible-core installed.
* [ ] ansible-lint installed.
* [ ] yamllint installed.
* [ ] Ansible version recorded.
* [ ] SSH automation key created.
* [ ] private key permissions verified.

## Managed Nodes

* [ ] Automation account created.
* [ ] SSH public key installed.
* [ ] sudo access configured.
* [ ] sudo configuration validated.
* [ ] manual SSH tested.
* [ ] manual sudo tested.

## Inventory

* [ ] YAML inventory created.
* [ ] web group created.
* [ ] application group created.
* [ ] database group created.
* [ ] monitoring group created.
* [ ] common variables created.
* [ ] inventory graph validated.
* [ ] host variables inspected.

## Baseline Automation

* [ ] common role created.
* [ ] common packages managed.
* [ ] administrator group managed.
* [ ] chrony managed.
* [ ] rsyslog managed.
* [ ] user-management tasks created.
* [ ] baseline playbook created.
* [ ] baseline syntax validated.
* [ ] baseline applied.
* [ ] second convergence tested.
* [ ] unexplained changes investigated.

## Security Automation

* [ ] security role created.
* [ ] OpenSSH managed.
* [ ] SSH template created.
* [ ] SSH candidate validation configured.
* [ ] root SSH disabled.
* [ ] password SSH disabled.
* [ ] SSH handler created.
* [ ] auditd managed.
* [ ] UFW managed.
* [ ] default-deny inbound policy applied.
* [ ] SSH firewall rule scoped.
* [ ] single-host SSH rollout tested.
* [ ] post-change SSH access verified.

## Web Automation

* [ ] webserver role created.
* [ ] Nginx installed.
* [ ] Nginx enabled.
* [ ] Nginx started.
* [ ] web template deployed.
* [ ] HTTP firewall rule deployed.
* [ ] WEB01 tested first.
* [ ] rolling deployment performed.
* [ ] all web servers return HTTP 200.

## Monitoring Automation

* [ ] monitoring-agent role created.
* [ ] Node Exporter installed.
* [ ] Node Exporter enabled.
* [ ] Node Exporter running.
* [ ] metrics port restricted.
* [ ] local endpoint verified.
* [ ] MONITOR01 endpoint access verified.
* [ ] unauthorized monitoring access tested.

## Configuration Management

* [ ] site playbook created.
* [ ] YAML validation configured.
* [ ] yamllint executed.
* [ ] ansible-lint executed.
* [ ] syntax checks executed.
* [ ] check mode executed.
* [ ] diffs reviewed.
* [ ] host targeting reviewed.

## Bash Automation

* [ ] idempotency script created.
* [ ] validation wrapper created.
* [ ] deployment wrapper created.
* [ ] scripts use safe shell options.
* [ ] deployment confirmation implemented.
* [ ] scripts return meaningful failures.

## Python Automation

* [ ] inventory reporter created.
* [ ] subprocess failure handled.
* [ ] JSON parsing handled.
* [ ] report generated.
* [ ] failure condition tested.
* [ ] fabricated successful output avoided.

## Secrets

* [ ] Vault file created.
* [ ] Vault file encrypted.
* [ ] Vault password excluded from Git.
* [ ] SSH private key excluded from Git.
* [ ] `no_log` concept understood.
* [ ] repository scanned for secrets.

## Drift

* [ ] configuration drift injected.
* [ ] configuration drift detected.
* [ ] configuration drift remediated.
* [ ] package drift injected.
* [ ] package drift remediated.
* [ ] service drift injected.
* [ ] service drift remediated.
* [ ] user drift injected.
* [ ] user drift remediated.
* [ ] firewall drift injected.
* [ ] firewall drift remediated.
* [ ] monitoring drift injected.
* [ ] monitoring drift remediated.

## Failure Engineering

* [ ] invalid SSH configuration tested.
* [ ] unreachable server tested.
* [ ] malformed inventory tested.
* [ ] wrong target selection tested.
* [ ] partial rolling failure tested.
* [ ] Vault exposure prevention tested.
* [ ] Python tooling failure tested.
* [ ] each failure investigated.
* [ ] each failure recovered.
* [ ] post-recovery state verified.

## Controlled Deployment

* [ ] `--list-hosts` used.
* [ ] `--list-tasks` used.
* [ ] `--limit` used.
* [ ] `serial` used.
* [ ] one-host deployment performed.
* [ ] rolling deployment performed.
* [ ] failure blast radius observed.
* [ ] rollback performed.
* [ ] rollback verified.

## Measurement

* [ ] baseline execution timed.
* [ ] forks=1 measured.
* [ ] forks=5 measured.
* [ ] forks=10 measured.
* [ ] actual results recorded.
* [ ] no unsupported scalability claim made.

## Documentation

* [ ] Architecture documented.
* [ ] Limitations documented.
* [ ] ADR-001 created.
* [ ] ADR-002 created.
* [ ] ADR-003 created.
* [ ] ADR-004 created.
* [ ] Acceptance tests created.
* [ ] Final validation document created.
* [ ] Actual results recorded.
* [ ] Resume entry reviewed for accuracy.
* [ ] Interview questions answered.

## Senior Completion Standard

The project is **not complete** because:

```text
ansible all -m ping
```

works.

It is not complete because you wrote several playbooks.

It is not complete because Nginx was installed automatically.

It is complete when you can demonstrate:

```text
I can design an automation control plane.

I can bootstrap a Linux host into automation safely.

I can distinguish bootstrap access from steady-state configuration management.

I can build structured inventories.

I can group infrastructure by responsibility.

I can separate inventory from configuration.

I can separate variables from tasks.

I understand Ansible facts.

I understand variable precedence.

I can create reusable roles.

I can explain why a role exists.

I can define desired state.

I can make automation converge infrastructure toward that state.

I can prove whether my automation is idempotent.

I can identify a non-idempotent task.

I can fix unnecessary changes instead of hiding them.

I can use Ansible modules instead of unnecessary shell commands.

I know when command is appropriate.

I know when shell is necessary.

I understand the security implications of shell automation.

I can template configuration.

I can validate dangerous configuration before installation.

I can use handlers to apply changes only when required.

I can explain when handlers run.

I can automate Linux users.

I can automate package state.

I can automate service state.

I can automate SSH security.

I can automate host firewalls.

I can automate monitoring agents.

I can verify deployed services.

I can use positive tests.

I can use negative tests.

I can protect automation secrets.

I can use Ansible Vault.

I understand that Vault protects data at rest.

I understand that Vault is not a complete enterprise secrets manager.

I can prevent private keys from entering Git.

I can use check mode appropriately.

I understand that check mode is not a guarantee of successful live execution.

I can review diffs before deployment.

I can validate YAML.

I can lint Ansible content.

I can inspect the exact hosts a play will target.

I can limit a deployment to one host.

I can perform a rolling deployment.

I can explain why rolling deployment reduces blast radius.

I understand how forks affect concurrency.

I can measure automation execution.

I do not confuse lab measurements with production capacity.

I can detect configuration drift.

I can remediate configuration drift.

I can detect package drift.

I can remediate package drift.

I can detect service drift.

I can remediate service drift.

I can detect firewall drift.

I can remediate firewall drift.

I can restore a removed managed identity.

I can restore a stopped monitoring agent.

I can distinguish state collection from remediation.

I can build Bash tooling around the automation platform.

I can build Python tooling around the automation platform.

My scripts fail explicitly instead of manufacturing successful output.

I can troubleshoot SSH failures.

I can troubleshoot sudo failures.

I can troubleshoot inventory failures.

I can troubleshoot variable problems.

I can troubleshoot template problems.

I can troubleshoot handlers.

I can troubleshoot unreachable hosts.

I can distinguish unreachable from failed.

I can inject a controlled deployment failure.

I can observe the resulting blast radius.

I can stop unsafe rollout.

I can recover failed systems.

I can roll back to a known-good desired state.

I understand why manual repair is not always a proper rollback.

I can rerun automation after recovery.

I can prove the recovered system matches desired state.

I can generate infrastructure reports.

I can generate validation evidence.

I can document limitations honestly.

I can explain the security risk of the automation control node.

I can explain the security risk of broad automation sudo.

I can describe how enterprise privilege management would improve the design.

I can explain what changes when the fleet grows from 10 systems to 1,000.

I can describe dynamic inventory.

I can describe centralized execution.

I can describe RBAC and approval workflows.

I can describe integration with CI/CD.

I can describe integration with enterprise secret management.

I can explain configuration promotion between environments.

I can explain why automation itself must be tested.

I can explain why automation can increase both operational speed and failure speed.

I can defend every major design decision in a senior Linux/DevOps/Platform/SRE interview.
```


