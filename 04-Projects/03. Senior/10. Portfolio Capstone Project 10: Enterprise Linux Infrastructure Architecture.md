# Portfolio Capstone Project 10: Enterprise Linux Infrastructure Architecture



- **Level:** Senior / Capstone
- **Reference operating system:** Ubuntu Server 24.04 LTS
- **Project type:** Production-style Linux infrastructure architecture and implementation
- **Estimated completion time:** 45–70 hours

- **Demonstrates:** Linux infrastructure engineering, architecture, networking, automation, configuration management, web infrastructure, application infrastructure, PostgreSQL, security, centralized logging, monitoring, alerting, backup engineering, disaster recovery, reliability engineering, troubleshooting, failure testing, operational documentation, and production-readiness review.

---

# 01. Project Title

# Enterprise Linux Infrastructure Architecture

Design, build, secure, automate, observe, operate, break, recover, and document a complete production-style Linux infrastructure.

This is not a project where you install ten unrelated tools.

You are building one system.

The finished environment will contain:

```text
Users
  │
  ▼
Load Balancer
  │
  ├─────────────┐
  ▼             ▼
Web01          Web02
  │             │
  └──────┬──────┘
         ▼
   Application Tier
   ┌───────────────┐
   │               │
 App01           App02
   │               │
   └───────┬───────┘
           ▼
     PostgreSQL Tier
     ┌─────────────┐
     │             │
   DB01          DB02
  Primary       Standby

Infrastructure-wide services:

ADMIN01
   │
   └── Ansible automation

OBS01
   ├── Prometheus
   ├── Grafana
   └── Loki

All Linux servers
   ├── Node Exporter
   └── Grafana Alloy

BACKUP01
   ├── database backups
   ├── configuration backups
   ├── application backups
   └── recovery artifacts

DR01
   └── isolated disaster-recovery target
```

You will then prove that the architecture survives controlled failures.

---

# 02. Project Scenario

VERIQTA Commerce operates a business-critical web application on Linux.

The original application started on one server.

Growth created predictable problems:

* one server is a single point of failure;
* configuration differs between machines;
* engineers make manual changes;
* database recovery has never been tested;
* logs exist only on individual machines;
* there is no centralized monitoring;
* backups exist but restores are rarely tested;
* network access is broader than necessary;
* operational documentation is incomplete;
* nobody knows the actual recovery time;
* failures are detected by users;
* production changes are difficult to reproduce.

You have been assigned to redesign the environment.

The business requires:

```text
redundant web infrastructure
redundant application infrastructure
database replication
centralized monitoring
centralized logging
controlled administrative access
host firewalls
configuration automation
backup automation
tested recovery
documented RTO/RPO
failure testing
operational runbooks
architecture documentation
```

The goal is not to claim that a small VM lab is equivalent to a real enterprise datacenter.

The goal is to demonstrate the engineering practices used to design and operate production Linux infrastructure.

---

## Business service

The application will be:

```text
VERIQTA Commerce API
```

Primary endpoint:

```text
/health
```

Business endpoint:

```text
/orders
```

Database-backed endpoint:

```text
/db-health
```

The final customer request path will be:

```text
CLIENT
  ↓
LB01
  ↓
WEB01 / WEB02
  ↓
APP01 / APP02
  ↓
DB01
  ↓
PostgreSQL
```

---

## Infrastructure objectives

The platform must demonstrate:

### Availability

Loss of one web server must not take down the application.

Loss of one application server must not take down the application.

Database replication must provide a usable standby.

---

### Security

Traffic should be allowed only where required.

Administrative access should be restricted.

Applications should not run as root.

Secrets should not be committed to Git.

Linux security controls should remain enabled.

---

### Observability

You must be able to answer:

```text
Is the infrastructure healthy?

Which server is unhealthy?

Is CPU saturated?

Is memory under pressure?

Is storage filling?

Is the application available?

Are services restarting?

What happened before the failure?

What do the logs show?

Is the database replica lagging?

Did backup succeed?
```

---

### Recoverability

You must prove:

```text
backup creation
backup integrity
database restore
configuration recovery
application recovery
DR reconstruction
measured recovery time
measured data-loss window
```

---

### Automation

A newly created Linux server should not require dozens of undocumented manual steps.

Ansible will provide reproducible configuration.

---

# 03. Project Difficulty

## Senior / Capstone

This project assumes you already know basic Linux administration.

The challenge is integration.

You are expected to reason about:

```text
architecture
dependencies
failure domains
security boundaries
service ownership
capacity
state
statelessness
replication
observability
configuration drift
backup
recovery
change management
rollback
incident response
```

A service being installed does not mean it is production-ready.

For every major component you must answer:

```text
How is it installed?

How is it configured?

How is it secured?

How is it monitored?

Where are its logs?

What depends on it?

What happens when it fails?

How is it backed up?

How is it recovered?

How do you verify recovery?

How is configuration reproduced?

Who should be able to access it?
```

---

# 04. Estimated Time

A realistic completion range is:

| Phase                    | Expected Time |
| ------------------------ | ------------: |
| Architecture/design      |         3–5 h |
| VM/network preparation   |         4–6 h |
| Base Linux configuration |         3–5 h |
| Automation               |         5–8 h |
| Web/application tiers    |         4–6 h |
| PostgreSQL/replication   |         5–8 h |
| Security hardening       |         3–5 h |
| Monitoring/logging       |         5–8 h |
| Backup/recovery          |         4–6 h |
| Failure engineering      |         3–5 h |
| DR exercise              |         3–5 h |
| Documentation            |         3–5 h |

Total:

**45–70 hours.**

Do not rush the project simply to finish it.

The evidence and investigation matter as much as deployment.

---

# 05. Prerequisites

You should understand:

* Linux command line;
* SSH;
* files and permissions;
* users and groups;
* systemd;
* networking;
* IP addressing;
* DNS/host resolution;
* TCP and ports;
* HTTP;
* NGINX;
* Bash;
* basic Python;
* PostgreSQL fundamentals;
* Ansible fundamentals;
* Git;
* logs;
* monitoring concepts;
* backup concepts;
* RTO/RPO;
* incident response.

Recommended host resources:

```text
16 GB RAM minimum
24–32 GB preferred
100+ GB free disk
virtualization support
```

If hardware is constrained, shut down DR01 when it is not being used and reduce VM memory carefully.

---

# 06. Learning Objectives

By completing this project you should be able to:

1. Translate business requirements into infrastructure.
2. Produce a Linux architecture diagram.
3. Define trust boundaries.
4. Define service dependencies.
5. Define failure domains.
6. Design an IP plan.
7. Build multiple Linux servers.
8. Establish administrative access.
9. Automate configuration with Ansible.
10. Build a reverse-proxy/load-balancing tier.
11. Build redundant web infrastructure.
12. Build redundant application infrastructure.
13. Build PostgreSQL infrastructure.
14. Configure database replication.
15. Verify replication.
16. Apply host firewall policies.
17. keep Linux security controls enabled.
18. run services as non-root users.
19. protect secrets.
20. implement centralized metrics.
21. implement centralized logging.
22. create dashboards.
23. implement alerts.
24. monitor service availability.
25. monitor Linux hosts.
26. monitor storage.
27. monitor systemd services.
28. investigate centralized logs.
29. create automated backups.
30. validate backups.
31. perform restores.
32. calculate RPO.
33. measure RTO.
34. build a DR environment.
35. test web-tier failure.
36. test application-tier failure.
37. test database failure.
38. test monitoring failure.
39. test disk pressure.
40. investigate incidents systematically.
41. perform controlled changes.
42. roll back failed changes.
43. identify configuration drift.
44. create operational runbooks.
45. document architecture decisions.
46. perform production-readiness review.
47. explain trade-offs.
48. document limitations honestly.
49. collect portfolio evidence.
50. defend the architecture in a senior engineering interview.

---

# 07. What You Will Build

## Server inventory

| Host     |            IP | Function                 |
| -------- | ------------: | ------------------------ |
| admin01  | 192.168.56.10 | Ansible/control host     |
| lb01     | 192.168.56.20 | External load balancer   |
| web01    | 192.168.56.31 | Web/reverse proxy        |
| web02    | 192.168.56.32 | Web/reverse proxy        |
| app01    | 192.168.56.41 | Application              |
| app02    | 192.168.56.42 | Application              |
| db01     | 192.168.56.51 | PostgreSQL primary       |
| db02     | 192.168.56.52 | PostgreSQL standby       |
| obs01    | 192.168.56.60 | Prometheus/Grafana/Loki  |
| backup01 | 192.168.56.70 | Backup repository        |
| dr01     | 192.168.56.80 | Disaster-recovery target |

Network:

```text
192.168.56.0/24
```

---

## Architecture

```text
                         USERS
                           │
                           │ HTTP
                           ▼
                  ┌─────────────────┐
                  │      LB01       │
                  │      NGINX      │
                  │ 192.168.56.20   │
                  └────────┬────────┘
                           │
               ┌───────────┴───────────┐
               ▼                       ▼
        ┌──────────────┐        ┌──────────────┐
        │    WEB01     │        │    WEB02     │
        │    NGINX     │        │    NGINX     │
        │ .56.31       │        │ .56.32       │
        └───────┬──────┘        └──────┬───────┘
                │                      │
                └──────────┬───────────┘
                           │
               ┌───────────┴───────────┐
               ▼                       ▼
        ┌──────────────┐        ┌──────────────┐
        │    APP01     │        │    APP02     │
        │ Commerce API │        │ Commerce API │
        │ .56.41       │        │ .56.42       │
        └───────┬──────┘        └──────┬───────┘
                │                      │
                └──────────┬───────────┘
                           │
                           ▼
                  ┌─────────────────┐
                  │      DB01       │
                  │   PostgreSQL    │
                  │    PRIMARY      │
                  │ .56.51          │
                  └────────┬────────┘
                           │
                     WAL replication
                           │
                           ▼
                  ┌─────────────────┐
                  │      DB02       │
                  │   PostgreSQL    │
                  │    STANDBY      │
                  │ .56.52          │
                  └─────────────────┘
```

Management plane:

```text
                    ADMIN01
                       │
                    Ansible
                       │
       ┌───────────────┼────────────────┐
       ▼               ▼                ▼
     Linux           Linux            Linux
     Hosts           Hosts            Hosts
```

Observability plane:

```text
ALL SERVERS
 │
 ├── Node Exporter ────────┐
 │                         ▼
 │                    PROMETHEUS
 │                         │
 │                         ▼
 │                      GRAFANA
 │
 └── Alloy ────────────────► LOKI
                              │
                              ▼
                           GRAFANA
```

Backup/recovery plane:

```text
DB01 ────────────┐
APP CONFIG ──────┼────► BACKUP01
SYSTEM CONFIG ───┘          │
                            │
                            ▼
                          DR01
```

---

# 08. Architecture

## 8.1 Failure domains

Identify failures before building.

### LB01 failure

Impact:

```text
customer entry point unavailable
```

This remains a deliberate lab limitation.

A real production design should normally remove this single point of failure with multiple load balancers, virtual IPs, managed load balancing, anycast, or another HA mechanism.

Do not hide this limitation.

---

### WEB01 failure

Expected:

```text
LB01 removes/fails around WEB01
WEB02 continues serving requests
```

---

### APP01 failure

Expected:

```text
web tier routes to APP02
```

---

### DB01 failure

Expected:

```text
application writes stop until controlled database recovery/failover occurs
```

Database failover will initially be **operator-controlled**.

Do not create unsafe automatic PostgreSQL promotion merely to claim automatic failover.

---

### OBS01 failure

Expected:

```text
application remains available
monitoring/log-query capability is degraded
```

Observability should not sit in the customer request path.

---

### BACKUP01 failure

Expected:

```text
production continues
new remote backups fail
```

That failure must become visible.

---

## 8.2 Trust zones

Conceptually divide infrastructure into:

```text
EDGE
WEB
APPLICATION
DATABASE
MANAGEMENT
OBSERVABILITY
BACKUP
DR
```

Even though the lab uses one virtual subnet, firewall policy will model these boundaries.

---

## 8.3 Required flows

```text
CLIENT → LB01        TCP/80

LB01 → WEB01/02      TCP/80

WEB01/02 → APP01/02  TCP/8080

APP01/02 → DB01      TCP/5432

DB01 → DB02          TCP/5432

ADMIN01 → SERVERS    TCP/22

OBS01 → SERVERS      TCP/9100

SERVERS → OBS01      Loki ingestion path

AUTHORIZED ADMIN → OBS01
Grafana
```

Everything else should be justified before being permitted.

---

## 8.4 Architecture Decision Records

Create:

```text
adrs/
```

Required ADRs:

```text
ADR-001-TIERED-ARCHITECTURE.md
ADR-002-ANSIBLE-CONFIGURATION-MANAGEMENT.md
ADR-003-POSTGRESQL-REPLICATION.md
ADR-004-CENTRALIZED-OBSERVABILITY.md
ADR-005-BACKUP-AND-DR.md
ADR-006-DATABASE-FAILOVER-POLICY.md
ADR-007-NETWORK-SEGMENTATION.md
```

Each ADR must contain:

```text
Context
Problem
Options considered
Decision
Why
Consequences
Operational impact
Security impact
Failure implications
Conditions that would cause reconsideration
```

---

# 09. Technologies

Core stack:

```text
Ubuntu Server 24.04 LTS
NGINX
Python 3
PostgreSQL
Ansible
Prometheus
Node Exporter
Grafana
Grafana Loki
Grafana Alloy
systemd
journald
OpenSSH
AppArmor
Linux firewalling
Bash
rsync
SSH
Git
```

No Kubernetes is required.

No cloud provider is required.

No Terraform is required.

The project is intentionally centered on Linux infrastructure engineering.

---

# 10. Environment Setup

## 10.1 Build VMs

Suggested resources:

```text
admin01     1 CPU   1 GB
lb01        1 CPU   1 GB
web01       1 CPU   1 GB
web02       1 CPU   1 GB
app01       2 CPU   2 GB
app02       2 CPU   2 GB
db01        2 CPU   2 GB
db02        2 CPU   2 GB
obs01       2 CPU   4 GB
backup01    1 CPU   2 GB
dr01        2 CPU   2 GB
```

Use:

```text
NAT NIC
+
isolated/host-only NIC
```

The NAT interface provides package access.

The private interface carries application traffic.

Do not configure a second default route on the private interface.

---

## 10.2 Set hostnames

Example:

```bash
sudo hostnamectl set-hostname admin01
```

Repeat with the appropriate hostname on each machine.

Verify:

```bash
hostnamectl
```

---

## 10.3 Temporary lab name resolution

Add to every host:

```bash
sudo tee -a /etc/hosts >/dev/null <<'EOF'
192.168.56.10 admin01
192.168.56.20 lb01
192.168.56.31 web01
192.168.56.32 web02
192.168.56.41 app01
192.168.56.42 app02
192.168.56.51 db01
192.168.56.52 db02
192.168.56.60 obs01
192.168.56.70 backup01
192.168.56.80 dr01
EOF
```

Verify:

```bash
getent hosts db01
getent hosts obs01
```

---

## 10.4 Base packages

All hosts:

```bash
sudo apt update
sudo apt upgrade -y
```

Install:

```bash
sudo apt install -y \
  curl \
  jq \
  vim \
  git \
  rsync \
  chrony \
  sysstat \
  lsof \
  unzip \
  ca-certificates
```

Enable time synchronization:

```bash
sudo systemctl enable --now chrony
```

Verify:

```bash
chronyc tracking
```

Time consistency matters for:

```text
logs
alerts
database recovery
incident timelines
backup timestamps
```

---

## 10.5 Verify AppArmor

```bash
sudo aa-status
```

Do not disable AppArmor merely to solve application problems.

---

## 10.6 Create administrative user

On each managed server:

```bash
sudo adduser opsadmin
```

Add sudo:

```bash
sudo usermod -aG sudo opsadmin
```

From ADMIN01 generate an SSH key:

```bash
ssh-keygen -t ed25519 \
  -f ~/.ssh/linuxworld_admin
```

Copy the public key to each server.

After confirming key authentication works, configure SSH deliberately rather than locking yourself out.

Create:

```bash
sudo tee /etc/ssh/sshd_config.d/99-linuxworld.conf >/dev/null <<'EOF'
PermitRootLogin no
PubkeyAuthentication yes
PasswordAuthentication no
EOF
```

Validate before reload:

```bash
sudo sshd -t
```

Only if successful:

```bash
sudo systemctl reload ssh
```

Keep an open console while testing.

---

# 11. Project Directory Structure

On ADMIN01:

```bash
mkdir -p ~/enterprise-linux-infrastructure/{\
ansible/{inventory,group_vars,roles,playbooks},\
application,\
nginx,\
postgresql,\
monitoring,\
logging,\
backup,\
dr,\
scripts,\
security,\
tests,\
failure-labs,\
evidence,\
runbooks,\
adrs,\
docs,\
postmortem}
```

Enter:

```bash
cd ~/enterprise-linux-infrastructure
```

Create:

```bash
touch README.md
```

---

# 12. Step-by-Step Implementation

# Phase A — Automation Control Plane

## 12.1 Install Ansible

ADMIN01:

```bash
sudo apt install -y ansible
```

Verify:

```bash
ansible --version
```

---

## 12.2 Create inventory

```bash
cat > ansible/inventory/production.ini <<'EOF'
[loadbalancers]
lb01 ansible_host=192.168.56.20

[web]
web01 ansible_host=192.168.56.31
web02 ansible_host=192.168.56.32

[application]
app01 ansible_host=192.168.56.41
app02 ansible_host=192.168.56.42

[database]
db01 ansible_host=192.168.56.51
db02 ansible_host=192.168.56.52

[observability]
obs01 ansible_host=192.168.56.60

[backup]
backup01 ansible_host=192.168.56.70

[dr]
dr01 ansible_host=192.168.56.80

[production:children]
loadbalancers
web
application
database
observability
backup
EOF
```

Create group variables:

```bash
cat > ansible/group_vars/all.yml <<'EOF'
---
ansible_user: opsadmin
ansible_ssh_private_key_file: ~/.ssh/linuxworld_admin
ansible_python_interpreter: /usr/bin/python3
EOF
```

Validate:

```bash
ansible-inventory \
  -i ansible/inventory/production.ini \
  --graph
```

Test:

```bash
ansible production \
  -i ansible/inventory/production.ini \
  -m ping
```

Do not continue until every intended host responds.

---

# Phase B — Baseline Linux Configuration

## 12.3 Create baseline playbook

```bash
cat > ansible/playbooks/baseline.yml <<'EOF'
---
- name: Apply Linux baseline
  hosts: production
  become: true

  tasks:

    - name: Install baseline packages
      ansible.builtin.apt:
        name:
          - curl
          - jq
          - rsync
          - chrony
          - sysstat
          - lsof
          - ca-certificates
        state: present
        update_cache: true

    - name: Ensure chrony is enabled
      ansible.builtin.service:
        name: chrony
        state: started
        enabled: true

    - name: Ensure sysstat collection is enabled
      ansible.builtin.lineinfile:
        path: /etc/default/sysstat
        regexp: '^ENABLED='
        line: 'ENABLED="true"'

    - name: Ensure sysstat service is enabled
      ansible.builtin.service:
        name: sysstat
        state: started
        enabled: true
EOF
```

Syntax check:

```bash
ansible-playbook \
  -i ansible/inventory/production.ini \
  ansible/playbooks/baseline.yml \
  --syntax-check
```

Dry-run where appropriate:

```bash
ansible-playbook \
  -i ansible/inventory/production.ini \
  ansible/playbooks/baseline.yml \
  --check
```

Apply:

```bash
ansible-playbook \
  -i ansible/inventory/production.ini \
  ansible/playbooks/baseline.yml
```

Run it again.

The second run should demonstrate idempotent behavior rather than repeatedly changing everything.

---

# Phase C — Application Tier

## 12.4 Create service account

APP01 and APP02:

```bash
sudo useradd \
  --system \
  --home /opt/commerce-api \
  --shell /usr/sbin/nologin \
  commerce
```

Create directories:

```bash
sudo mkdir -p \
  /opt/commerce-api \
  /var/log/commerce-api
```

Ownership:

```bash
sudo chown -R commerce:commerce \
  /opt/commerce-api \
  /var/log/commerce-api
```

---

## 12.5 Create application

Create:

```text
application/app.py
```

with:

```python
#!/usr/bin/env python3

import json
import os
import socket
from http.server import BaseHTTPRequestHandler, ThreadingHTTPServer

HOST = "0.0.0.0"
PORT = 8080


class Handler(BaseHTTPRequestHandler):

    def send_json(self, status, payload):
        body = json.dumps(payload).encode()

        self.send_response(status)
        self.send_header("Content-Type", "application/json")
        self.send_header("Content-Length", str(len(body)))
        self.end_headers()

        self.wfile.write(body)

    def do_GET(self):

        if self.path == "/health":
            self.send_json(
                200,
                {
                    "status": "healthy",
                    "host": socket.gethostname()
                }
            )
            return

        if self.path == "/orders":
            self.send_json(
                200,
                {
                    "service": "commerce-api",
                    "host": socket.gethostname(),
                    "orders": [
                        {"id": 1001, "status": "paid"},
                        {"id": 1002, "status": "processing"}
                    ]
                }
            )
            return

        self.send_json(404, {"error": "not found"})

    def log_message(self, fmt, *args):
        print(
            "%s - %s" %
            (self.address_string(), fmt % args),
            flush=True
        )


server = ThreadingHTTPServer((HOST, PORT), Handler)
server.serve_forever()
```

Copy to both application servers:

```bash
scp application/app.py \
  app01:/tmp/app.py

scp application/app.py \
  app02:/tmp/app.py
```

Install:

```bash
sudo cp /tmp/app.py \
  /opt/commerce-api/app.py

sudo chown commerce:commerce \
  /opt/commerce-api/app.py

sudo chmod 750 \
  /opt/commerce-api/app.py
```

---

## 12.6 Create systemd unit

```ini
[Unit]
Description=VERIQTA Commerce API
After=network-online.target
Wants=network-online.target

[Service]
Type=simple

User=commerce
Group=commerce

WorkingDirectory=/opt/commerce-api

ExecStart=/usr/bin/python3 /opt/commerce-api/app.py

Restart=on-failure
RestartSec=3

NoNewPrivileges=true
PrivateTmp=true
ProtectHome=true

StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

Save as:

```text
/etc/systemd/system/commerce-api.service
```

Then:

```bash
sudo systemctl daemon-reload

sudo systemctl enable --now \
  commerce-api.service
```

Verify:

```bash
systemctl status \
  commerce-api \
  --no-pager
```

Check socket:

```bash
sudo ss -lntp \
  '( sport = :8080 )'
```

Test:

```bash
curl -f \
  http://127.0.0.1:8080/health
```

---

# Phase D — Web Tier

Install NGINX on WEB01 and WEB02:

```bash
sudo apt install -y nginx
```

Create:

```text
/etc/nginx/sites-available/commerce
```

with:

```nginx
upstream commerce_backend {
    least_conn;

    server 192.168.56.41:8080 max_fails=3 fail_timeout=10s;
    server 192.168.56.42:8080 max_fails=3 fail_timeout=10s;
}

server {
    listen 80 default_server;
    listen [::]:80 default_server;

    server_name _;

    access_log /var/log/nginx/commerce_access.log;
    error_log  /var/log/nginx/commerce_error.log;

    location / {
        proxy_pass http://commerce_backend;

        proxy_connect_timeout 2s;
        proxy_read_timeout 10s;
        proxy_send_timeout 10s;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

Enable:

```bash
sudo rm -f \
  /etc/nginx/sites-enabled/default

sudo ln -s \
  /etc/nginx/sites-available/commerce \
  /etc/nginx/sites-enabled/commerce
```

Validate:

```bash
sudo nginx -t
```

Reload:

```bash
sudo systemctl reload nginx
```

Test repeatedly:

```bash
for i in $(seq 1 10); do
    curl -s \
      http://127.0.0.1/orders
    echo
done
```

You should observe requests reaching both application hosts.

---

# Phase E — Edge Load Balancer

LB01:

```bash
sudo apt install -y nginx
```

Create:

```text
/etc/nginx/sites-available/commerce
```

```nginx
upstream web_tier {
    least_conn;

    server 192.168.56.31:80 max_fails=3 fail_timeout=10s;
    server 192.168.56.32:80 max_fails=3 fail_timeout=10s;
}

server {
    listen 80 default_server;
    listen [::]:80 default_server;

    server_name _;

    location / {
        proxy_pass http://web_tier;

        proxy_connect_timeout 2s;
        proxy_read_timeout 10s;

        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

Validate:

```bash
sudo nginx -t
```

Enable and reload.

External test:

```bash
curl \
  http://192.168.56.20/orders
```

Run:

```bash
for i in $(seq 1 20); do
    curl -s \
      http://192.168.56.20/orders
    echo
done
```

The request path is now:

```text
LB01
 ↓
WEB01/WEB02
 ↓
APP01/APP02
```

---

# Phase F — Database Tier

Install PostgreSQL on DB01 and DB02:

```bash
sudo apt install -y postgresql postgresql-contrib
```

Determine installed version:

```bash
pg_lsclusters
```

Do not hard-code a PostgreSQL major-version directory until you know the installed version.

Record it:

```bash
psql --version
```

---

## 12.7 Create application database

DB01:

```bash
sudo -u postgres psql
```

Create:

```sql
CREATE ROLE commerce_app
LOGIN
PASSWORD 'CHANGE_THIS_IN_THE_LAB';

CREATE DATABASE commerce
OWNER commerce_app;
```

Connect:

```sql
\c commerce
```

Create:

```sql
CREATE TABLE orders (
    id BIGSERIAL PRIMARY KEY,
    customer_name TEXT NOT NULL,
    status TEXT NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT now()
);
```

Seed:

```sql
INSERT INTO orders
(customer_name, status)
VALUES
('Test Customer 1', 'paid'),
('Test Customer 2', 'processing');
```

Verify:

```sql
SELECT * FROM orders;
```

Exit:

```text
\q
```

Do not commit the example password to Git.

Replace the lab password and store the real secret using an appropriate secret-management mechanism such as encrypted Ansible variables.

---

# PostgreSQL Replication

Create replication identity on DB01:

```sql
CREATE ROLE replicator
WITH REPLICATION
LOGIN
PASSWORD 'CHANGE_REPLICATION_PASSWORD';
```

Inspect PostgreSQL configuration locations:

```bash
sudo -u postgres psql \
  -c "SHOW config_file;"

sudo -u postgres psql \
  -c "SHOW hba_file;"

sudo -u postgres psql \
  -c "SHOW data_directory;"
```

Do not assume paths.

---

## Configure primary

Set:

```text
wal_level = replica
max_wal_senders = 10
max_replication_slots = 10
```

Create a physical replication slot:

```bash
sudo -u postgres psql \
  -c "SELECT pg_create_physical_replication_slot('db02_slot');"
```

Restrict replication access to DB02 in `pg_hba.conf`.

Example:

```text
host replication replicator 192.168.56.52/32 scram-sha-256
```

Restrict application database access to APP01/APP02.

Example:

```text
host commerce commerce_app 192.168.56.41/32 scram-sha-256
host commerce commerce_app 192.168.56.42/32 scram-sha-256
```

Reload:

```bash
sudo systemctl reload postgresql
```

---

## Initialize DB02

Stop PostgreSQL on DB02:

```bash
sudo systemctl stop postgresql
```

Determine data directory first.

Back up or remove only the intended empty lab cluster.

Then run `pg_basebackup` using the actual data directory for your installed cluster.

Conceptually:

```bash
sudo -u postgres pg_basebackup \
  -h 192.168.56.51 \
  -U replicator \
  -D <DB02_DATA_DIRECTORY> \
  -P \
  -R \
  -X stream \
  -S db02_slot
```

The `-R` option creates standby configuration needed for the replica to reconnect.

Start:

```bash
sudo systemctl start postgresql
```

Verify DB02:

```bash
sudo -u postgres psql \
  -c "SELECT pg_is_in_recovery();"
```

Expected:

```text
t
```

DB01:

```bash
sudo -u postgres psql \
  -c "SELECT client_addr,state,sync_state FROM pg_stat_replication;"
```

DB02 should appear.

---

## Replication test

DB01:

```bash
sudo -u postgres psql \
  commerce \
  -c "INSERT INTO orders(customer_name,status) VALUES('Replication Test','paid');"
```

DB02:

```bash
sudo -u postgres psql \
  commerce \
  -c "SELECT * FROM orders ORDER BY id DESC LIMIT 3;"
```

The new record should appear.

---

# Phase G — Connect Application to PostgreSQL

Install Python PostgreSQL support on APP01/APP02:

```bash
sudo apt install -y \
  python3-psycopg2
```

Extend the application so `/db-health` performs:

```sql
SELECT 1;
```

and `/orders` reads from PostgreSQL rather than static data.

Store database configuration outside application source code.

Example environment file:

```text
/etc/commerce-api/commerce.env
```

Permissions:

```bash
sudo mkdir -p \
  /etc/commerce-api

sudo chown root:commerce \
  /etc/commerce-api

sudo chmod 750 \
  /etc/commerce-api
```

Environment file:

```text
DB_HOST=192.168.56.51
DB_PORT=5432
DB_NAME=commerce
DB_USER=commerce_app
DB_PASSWORD=<SECRET>
```

Permissions:

```bash
sudo chown root:commerce \
  /etc/commerce-api/commerce.env

sudo chmod 640 \
  /etc/commerce-api/commerce.env
```

Add to systemd:

```ini
EnvironmentFile=/etc/commerce-api/commerce.env
```

Then:

```bash
sudo systemctl daemon-reload
sudo systemctl restart commerce-api
```

Verify:

```bash
curl -f \
  http://127.0.0.1:8080/db-health
```

---

# 13. Configuration

At this stage convert manually proven configuration into Ansible-managed configuration.

Required roles:

```text
roles/
├── common/
├── ssh/
├── firewall/
├── node_exporter/
├── alloy/
├── nginx_lb/
├── nginx_web/
├── commerce_api/
├── postgresql_primary/
├── postgresql_standby/
├── backup/
└── monitoring/
```

The important workflow is:

```text
MANUAL PROOF
     ↓
UNDERSTAND CONFIGURATION
     ↓
AUTOMATE
     ↓
TEST
     ↓
RE-RUN
     ↓
PROVE IDEMPOTENCY
```

Do not automate something you do not yet understand.

---

# 14. Automation

Create:

```text
ansible/playbooks/site.yml
```

```yaml
---
- import_playbook: baseline.yml
- import_playbook: security.yml
- import_playbook: web.yml
- import_playbook: application.yml
- import_playbook: database.yml
- import_playbook: observability.yml
- import_playbook: backup.yml
```

Before applying changes:

```bash
ansible-playbook \
  -i ansible/inventory/production.ini \
  ansible/playbooks/site.yml \
  --syntax-check
```

Then:

```bash
ansible-playbook \
  -i ansible/inventory/production.ini \
  ansible/playbooks/site.yml \
  --check \
  --diff
```

Review carefully.

Apply:

```bash
ansible-playbook \
  -i ansible/inventory/production.ini \
  ansible/playbooks/site.yml
```

Re-run:

```bash
ansible-playbook \
  -i ansible/inventory/production.ini \
  ansible/playbooks/site.yml
```

Record:

```text
changed=
```

The second run should not unnecessarily modify stable configuration.

---

## Configuration drift exercise

On WEB02 deliberately alter a managed configuration value.

Then run:

```bash
ansible-playbook \
  -i ansible/inventory/production.ini \
  ansible/playbooks/web.yml \
  --check \
  --diff
```

Observe drift.

Apply the playbook.

Verify the approved configuration is restored.

Document:

```text
drift introduced
drift detected
configuration source of truth
remediation
verification
```

---

# 15. Security

Security is part of the architecture, not a final checklist.

## SSH

Required:

```text
root SSH disabled
key authentication
password SSH disabled after verification
restricted administrative users
```

Validate:

```bash
sudo sshd -t
```

Inspect effective configuration:

```bash
sudo sshd -T
```

---

## Firewall policy

Document a matrix before applying rules.

Example:

| Destination         | Source           |                   Port |
| ------------------- | ---------------- | ---------------------: |
| LB01                | clients          |                     80 |
| WEB01/02            | LB01             |                     80 |
| APP01/02            | WEB01/02         |                   8080 |
| DB01                | APP01/02         |                   5432 |
| DB01                | DB02             | PostgreSQL replication |
| all managed hosts   | ADMIN01          |                     22 |
| all monitored hosts | OBS01            |                   9100 |
| OBS01               | authorized admin |                Grafana |

Never write:

```text
allow everything from 192.168.56.0/24
```

and call it segmentation.

---

## Application privileges

Verify:

```bash
ps -eo \
  user,pid,cmd \
  | grep commerce-api
```

The application must not run as root.

---

## File permissions

Inspect:

```bash
namei -l \
  /etc/commerce-api/commerce.env
```

Verify secrets are not world-readable.

---

## Secret scanning

Before Git commit:

```bash
git diff --cached
```

Then:

```bash
grep -RniE \
  'password|secret|token|api[_-]?key' \
  . \
  --exclude-dir=.git
```

Investigate every result.

Do not blindly publish it.

---

## AppArmor

Verify:

```bash
sudo aa-status
```

Do not disable AppArmor to make the project easier.

If a denial occurs, investigate:

```bash
sudo journalctl \
  -k \
  | grep -i apparmor
```

---

## Patch state

Check:

```bash
apt list --upgradable
```

Document your patch-management policy.

Do not claim automated production patching merely because unattended upgrades exist.

Production patching requires:

```text
testing
maintenance policy
reboot strategy
rollback thinking
availability planning
```

---

# 16. Testing

Create a test matrix.

## Infrastructure tests

```bash
ansible production \
  -i ansible/inventory/production.ini \
  -m ping
```

---

## Load balancer

```bash
curl -f \
  http://192.168.56.20/health
```

---

## Application

```bash
curl -f \
  http://192.168.56.41:8080/health

curl -f \
  http://192.168.56.42:8080/health
```

---

## Database

DB01:

```bash
sudo -u postgres \
  pg_isready
```

DB02:

```bash
sudo -u postgres \
  pg_isready
```

---

## Replication

DB01:

```bash
sudo -u postgres psql \
  -c "SELECT client_addr,state,sync_state FROM pg_stat_replication;"
```

DB02:

```bash
sudo -u postgres psql \
  -c "SELECT pg_is_in_recovery();"
```

---

## Load test

From ADMIN01 or a dedicated client:

```bash
sudo apt install -y apache2-utils
```

Run:

```bash
ab \
  -n 5000 \
  -c 25 \
  http://192.168.56.20/orders
```

Record actual:

```text
requests/sec
mean latency
failed requests
percentile behavior
```

Do not invent results.

---

# Observability Implementation

Install Node Exporter on every infrastructure host.

Verify locally:

```bash
curl -s \
  http://127.0.0.1:9100/metrics \
  | head
```

Restrict port 9100 so only OBS01 can scrape it.

---

## Prometheus

On OBS01 install and configure Prometheus.

Example scrape configuration:

```yaml
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:

  - job_name: linux
    static_configs:
      - targets:
          - '192.168.56.20:9100'
          - '192.168.56.31:9100'
          - '192.168.56.32:9100'
          - '192.168.56.41:9100'
          - '192.168.56.42:9100'
          - '192.168.56.51:9100'
          - '192.168.56.52:9100'
          - '192.168.56.70:9100'
```

Validate configuration with the Prometheus tooling available with your installation before restart/reload.

Verify targets from Prometheus.

All expected hosts should become:

```text
UP
```

---

## Required Linux monitoring

Build dashboards for:

```text
CPU utilization
load
memory
swap
filesystem usage
inode usage
disk I/O
network traffic
network errors
uptime
host availability
```

---

## Service monitoring

At minimum monitor:

```text
NGINX availability
Commerce API availability
PostgreSQL availability
Node Exporter availability
backup age
```

---

## Alerts

Required alerts:

```text
HostDown
HighCPU
LowAvailableMemory
FilesystemNearlyFull
InodesNearlyFull
ExporterDown
ApplicationUnavailable
DatabaseUnavailable
BackupTooOld
```

Every alert must answer:

```text
What happened?
Which system?
Why does it matter?
What should the responder inspect?
Which runbook applies?
```

---

# Centralized Logging

Use:

```text
Linux hosts
   ↓
Grafana Alloy
   ↓
Loki
   ↓
Grafana
```

Collect appropriate:

```text
journald
NGINX access/error logs
application logs
PostgreSQL logs
backup logs
```

Do not expose Loki directly to untrusted networks.

---

## Install Alloy

Install Alloy on managed Linux hosts.

Verify:

```bash
systemctl status \
  alloy \
  --no-pager
```

Configure collection carefully so high-cardinality or sensitive information is not accidentally published.

---

## Log validation

Generate an NGINX request:

```bash
curl \
  http://192.168.56.20/orders
```

Search centralized logs.

Then create an application log entry.

Verify it reaches Loki.

Then inspect a PostgreSQL log event.

The project is not complete because Alloy is "running."

You must prove:

```text
source log
→ collector
→ transport
→ Loki
→ Grafana query
```

---

# 17. Failure Simulation

All failure experiments must occur in the lab.

Take snapshots first.

## Failure 1 — WEB01 outage

Stop:

```bash
sudo systemctl stop nginx
```

From client:

```bash
for i in $(seq 1 20); do
    curl -sS \
      -o /dev/null \
      -w '%{http_code}\n' \
      http://192.168.56.20/health
done
```

Determine:

```text
Did customer traffic survive?
How quickly?
What did LB01 log?
What alert fired?
```

Restore:

```bash
sudo systemctl start nginx
```

---

## Failure 2 — APP01 outage

```bash
sudo systemctl stop \
  commerce-api
```

Repeat customer requests.

Verify APP02 continues serving.

Restore APP01.

---

## Failure 3 — DB02 replication failure

Stop PostgreSQL on DB02:

```bash
sudo systemctl stop postgresql
```

Observe DB01 replication state.

Check monitoring.

Generate writes.

Restore DB02:

```bash
sudo systemctl start postgresql
```

Verify replication catches up.

---

## Failure 4 — Disk pressure

On a disposable filesystem, generate controlled disk consumption.

Do not fill `/` blindly.

Monitor:

```bash
df -h
df -ih
```

Confirm alerting occurs before complete exhaustion.

Remove test data.

Verify alert clears.

---

## Failure 5 — Monitoring agent failure

Stop Node Exporter:

```bash
sudo systemctl stop \
  prometheus-node-exporter
```

Verify Prometheus reports the target unavailable.

Verify alerting.

Restore.

---

## Failure 6 — Configuration error

Introduce an invalid NGINX configuration on one web node.

Before reload:

```bash
sudo nginx -t
```

The test must fail.

This demonstrates why configuration validation belongs before service reload.

Restore approved configuration through Ansible.

---

# 18. Troubleshooting

Use the same investigation model throughout Linux World:

```text
SYMPTOM
   ↓
USER IMPACT
   ↓
BLAST RADIUS
   ↓
RECENT CHANGES
   ↓
SERVICE PATH
   ↓
METRICS
   ↓
LOGS
   ↓
EVENTS
   ↓
HYPOTHESES
   ↓
TEST
   ↓
NARROW
   ↓
FAILURE DOMAIN
   ↓
MITIGATE
   ↓
RECOVER
   ↓
VERIFY
   ↓
ROOT CAUSE
   ↓
PREVENT
```

---

## Customer receives 502

Investigate:

```bash
curl -v \
  http://192.168.56.20/orders
```

LB01:

```bash
sudo tail -100 \
  /var/log/nginx/error.log
```

WEB servers:

```bash
systemctl status \
  nginx \
  --no-pager
```

Application servers:

```bash
systemctl status \
  commerce-api \
  --no-pager
```

Sockets:

```bash
sudo ss -lntp
```

Journal:

```bash
sudo journalctl \
  -u commerce-api \
  --since "-15 minutes" \
  --no-pager
```

Do not restart everything.

Follow the request path.

---

## PostgreSQL connection failure

APP01:

```bash
nc -vz \
  192.168.56.51 \
  5432
```

DB01:

```bash
sudo ss -lntp \
  '( sport = :5432 )'
```

Then:

```bash
sudo -u postgres \
  pg_isready
```

Check PostgreSQL journal.

Inspect firewall.

Inspect `pg_hba.conf`.

Test credentials separately.

Do not assume every connection failure is a database crash.

---

## Slow system

Begin:

```bash
uptime
```

Then:

```bash
vmstat 1 10
```

Then:

```bash
free -h
```

Then:

```bash
iostat -xz 1 10
```

Then:

```bash
cat /proc/pressure/cpu
cat /proc/pressure/memory
cat /proc/pressure/io
```

Then inspect processes:

```bash
ps -eo \
  pid,user,stat,%cpu,%mem,rss,comm,args \
  --sort=-%cpu \
  | head -30
```

Do not equate high load with CPU saturation automatically.

---

# 19. Final Validation

The platform is not complete until the entire architecture is tested from the outside inward.

## Customer path

```bash
curl -f \
  http://192.168.56.20/health
```

---

## Business endpoint

```bash
curl -f \
  http://192.168.56.20/orders
```

---

## Database-backed health

```bash
curl -f \
  http://192.168.56.20/db-health
```

---

## Web redundancy

Stop WEB01.

Verify service.

Restore WEB01.

Stop WEB02.

Verify service.

Restore WEB02.

---

## Application redundancy

Repeat for APP01 and APP02.

---

## Database replication

Generate data.

Verify DB02 receives it.

Measure replication lag rather than simply stating replication works.

---

## Monitoring

Confirm all expected targets.

---

## Logging

Generate events on:

```text
LB01
WEB01
APP01
DB01
```

Find each event centrally.

---

## Automation

Run:

```bash
ansible-playbook \
  -i ansible/inventory/production.ini \
  ansible/playbooks/site.yml \
  --check \
  --diff
```

Unexpected drift should be investigated.

---

# Backup Architecture

A backup is not successful merely because a command exited zero.

The architecture must cover:

```text
database
application configuration
NGINX configuration
Ansible source
critical system configuration
monitoring configuration
recovery documentation
```

---

## Database backup

Use PostgreSQL-supported base backup/WAL strategy.

Create a dedicated backup destination on BACKUP01.

Example:

```text
/srv/backups/postgresql/
```

Permissions:

```bash
sudo mkdir -p \
  /srv/backups/postgresql

sudo chown backup:backup \
  /srv/backups/postgresql

sudo chmod 700 \
  /srv/backups/postgresql
```

The exact backup identity and transport permissions must be restricted.

---

## WAL archiving

Configure PostgreSQL so completed WAL segments are archived to a protected archive location.

Verify:

```sql
SELECT
    archived_count,
    failed_count,
    last_archived_wal,
    last_archived_time
FROM pg_stat_archiver;
```

Do not assume WAL archiving works because the configuration exists.

Generate database activity.

Force/await WAL switch as appropriate.

Verify archive files actually arrive.

---

## Base backup

Create a base backup using:

```text
pg_basebackup
```

Record:

```text
start time
completion time
backup size
exit status
location
database version
```

---

## Configuration backup

Create:

```text
scripts/backup-config.sh
```

It should:

1. create a timestamp;
2. create a staging directory;
3. copy approved configuration;
4. create an archive;
5. calculate SHA-256 checksum;
6. transfer archive to BACKUP01;
7. verify destination;
8. log result;
9. return non-zero on failure.

Do not silently ignore backup failures.

---

# Disaster Recovery

## Recovery objectives

Define before testing:

```text
RTO target
RPO target
```

Example lab objectives:

```text
RTO ≤ 60 minutes
RPO ≤ 15 minutes
```

These are lab objectives, not claims.

Your measured results determine whether they were achieved.

---

## DR exercise

Assume:

```text
LB01 lost
WEB01/02 lost
APP01/02 lost
DB01/02 unavailable
```

BACKUP01 remains available.

DR01 starts as a clean Ubuntu machine.

Start timer.

Record:

```text
T0 = disaster declared
```

---

## Rebuild DR host

Use Ansible to apply baseline:

```bash
ansible-playbook \
  -i ansible/inventory/dr.ini \
  ansible/playbooks/baseline.yml
```

Install required application/database components.

Retrieve backup.

Verify checksum.

Restore PostgreSQL using the supported backup/recovery process appropriate to the backup created.

Start PostgreSQL.

Validate:

```bash
sudo -u postgres \
  pg_isready
```

Query restored data.

Determine newest successfully recovered transaction timestamp.

---

## Restore application

Deploy application through Ansible.

Restore required configuration/secrets through the approved recovery mechanism.

Start:

```bash
sudo systemctl start \
  commerce-api
```

Verify:

```bash
curl -f \
  http://127.0.0.1:8080/health
```

Then:

```bash
curl -f \
  http://127.0.0.1:8080/db-health
```

Record:

```text
T1 = service technically available
```

---

## Calculate RTO

```text
RTO achieved = T1 - T0
```

Do not write:

```text
RTO = 27 minutes
```

unless your exercise actually measured 27 minutes.

---

## Calculate observed RPO

Determine:

```text
timestamp of last known production transaction

timestamp of newest recovered transaction
```

Calculate the difference.

That is the observed data-loss window for the exercise.

---

# Safe Change and Rollback

Production engineering includes failed changes.

Create a controlled NGINX change.

Before change:

```bash
sudo cp \
  /etc/nginx/sites-available/commerce \
  /var/tmp/commerce.pre-change
```

Validate current:

```bash
sudo nginx -t
```

Apply proposed change.

Validate:

```bash
sudo nginx -t
```

If invalid:

```bash
sudo cp \
  /var/tmp/commerce.pre-change \
  /etc/nginx/sites-available/commerce
```

Validate again:

```bash
sudo nginx -t
```

Only then reload.

Document:

```text
change
expected impact
validation
failure
rollback decision
rollback
post-rollback verification
```

---

# 20. Portfolio Documentation

Create:

```text
docs/
├── ARCHITECTURE.md
├── NETWORK-DESIGN.md
├── SECURITY-MODEL.md
├── SERVICE-DEPENDENCIES.md
├── FAILURE-DOMAINS.md
├── OBSERVABILITY.md
├── BACKUP-STRATEGY.md
├── DISASTER-RECOVERY.md
├── RTO-RPO-RESULTS.md
├── CAPACITY-BASELINE.md
└── PRODUCTION-READINESS-REVIEW.md
```

---

## Architecture document

Include:

```text
business requirements
logical architecture
physical lab architecture
network flows
server inventory
service dependencies
stateful/stateless classification
failure domains
security boundaries
observability architecture
backup architecture
DR architecture
known limitations
```

---

## Production-readiness review

Answer:

### Reliability

```text
What are the remaining SPOFs?

What happens when each dependency fails?

What requires manual intervention?

What is automatically recovered?
```

### Security

```text
Are secrets protected?

Are services non-root?

Are ports restricted?

Is root SSH disabled?

Are security controls enabled?
```

### Observability

```text
Can failures be detected?

Are logs centralized?

Can responders correlate events?

Are important capacity conditions alerted?
```

### Backup

```text
When was the last successful backup?

How do you know?

When was it last restored?
```

### DR

```text
What was measured RTO?

What was measured RPO?

Did the test meet objectives?
```

### Operations

```text
Are runbooks available?

Can another engineer rebuild the environment?

Can configuration drift be detected?
```

---

# 21. GitHub Structure

Final repository:

```text
10-Enterprise-Linux-Infrastructure-Architecture/
│
├── README.md
│
├── ansible/
│   ├── inventory/
│   │   ├── production.ini
│   │   └── dr.ini
│   ├── group_vars/
│   │   └── all.yml
│   ├── playbooks/
│   │   ├── site.yml
│   │   ├── baseline.yml
│   │   ├── security.yml
│   │   ├── web.yml
│   │   ├── application.yml
│   │   ├── database.yml
│   │   ├── observability.yml
│   │   └── backup.yml
│   └── roles/
│       ├── common/
│       ├── ssh/
│       ├── firewall/
│       ├── node_exporter/
│       ├── alloy/
│       ├── nginx_lb/
│       ├── nginx_web/
│       ├── commerce_api/
│       ├── postgresql_primary/
│       ├── postgresql_standby/
│       ├── backup/
│       └── monitoring/
│
├── application/
│   └── app.py
│
├── nginx/
│   ├── lb-commerce.conf
│   └── web-commerce.conf
│
├── postgresql/
│   ├── README.md
│   ├── schema.sql
│   └── replication-validation.sql
│
├── monitoring/
│   ├── prometheus.yml
│   ├── alerts.yml
│   └── dashboards/
│
├── logging/
│   ├── loki-config.yml
│   └── alloy/
│
├── backup/
│   ├── README.md
│   └── retention-policy.md
│
├── dr/
│   ├── DR-PLAN.md
│   ├── RECOVERY-CHECKLIST.md
│   └── DR-TEST-RESULTS.md
│
├── scripts/
│   ├── backup-config.sh
│   ├── health-check.sh
│   ├── validate-infrastructure.sh
│   └── collect-evidence.sh
│
├── security/
│   ├── FIREWALL-MATRIX.md
│   ├── SSH-STANDARD.md
│   └── ACCESS-MODEL.md
│
├── tests/
│   ├── availability-tests.sh
│   ├── dependency-tests.sh
│   └── recovery-tests.sh
│
├── failure-labs/
│   ├── WEB-NODE-FAILURE.md
│   ├── APP-NODE-FAILURE.md
│   ├── DATABASE-REPLICATION-FAILURE.md
│   ├── DISK-PRESSURE.md
│   └── MONITORING-FAILURE.md
│
├── evidence/
│   └── README.md
│
├── runbooks/
│   ├── APPLICATION-DOWN.md
│   ├── WEB-TIER-FAILURE.md
│   ├── DATABASE-FAILURE.md
│   ├── DISK-CAPACITY.md
│   ├── HIGH-CPU.md
│   ├── REPLICATION-FAILURE.md
│   ├── BACKUP-FAILURE.md
│   └── DR-ACTIVATION.md
│
├── adrs/
│   ├── ADR-001-TIERED-ARCHITECTURE.md
│   ├── ADR-002-ANSIBLE-CONFIGURATION-MANAGEMENT.md
│   ├── ADR-003-POSTGRESQL-REPLICATION.md
│   ├── ADR-004-CENTRALIZED-OBSERVABILITY.md
│   ├── ADR-005-BACKUP-AND-DR.md
│   ├── ADR-006-DATABASE-FAILOVER-POLICY.md
│   └── ADR-007-NETWORK-SEGMENTATION.md
│
├── docs/
│   ├── ARCHITECTURE.md
│   ├── NETWORK-DESIGN.md
│   ├── SECURITY-MODEL.md
│   ├── SERVICE-DEPENDENCIES.md
│   ├── FAILURE-DOMAINS.md
│   ├── OBSERVABILITY.md
│   ├── BACKUP-STRATEGY.md
│   ├── DISASTER-RECOVERY.md
│   ├── RTO-RPO-RESULTS.md
│   ├── CAPACITY-BASELINE.md
│   └── PRODUCTION-READINESS-REVIEW.md
│
└── postmortem/
    └── CAPSTONE-FAILURE-REVIEW.md
```

**Important:** this is the target structure. Do not create empty decorative files merely so the repository looks large. Each file should be created when its corresponding project step is completed.

---

# 22. Resume Entry

After actually completing the project:

**Enterprise Linux Infrastructure Architecture — Senior Infrastructure Engineering Capstone**

Designed and implemented a multi-tier production-style Linux environment with NGINX load balancing and web tiers, redundant application services, PostgreSQL primary/standby replication, Ansible configuration management, centralized Prometheus/Grafana monitoring, Loki-based log aggregation, host-level security controls, automated backups, and disaster-recovery procedures.

Implemented service health validation, infrastructure monitoring, configuration-drift remediation, controlled failure simulations, database replication testing, backup verification, restore exercises, and measured RTO/RPO validation.

Documented infrastructure architecture, trust boundaries, service dependencies, failure domains, operational runbooks, architecture decisions, security controls, recovery procedures, known limitations, and production-readiness findings.

Do not add numerical availability, performance, RTO, or RPO claims unless your actual project evidence supports them.

---

# 23. Interview Preparation

You should be able to defend the architecture without looking at the README.

## Architecture

1. Walk me through the architecture.
2. Why did you separate web and application tiers?
3. Why are application servers redundant?
4. Why is the database stateful?
5. Which components are stateless?
6. What are the failure domains?
7. What are the remaining single points of failure?
8. Why did you accept those SPOFs in the lab?
9. How would production architecture remove them?
10. What is the customer request path?

## Linux

11. How are services managed?
12. Why use systemd?
13. How do you investigate a failed service?
14. How do you inspect sockets?
15. How do you inspect resource pressure?
16. How do you inspect filesystem capacity?
17. How do you inspect inode exhaustion?
18. How do you investigate high load?
19. Why does high load not necessarily mean high CPU?
20. What does PSI provide?

## Networking

21. Which ports are required?
22. Why should APP01 not be publicly accessible?
23. Why should PostgreSQL not be publicly accessible?
24. How does traffic reach the application?
25. What happens when WEB01 fails?
26. What happens when APP01 fails?
27. How would you troubleshoot connection refused?
28. How would you troubleshoot a timeout?
29. How would you determine whether a firewall caused a failure?
30. Why maintain a network-flow matrix?

## NGINX

31. What role does LB01 perform?
32. What role do WEB01/02 perform?
33. Why use `nginx -t` before reload?
34. What does `least_conn` do?
35. What is the difference between 502 and 504?
36. How would you troubleshoot an upstream failure?
37. Why configure timeouts?
38. How do you safely roll back NGINX configuration?

## Application

39. Why does the application run as a dedicated user?
40. Why not run it as root?
41. Why use systemd?
42. Why use `Restart=on-failure`?
43. When can automatic restart hide a deeper problem?
44. What should `/health` represent?
45. What should `/db-health` represent?

## PostgreSQL

46. Explain PostgreSQL streaming replication.
47. What is WAL?
48. What is `pg_basebackup`?
49. What is a replication slot?
50. What is `pg_stat_replication`?
51. What does `pg_is_in_recovery()` tell you?
52. What happens if DB01 fails?
53. Why did you avoid unsafe automatic promotion?
54. How would controlled promotion work?
55. What happens to the old primary after promotion?
56. What is split brain?
57. Why is split brain dangerous?
58. How do you measure replication lag?
59. What is PITR?
60. Why are WAL archives required for PITR?

## Security

61. Why disable root SSH?
62. Why prefer SSH keys?
63. Why validate SSH configuration before reload?
64. How did you protect database credentials?
65. Why should secrets not enter Git?
66. How did you restrict service ports?
67. Why retain AppArmor?
68. Why shouldn't troubleshooting begin by disabling security?
69. What is least privilege?
70. How is it applied here?

## Automation

71. Why Ansible?
72. What is inventory?
73. Why group hosts?
74. What is idempotency?
75. How did you test idempotency?
76. What is configuration drift?
77. How did you simulate drift?
78. How did Ansible remediate it?
79. Why use `--check`?
80. Why use `--diff`?
81. When is check mode insufficient?
82. How do you handle secrets in automation?

## Observability

83. What is Node Exporter?
84. What does Prometheus collect?
85. Why is OBS01 outside the request path?
86. What happens if Prometheus fails?
87. What should a HostDown alert mean?
88. How would you monitor disk capacity?
89. Why monitor inodes?
90. Why monitor memory availability rather than just used memory?
91. What is alert fatigue?
92. What makes an alert actionable?

## Logging

93. Why centralize logs?
94. Why use Alloy?
95. What does Loki provide?
96. Why shouldn't Loki be exposed openly?
97. How do you prove logs reach the central platform?
98. What logs are most useful for a 502 incident?
99. Why is time synchronization important for log correlation?
100. What information should never be exposed in public logs?

## Backup

101. What is your backup strategy?
102. What is the difference between replication and backup?
103. Why is DB02 not a backup?
104. What is WAL archiving?
105. How did you verify backup success?
106. Why isn't exit code alone enough?
107. Why calculate checksums?
108. How often should restore testing occur?
109. What would happen if BACKUP01 failed?
110. How would production improve backup durability?

## Disaster Recovery

111. Define RTO.
112. Define RPO.
113. What were your targets?
114. What were your measured results?
115. How did you calculate actual RTO?
116. How did you determine observed RPO?
117. Why is redundancy different from DR?
118. Why is backup different from DR?
119. What did DR01 represent?
120. What did the DR exercise teach you?

## Failure Engineering

121. What happened when WEB01 failed?
122. What happened when APP01 failed?
123. What happened when DB02 failed?
124. How did you detect replication failure?
125. What happened when Node Exporter failed?
126. How did you test disk pressure safely?
127. Why avoid filling `/`?
128. What evidence did you collect?
129. Which failures affected customers?
130. Which failures affected only operational visibility?

## Reliability

131. Where is redundancy implemented?
132. Where is redundancy missing?
133. What is graceful degradation?
134. Which services can fail without customer impact?
135. Which failures require human intervention?
136. How would you increase database availability?
137. How would you make the load-balancer tier highly available?
138. How would you distribute the infrastructure across failure zones?
139. How would you remove OBS01 as an observability SPOF?
140. How would you protect BACKUP01?

## Production Thinking

141. What would you change before real production use?
142. How would you handle TLS?
143. How would you manage certificates?
144. How would you implement centralized identity?
145. How would you implement secrets management?
146. How would you manage OS patching?
147. How would you handle database connection pooling?
148. How would you perform zero/minimal-downtime deployments?
149. How would you perform database schema migrations?
150. How would you capacity-plan this infrastructure?

## Final Defense

151. What was the hardest engineering decision?
152. What trade-off did you deliberately accept?
153. What did automation prevent?
154. What did observability reveal?
155. What failure surprised you?
156. Which recovery procedure was slowest?
157. Which corrective action would you prioritize?
158. What would you redesign with twice the budget?
159. What would you redesign for 100× traffic?
160. Walk me from a customer request through every Linux component and explain what happens if each component fails.

---

# 24. Cleanup

Cleanup must be controlled.

Do not begin by deleting VMs.

## Preserve evidence

Export:

```text
architecture documentation
DR measurements
backup test results
failure-test results
sanitized logs
screenshots
Ansible output
configuration validation
```

Never publish credentials.

---

## Verify backups

Before destroying DB01:

```bash
sudo -u postgres psql \
  commerce \
  -c "SELECT count(*) FROM orders;"
```

Record the expected data.

Verify the recovery copy contains the expected data.

---

## Stop application tier

```bash
sudo systemctl stop \
  commerce-api
```

---

## Stop web tier

```bash
sudo systemctl stop nginx
```

---

## Stop monitoring collectors if dismantling lab

```bash
sudo systemctl stop \
  prometheus-node-exporter
```

Stop Alloy only when the final evidence has been forwarded.

---

## Database

Do not delete DB01 until:

```text
backup verified
restore tested
DR exercise complete
replication evidence preserved
```

---

## Remove secrets

Search working tree:

```bash
grep -RniE \
  'password|secret|token|api[_-]?key' \
  . \
  --exclude-dir=.git
```

Review manually.

---

# 25. Cleanup Verification

Confirm no unintended lab processes remain.

Check:

```bash
ps aux
```

Check listening ports:

```bash
sudo ss -lntup
```

Check mounts:

```bash
findmnt
```

Check disk:

```bash
df -hT
```

Check VM snapshots.

Confirm backups intended for retention remain accessible.

Confirm secrets are not present in Git:

```bash
git status
```

Then:

```bash
git diff --cached
```

Do not push until reviewed.

---

# 26. Project Reflection

Write answers based on actual evidence.

## Architecture

Which architecture decision had the largest reliability impact?

Which component remained the largest SPOF?

Why?

---

## Automation

How much manual configuration remained?

Which configuration was hardest to automate?

Did repeated Ansible runs remain stable?

---

## Security

Which ports were actually necessary?

Did any component require broader access than expected?

Were secrets ever exposed during troubleshooting?

---

## Observability

Which metric proved most useful?

Which alert generated useful action?

Which alert was noisy?

Which logs were most valuable?

---

## Database

How quickly did DB02 receive writes?

What happened during replica failure?

What would be required for safe automated database failover?

---

## Backup

How long did backup take?

How long did restore take?

Was the backup usable?

What would have happened if the backup repository had been lost?

---

## DR

What was:

```text
target RTO?
measured RTO?
target RPO?
observed RPO?
```

Did the architecture meet the objectives?

If not, why?

Do not alter the result to make the project look better.

A failed objective can provide stronger engineering evidence than a fabricated success.

---

## Reliability

Which failure caused customer impact?

Which failure was absorbed?

Which failure degraded only monitoring?

Which failure required human intervention?

---

## Production gap

Write a section titled:

```text
What This Lab Does Not Pretend to Be
```

Discuss limitations such as:

```text
single lab subnet
single load balancer
single observability server
single backup server
small VM scale
manual database failover
no multi-datacenter networking
no production PKI
no external secrets platform
no enterprise identity provider
no dedicated storage platform
no real organizational change process
```

This strengthens the project.

Senior engineering includes understanding where the architecture stops being sufficient.

---

# 27. Final Completion Checklist

## Architecture

* [ ] Business requirements documented.
* [ ] Server inventory documented.
* [ ] Network plan documented.
* [ ] Architecture diagram created.
* [ ] Request path documented.
* [ ] Trust boundaries documented.
* [ ] Failure domains documented.
* [ ] SPOFs documented.
* [ ] ADRs written.
* [ ] Known limitations documented.

## Linux

* [ ] All required VMs built.
* [ ] Hostnames configured.
* [ ] Time synchronization working.
* [ ] Administrative account created.
* [ ] SSH key access working.
* [ ] Root SSH disabled.
* [ ] Security controls remain enabled.
* [ ] Baseline packages automated.

## Automation

* [ ] Ansible installed.
* [ ] Inventory created.
* [ ] Host groups created.
* [ ] Connectivity verified.
* [ ] Baseline playbook created.
* [ ] Security automation created.
* [ ] Web automation created.
* [ ] Application automation created.
* [ ] Database automation created.
* [ ] Observability automation created.
* [ ] Backup automation created.
* [ ] Syntax validation completed.
* [ ] Check-mode review completed.
* [ ] Idempotency tested.
* [ ] Configuration drift simulated.
* [ ] Drift remediated.

## Load Balancing

* [ ] LB01 configured.
* [ ] WEB01 configured.
* [ ] WEB02 configured.
* [ ] NGINX configuration validated.
* [ ] WEB01 failure tested.
* [ ] WEB02 failure tested.
* [ ] customer path verified.

## Application

* [ ] Dedicated service identity created.
* [ ] Application installed.
* [ ] systemd service created.
* [ ] Application runs non-root.
* [ ] `/health` works.
* [ ] `/orders` works.
* [ ] `/db-health` works.
* [ ] APP01 failure tested.
* [ ] APP02 failure tested.
* [ ] Application logs captured.

## PostgreSQL

* [ ] PostgreSQL installed.
* [ ] Database created.
* [ ] Application role created.
* [ ] Schema created.
* [ ] DB01 configured as primary.
* [ ] DB02 configured as standby.
* [ ] Replication identity created.
* [ ] Replication slot created.
* [ ] `pg_hba.conf` restricted.
* [ ] Base backup used to initialize standby.
* [ ] `pg_is_in_recovery()` verified.
* [ ] `pg_stat_replication` verified.
* [ ] Replication tested.
* [ ] Replica outage tested.
* [ ] Recovery verified.

## Security

* [ ] SSH hardened.
* [ ] Firewall matrix documented.
* [ ] Required flows identified.
* [ ] Unnecessary exposure blocked.
* [ ] Database credentials protected.
* [ ] Secret files have restricted permissions.
* [ ] Application runs non-root.
* [ ] AppArmor status verified.
* [ ] Git tree checked for secrets.
* [ ] Security trade-offs documented.

## Monitoring

* [ ] Node Exporter installed.
* [ ] Node Exporter restricted to OBS01.
* [ ] Prometheus configured.
* [ ] Expected targets visible.
* [ ] CPU monitored.
* [ ] Memory monitored.
* [ ] Filesystems monitored.
* [ ] Inodes monitored.
* [ ] Disk I/O monitored.
* [ ] Network monitored.
* [ ] Host availability monitored.
* [ ] Application availability monitored.
* [ ] Database availability monitored.
* [ ] Backup freshness monitored.
* [ ] Alerts configured.
* [ ] Alert behavior tested.

## Logging

* [ ] Loki deployed.
* [ ] Loki not openly exposed.
* [ ] Alloy installed.
* [ ] system logs collected.
* [ ] NGINX logs collected.
* [ ] application logs collected.
* [ ] PostgreSQL logs collected.
* [ ] backup logs collected.
* [ ] centralized queries tested.
* [ ] timestamps correlated.
* [ ] sensitive data reviewed.

## Performance

* [ ] Baseline captured.
* [ ] Load test executed.
* [ ] Requests/sec measured.
* [ ] latency measured.
* [ ] failed requests measured.
* [ ] CPU behavior observed.
* [ ] memory behavior observed.
* [ ] disk behavior observed.
* [ ] network behavior observed.
* [ ] no fabricated performance claims.

## Backup

* [ ] Backup architecture documented.
* [ ] Database base backup created.
* [ ] WAL archive configured.
* [ ] WAL archive verified.
* [ ] configuration backup created.
* [ ] checksums generated.
* [ ] remote backup verified.
* [ ] backup failure detectable.
* [ ] retention policy documented.
* [ ] restore tested.

## Disaster Recovery

* [ ] DR01 prepared.
* [ ] DR plan documented.
* [ ] RTO objective defined.
* [ ] RPO objective defined.
* [ ] disaster scenario declared.
* [ ] recovery timer started.
* [ ] infrastructure rebuilt.
* [ ] database restored.
* [ ] application restored.
* [ ] database health verified.
* [ ] application health verified.
* [ ] RTO measured.
* [ ] observed RPO measured.
* [ ] results compared with objectives.
* [ ] gaps documented.

## Failure Engineering

* [ ] WEB01 failure performed.
* [ ] WEB02 failure performed.
* [ ] APP01 failure performed.
* [ ] APP02 failure performed.
* [ ] DB02 replication failure performed.
* [ ] monitoring failure performed.
* [ ] disk-pressure test performed safely.
* [ ] invalid configuration tested.
* [ ] rollback tested.
* [ ] recovery verified after every experiment.
* [ ] evidence preserved.

## Operations

* [ ] Application-down runbook written.
* [ ] Web-tier runbook written.
* [ ] Database-failure runbook written.
* [ ] Disk-capacity runbook written.
* [ ] Replication runbook written.
* [ ] Backup-failure runbook written.
* [ ] DR activation runbook written.
* [ ] production-readiness review completed.

## Portfolio Integrity

* [ ] Every repository file has a purpose.
* [ ] Empty decorative files removed.
* [ ] No fake screenshots.
* [ ] No fabricated command output.
* [ ] No fabricated uptime.
* [ ] No fabricated performance results.
* [ ] No fabricated RTO/RPO.
* [ ] Secrets removed.
* [ ] Evidence sanitized.
* [ ] Resume statements correspond to actual work.
* [ ] Lab limitations clearly disclosed.

## Senior Capstone Completion Standard

You have completed Senior Project 10 when you can truthfully say:

```text
I can design a Linux infrastructure from requirements rather than simply installing servers.

I can separate infrastructure into web, application, database, management, observability, backup, and recovery responsibilities.

I understand the request path from client to database.

I understand every major service dependency.

I can identify stateful and stateless components.

I can identify failure domains.

I can identify remaining single points of failure.

I can explain why those SPOFs exist in my lab.

I can explain how I would remove them in production.

I can build Linux systems reproducibly.

I can manage infrastructure with Ansible.

I can organize infrastructure inventory.

I can validate automation before applying it.

I can detect configuration drift.

I can remediate configuration drift.

I can test idempotency.

I can operate NGINX as a load-balancing and reverse-proxy layer.

I can validate NGINX configuration before reload.

I can build redundant web infrastructure.

I can build redundant application infrastructure.

I can run application services as dedicated non-root users.

I can manage application services with systemd.

I can troubleshoot failed systemd services.

I can investigate sockets and network paths.

I can build PostgreSQL infrastructure.

I understand PostgreSQL WAL.

I understand physical replication.

I can initialize a standby from a base backup.

I can inspect replication state.

I can measure replication behavior.

I understand why replication is not backup.

I understand why redundancy is not disaster recovery.

I understand why backup is not disaster recovery.

I can protect database access.

I can design network-flow policy.

I can restrict unnecessary ports.

I can harden administrative SSH access.

I can protect infrastructure secrets.

I can keep Linux security controls enabled while troubleshooting.

I can centralize Linux metrics.

I can monitor CPU, memory, storage, inodes, disk I/O, network, and availability.

I can build actionable alerts.

I can centralize Linux logs.

I can trace an event from a Linux host into the centralized logging platform.

I can correlate metrics and logs.

I can detect infrastructure failure before relying solely on user reports.

I can design database backups.

I can archive PostgreSQL WAL.

I can verify backup creation.

I can verify backup integrity.

I can restore from backup.

I understand point-in-time recovery.

I can define RTO and RPO before a recovery test.

I can conduct a disaster-recovery exercise.

I can measure actual recovery time.

I can determine the observed data-loss window.

I can compare measured recovery with business objectives.

I can report when the objective was not achieved instead of changing the numbers.

I can simulate web-node failure.

I can simulate application-node failure.

I can simulate database-replication failure.

I can simulate monitoring failure.

I can simulate storage pressure safely.

I can introduce a controlled configuration failure.

I can investigate failure using evidence.

I can follow a dependency path.

I can create competing hypotheses.

I can narrow the failure domain.

I can mitigate an outage.

I can recover a service.

I can verify recovery from the customer perspective.

I can perform a controlled infrastructure change.

I can validate before deployment.

I can roll back a failed change.

I can write operational runbooks.

I can write architecture decision records.

I can document security boundaries.

I can document failure domains.

I can perform a production-readiness review.

I can identify where my lab architecture differs from real production architecture.

I can explain what I would redesign for higher availability, stronger security, larger scale, multiple sites, stricter recovery objectives, and heavier traffic.

I can defend the architecture, implementation, security model, observability model, automation strategy, backup strategy, failure behavior, and disaster-recovery design in a senior Linux, DevOps, SRE, Platform Engineering, or Infrastructure Engineering interview.

Most importantly:

I did not build a collection of Linux servers.

I built a system, operated it as a system, failed it as a system, recovered it as a system, measured it as a system, and documented the engineering decisions that hold the system together.
```


