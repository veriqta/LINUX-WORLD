# Portfolio Project 08: Linux Performance Engineering Platform

- **Level:** Senior
- **Reference OS:** Ubuntu Server 24.04 LTS
- **Primary disciplines:** Linux performance engineering, kernel/system analysis, workload characterization, benchmarking, troubleshooting, capacity analysis

- **Demonstrates:** Linux performance engineering, CPU analysis, memory analysis, storage analysis, network analysis, process analysis, kernel observability, workload benchmarking, bottleneck isolation, controlled tuning, performance regression analysis, evidence-based optimization.

---

# 01. Project Title

## Linux Performance Engineering Platform

Build a controlled Linux performance engineering laboratory where you deliberately create and investigate:

* CPU saturation;
* CPU contention;
* scheduler pressure;
* memory pressure;
* swapping;
* OOM risk;
* storage latency;
* storage queueing;
* random I/O pressure;
* sequential I/O pressure;
* network throughput limitations;
* network connection problems;
* process-level bottlenecks;
* system-wide resource contention.

The portfolio must demonstrate this engineering chain:

```text
BASELINE
   ↓
REPRODUCE PROBLEM
   ↓
OBSERVE SYMPTOM
   ↓
MEASURE
   ↓
FORM HYPOTHESES
   ↓
TEST HYPOTHESES
   ↓
IDENTIFY BOTTLENECK
   ↓
CHANGE ONE THING
   ↓
RE-MEASURE
   ↓
COMPARE
   ↓
VALIDATE
   ↓
ROLL BACK IF NECESSARY
   ↓
DOCUMENT RESULT
```

The goal is not:

```text
Run top.
Find high CPU.
```

The goal is to be able to explain:

> The workload became slower, these measurements prove where time was being lost, these competing explanations were eliminated, this resource became the limiting factor, this change addressed that specific bottleneck, and these before/after measurements demonstrate the result.

---

# 02. Project Scenario

Your organization operates a Linux-hosted application.

Users report:

```text
"The application is slow."
```

That statement tells you almost nothing.

Operations teams provide conflicting explanations:

```text
"The CPU is overloaded."

"We need more RAM."

"The disk is slow."

"The network is saturated."

"Linux is swapping."

"The database is probably the problem."

"We should increase some kernel parameters."

"Let's restart the server."
```

None has supplied evidence.

Your assignment is to investigate the system as a performance engineer.

You are prohibited from starting with random tuning.

You must establish:

1. what normal performance looks like;
2. what workload is being measured;
3. what changed;
4. which resource is saturated;
5. whether saturation actually affects the workload;
6. which process or subsystem is responsible;
7. whether the problem is utilization, contention, latency, queueing, errors, or capacity;
8. which optimization addresses the identified bottleneck;
9. whether the optimization improved the workload;
10. whether the change created a regression elsewhere.

---

# 03. Project Difficulty

## Senior

A junior investigation often stops at:

```bash
top
free -h
df -h
```

Those tools are useful.

They are not sufficient for this project.

You will work across:

```text
workload
process
scheduler
CPU
memory
virtual memory
page cache
block I/O
filesystem
network
kernel
system calls
pressure
latency
throughput
queueing
```

You must distinguish:

```text
utilization ≠ saturation

free memory ≠ available memory

load average ≠ CPU utilization

IOPS ≠ latency

bandwidth ≠ application performance

high CPU ≠ CPU bottleneck

swap configured ≠ harmful swapping

cache use ≠ memory leak

fast benchmark ≠ healthy application

correlation ≠ root cause
```

---

# 04. Estimated Time

A complete implementation should take approximately:

| Phase                        | Estimated time |
| ---------------------------- | -------------: |
| Lab architecture             |          2–4 h |
| Tooling and baseline         |          4–6 h |
| Workload construction        |          4–6 h |
| CPU investigation            |          5–8 h |
| Memory investigation         |          5–8 h |
| Storage investigation        |         6–10 h |
| Network investigation        |          5–8 h |
| Process/kernel investigation |          5–8 h |
| Cross-resource contention    |          5–8 h |
| Optimization experiments     |         6–10 h |
| Regression/rollback testing  |          3–5 h |
| Portfolio/evidence           |          4–7 h |

Expected total:

**50–80+ hours.**

The actual duration depends heavily on how deeply you investigate each experiment.

---

# 05. Prerequisites

You should understand:

* Linux processes;
* threads;
* systemd;
* filesystems;
* block devices;
* TCP/IP;
* sockets;
* memory basics;
* virtual memory basics;
* CPU basics;
* Bash;
* SSH;
* package management;
* permissions;
* logs;
* `/proc`;
* basic Python.

You do not need to be a kernel developer.

---

## Required laboratory

Build three Ubuntu Server 24.04 LTS VMs.

| Host   |       Address | Purpose                   |
| ------ | ------------: | ------------------------- |
| perf01 | 192.168.56.21 | primary system under test |
| load01 | 192.168.56.22 | workload generator        |
| net01  | 192.168.56.23 | network benchmark peer    |

Recommended PERF01:

```text
4 vCPU
4–8 GB RAM
40+ GB disk
```

For some memory experiments, a smaller RAM allocation makes pressure easier to reproduce.

Take a VM snapshot before tuning.

---

# 06. Learning Objectives

By completing this project, you should be able to:

1. Establish a reproducible performance baseline.
2. Characterize a workload.
3. Define performance objectives.
4. Distinguish latency from throughput.
5. Interpret load average.
6. Measure CPU utilization.
7. Measure per-CPU utilization.
8. Measure process CPU usage.
9. Detect CPU saturation.
10. Investigate scheduler pressure.
11. Interpret context switches.
12. Investigate run queues.
13. Measure memory consumption.
14. Interpret available memory correctly.
15. Understand page cache behavior.
16. Detect reclaim pressure.
17. Detect swapping.
18. Investigate OOM conditions.
19. Use PSI.
20. Measure block-device behavior.
21. Interpret I/O latency.
22. Interpret queueing.
23. Measure IOPS and throughput.
24. Analyze process I/O.
25. Measure network throughput.
26. Investigate retransmissions.
27. Inspect sockets.
28. Measure application response time.
29. Use `strace` selectively.
30. Use `perf` where permissions and platform support permit.
31. Correlate symptoms across subsystems.
32. Create competing hypotheses.
33. Design controlled experiments.
34. Change one variable at a time.
35. Compare before/after results.
36. Detect regressions.
37. Roll back ineffective tuning.
38. Build repeatable measurement scripts.
39. Produce evidence instead of anecdotes.
40. defend an optimization decision in a senior engineering interview.

---

# 07. What You Will Build

The laboratory:

```text
                    ┌──────────────────────────┐
                    │         LOAD01           │
                    │                          │
                    │ HTTP workload            │
                    │ network client           │
                    │ benchmark generator      │
                    └────────────┬─────────────┘
                                 │
                                 │ requests
                                 ▼
                    ┌──────────────────────────┐
                    │          PERF01          │
                    │                          │
                    │ application workload     │
                    │ CPU experiments          │
                    │ memory experiments       │
                    │ storage experiments      │
                    │ process analysis         │
                    │ kernel analysis          │
                    └────────────┬─────────────┘
                                 │
                                 │ network tests
                                 ▼
                    ┌──────────────────────────┐
                    │          NET01           │
                    │                          │
                    │ iperf3 endpoint          │
                    │ network comparison peer  │
                    └──────────────────────────┘
```

Your evidence pipeline:

```text
workload
   ↓
baseline
   ↓
sar / mpstat / pidstat
   ↓
vmstat
   ↓
iostat
   ↓
PSI
   ↓
ss
   ↓
application timing
   ↓
strace/perf when necessary
   ↓
hypothesis
   ↓
controlled change
   ↓
same workload
   ↓
same measurement
   ↓
comparison report
```

---

# 08. Architecture

## 8.1 Performance model

For every investigation use four questions:

### What is the workload?

Example:

```text
100 HTTP requests
10 concurrent clients
1 GiB sequential write
4 KiB random reads
30-second CPU workload
60-second network transfer
```

### What is the metric?

Example:

```text
requests/second
mean latency
p95 latency
CPU utilization
run queue
IOPS
await
throughput
retransmissions
PSI stall percentage
```

### What is the bottleneck hypothesis?

Example:

```text
CPU saturation causes request queueing.
```

### What result would falsify the hypothesis?

Example:

```text
CPU remains mostly idle while request latency increases.
```

That last question prevents confirmation bias.

---

## 8.2 USE-style investigation

For each physical resource investigate:

```text
UTILIZATION
SATURATION
ERRORS
```

Examples:

### CPU

```text
Utilization → %user / %system / %idle
Saturation  → run queue / CPU PSI
Errors      → hardware/kernel errors where relevant
```

### Memory

```text
Utilization → working set / available memory
Saturation  → reclaim / swapping / memory PSI
Errors      → OOM events / allocation failures
```

### Storage

```text
Utilization → device activity
Saturation  → queues / latency / I/O PSI
Errors      → kernel/device/filesystem errors
```

### Network

```text
Utilization → throughput
Saturation  → queues / dropped packets / retransmission effects
Errors      → interface/TCP errors
```

---

## 8.3 Scientific tuning rule

Never do this:

```text
change swappiness
change dirty ratios
change scheduler settings
increase buffers
change application workers
restart service
run benchmark
```

You would not know which change mattered.

Instead:

```text
baseline
   ↓
one hypothesis
   ↓
one controlled change
   ↓
same workload
   ↓
same measurement
   ↓
compare
```

---

# 09. Technologies

Install and use:

```text
sysstat
procps
util-linux
iproute2
iperf3
fio
stress-ng
linux-tools
strace
lsof
curl
Python 3
GNU time
```

Important commands include:

```text
uptime
top
ps
pidstat
mpstat
vmstat
iostat
sar
free
slabtop
ss
ip
ethtool
fio
iperf3
stress-ng
strace
perf
journalctl
dmesg
```

Ubuntu 24.04 provides the `sysstat` package containing the familiar system-performance tooling used throughout this lab.

---

# 10. Environment Setup

## 10.1 Configure hostnames

PERF01:

```bash
sudo hostnamectl set-hostname perf01
```

LOAD01:

```bash
sudo hostnamectl set-hostname load01
```

NET01:

```bash
sudo hostnamectl set-hostname net01
```

Verify:

```bash
hostnamectl
```

---

## 10.2 Update systems

Run on all three:

```bash
sudo apt update
sudo apt upgrade -y
```

Reboot if the update requires it:

```bash
sudo reboot
```

After reconnecting:

```bash
uname -r
```

Record the actual kernel version.

---

## 10.3 Install tools on PERF01

```bash
sudo apt install -y \
  sysstat \
  procps \
  util-linux \
  stress-ng \
  fio \
  iperf3 \
  strace \
  lsof \
  curl \
  jq \
  python3 \
  linux-tools-common \
  linux-tools-generic
```

Verify:

```bash
mpstat -V
```

```bash
iostat -V
```

```bash
pidstat -V
```

```bash
fio --version
```

```bash
stress-ng --version
```

```bash
perf --version
```

Do not hard-code your actual versions into the portfolio before running these commands.

---

## 10.4 Install load tools

On LOAD01:

```bash
sudo apt install -y \
  curl \
  apache2-utils \
  iperf3
```

NET01:

```bash
sudo apt install -y \
  iperf3
```

---

## 10.5 Enable sysstat collection

On PERF01:

```bash
sudo systemctl enable --now sysstat
```

Check:

```bash
systemctl status sysstat --no-pager
```

Inspect timers:

```bash
systemctl list-timers --all | grep -i sysstat
```

Do not assume historical `sar` data exists immediately after installation.

---

## 10.6 Check PSI

```bash
ls -l /proc/pressure
```

Expected resource files normally include:

```text
cpu
io
memory
```

Inspect:

```bash
cat /proc/pressure/cpu
```

```bash
cat /proc/pressure/memory
```

```bash
cat /proc/pressure/io
```

PSI tells you about **time lost because workloads are waiting on constrained resources**.

---

# 11. Project Directory Structure

On PERF01:

```bash
mkdir -p ~/linux-performance-platform/{\
scripts,\
baselines,\
experiments/cpu,\
experiments/memory,\
experiments/storage,\
experiments/network,\
experiments/mixed,\
results/before,\
results/after,\
reports,\
evidence,\
runbooks,\
docs,\
adrs}
```

Enter:

```bash
cd ~/linux-performance-platform
```

Create:

```bash
touch README.md
```

---

# 12. Step-by-Step Implementation

## 12.1 Capture hardware baseline

Create:

```bash
cat > scripts/capture-system-baseline.sh <<'EOF'
#!/usr/bin/env bash
set -euo pipefail

OUT="${1:-baselines/system-baseline.txt}"

mkdir -p "$(dirname "$OUT")"

{
    echo "=== TIMESTAMP ==="
    date --iso-8601=seconds

    echo
    echo "=== HOST ==="
    hostnamectl

    echo
    echo "=== KERNEL ==="
    uname -a

    echo
    echo "=== CPU ==="
    lscpu

    echo
    echo "=== MEMORY ==="
    free -h
    cat /proc/meminfo

    echo
    echo "=== BLOCK DEVICES ==="
    lsblk -o NAME,SIZE,TYPE,FSTYPE,MOUNTPOINTS,ROTA

    echo
    echo "=== FILESYSTEMS ==="
    df -hT

    echo
    echo "=== NETWORK ==="
    ip -br addr
    ip route

    echo
    echo "=== LOAD ==="
    uptime

    echo
    echo "=== PSI CPU ==="
    cat /proc/pressure/cpu

    echo
    echo "=== PSI MEMORY ==="
    cat /proc/pressure/memory

    echo
    echo "=== PSI IO ==="
    cat /proc/pressure/io

    echo
    echo "=== VM SYSCTLS ==="
    sysctl vm.swappiness
    sysctl vm.dirty_ratio
    sysctl vm.dirty_background_ratio
} | tee "$OUT"
EOF
```

Make executable:

```bash
chmod +x scripts/capture-system-baseline.sh
```

Run:

```bash
./scripts/capture-system-baseline.sh
```

---

## 12.2 Capture live performance snapshot

Create:

```bash
cat > scripts/capture-performance.sh <<'EOF'
#!/usr/bin/env bash
set -euo pipefail

LABEL="${1:-capture}"
DURATION="${2:-30}"
OUT="evidence/${LABEL}-$(date +%Y%m%d-%H%M%S)"

mkdir -p "$OUT"

echo "Capturing ${DURATION}s performance evidence in ${OUT}"

uptime > "${OUT}/uptime.txt"
free -h > "${OUT}/free.txt"
cat /proc/pressure/cpu > "${OUT}/psi-cpu-start.txt"
cat /proc/pressure/memory > "${OUT}/psi-memory-start.txt"
cat /proc/pressure/io > "${OUT}/psi-io-start.txt"

vmstat 1 "$DURATION" > "${OUT}/vmstat.txt" &
P1=$!

mpstat -P ALL 1 "$DURATION" > "${OUT}/mpstat.txt" &
P2=$!

pidstat -u -r -d -w 1 "$DURATION" > "${OUT}/pidstat.txt" &
P3=$!

iostat -xz 1 "$DURATION" > "${OUT}/iostat.txt" &
P4=$!

sar -n DEV 1 "$DURATION" > "${OUT}/sar-network.txt" &
P5=$!

wait "$P1" "$P2" "$P3" "$P4" "$P5"

cat /proc/pressure/cpu > "${OUT}/psi-cpu-end.txt"
cat /proc/pressure/memory > "${OUT}/psi-memory-end.txt"
cat /proc/pressure/io > "${OUT}/psi-io-end.txt"

echo "$OUT"
EOF
```

Make executable:

```bash
chmod +x scripts/capture-performance.sh
```

Test:

```bash
./scripts/capture-performance.sh idle 10
```

Inspect:

```bash
find evidence -type f | sort
```

---

## 12.3 Establish idle baseline

Do not start by stressing the machine.

Measure normal state first:

```bash
./scripts/capture-performance.sh idle-baseline 60
```

Record:

```text
load average
CPU idle
CPU user
CPU system
run queue
context switches
available memory
swap use
disk await
disk utilization
network throughput
CPU PSI
memory PSI
I/O PSI
```

This becomes your comparison point.

---

# 13. Configuration

## Performance experiment record

Every experiment must use the same structure.

Create:

```bash
cat > docs/EXPERIMENT-TEMPLATE.md <<'EOF'
# Performance Experiment

## Experiment ID

## Date

## Host

## Question

What performance question are we answering?

## Workload

Exactly what workload was executed?

## Baseline

What was measured before the problem?

## Symptom

What changed from the user/application perspective?

## Competing Hypotheses

H1:

H2:

H3:

## Measurements

Commands:

Results:

## Bottleneck

What resource or subsystem was actually limiting performance?

## Evidence

Which measurements support this conclusion?

## Optimization

What single controlled change was made?

## Reason

Why should this change affect the identified bottleneck?

## Before

Throughput:

Latency:

CPU:

Memory:

Storage:

Network:

Pressure:

## After

Throughput:

Latency:

CPU:

Memory:

Storage:

Network:

Pressure:

## Difference

Absolute:

Percentage:

## Regression Check

What became worse?

## Rollback

Was the change retained or rolled back?

## Conclusion

What did this experiment prove?

## Limitations

What does this experiment not prove?
EOF
```

---

# 14. Automation

## 14.1 Automated comparison utility

Create:

```bash
cat > scripts/compare.py <<'EOF'
#!/usr/bin/env python3

import argparse


def percentage_change(before, after):
    if before == 0:
        return None
    return ((after - before) / before) * 100.0


def main():
    parser = argparse.ArgumentParser(
        description="Compare before and after performance measurements."
    )

    parser.add_argument("--metric", required=True)
    parser.add_argument("--before", required=True, type=float)
    parser.add_argument("--after", required=True, type=float)
    parser.add_argument(
        "--lower-is-better",
        action="store_true",
        help="Interpret lower values as improvement, for example latency.",
    )

    args = parser.parse_args()

    change = percentage_change(args.before, args.after)

    print(f"Metric: {args.metric}")
    print(f"Before: {args.before}")
    print(f"After:  {args.after}")

    if change is None:
        print("Percentage change: undefined because baseline is zero")
        return

    print(f"Percentage change: {change:.2f}%")

    improved = (
        args.after < args.before
        if args.lower_is_better
        else args.after > args.before
    )

    print(f"Direction: {'improved' if improved else 'not improved'}")


if __name__ == "__main__":
    main()
EOF
```

Make executable:

```bash
chmod +x scripts/compare.py
```

Example:

```bash
./scripts/compare.py \
  --metric p95_latency_ms \
  --before 250 \
  --after 175 \
  --lower-is-better
```

Do not use made-up numbers in the final portfolio.

Use your measurements.

---

# 15. Security

Performance tools can expose sensitive operational information.

Your evidence may reveal:

```text
process names
command lines
PIDs
paths
network endpoints
usernames
application behavior
memory characteristics
system topology
kernel information
```

Do not publish sensitive production captures.

This project uses a laboratory.

---

## Perf permissions

Inspect:

```bash
sysctl kernel.perf_event_paranoid
```

Do not immediately change it because `perf` returns a permission error.

Understand why access is restricted.

Prefer:

```bash
sudo perf ...
```

inside the isolated lab when privileged profiling is required.

---

## Performance tuning safety

Never copy random commands such as:

```bash
sysctl -w ...
```

from an optimization article into production simply because someone labels them "performance tuning."

Kernel tuning changes resource-management policy.

Every tuning change in this project requires:

```text
baseline
reason
change
measurement
regression check
rollback
```

---

# 16. Testing

# CPU Performance Laboratory

## CPU-01 — Establish CPU baseline

Run:

```bash
mpstat -P ALL 1 30
```

Observe:

```text
%usr
%sys
%iowait
%idle
```

Then:

```bash
vmstat 1 30
```

Pay attention to:

```text
r
b
us
sy
id
wa
cs
in
```

Do not interpret any column independently.

---

## CPU-02 — Generate CPU pressure

Check CPU count:

```bash
nproc
```

Start a CPU workload using all logical CPUs:

```bash
stress-ng \
  --cpu "$(nproc)" \
  --timeout 60s \
  --metrics-brief
```

In another terminal:

```bash
mpstat -P ALL 1
```

Another:

```bash
vmstat 1
```

Another:

```bash
pidstat -u -w 1
```

Another:

```bash
watch -n 1 cat /proc/pressure/cpu
```

Investigate.

---

## CPU-03 — Create CPU oversubscription

Instead of one worker per CPU:

```bash
stress-ng \
  --cpu "$(( $(nproc) * 4 ))" \
  --timeout 60s \
  --metrics-brief
```

Observe:

```bash
vmstat 1
```

Compare `r` against logical CPU count.

Then:

```bash
cat /proc/pressure/cpu
```

Question:

Did CPU utilization tell the entire story?

No assumption is allowed.

Use the measurements.

---

## CPU-04 — Process attribution

While stress is running:

```bash
pidstat -u 1
```

Then:

```bash
ps -eo \
  pid,ppid,comm,ni,pri,psr,%cpu,%mem \
  --sort=-%cpu \
  | head -20
```

Determine:

```text
which process
how much CPU
which CPUs
user versus system time
scheduler contention
```

---

## CPU-05 — CPU affinity experiment

Start:

```bash
stress-ng \
  --cpu 2 \
  --timeout 120s &
```

Find PIDs:

```bash
pgrep -a stress-ng
```

Inspect affinity:

```bash
taskset -cp PID
```

Pin a worker where appropriate:

```bash
sudo taskset -cp 0 PID
```

Observe:

```bash
mpstat -P ALL 1
```

Explain how aggregate CPU utilization can hide a hot CPU.

---

## CPU-06 — Context-switch investigation

Run:

```bash
vmstat 1
```

and:

```bash
pidstat -w 1
```

Generate additional processes:

```bash
stress-ng \
  --cpu "$(nproc)" \
  --switch 4 \
  --timeout 60s \
  --metrics-brief
```

Compare context-switch behavior.

Do not conclude that "high context switches are bad."

Interpret them relative to workload.

---

## CPU-07 — perf stat

Run a controlled workload:

```bash
sudo perf stat \
  stress-ng \
  --cpu 1 \
  --timeout 20s
```

Depending on CPU and virtualization support, available counters differ.

Record what your environment actually exposes.

Do not invent missing hardware counters.

---

## CPU-08 — Profiling

Create:

```bash
sudo perf record \
  -g \
  -- \
  stress-ng \
  --cpu 1 \
  --timeout 20s
```

Then:

```bash
sudo perf report
```

Investigate hot execution paths.

Remember that profiling itself has overhead.

---

# Memory Performance Laboratory

## MEM-01 — Understand memory baseline

Run:

```bash
free -h
```

Then:

```bash
cat /proc/meminfo
```

Study:

```text
MemTotal
MemFree
MemAvailable
Buffers
Cached
SwapTotal
SwapFree
Dirty
Writeback
AnonPages
Mapped
Slab
SReclaimable
SUnreclaim
```

Do not use `MemFree` alone to decide whether Linux has a memory problem.

---

## MEM-02 — Process memory

Run:

```bash
ps -eo \
  pid,user,comm,rss,vsz,%mem \
  --sort=-rss \
  | head -20
```

Choose a process:

```bash
cat /proc/PID/status
```

Inspect:

```text
VmPeak
VmSize
VmRSS
VmData
VmSwap
```

---

## MEM-03 — Controlled memory pressure

First inspect:

```bash
free -h
```

Do not allocate an arbitrary amount.

Choose a value safely below total memory.

For example on a disposable 4 GiB VM you might test:

```bash
stress-ng \
  --vm 2 \
  --vm-bytes 1G \
  --vm-keep \
  --timeout 60s \
  --metrics-brief
```

The appropriate value depends on your VM.

Monitor:

```bash
vmstat 1
```

```bash
pidstat -r 1
```

```bash
watch -n 1 cat /proc/pressure/memory
```

and:

```bash
free -h
```

---

## MEM-04 — Page-fault analysis

Run:

```bash
pidstat -r 1
```

Observe:

```text
minflt/s
majflt/s
```

Understand the distinction.

A page fault is not automatically a performance failure.

Major faults can require storage access and are generally more expensive.

---

## MEM-05 — Swap behavior

Inspect:

```bash
swapon --show
```

Then:

```bash
vmstat 1
```

Pay attention to:

```text
si
so
```

These indicate swap-in and swap-out activity.

Do not claim a swap problem merely because:

```text
SwapUsed > 0
```

Historical swap occupancy can remain even when active swapping is not currently hurting the workload.

---

## MEM-06 — Memory pressure and PSI

While memory stress runs:

```bash
cat /proc/pressure/memory
```

Observe:

```text
some
full
avg10
avg60
avg300
total
```

Interpret stall time rather than simply memory occupancy.

---

## MEM-07 — Slab investigation

Run:

```bash
sudo slabtop
```

Then:

```bash
grep -E \
  'Slab|SReclaimable|SUnreclaim' \
  /proc/meminfo
```

Understand that kernel memory consumption is not limited to application RSS.

---

## MEM-08 — OOM experiment

**Disposable VM/snapshot only.**

Before testing:

```bash
sudo dmesg -T | tail
```

Start with a bounded workload and gradually increase pressure.

Do not jump immediately to exhausting the entire machine.

If an OOM event occurs, inspect:

```bash
sudo journalctl -k \
  --since "-10 minutes" \
  | grep -iE 'oom|out of memory|killed process'
```

Record:

```text
which process was killed
memory conditions
what the kernel reported
application impact
recovery
```

Never perform this experiment on an important system.

---

# Storage Performance Laboratory

## STORAGE-01 — Identify storage

Run:

```bash
lsblk \
  -o NAME,MAJ:MIN,SIZE,TYPE,FSTYPE,MOUNTPOINTS,ROTA
```

Then:

```bash
findmnt
```

Then:

```bash
df -hT
```

Document whether your VM storage is virtualized.

That matters when interpreting benchmark results.

---

## STORAGE-02 — Baseline I/O

Run:

```bash
iostat -xz 1 30
```

Record relevant fields exposed by your installed sysstat version.

Pay attention to:

```text
read/write throughput
request rates
await
queue behavior
utilization
```

Do not mechanically use `%util == 100` as universal proof of a bottleneck.

Interpret it with latency, queueing and workload behavior.

---

## STORAGE-03 — Sequential write workload

Create test directory:

```bash
sudo mkdir -p /var/tmp/perf-lab
sudo chown "$USER":"$USER" /var/tmp/perf-lab
```

Run:

```bash
fio \
  --name=seqwrite \
  --filename=/var/tmp/perf-lab/seqwrite.dat \
  --size=1G \
  --rw=write \
  --bs=1M \
  --direct=1 \
  --ioengine=libaio \
  --iodepth=16 \
  --runtime=30 \
  --time_based \
  --group_reporting
```

In another terminal:

```bash
iostat -xz 1
```

Another:

```bash
pidstat -d 1
```

Another:

```bash
watch -n 1 cat /proc/pressure/io
```

Record actual throughput and latency.

---

## STORAGE-04 — Sequential read

After creating the test file:

```bash
fio \
  --name=seqread \
  --filename=/var/tmp/perf-lab/seqwrite.dat \
  --size=1G \
  --rw=read \
  --bs=1M \
  --direct=1 \
  --ioengine=libaio \
  --iodepth=16 \
  --runtime=30 \
  --time_based \
  --group_reporting
```

Compare against write behavior.

---

## STORAGE-05 — Random reads

```bash
fio \
  --name=randread \
  --filename=/var/tmp/perf-lab/random.dat \
  --size=1G \
  --rw=randread \
  --bs=4k \
  --direct=1 \
  --ioengine=libaio \
  --iodepth=32 \
  --runtime=30 \
  --time_based \
  --group_reporting
```

Measure:

```text
IOPS
bandwidth
average latency
tail latency where reported
device queue
await
```

---

## STORAGE-06 — Queue-depth experiment

Repeat with:

```text
iodepth=1
iodepth=4
iodepth=16
iodepth=32
```

Do not assume greater queue depth means better performance.

Create a table:

| Queue depth |     IOPS |       BW | Avg latency | Tail latency |
| ----------: | -------: | -------: | ----------: | -----------: |
|           1 | measured | measured |    measured |     measured |
|           4 | measured | measured |    measured |     measured |
|          16 | measured | measured |    measured |     measured |
|          32 | measured | measured |    measured |     measured |

Explain the trade-off.

---

## STORAGE-07 — Buffered versus direct I/O

Compare controlled runs using:

```text
direct=0
```

and:

```text
direct=1
```

Explain why page cache can radically change apparent storage performance.

---

# Network Performance Laboratory

## NET-01 — Inspect network

Run on PERF01:

```bash
ip -br addr
```

```bash
ip route
```

```bash
ip -s link
```

```bash
ss -s
```

Record interface names.

Never assume they are `eth0`.

---

## NET-02 — Start iperf server

On NET01:

```bash
iperf3 -s
```

On PERF01:

```bash
iperf3 \
  -c 192.168.56.23 \
  -t 30
```

Record actual throughput.

---

## NET-03 — Reverse direction

```bash
iperf3 \
  -c 192.168.56.23 \
  -R \
  -t 30
```

Compare directions.

---

## NET-04 — Parallel streams

```bash
iperf3 \
  -c 192.168.56.23 \
  -P 4 \
  -t 30
```

Then:

```bash
iperf3 \
  -c 192.168.56.23 \
  -P 8 \
  -t 30
```

Ask:

Did throughput improve?

Did CPU consumption increase?

Did retransmissions change?

---

## NET-05 — Observe during workload

While `iperf3` runs:

```bash
sar -n DEV 1
```

Then:

```bash
sar -n TCP,ETCP 1
```

Then:

```bash
ss -ti
```

Then:

```bash
ip -s link
```

Correlate application-level throughput with TCP/interface evidence.

---

## NET-06 — Network versus CPU

During high-throughput network test:

```bash
mpstat -P ALL 1
```

and:

```bash
pidstat -u 1
```

Ask:

Is the network actually limiting throughput?

Or is CPU consumption limiting the workload?

Do not assume.

---

# Process and Kernel Investigation

## PROC-01 — Process tree

```bash
ps -ef --forest
```

Understand:

```text
parent
child
worker
thread
daemon
```

---

## PROC-02 — Thread-level analysis

For a selected PID:

```bash
ps -L \
  -p PID \
  -o pid,tid,psr,pcpu,stat,comm
```

A process-level average can hide one saturated worker thread.

---

## PROC-03 — Open files

```bash
sudo lsof -p PID
```

Use it to understand what the process interacts with.

Do not treat a large number of open files as automatically problematic.

---

## PROC-04 — System calls

Attach briefly:

```bash
sudo strace \
  -tt \
  -T \
  -p PID
```

Use `Ctrl+C` after a short capture.

Look for repeated or slow calls.

Do not leave tracing attached unnecessarily.

---

## PROC-05 — Summary syscall profile

For a disposable workload you control:

```bash
strace \
  -c \
  COMMAND
```

This can help determine where system-call time is being spent.

---

# 17. Failure Simulation

# Failure Lab 01 — CPU Saturation

### Baseline

```bash
./scripts/capture-performance.sh cpu-before 30
```

### Inject

```bash
stress-ng \
  --cpu "$(( $(nproc) * 4 ))" \
  --timeout 120s \
  --metrics-brief
```

### Observe

```bash
vmstat 1
```

```bash
mpstat -P ALL 1
```

```bash
pidstat -u -w 1
```

```bash
cat /proc/pressure/cpu
```

### Investigate

Determine:

```text
CPU utilization
run queue
per-CPU distribution
context switches
CPU pressure
responsible processes
```

### Recover

Wait for the bounded workload to terminate.

Verify:

```bash
pgrep -a stress-ng || true
```

### Re-measure

```bash
./scripts/capture-performance.sh cpu-recovered 30
```

---

# Failure Lab 02 — Memory Pressure

Baseline:

```bash
./scripts/capture-performance.sh memory-before 30
```

Run a deliberately bounded memory workload appropriate for your VM.

Monitor:

```bash
vmstat 1
```

```bash
pidstat -r 1
```

```bash
free -h
```

```bash
cat /proc/pressure/memory
```

Investigate:

```text
available memory
page faults
reclaim
swap activity
stall pressure
```

Recover.

Then:

```bash
./scripts/capture-performance.sh memory-recovered 30
```

---

# Failure Lab 03 — Storage Saturation

Run FIO.

Simultaneously:

```bash
iostat -xz 1
```

```bash
pidstat -d 1
```

```bash
vmstat 1
```

```bash
cat /proc/pressure/io
```

Determine whether the workload creates:

```text
latency
queueing
throughput saturation
CPU iowait
application slowdown
```

---

# Failure Lab 04 — Network Load

On NET01:

```bash
iperf3 -s
```

On PERF01:

```bash
iperf3 \
  -c 192.168.56.23 \
  -P 8 \
  -t 120
```

Monitor:

```bash
sar -n DEV 1
```

```bash
sar -n TCP,ETCP 1
```

```bash
mpstat -P ALL 1
```

Determine whether:

```text
network
CPU
virtualization
TCP behavior
```

limits throughput.

---

# Failure Lab 05 — Mixed Resource Contention

This is where the project becomes substantially more realistic.

Start CPU workload:

```bash
stress-ng \
  --cpu "$(nproc)" \
  --timeout 120s &
```

Start storage workload:

```bash
fio \
  --name=mixed \
  --filename=/var/tmp/perf-lab/mixed.dat \
  --size=1G \
  --rw=randrw \
  --rwmixread=70 \
  --bs=4k \
  --direct=1 \
  --ioengine=libaio \
  --iodepth=16 \
  --runtime=120 \
  --time_based \
  --group_reporting &
```

Capture:

```bash
./scripts/capture-performance.sh mixed-pressure 120
```

Your job is to determine which measurements represent:

```text
cause
effect
secondary pressure
unrelated activity
```

---

# Failure Lab 06 — Misleading Load Average

Create storage pressure.

Observe:

```bash
uptime
```

Then:

```bash
mpstat 1
```

Then:

```bash
vmstat 1
```

Ask:

Can load average rise even when CPUs are not fully busy?

Investigate runnable and uninterruptible tasks.

This is why:

```text
high load average
```

does not automatically mean:

```text
CPU saturation
```

---

# Failure Lab 07 — One Hot CPU

Create a workload constrained to one CPU.

Observe:

```bash
mpstat -P ALL 1
```

Compare with aggregate CPU utilization.

Explain why:

```text
75% system-wide idle
```

could coexist with:

```text
one CPU at 100%
```

and still hurt a single-threaded workload.

---

# Failure Lab 08 — Swap Misdiagnosis

Find a state where swap contains data.

Then inspect:

```bash
free -h
```

and:

```bash
vmstat 1
```

If:

```text
si = 0
so = 0
```

during the problem period, ask whether existing swap occupancy actually explains current latency.

The point is to eliminate a false hypothesis.

---

# Failure Lab 09 — Cache Misdiagnosis

Inspect:

```bash
free -h
```

If cache consumption is large, do not immediately conclude:

```text
Linux is running out of memory.
```

Compare:

```text
MemAvailable
memory PSI
swap activity
reclaim
application performance
```

Document why filesystem cache is normally useful.

---

# Failure Lab 10 — Bad Optimization

This lab is mandatory.

Choose a proposed optimization.

Example:

```text
change vm.swappiness
```

Record current:

```bash
sysctl vm.swappiness
```

Save:

```bash
sysctl vm.swappiness \
  > evidence/swappiness-before.txt
```

Change only if your memory experiment provides a legitimate reason.

Example laboratory experiment:

```bash
sudo sysctl \
  -w vm.swappiness=10
```

Run the exact same workload.

Measure.

If there is no meaningful improvement, restore the original value.

Do not keep a change merely because it sounds like tuning.

---

# Failure Lab 11 — Performance Regression

Take an optimization that improves one metric.

Then examine:

```text
CPU
memory
storage
network
latency
throughput
tail latency
```

Ask:

Did improving throughput increase latency?

Did reducing memory consumption increase disk I/O?

Did more concurrency increase context switching?

Did greater queue depth improve IOPS but worsen tail latency?

An optimization is not automatically an overall improvement.

---

# Failure Lab 12 — Recovery Verification

After all experiments:

```bash
pgrep -a stress-ng || true
```

```bash
pgrep -a fio || true
```

Check:

```bash
uptime
```

```bash
free -h
```

```bash
iostat -xz 1 5
```

```bash
cat /proc/pressure/cpu
cat /proc/pressure/memory
cat /proc/pressure/io
```

Compare with original baseline.

---

# 18. Troubleshooting

Use this investigation model every time:

```text
SYMPTOM
   ↓
USER IMPACT
   ↓
WORKLOAD
   ↓
BLAST RADIUS
   ↓
RECENT CHANGE
   ↓
BASELINE COMPARISON
   ↓
CPU
   ↓
MEMORY
   ↓
STORAGE
   ↓
NETWORK
   ↓
PROCESS
   ↓
KERNEL/PRESSURE
   ↓
COMPETING HYPOTHESES
   ↓
TARGETED MEASUREMENT
   ↓
NARROW
   ↓
BOTTLENECK
   ↓
MITIGATE
   ↓
RE-MEASURE
   ↓
REGRESSION CHECK
   ↓
ROOT CAUSE
   ↓
PREVENT
```

---

## CPU checklist

```bash
uptime
```

```bash
mpstat -P ALL 1
```

```bash
vmstat 1
```

```bash
pidstat -u -w 1
```

```bash
ps -eo pid,comm,psr,%cpu,%mem --sort=-%cpu | head
```

```bash
cat /proc/pressure/cpu
```

Questions:

```text
Are CPUs actually saturated?

All CPUs or one?

User or kernel time?

Is the run queue growing?

Which processes?

Which threads?

Is CPU pressure increasing?
```

---

## Memory checklist

```bash
free -h
```

```bash
cat /proc/meminfo
```

```bash
vmstat 1
```

```bash
pidstat -r 1
```

```bash
cat /proc/pressure/memory
```

```bash
sudo journalctl -k | grep -iE 'oom|out of memory'
```

Questions:

```text
Is available memory actually low?

Is reclaim occurring?

Are major faults occurring?

Is active swapping occurring?

Is the workload stalled?

Did OOM occur?

Which process owns memory?
```

---

## Storage checklist

```bash
iostat -xz 1
```

```bash
pidstat -d 1
```

```bash
vmstat 1
```

```bash
cat /proc/pressure/io
```

```bash
sudo dmesg -T | grep -iE 'error|fail|nvme|scsi|blk'
```

Questions:

```text
Which device?

What workload?

Reads or writes?

Sequential or random?

Latency?

Queue?

IOPS?

Bandwidth?

Errors?

Application impact?
```

---

## Network checklist

```bash
ip -s link
```

```bash
ss -s
```

```bash
ss -ti
```

```bash
sar -n DEV 1
```

```bash
sar -n TCP,ETCP 1
```

Questions:

```text
Which interface?

Expected link capacity?

Current throughput?

Drops?

Errors?

Retransmissions?

Socket backlog?

CPU limitation?

Application limitation?
```

---

# 19. Final Validation

Create:

```bash
cat > evidence/FINAL-VALIDATION.md <<'EOF'
# Linux Performance Engineering Platform — Final Validation

Use measured values only.

## Environment

Kernel:
CPU:
vCPU count:
RAM:
Storage:
Filesystem:
Network:

## Idle Baseline

CPU idle:
Run queue:
Available memory:
Swap activity:
Storage latency:
CPU PSI:
Memory PSI:
IO PSI:

## CPU Experiment

Workload:
Baseline:
Symptom:
Bottleneck:
Evidence:
Optimization:
Before:
After:
Percentage change:
Regression:
Conclusion:

## Memory Experiment

Workload:
Baseline:
Symptom:
Bottleneck:
Evidence:
Optimization:
Before:
After:
Percentage change:
Regression:
Conclusion:

## Storage Experiment

Workload:
Baseline:
Symptom:
Bottleneck:
Evidence:
Optimization:
Before:
After:
Percentage change:
Regression:
Conclusion:

## Network Experiment

Workload:
Baseline:
Symptom:
Bottleneck:
Evidence:
Optimization:
Before:
After:
Percentage change:
Regression:
Conclusion:

## Mixed Workload

Observed contention:
Primary bottleneck:
Secondary effects:
Evidence:

## Rollback

Change tested:
Reason:
Result:
Retained or rolled back:
Evidence:

## Final Conclusion

What did the measurements actually prove?

## Limitations

What can this laboratory not prove?
EOF
```

---

# 20. Portfolio Documentation

Create a performance case study for each subsystem.

Required:

```text
reports/
├── CPU-CASE-STUDY.md
├── MEMORY-CASE-STUDY.md
├── STORAGE-CASE-STUDY.md
├── NETWORK-CASE-STUDY.md
└── MIXED-CONTENTION-CASE-STUDY.md
```

Each case study must show:

```text
1. Problem
2. Environment
3. Workload
4. Baseline
5. User-visible symptom
6. Competing hypotheses
7. Measurements
8. Bottleneck
9. Root cause
10. Proposed optimization
11. Controlled change
12. Before result
13. After result
14. Percentage difference
15. Regression check
16. Rollback decision
17. Final conclusion
18. Limitations
```

---

## Architecture decision

Create:

```bash
cat > adrs/ADR-001-MEASURE-BEFORE-TUNING.md <<'EOF'
# ADR-001 — Measure Before Tuning

## Status

Accepted.

## Context

Linux exposes many tunable parameters, but changing configuration without
evidence makes performance investigations difficult to reproduce and can
introduce regressions.

## Decision

No persistent performance tuning is applied until a reproducible workload and
baseline measurement exist.

Each tuning experiment changes one primary variable at a time.

## Alternatives

### Apply common performance tuning recommendations immediately

Rejected because generic tuning may not address the actual workload bottleneck.

### Change multiple parameters together

Rejected because the effect of each individual change cannot be isolated.

## Consequences

Investigations take longer initially.

However, changes become measurable, reversible and defensible.
EOF
```

---

## ADR 002

```bash
cat > adrs/ADR-002-PSI.md <<'EOF'
# ADR-002 — Pressure Stall Information

## Status

Accepted.

## Decision

CPU, memory and I/O PSI measurements are collected alongside conventional
utilization metrics.

## Reason

Utilization alone does not describe how much workload time is lost waiting for
constrained resources.

## Consequence

Performance investigations consider both resource consumption and workload
stalling.
EOF
```

---

## ADR 003

```bash
cat > adrs/ADR-003-REPRODUCIBLE-WORKLOADS.md <<'EOF'
# ADR-003 — Reproducible Workloads

## Status

Accepted.

## Decision

Before/after comparisons must use equivalent workload parameters.

## Reason

Changing both workload and configuration prevents meaningful comparison.

## Consequence

Experiment commands, duration, concurrency and dataset sizes must be recorded.
EOF
```

---

# 21. GitHub Structure

The finished repository should look approximately like this:

```text
08-Linux-Performance-Engineering-Platform/
│
├── README.md
│
├── scripts/
│   ├── capture-system-baseline.sh
│   ├── capture-performance.sh
│   └── compare.py
│
├── baselines/
│   └── system-baseline.txt
│
├── experiments/
│   ├── cpu/
│   ├── memory/
│   ├── storage/
│   ├── network/
│   └── mixed/
│
├── results/
│   ├── before/
│   └── after/
│
├── reports/
│   ├── CPU-CASE-STUDY.md
│   ├── MEMORY-CASE-STUDY.md
│   ├── STORAGE-CASE-STUDY.md
│   ├── NETWORK-CASE-STUDY.md
│   └── MIXED-CONTENTION-CASE-STUDY.md
│
├── evidence/
│   └── FINAL-VALIDATION.md
│
├── docs/
│   └── EXPERIMENT-TEMPLATE.md
│
├── runbooks/
│
└── adrs/
    ├── ADR-001-MEASURE-BEFORE-TUNING.md
    ├── ADR-002-PSI.md
    └── ADR-003-REPRODUCIBLE-WORKLOADS.md
```

Do not add generated result files until you actually generate them.

Do not publish fake benchmark results.

---

# 22. Resume Entry

After actually completing the project:

**Linux Performance Engineering Platform — Senior Linux Project**

Built a controlled Linux performance-engineering laboratory to reproduce, diagnose and optimize CPU, memory, storage and network bottlenecks using `mpstat`, `vmstat`, `pidstat`, `iostat`, `sar`, PSI, `fio`, `iperf3`, `strace`, `perf` and process/kernel telemetry.

Performed evidence-driven investigations using reproducible workloads, competing hypotheses, baseline measurements and controlled tuning experiments; correlated utilization, saturation, latency, queueing and resource-pressure data to isolate bottlenecks and validate measurable before/after performance changes.

Documented CPU contention, memory pressure, storage queueing, network behavior and mixed-resource contention as portfolio case studies, including regression analysis, rollback decisions and limitations.

Only use statements corresponding to work you actually completed.

---

# 23. Interview Preparation

You should be able to explain:

1. What is performance engineering?
2. Performance engineering versus monitoring.
3. Performance engineering versus capacity planning.
4. Latency versus throughput.
5. Utilization versus saturation.
6. Why isn't high CPU automatically a problem?
7. Why isn't low free memory automatically a problem?
8. Why isn't swap usage automatically a problem?
9. What is active swapping?
10. What do `si` and `so` mean in `vmstat`?
11. What is load average?
12. Why isn't load average equivalent to CPU utilization?
13. What is a run queue?
14. What does `r` mean in `vmstat`?
15. What does `b` mean?
16. What does `%user` represent?
17. What does `%system` represent?
18. What does `%iowait` mean?
19. What can `%idle` hide?
20. How can one CPU be saturated while aggregate utilization looks acceptable?
21. What does `mpstat -P ALL` reveal?
22. What does `pidstat` provide?
23. What is a context switch?
24. Are context switches inherently bad?
25. What is CPU affinity?
26. When can CPU affinity matter?
27. What is PSI?
28. What does CPU PSI represent?
29. What does memory PSI represent?
30. What does I/O PSI represent?
31. What is the difference between PSI `some` and `full`?
32. Why can PSI add information beyond utilization?
33. What is RSS?
34. What is VSZ?
35. What is `MemAvailable`?
36. What is page cache?
37. Why does Linux use otherwise idle RAM for caching?
38. What is a minor page fault?
39. What is a major page fault?
40. Why are major faults generally more expensive?
41. What is memory reclaim?
42. What is swapping?
43. What is thrashing?
44. What is an OOM event?
45. How do you investigate an OOM kill?
46. What is slab memory?
47. What are reclaimable and unreclaimable slab?
48. What is block I/O?
49. What is IOPS?
50. What is storage throughput?
51. What is I/O latency?
52. What is queue depth?
53. Why can greater queue depth increase throughput but hurt latency?
54. Sequential versus random I/O.
55. Buffered versus direct I/O.
56. Why can filesystem cache distort storage benchmarks?
57. What does `iostat -x` provide?
58. What does `pidstat -d` provide?
59. Why isn't device utilization sufficient to prove a bottleneck?
60. What is `fio`?
61. Why record block size?
62. Why record I/O depth?
63. Why record read/write mix?
64. Why must benchmark parameters stay consistent?
65. What does `iperf3` measure?
66. Why test both network directions?
67. Why test parallel streams?
68. What are TCP retransmissions?
69. What does `ss -ti` provide?
70. How can CPU limit network throughput?
71. How can virtualization affect network benchmarks?
72. What does `strace` do?
73. Why can tracing affect performance?
74. What does `perf stat` provide?
75. What does `perf record` do?
76. What does `perf report` do?
77. Why can `perf` require elevated permissions?
78. Why is profiling sensitive?
79. What is a performance baseline?
80. Why must a baseline use a defined workload?
81. What is a competing hypothesis?
82. Why should you define what would falsify a hypothesis?
83. Why change one variable at a time?
84. Why rerun the exact workload after tuning?
85. What is a performance regression?
86. Can throughput improve while latency becomes worse?
87. Can reducing memory usage increase storage activity?
88. Can additional concurrency reduce performance?
89. Why isn't a benchmark result automatically representative of production?
90. What is coordinated measurement?
91. Why collect CPU, memory, storage and network evidence simultaneously?
92. What is the USE method?
93. What does utilization mean in USE?
94. What does saturation mean?
95. What does errors mean?
96. How did you identify your CPU bottleneck?
97. How did you identify your memory bottleneck?
98. How did you identify your storage bottleneck?
99. How did you identify your network bottleneck?
100. Walk through your strongest performance investigation from symptom to measurable result.

---

# 24. Cleanup

Terminate laboratory workloads:

```bash
pkill -f stress-ng || true
```

Check:

```bash
pgrep -a stress-ng || true
```

Terminate accidental FIO workloads only after confirming they belong to your lab:

```bash
pgrep -a fio || true
```

Do not blindly `pkill fio` on a shared system.

---

## Remove test data

Inspect first:

```bash
du -sh /var/tmp/perf-lab
```

List:

```bash
find /var/tmp/perf-lab \
  -maxdepth 1 \
  -type f \
  -ls
```

Remove only the laboratory directory:

```bash
rm -rf /var/tmp/perf-lab
```

---

## Restore temporary sysctls

If you changed:

```text
vm.swappiness
```

restore the value recorded before the experiment.

Example only:

```bash
sudo sysctl -w vm.swappiness=ORIGINAL_VALUE
```

Do not blindly assume the original value was `60`.

---

## Remove persistent tuning

If you created a tuning file under:

```text
/etc/sysctl.d/
```

remove only the file created by this project.

Then reload the intended configuration appropriately.

Do not delete unrelated system tuning.

---

# 25. Cleanup Verification

Verify:

```bash
pgrep -a stress-ng || true
```

Then:

```bash
pgrep -a fio || true
```

Check memory:

```bash
free -h
```

Check load:

```bash
uptime
```

Check storage:

```bash
iostat -xz 1 5
```

Check pressure:

```bash
cat /proc/pressure/cpu
```

```bash
cat /proc/pressure/memory
```

```bash
cat /proc/pressure/io
```

Check kernel warnings:

```bash
sudo journalctl -k \
  --since "-30 minutes" \
  -p warning
```

Compare with the original baseline.

---

# 26. Project Reflection

Answer from your actual evidence.

## CPU

What was your highest reproducible CPU pressure condition?

Was the problem:

```text
utilization
saturation
single-thread limitation
scheduler contention
system time
user time
```

What evidence distinguished them?

---

## Memory

Did low `MemFree` correlate with performance loss?

What did `MemAvailable` show?

Did PSI show stalls?

Was swap active?

Were major faults occurring?

What evidence demonstrated actual memory pressure?

---

## Storage

Which workload produced the highest IOPS?

Which produced the greatest bandwidth?

Which produced the worst latency?

How did queue depth affect results?

Did increased throughput hurt tail latency?

---

## Network

What was your maximum measured throughput?

Was it symmetrical?

Did parallel streams help?

What happened to CPU consumption?

Were retransmissions visible?

What limits came from virtualization?

---

## Mixed contention

Which resource appeared saturated first?

What became a secondary bottleneck?

Did a metric that looked suspicious turn out not to be causal?

How did you prove that?

---

## Optimization

Which optimization produced the clearest measurable improvement?

Calculate:

```text
absolute difference
percentage difference
```

What other metrics changed?

Was there a regression?

---

## Failed optimization

Which change failed to improve performance?

Why did you reject or roll it back?

This is an important senior-level result.

A failed hypothesis investigated correctly is useful engineering evidence.

---

## Production reasoning

If the same symptom appeared on a production server, which commands would you consider low-risk first?

Which measurements could be expensive?

Which profiling tools require additional caution?

What evidence would you collect before restarting anything?

What information would be destroyed by restarting too early?

---

# 27. Final Completion Checklist

## Environment

* [ ] PERF01 built.
* [ ] LOAD01 built.
* [ ] NET01 built.
* [ ] Networking verified.
* [ ] Kernel version recorded.
* [ ] CPU topology recorded.
* [ ] RAM recorded.
* [ ] Storage architecture recorded.
* [ ] Filesystem recorded.

## Tooling

* [ ] sysstat installed.
* [ ] stress-ng installed.
* [ ] fio installed.
* [ ] iperf3 installed.
* [ ] strace installed.
* [ ] perf available/tested where platform permits.
* [ ] sysstat collection enabled.
* [ ] PSI availability verified.

## Baseline

* [ ] system baseline script created.
* [ ] performance capture script created.
* [ ] idle baseline captured.
* [ ] CPU baseline recorded.
* [ ] memory baseline recorded.
* [ ] storage baseline recorded.
* [ ] network baseline recorded.
* [ ] PSI baseline recorded.

## CPU

* [ ] per-CPU utilization measured.
* [ ] CPU pressure generated.
* [ ] oversubscription generated.
* [ ] run queue analyzed.
* [ ] context switches analyzed.
* [ ] responsible process identified.
* [ ] thread-level behavior inspected.
* [ ] CPU affinity investigated.
* [ ] PSI inspected.
* [ ] perf stat tested.
* [ ] profiling tested where supported.
* [ ] CPU case study documented.

## Memory

* [ ] `/proc/meminfo` analyzed.
* [ ] `MemAvailable` understood.
* [ ] process RSS inspected.
* [ ] controlled pressure generated.
* [ ] page faults analyzed.
* [ ] swap activity analyzed.
* [ ] memory PSI analyzed.
* [ ] slab inspected.
* [ ] OOM evidence investigated safely.
* [ ] memory case study documented.

## Storage

* [ ] block devices documented.
* [ ] filesystem documented.
* [ ] extended iostat captured.
* [ ] sequential write tested.
* [ ] sequential read tested.
* [ ] random read tested.
* [ ] queue-depth experiment completed.
* [ ] IOPS recorded.
* [ ] bandwidth recorded.
* [ ] latency recorded.
* [ ] tail latency recorded where available.
* [ ] buffered/direct I/O compared.
* [ ] I/O PSI inspected.
* [ ] storage case study documented.

## Network

* [ ] interfaces documented.
* [ ] routes documented.
* [ ] TCP/socket state inspected.
* [ ] forward throughput tested.
* [ ] reverse throughput tested.
* [ ] parallel streams tested.
* [ ] interface statistics captured.
* [ ] TCP statistics captured.
* [ ] retransmission behavior inspected.
* [ ] CPU/network relationship investigated.
* [ ] network case study documented.

## Kernel/Process

* [ ] process tree inspected.
* [ ] thread-level CPU inspected.
* [ ] open files inspected.
* [ ] controlled `strace` performed.
* [ ] syscall summary tested.
* [ ] perf permissions understood.
* [ ] profiling overhead considered.

## Failure Engineering

* [ ] CPU saturation reproduced.
* [ ] memory pressure reproduced.
* [ ] storage pressure reproduced.
* [ ] network load reproduced.
* [ ] mixed contention reproduced.
* [ ] misleading load average investigated.
* [ ] hot CPU scenario investigated.
* [ ] swap misdiagnosis investigated.
* [ ] cache misdiagnosis investigated.
* [ ] bad optimization tested.
* [ ] regression analysis completed.
* [ ] recovery verified.

## Performance Engineering

* [ ] every investigation starts with a baseline.
* [ ] workload parameters documented.
* [ ] competing hypotheses documented.
* [ ] bottleneck supported by measurements.
* [ ] optimization linked to identified bottleneck.
* [ ] one primary variable changed at a time.
* [ ] same workload used before and after.
* [ ] before values recorded.
* [ ] after values recorded.
* [ ] percentage difference calculated.
* [ ] regression check completed.
* [ ] ineffective change rolled back.
* [ ] limitations documented.

## Portfolio

* [ ] CPU case study complete.
* [ ] memory case study complete.
* [ ] storage case study complete.
* [ ] network case study complete.
* [ ] mixed-contention case study complete.
* [ ] final validation complete.
* [ ] actual evidence stored.
* [ ] no benchmark results fabricated.
* [ ] ADRs created.
* [ ] README complete.
* [ ] resume statements verified against completed work.

## Senior Completion Standard

You have completed this project when you can truthfully say:

```text
I do not begin performance investigations by tuning.

I begin by defining the workload.

I establish a baseline.

I reproduce the problem.

I measure user-visible impact.

I distinguish latency from throughput.

I distinguish utilization from saturation.

I investigate CPU at system, CPU, process and thread level.

I understand run queues.

I understand context switching.

I can identify CPU contention.

I can detect a single hot CPU hidden by aggregate statistics.

I can use CPU PSI to investigate stall pressure.

I understand Linux memory accounting.

I do not mistake filesystem cache for wasted memory.

I understand MemAvailable.

I can identify active swapping.

I can investigate page faults.

I can identify memory pressure.

I can investigate an OOM event.

I can inspect kernel slab consumption.

I can use memory PSI.

I can characterize storage workloads.

I distinguish IOPS from throughput.

I distinguish throughput from latency.

I understand queue depth.

I can investigate storage queueing.

I can compare sequential and random I/O.

I understand buffered versus direct I/O.

I can use fio safely in a laboratory.

I can use iostat to investigate devices.

I can use pidstat to attribute I/O to processes.

I can use I/O PSI to identify workload stalls.

I can measure network throughput.

I can inspect interfaces.

I can inspect sockets.

I can investigate retransmissions.

I can distinguish network limitations from CPU limitations.

I can use strace selectively.

I understand that tracing changes the system I am observing.

I can use perf where the environment permits it.

I understand the security implications of profiling.

I can correlate measurements across CPU, memory, storage and network.

I form competing hypotheses.

I deliberately try to disprove my own hypothesis.

I identify the limiting resource before tuning it.

I change one primary variable at a time.

I rerun the same workload.

I compare before and after measurements.

I calculate measurable differences.

I inspect secondary metrics for regressions.

I do not keep a tuning change merely because it sounds sophisticated.

I roll back changes that do not produce defensible improvement.

I document failed hypotheses.

I document limitations.

I do not claim a VM benchmark represents production capacity.

I do not fabricate benchmark results.

I preserve evidence.

I can take a vague report such as "Linux is slow" and turn it into a structured, measurable investigation.

I can explain the symptom.

I can explain the workload.

I can explain the bottleneck.

I can show the evidence.

I can explain the optimization.

I can show the measurable result.

I can explain the trade-off.

I can explain the rollback.

I can defend the entire investigation in a senior Linux, SRE, Platform or Infrastructure Engineering interview.
```

