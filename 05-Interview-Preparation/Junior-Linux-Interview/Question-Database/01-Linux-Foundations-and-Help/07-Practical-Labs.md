# Linux Foundations and Help: Practical Labs

Run only in an authorized disposable environment. Each lab requires a before state, implementation, independent verification, controlled failure, recovery and cleanup.

## Lab 1: kernel

1. Record the current kernel state and relevant versions.
2. Define a measurable target related to Linux Foundations and Help.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `uname -a`, `cat /etc/os-release`, `type cd`, `command -v ls`, `man hier`, `printf "%s\n" "$SHELL"`, `printf "%s\n" "$?"`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 2: distribution

1. Record the current distribution state and relevant versions.
2. Define a measurable target related to Linux Foundations and Help.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `uname -a`, `cat /etc/os-release`, `type cd`, `command -v ls`, `man hier`, `printf "%s\n" "$SHELL"`, `printf "%s\n" "$?"`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 3: shell

1. Record the current shell state and relevant versions.
2. Define a measurable target related to Linux Foundations and Help.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `uname -a`, `cat /etc/os-release`, `type cd`, `command -v ls`, `man hier`, `printf "%s\n" "$SHELL"`, `printf "%s\n" "$?"`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 4: terminal

1. Record the current terminal state and relevant versions.
2. Define a measurable target related to Linux Foundations and Help.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `uname -a`, `cat /etc/os-release`, `type cd`, `command -v ls`, `man hier`, `printf "%s\n" "$SHELL"`, `printf "%s\n" "$?"`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 5: GNU user space

1. Record the current GNU user space state and relevant versions.
2. Define a measurable target related to Linux Foundations and Help.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `uname -a`, `cat /etc/os-release`, `type cd`, `command -v ls`, `man hier`, `printf "%s\n" "$SHELL"`, `printf "%s\n" "$?"`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 6: paths

1. Record the current paths state and relevant versions.
2. Define a measurable target related to Linux Foundations and Help.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `uname -a`, `cat /etc/os-release`, `type cd`, `command -v ls`, `man hier`, `printf "%s\n" "$SHELL"`, `printf "%s\n" "$?"`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 7: command syntax

1. Record the current command syntax state and relevant versions.
2. Define a measurable target related to Linux Foundations and Help.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `uname -a`, `cat /etc/os-release`, `type cd`, `command -v ls`, `man hier`, `printf "%s\n" "$SHELL"`, `printf "%s\n" "$?"`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 8: exit status and documentation

1. Record the current exit status and documentation state and relevant versions.
2. Define a measurable target related to Linux Foundations and Help.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `uname -a`, `cat /etc/os-release`, `type cd`, `command -v ls`, `man hier`, `printf "%s\n" "$SHELL"`, `printf "%s\n" "$?"`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.
