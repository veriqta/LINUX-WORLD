# Users Groups Sudo and Permissions: Concepts and Mechanisms

## 1. What is UID, and why does it matter?

**Model answer:** In Users Groups Sudo and Permissions, **UID** must be understood as part of UID, GID, primary and supplementary groups, ownership, rwx, octal mode, umask, sudo and service accounts.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect UID without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats UID as an isolated definition. In Users Groups Sudo and Permissions, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 2. What is GID, and why does it matter?

**Model answer:** In Users Groups Sudo and Permissions, **GID** must be understood as part of UID, GID, primary and supplementary groups, ownership, rwx, octal mode, umask, sudo and service accounts.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect GID without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats GID as an isolated definition. In Users Groups Sudo and Permissions, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 3. What is primary and supplementary groups, and why does it matter?

**Model answer:** In Users Groups Sudo and Permissions, **primary and supplementary groups** must be understood as part of UID, GID, primary and supplementary groups, ownership, rwx, octal mode, umask, sudo and service accounts.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect primary and supplementary groups without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats primary and supplementary groups as an isolated definition. In Users Groups Sudo and Permissions, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 4. What is ownership, and why does it matter?

**Model answer:** In Users Groups Sudo and Permissions, **ownership** must be understood as part of UID, GID, primary and supplementary groups, ownership, rwx, octal mode, umask, sudo and service accounts.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect ownership without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats ownership as an isolated definition. In Users Groups Sudo and Permissions, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 5. What is rwx, and why does it matter?

**Model answer:** In Users Groups Sudo and Permissions, **rwx** must be understood as part of UID, GID, primary and supplementary groups, ownership, rwx, octal mode, umask, sudo and service accounts.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect rwx without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats rwx as an isolated definition. In Users Groups Sudo and Permissions, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 6. What is octal mode, and why does it matter?

**Model answer:** In Users Groups Sudo and Permissions, **octal mode** must be understood as part of UID, GID, primary and supplementary groups, ownership, rwx, octal mode, umask, sudo and service accounts.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect octal mode without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats octal mode as an isolated definition. In Users Groups Sudo and Permissions, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 7. What is umask, and why does it matter?

**Model answer:** In Users Groups Sudo and Permissions, **umask** must be understood as part of UID, GID, primary and supplementary groups, ownership, rwx, octal mode, umask, sudo and service accounts.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect umask without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats umask as an isolated definition. In Users Groups Sudo and Permissions, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 8. What is sudo and service accounts, and why does it matter?

**Model answer:** In Users Groups Sudo and Permissions, **sudo and service accounts** must be understood as part of UID, GID, primary and supplementary groups, ownership, rwx, octal mode, umask, sudo and service accounts.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect sudo and service accounts without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats sudo and service accounts as an isolated definition. In Users Groups Sudo and Permissions, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.
