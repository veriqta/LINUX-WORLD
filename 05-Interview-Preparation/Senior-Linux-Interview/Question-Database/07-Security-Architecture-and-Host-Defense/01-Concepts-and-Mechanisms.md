# Security Architecture and Host Defense: Concepts and Mechanisms

## 1. What is identity boundary, and why does it matter?

**Model answer:** In Security Architecture and Host Defense, **identity boundary** must be understood as part of identity boundary, privilege path, secure boot, kernel hardening, MAC, secrets, audit, integrity, supply chain and response.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect identity boundary without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats identity boundary as an isolated definition. In Security Architecture and Host Defense, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 2. What is privilege path, and why does it matter?

**Model answer:** In Security Architecture and Host Defense, **privilege path** must be understood as part of identity boundary, privilege path, secure boot, kernel hardening, MAC, secrets, audit, integrity, supply chain and response.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect privilege path without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats privilege path as an isolated definition. In Security Architecture and Host Defense, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 3. What is secure boot, and why does it matter?

**Model answer:** In Security Architecture and Host Defense, **secure boot** must be understood as part of identity boundary, privilege path, secure boot, kernel hardening, MAC, secrets, audit, integrity, supply chain and response.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect secure boot without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats secure boot as an isolated definition. In Security Architecture and Host Defense, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 4. What is kernel hardening, and why does it matter?

**Model answer:** In Security Architecture and Host Defense, **kernel hardening** must be understood as part of identity boundary, privilege path, secure boot, kernel hardening, MAC, secrets, audit, integrity, supply chain and response.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect kernel hardening without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats kernel hardening as an isolated definition. In Security Architecture and Host Defense, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 5. What is MAC, and why does it matter?

**Model answer:** In Security Architecture and Host Defense, **MAC** must be understood as part of identity boundary, privilege path, secure boot, kernel hardening, MAC, secrets, audit, integrity, supply chain and response.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect MAC without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats MAC as an isolated definition. In Security Architecture and Host Defense, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 6. What is secrets, and why does it matter?

**Model answer:** In Security Architecture and Host Defense, **secrets** must be understood as part of identity boundary, privilege path, secure boot, kernel hardening, MAC, secrets, audit, integrity, supply chain and response.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect secrets without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats secrets as an isolated definition. In Security Architecture and Host Defense, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 7. What is audit, and why does it matter?

**Model answer:** In Security Architecture and Host Defense, **audit** must be understood as part of identity boundary, privilege path, secure boot, kernel hardening, MAC, secrets, audit, integrity, supply chain and response.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect audit without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats audit as an isolated definition. In Security Architecture and Host Defense, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 8. What is integrity, and why does it matter?

**Model answer:** In Security Architecture and Host Defense, **integrity** must be understood as part of identity boundary, privilege path, secure boot, kernel hardening, MAC, secrets, audit, integrity, supply chain and response.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect integrity without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats integrity as an isolated definition. In Security Architecture and Host Defense, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 9. What is supply chain and response, and why does it matter?

**Model answer:** In Security Architecture and Host Defense, **supply chain and response** must be understood as part of identity boundary, privilege path, secure boot, kernel hardening, MAC, secrets, audit, integrity, supply chain and response.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect supply chain and response without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats supply chain and response as an isolated definition. In Security Architecture and Host Defense, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.
