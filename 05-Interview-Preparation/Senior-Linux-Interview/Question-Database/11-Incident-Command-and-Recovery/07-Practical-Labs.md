# Incident Command and Recovery: Practical Labs

Run only in an authorized disposable environment. Each lab requires a before state, implementation, independent verification, controlled failure, recovery and cleanup.

## Lab 1: severity

1. Record the current severity state and relevant versions.
2. Define a measurable target related to Incident Command and Recovery.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `date -u +%FT%TZ`, `last -x`, `journalctl --since`, `dmesg -T`, `ss`, `ip`, `lsof`, `package history`, `configuration diff`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 2: roles

1. Record the current roles state and relevant versions.
2. Define a measurable target related to Incident Command and Recovery.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `date -u +%FT%TZ`, `last -x`, `journalctl --since`, `dmesg -T`, `ss`, `ip`, `lsof`, `package history`, `configuration diff`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 3: timeline

1. Record the current timeline state and relevant versions.
2. Define a measurable target related to Incident Command and Recovery.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `date -u +%FT%TZ`, `last -x`, `journalctl --since`, `dmesg -T`, `ss`, `ip`, `lsof`, `package history`, `configuration diff`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 4: evidence

1. Record the current evidence state and relevant versions.
2. Define a measurable target related to Incident Command and Recovery.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `date -u +%FT%TZ`, `last -x`, `journalctl --since`, `dmesg -T`, `ss`, `ip`, `lsof`, `package history`, `configuration diff`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 5: containment

1. Record the current containment state and relevant versions.
2. Define a measurable target related to Incident Command and Recovery.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `date -u +%FT%TZ`, `last -x`, `journalctl --since`, `dmesg -T`, `ss`, `ip`, `lsof`, `package history`, `configuration diff`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 6: mitigation

1. Record the current mitigation state and relevant versions.
2. Define a measurable target related to Incident Command and Recovery.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `date -u +%FT%TZ`, `last -x`, `journalctl --since`, `dmesg -T`, `ss`, `ip`, `lsof`, `package history`, `configuration diff`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 7: recovery

1. Record the current recovery state and relevant versions.
2. Define a measurable target related to Incident Command and Recovery.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `date -u +%FT%TZ`, `last -x`, `journalctl --since`, `dmesg -T`, `ss`, `ip`, `lsof`, `package history`, `configuration diff`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 8: communication

1. Record the current communication state and relevant versions.
2. Define a measurable target related to Incident Command and Recovery.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `date -u +%FT%TZ`, `last -x`, `journalctl --since`, `dmesg -T`, `ss`, `ip`, `lsof`, `package history`, `configuration diff`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 9: decision log and postmortem

1. Record the current decision log and postmortem state and relevant versions.
2. Define a measurable target related to Incident Command and Recovery.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `date -u +%FT%TZ`, `last -x`, `journalctl --since`, `dmesg -T`, `ss`, `ip`, `lsof`, `package history`, `configuration diff`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.
