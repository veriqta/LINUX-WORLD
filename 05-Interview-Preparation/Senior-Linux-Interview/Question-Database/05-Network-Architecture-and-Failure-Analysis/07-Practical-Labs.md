# Network Architecture and Failure Analysis: Practical Labs

Run only in an authorized disposable environment. Each lab requires a before state, implementation, independent verification, controlled failure, recovery and cleanup.

## Lab 1: routing domains

1. Record the current routing domains state and relevant versions.
2. Define a measurable target related to Network Architecture and Failure Analysis.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ip route get`, `ss -ti`, `nstat`, `ethtool -S IFACE`, `conntrack -S`, `tcpdump`, `tracepath`, `dig +trace`, `openssl s_client`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 2: ECMP

1. Record the current ECMP state and relevant versions.
2. Define a measurable target related to Network Architecture and Failure Analysis.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ip route get`, `ss -ti`, `nstat`, `ethtool -S IFACE`, `conntrack -S`, `tcpdump`, `tracepath`, `dig +trace`, `openssl s_client`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 3: conntrack

1. Record the current conntrack state and relevant versions.
2. Define a measurable target related to Network Architecture and Failure Analysis.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ip route get`, `ss -ti`, `nstat`, `ethtool -S IFACE`, `conntrack -S`, `tcpdump`, `tracepath`, `dig +trace`, `openssl s_client`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 4: NAT

1. Record the current NAT state and relevant versions.
2. Define a measurable target related to Network Architecture and Failure Analysis.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ip route get`, `ss -ti`, `nstat`, `ethtool -S IFACE`, `conntrack -S`, `tcpdump`, `tracepath`, `dig +trace`, `openssl s_client`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 5: MTU

1. Record the current MTU state and relevant versions.
2. Define a measurable target related to Network Architecture and Failure Analysis.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ip route get`, `ss -ti`, `nstat`, `ethtool -S IFACE`, `conntrack -S`, `tcpdump`, `tracepath`, `dig +trace`, `openssl s_client`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 6: offload

1. Record the current offload state and relevant versions.
2. Define a measurable target related to Network Architecture and Failure Analysis.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ip route get`, `ss -ti`, `nstat`, `ethtool -S IFACE`, `conntrack -S`, `tcpdump`, `tracepath`, `dig +trace`, `openssl s_client`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 7: queues

1. Record the current queues state and relevant versions.
2. Define a measurable target related to Network Architecture and Failure Analysis.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ip route get`, `ss -ti`, `nstat`, `ethtool -S IFACE`, `conntrack -S`, `tcpdump`, `tracepath`, `dig +trace`, `openssl s_client`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 8: DNS

1. Record the current DNS state and relevant versions.
2. Define a measurable target related to Network Architecture and Failure Analysis.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ip route get`, `ss -ti`, `nstat`, `ethtool -S IFACE`, `conntrack -S`, `tcpdump`, `tracepath`, `dig +trace`, `openssl s_client`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 9: TLS

1. Record the current TLS state and relevant versions.
2. Define a measurable target related to Network Architecture and Failure Analysis.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ip route get`, `ss -ti`, `nstat`, `ethtool -S IFACE`, `conntrack -S`, `tcpdump`, `tracepath`, `dig +trace`, `openssl s_client`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 10: load balancing and asymmetric paths

1. Record the current load balancing and asymmetric paths state and relevant versions.
2. Define a measurable target related to Network Architecture and Failure Analysis.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ip route get`, `ss -ti`, `nstat`, `ethtool -S IFACE`, `conntrack -S`, `tcpdump`, `tracepath`, `dig +trace`, `openssl s_client`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.
