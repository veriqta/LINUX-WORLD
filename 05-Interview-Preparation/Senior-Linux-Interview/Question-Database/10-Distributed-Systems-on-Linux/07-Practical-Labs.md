# Distributed Systems on Linux: Practical Labs

Run only in an authorized disposable environment. Each lab requires a before state, implementation, independent verification, controlled failure, recovery and cleanup.

## Lab 1: timeouts

1. Record the current timeouts state and relevant versions.
2. Define a measurable target related to Distributed Systems on Linux.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ss -ti`, `tc netem in a lab`, `chronyc tracking`, `dig`, `curl --trace-time`, `systemd-run`, `coredumpctl`, `tcpdump`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 2: retries

1. Record the current retries state and relevant versions.
2. Define a measurable target related to Distributed Systems on Linux.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ss -ti`, `tc netem in a lab`, `chronyc tracking`, `dig`, `curl --trace-time`, `systemd-run`, `coredumpctl`, `tcpdump`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 3: backoff

1. Record the current backoff state and relevant versions.
2. Define a measurable target related to Distributed Systems on Linux.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ss -ti`, `tc netem in a lab`, `chronyc tracking`, `dig`, `curl --trace-time`, `systemd-run`, `coredumpctl`, `tcpdump`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 4: idempotency

1. Record the current idempotency state and relevant versions.
2. Define a measurable target related to Distributed Systems on Linux.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ss -ti`, `tc netem in a lab`, `chronyc tracking`, `dig`, `curl --trace-time`, `systemd-run`, `coredumpctl`, `tcpdump`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 5: quorum

1. Record the current quorum state and relevant versions.
2. Define a measurable target related to Distributed Systems on Linux.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ss -ti`, `tc netem in a lab`, `chronyc tracking`, `dig`, `curl --trace-time`, `systemd-run`, `coredumpctl`, `tcpdump`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 6: leader election

1. Record the current leader election state and relevant versions.
2. Define a measurable target related to Distributed Systems on Linux.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ss -ti`, `tc netem in a lab`, `chronyc tracking`, `dig`, `curl --trace-time`, `systemd-run`, `coredumpctl`, `tcpdump`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 7: split brain

1. Record the current split brain state and relevant versions.
2. Define a measurable target related to Distributed Systems on Linux.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ss -ti`, `tc netem in a lab`, `chronyc tracking`, `dig`, `curl --trace-time`, `systemd-run`, `coredumpctl`, `tcpdump`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 8: consistency

1. Record the current consistency state and relevant versions.
2. Define a measurable target related to Distributed Systems on Linux.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ss -ti`, `tc netem in a lab`, `chronyc tracking`, `dig`, `curl --trace-time`, `systemd-run`, `coredumpctl`, `tcpdump`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 9: clock and backpressure

1. Record the current clock and backpressure state and relevant versions.
2. Define a measurable target related to Distributed Systems on Linux.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ss -ti`, `tc netem in a lab`, `chronyc tracking`, `dig`, `curl --trace-time`, `systemd-run`, `coredumpctl`, `tcpdump`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.
