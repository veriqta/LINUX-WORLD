# Automation Platform and Safe Delivery: Practical Labs

Run only in an authorized disposable environment. Each lab requires a before state, implementation, independent verification, controlled failure, recovery and cleanup.

## Lab 1: control plane

1. Record the current control plane state and relevant versions.
2. Define a measurable target related to Automation Platform and Safe Delivery.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ansible-playbook --check --diff`, `systemd-analyze verify`, `conftest test`, `cosign verify`, `git verify-commit`, `bats`, `pytest`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 2: convergence

1. Record the current convergence state and relevant versions.
2. Define a measurable target related to Automation Platform and Safe Delivery.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ansible-playbook --check --diff`, `systemd-analyze verify`, `conftest test`, `cosign verify`, `git verify-commit`, `bats`, `pytest`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 3: idempotence

1. Record the current idempotence state and relevant versions.
2. Define a measurable target related to Automation Platform and Safe Delivery.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ansible-playbook --check --diff`, `systemd-analyze verify`, `conftest test`, `cosign verify`, `git verify-commit`, `bats`, `pytest`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 4: policy

1. Record the current policy state and relevant versions.
2. Define a measurable target related to Automation Platform and Safe Delivery.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ansible-playbook --check --diff`, `systemd-analyze verify`, `conftest test`, `cosign verify`, `git verify-commit`, `bats`, `pytest`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 5: test pyramid

1. Record the current test pyramid state and relevant versions.
2. Define a measurable target related to Automation Platform and Safe Delivery.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ansible-playbook --check --diff`, `systemd-analyze verify`, `conftest test`, `cosign verify`, `git verify-commit`, `bats`, `pytest`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 6: artifact provenance

1. Record the current artifact provenance state and relevant versions.
2. Define a measurable target related to Automation Platform and Safe Delivery.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ansible-playbook --check --diff`, `systemd-analyze verify`, `conftest test`, `cosign verify`, `git verify-commit`, `bats`, `pytest`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 7: canary

1. Record the current canary state and relevant versions.
2. Define a measurable target related to Automation Platform and Safe Delivery.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ansible-playbook --check --diff`, `systemd-analyze verify`, `conftest test`, `cosign verify`, `git verify-commit`, `bats`, `pytest`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 8: feature flag and rollback

1. Record the current feature flag and rollback state and relevant versions.
2. Define a measurable target related to Automation Platform and Safe Delivery.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ansible-playbook --check --diff`, `systemd-analyze verify`, `conftest test`, `cosign verify`, `git verify-commit`, `bats`, `pytest`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.
