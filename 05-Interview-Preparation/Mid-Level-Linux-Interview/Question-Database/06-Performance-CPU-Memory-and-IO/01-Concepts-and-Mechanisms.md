# Performance CPU Memory and IO: Concepts and Mechanisms

## 1. What is load, and why does it matter?

**Model answer:** In Performance CPU Memory and IO, **load** must be understood as part of load, run queue, context switch, CPU steal, PSI, page cache, swap, OOM, disk latency, queue depth and saturation.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect load without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats load as an isolated definition. In Performance CPU Memory and IO, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 2. What is run queue, and why does it matter?

**Model answer:** In Performance CPU Memory and IO, **run queue** must be understood as part of load, run queue, context switch, CPU steal, PSI, page cache, swap, OOM, disk latency, queue depth and saturation.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect run queue without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats run queue as an isolated definition. In Performance CPU Memory and IO, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 3. What is context switch, and why does it matter?

**Model answer:** In Performance CPU Memory and IO, **context switch** must be understood as part of load, run queue, context switch, CPU steal, PSI, page cache, swap, OOM, disk latency, queue depth and saturation.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect context switch without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats context switch as an isolated definition. In Performance CPU Memory and IO, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 4. What is CPU steal, and why does it matter?

**Model answer:** In Performance CPU Memory and IO, **CPU steal** must be understood as part of load, run queue, context switch, CPU steal, PSI, page cache, swap, OOM, disk latency, queue depth and saturation.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect CPU steal without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats CPU steal as an isolated definition. In Performance CPU Memory and IO, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 5. What is PSI, and why does it matter?

**Model answer:** In Performance CPU Memory and IO, **PSI** must be understood as part of load, run queue, context switch, CPU steal, PSI, page cache, swap, OOM, disk latency, queue depth and saturation.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect PSI without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats PSI as an isolated definition. In Performance CPU Memory and IO, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 6. What is page cache, and why does it matter?

**Model answer:** In Performance CPU Memory and IO, **page cache** must be understood as part of load, run queue, context switch, CPU steal, PSI, page cache, swap, OOM, disk latency, queue depth and saturation.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect page cache without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats page cache as an isolated definition. In Performance CPU Memory and IO, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 7. What is swap, and why does it matter?

**Model answer:** In Performance CPU Memory and IO, **swap** must be understood as part of load, run queue, context switch, CPU steal, PSI, page cache, swap, OOM, disk latency, queue depth and saturation.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect swap without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats swap as an isolated definition. In Performance CPU Memory and IO, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 8. What is OOM, and why does it matter?

**Model answer:** In Performance CPU Memory and IO, **OOM** must be understood as part of load, run queue, context switch, CPU steal, PSI, page cache, swap, OOM, disk latency, queue depth and saturation.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect OOM without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats OOM as an isolated definition. In Performance CPU Memory and IO, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 9. What is disk latency, and why does it matter?

**Model answer:** In Performance CPU Memory and IO, **disk latency** must be understood as part of load, run queue, context switch, CPU steal, PSI, page cache, swap, OOM, disk latency, queue depth and saturation.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect disk latency without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats disk latency as an isolated definition. In Performance CPU Memory and IO, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 10. What is queue depth and saturation, and why does it matter?

**Model answer:** In Performance CPU Memory and IO, **queue depth and saturation** must be understood as part of load, run queue, context switch, CPU steal, PSI, page cache, swap, OOM, disk latency, queue depth and saturation.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect queue depth and saturation without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats queue depth and saturation as an isolated definition. In Performance CPU Memory and IO, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.
