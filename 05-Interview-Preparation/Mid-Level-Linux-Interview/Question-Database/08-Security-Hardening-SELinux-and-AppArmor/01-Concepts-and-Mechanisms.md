# Security Hardening SELinux and AppArmor: Concepts and Mechanisms

## 1. What is MAC versus DAC, and why does it matter?

**Model answer:** In Security Hardening SELinux and AppArmor, **MAC versus DAC** must be understood as part of MAC versus DAC, labels, domains, booleans, profiles, audit evidence, firewall default deny, patching and integrity.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect MAC versus DAC without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats MAC versus DAC as an isolated definition. In Security Hardening SELinux and AppArmor, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 2. What is labels, and why does it matter?

**Model answer:** In Security Hardening SELinux and AppArmor, **labels** must be understood as part of MAC versus DAC, labels, domains, booleans, profiles, audit evidence, firewall default deny, patching and integrity.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect labels without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats labels as an isolated definition. In Security Hardening SELinux and AppArmor, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 3. What is domains, and why does it matter?

**Model answer:** In Security Hardening SELinux and AppArmor, **domains** must be understood as part of MAC versus DAC, labels, domains, booleans, profiles, audit evidence, firewall default deny, patching and integrity.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect domains without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats domains as an isolated definition. In Security Hardening SELinux and AppArmor, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 4. What is booleans, and why does it matter?

**Model answer:** In Security Hardening SELinux and AppArmor, **booleans** must be understood as part of MAC versus DAC, labels, domains, booleans, profiles, audit evidence, firewall default deny, patching and integrity.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect booleans without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats booleans as an isolated definition. In Security Hardening SELinux and AppArmor, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 5. What is profiles, and why does it matter?

**Model answer:** In Security Hardening SELinux and AppArmor, **profiles** must be understood as part of MAC versus DAC, labels, domains, booleans, profiles, audit evidence, firewall default deny, patching and integrity.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect profiles without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats profiles as an isolated definition. In Security Hardening SELinux and AppArmor, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 6. What is audit evidence, and why does it matter?

**Model answer:** In Security Hardening SELinux and AppArmor, **audit evidence** must be understood as part of MAC versus DAC, labels, domains, booleans, profiles, audit evidence, firewall default deny, patching and integrity.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect audit evidence without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats audit evidence as an isolated definition. In Security Hardening SELinux and AppArmor, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 7. What is firewall default deny, and why does it matter?

**Model answer:** In Security Hardening SELinux and AppArmor, **firewall default deny** must be understood as part of MAC versus DAC, labels, domains, booleans, profiles, audit evidence, firewall default deny, patching and integrity.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect firewall default deny without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats firewall default deny as an isolated definition. In Security Hardening SELinux and AppArmor, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.

## 8. What is patching and integrity, and why does it matter?

**Model answer:** In Security Hardening SELinux and AppArmor, **patching and integrity** must be understood as part of MAC versus DAC, labels, domains, booleans, profiles, audit evidence, firewall default deny, patching and integrity.. A complete answer defines what it controls or represents, identifies where its effective state can be observed, explains at least one failure mode, and shows a safe verification. The candidate should distinguish configuration from runtime state and avoid claiming success from a single command exit code.

**Follow-up:** How would you inspect patching and integrity without changing the system? What would incorrect state look like?

**Common mistake:** A weak answer treats patching and integrity as an isolated definition. In Security Hardening SELinux and AppArmor, the candidate must connect it to effective state, privilege, persistence, failure behavior and a verification method.
