# Networking Routing Firewall and DNS: Practical Labs

Run only in an authorized disposable environment. Each lab requires a before state, implementation, independent verification, controlled failure, recovery and cleanup.

## Lab 1: CIDR

1. Record the current CIDR state and relevant versions.
2. Define a measurable target related to Networking Routing Firewall and DNS.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ip route get ADDRESS`, `ip rule`, `ip neigh`, `ss -s`, `tracepath HOST`, `tcpdump -ni IFACE FILTER`, `nft list ruleset`, `dig +trace NAME`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 2: policy routing

1. Record the current policy routing state and relevant versions.
2. Define a measurable target related to Networking Routing Firewall and DNS.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ip route get ADDRESS`, `ip rule`, `ip neigh`, `ss -s`, `tracepath HOST`, `tcpdump -ni IFACE FILTER`, `nft list ruleset`, `dig +trace NAME`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 3: neighbor discovery

1. Record the current neighbor discovery state and relevant versions.
2. Define a measurable target related to Networking Routing Firewall and DNS.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ip route get ADDRESS`, `ip rule`, `ip neigh`, `ss -s`, `tracepath HOST`, `tcpdump -ni IFACE FILTER`, `nft list ruleset`, `dig +trace NAME`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 4: TCP state

1. Record the current TCP state state and relevant versions.
2. Define a measurable target related to Networking Routing Firewall and DNS.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ip route get ADDRESS`, `ip rule`, `ip neigh`, `ss -s`, `tracepath HOST`, `tcpdump -ni IFACE FILTER`, `nft list ruleset`, `dig +trace NAME`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 5: MTU

1. Record the current MTU state and relevant versions.
2. Define a measurable target related to Networking Routing Firewall and DNS.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ip route get ADDRESS`, `ip rule`, `ip neigh`, `ss -s`, `tracepath HOST`, `tcpdump -ni IFACE FILTER`, `nft list ruleset`, `dig +trace NAME`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 6: bonding

1. Record the current bonding state and relevant versions.
2. Define a measurable target related to Networking Routing Firewall and DNS.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ip route get ADDRESS`, `ip rule`, `ip neigh`, `ss -s`, `tracepath HOST`, `tcpdump -ni IFACE FILTER`, `nft list ruleset`, `dig +trace NAME`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 7: VLAN

1. Record the current VLAN state and relevant versions.
2. Define a measurable target related to Networking Routing Firewall and DNS.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ip route get ADDRESS`, `ip rule`, `ip neigh`, `ss -s`, `tracepath HOST`, `tcpdump -ni IFACE FILTER`, `nft list ruleset`, `dig +trace NAME`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 8: nftables

1. Record the current nftables state and relevant versions.
2. Define a measurable target related to Networking Routing Firewall and DNS.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ip route get ADDRESS`, `ip rule`, `ip neigh`, `ss -s`, `tracepath HOST`, `tcpdump -ni IFACE FILTER`, `nft list ruleset`, `dig +trace NAME`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 9: DNS delegation and caching

1. Record the current DNS delegation and caching state and relevant versions.
2. Define a measurable target related to Networking Routing Firewall and DNS.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `ip route get ADDRESS`, `ip rule`, `ip neigh`, `ss -s`, `tracepath HOST`, `tcpdump -ni IFACE FILTER`, `nft list ruleset`, `dig +trace NAME`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.
