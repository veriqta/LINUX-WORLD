# Incident Troubleshooting and Change: Practical Labs

Run only in an authorized disposable environment. Each lab requires a before state, implementation, independent verification, controlled failure, recovery and cleanup.

## Lab 1: impact

1. Record the current impact state and relevant versions.
2. Define a measurable target related to Incident Troubleshooting and Change.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `date -Is`, `uptime -s`, `last -x`, `journalctl --since TIME`, `systemctl list-jobs`, `ss -s`, `git diff`, `logger INCIDENT_MARKER`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 2: timeline

1. Record the current timeline state and relevant versions.
2. Define a measurable target related to Incident Troubleshooting and Change.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `date -Is`, `uptime -s`, `last -x`, `journalctl --since TIME`, `systemctl list-jobs`, `ss -s`, `git diff`, `logger INCIDENT_MARKER`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 3: recent change

1. Record the current recent change state and relevant versions.
2. Define a measurable target related to Incident Troubleshooting and Change.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `date -Is`, `uptime -s`, `last -x`, `journalctl --since TIME`, `systemctl list-jobs`, `ss -s`, `git diff`, `logger INCIDENT_MARKER`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 4: evidence preservation

1. Record the current evidence preservation state and relevant versions.
2. Define a measurable target related to Incident Troubleshooting and Change.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `date -Is`, `uptime -s`, `last -x`, `journalctl --since TIME`, `systemctl list-jobs`, `ss -s`, `git diff`, `logger INCIDENT_MARKER`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 5: mitigation

1. Record the current mitigation state and relevant versions.
2. Define a measurable target related to Incident Troubleshooting and Change.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `date -Is`, `uptime -s`, `last -x`, `journalctl --since TIME`, `systemctl list-jobs`, `ss -s`, `git diff`, `logger INCIDENT_MARKER`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 6: rollback

1. Record the current rollback state and relevant versions.
2. Define a measurable target related to Incident Troubleshooting and Change.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `date -Is`, `uptime -s`, `last -x`, `journalctl --since TIME`, `systemctl list-jobs`, `ss -s`, `git diff`, `logger INCIDENT_MARKER`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 7: escalation

1. Record the current escalation state and relevant versions.
2. Define a measurable target related to Incident Troubleshooting and Change.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `date -Is`, `uptime -s`, `last -x`, `journalctl --since TIME`, `systemctl list-jobs`, `ss -s`, `git diff`, `logger INCIDENT_MARKER`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.

## Lab 8: postmortem and corrective action

1. Record the current postmortem and corrective action state and relevant versions.
2. Define a measurable target related to Incident Troubleshooting and Change.
3. Implement the smallest reversible change.
4. Validate syntax or configuration before activation.
5. Prove intended behavior using an identity or client different from the administrator.
6. Prove prohibited behavior remains denied.
7. Introduce one bounded failure and predict its evidence.
8. Detect and recover without using an undocumented shortcut.
9. Reboot or recreate where persistence matters.
10. Clean up and prove unrelated state remains intact.

**Suggested evidence:** `date -Is`, `uptime -s`, `last -x`, `journalctl --since TIME`, `systemctl list-jobs`, `ss -s`, `git diff`, `logger INCIDENT_MARKER`, configuration diff, timestamped logs, exit status and end-to-end result.

**Scoring:** accuracy 3, safety 2, verification 2, recovery 1, explanation 1, cleanup 1.
