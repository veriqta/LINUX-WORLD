# Packaging

These scripts install a reviewed snapshot into an explicitly selected prefix, remove only manifest-listed files, build a reproducible archive, and verify release checksums. They do not install system packages, start services, or create users.
