[build-system]
requires = []
build-backend = "setuptools.build_meta"

[project]
name = "linux-world-certificate-inventory"
version = "1.0.0"
description = "Inspects PEM certificate files and reports expiry metadata."
requires-python = ">=3.10"
